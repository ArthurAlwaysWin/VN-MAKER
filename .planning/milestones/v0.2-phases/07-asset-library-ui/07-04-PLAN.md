---
phase: 07-asset-library-ui
plan: 04
type: execute
wave: 3
depends_on: [07-02]
files_modified:
  - src/editor/components/resource-library/CharacterEditor.vue
autonomous: true
requirements: [ASSET-07, ASSET-08, ASSET-09]
must_haves:
  truths:
    - "Left sidebar shows character list with avatar thumbnails (first expression, CSS object-position: top crop)"
    - "Clicking a character in sidebar shows their name, color, and ID in the editor pane"
    - "User can edit character name and color via form inputs"
    - "Expression grid shows thumbnails grouped by selected character"
    - "User can import new expression images via file picker (+ 导入表情 button)"
    - "Right-click on expression shows context menu with rename/delete"
    - "User can add new characters and delete characters with confirmation"
    - "All character data modifications call script.pushState() for undo support"
  artifacts:
    - path: "src/editor/components/resource-library/CharacterEditor.vue"
      provides: "Sidebar + editor pane for character management with expression grid"
      min_lines: 200
  key_links:
    - from: "src/editor/components/resource-library/CharacterEditor.vue"
      to: "src/editor/stores/script.js"
      via: "useScriptStore — data.characters"
      pattern: "script\\.data\\.characters"
    - from: "src/editor/components/resource-library/CharacterEditor.vue"
      to: "src/editor/stores/assets.js"
      via: "useAssetStore — importAssets for expression files"
      pattern: "assets\\.importAssets"
---

<objective>
Replace the CharacterEditor.vue scaffold with the full character editor implementation: left sidebar with character list + avatars, right editor pane with name/color forms + expression thumbnail grid, expression import via file picker, and context menu operations (rename/delete) on expressions.

Purpose: Users manage character data (name, color, expressions) in the unified 资源库 view without switching to a separate Characters tab (ASSET-07, ASSET-08, ASSET-09).
Output: Full CharacterEditor.vue (replacing scaffold)
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-asset-library-ui/07-CONTEXT.md
@.planning/phases/07-asset-library-ui/07-UI-SPEC.md
@.planning/phases/07-asset-library-ui/07-RESEARCH.md
@.planning/phases/07-asset-library-ui/07-02-SUMMARY.md

<interfaces>
<!-- From Plan 01 shared components -->
InlineEdit: Props { value, preserveExtension }, Emits { save, cancel }, Expose { startEdit() }
ContextMenu: Props { visible, x, y, items }, Emits { select, close }

<!-- From src/editor/stores/script.js -->
script.data.characters — Object keyed by character ID:
  { [id]: { name: String, color: String, expressions: { [exprName]: String (relative path) } } }
script.pushState() — push current state for undo
script.data.assets.fonts — Array (not relevant here but same data ref)

<!-- From src/editor/stores/assets.js -->
assets.importAssets('characters', fileDataArray) → { success, imported: [{ original, saved }], errors: [] }
assets.files.characters — array of filename strings in assets/characters/

<!-- From existing Characters.vue (being replaced — pattern reference) -->
addCharacter(): prompt() for ID, create with { name: '新角色', color: '#FFFFFF', expressions: {} }
deleteExpression(name): confirm() then delete key from expressions
selectedChar.expressions[exprName] = 'characters/{path}' — expression path format

<!-- Expression path format (from Research open question #3): -->
Use 'characters/{filename}' — matches existing format in defaultScript() and asset:// protocol
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CharacterEditor.vue — sidebar with character list and avatars</name>
  <files>src/editor/components/resource-library/CharacterEditor.vue</files>
  <read_first>
    - src/editor/views/Characters.vue (full file — existing character editor implementation being replaced. Sidebar at lines 3-17, editor pane at lines 20-67, addCharacter at lines 101-113, script store usage pattern)
    - src/editor/stores/script.js (data ref structure, pushState at lines 13-25)
    - .planning/phases/07-asset-library-ui/07-UI-SPEC.md (§2.5 Characters Sub-Tab layout, §3.7 Character Avatar, §3.8 New Character Dialog, §4.4-4.5 Empty States)
    - .planning/phases/07-asset-library-ui/07-RESEARCH.md (Pattern 6: Store Coordination, Expression First Key Access code, Character Avatar Crop CSS)
  </read_first>
  <action>
**Replace src/editor/components/resource-library/CharacterEditor.vue entirely:**

Script setup:
```javascript
import { ref, computed } from 'vue';
import { useScriptStore } from '../../stores/script.js';
import { useAssetStore } from '../../stores/assets.js';
import InlineEdit from './InlineEdit.vue';
import ContextMenu from './ContextMenu.vue';

const script = useScriptStore();
const assets = useAssetStore();

const selectedId = ref('');
const fileInputRef = ref(null);

// Computed
const characters = computed(() => {
  if (!script.data?.characters) return {};
  return script.data.characters;
});

const characterList = computed(() => {
  return Object.entries(characters.value).map(([id, char]) => ({
    id,
    name: char.name,
    color: char.color,
    avatar: getAvatarSrc(char),
  }));
});

const selectedChar = computed(() => {
  if (!selectedId.value || !characters.value[selectedId.value]) return null;
  return characters.value[selectedId.value];
});

const expressions = computed(() => {
  if (!selectedChar.value?.expressions) return [];
  return Object.entries(selectedChar.value.expressions).map(([name, path]) => ({
    name,
    path,
    src: `asset://${path}`,
  }));
});
```

Helper functions:
```javascript
// Get avatar source — first expression image, top-cropped (D-04)
function getAvatarSrc(character) {
  const exprKeys = Object.keys(character.expressions || {});
  if (exprKeys.length === 0) return null;
  const firstPath = character.expressions[exprKeys[0]];
  return `asset://${firstPath}`;
}

function selectCharacter(id) {
  selectedId.value = id;
}

// New character (D-03, matching existing Characters.vue pattern)
function addCharacter() {
  const id = prompt('请输入角色 ID（如 hero、heroine）:');
  if (!id || !id.trim()) return;
  const trimmedId = id.trim();
  if (script.data.characters[trimmedId]) {
    alert('该角色 ID 已存在');
    return;
  }
  script.data.characters[trimmedId] = {
    name: '新角色',
    color: '#FFFFFF',
    expressions: {},
  };
  script.pushState();
  selectedId.value = trimmedId;
}

// Delete character
function deleteCharacter(id) {
  const char = script.data.characters[id];
  if (!char) return;
  if (confirm(`确定要删除角色 "${char.name}" 吗？该角色的所有表情数据也会被移除。`)) {
    delete script.data.characters[id];
    script.pushState();
    if (selectedId.value === id) {
      const remaining = Object.keys(script.data.characters);
      selectedId.value = remaining.length > 0 ? remaining[0] : '';
    }
  }
}

// Name/color editing — direct mutation triggers auto-save watcher in App.vue
function updateName(newName) {
  if (!selectedChar.value) return;
  selectedChar.value.name = newName;
  script.pushState();
}

function updateColor(newColor) {
  if (!selectedChar.value) return;
  selectedChar.value.color = newColor;
  script.pushState();
}
```

Expression management:
```javascript
// Context menu state for expressions
const menuVisible = ref(false);
const menuX = ref(0);
const menuY = ref(0);
const menuTarget = ref(''); // expression name
const exprEditRefs = {};

const menuItems = [
  { label: '重命名', action: 'rename' },
  { separator: true },
  { label: '删除', action: 'delete', destructive: true },
];

function showExprMenu(event, exprName) {
  menuX.value = event.clientX;
  menuY.value = event.clientY;
  menuTarget.value = exprName;
  menuVisible.value = true;
}

function onExprMenuAction(action) {
  if (action === 'rename' && exprEditRefs[menuTarget.value]) {
    exprEditRefs[menuTarget.value].startEdit();
  } else if (action === 'delete') {
    deleteExpression(menuTarget.value);
  }
}

function setExprEditRef(name, el) {
  if (el) exprEditRefs[name] = el;
}

// Import expression via file picker (ASSET-09 — not typing paths)
async function importExpression() {
  fileInputRef.value?.click();
}

async function handleExpressionFiles(event) {
  const fileList = event.target.files;
  if (!fileList || fileList.length === 0 || !selectedChar.value) return;

  const fileDataArray = [];
  for (const file of fileList) {
    const buffer = await file.arrayBuffer();
    fileDataArray.push({
      name: file.name,
      data: Array.from(new Uint8Array(buffer)),
    });
  }

  const result = await assets.importAssets('characters', fileDataArray);
  if (result.success && result.imported.length > 0) {
    for (const item of result.imported) {
      // Expression name = filename without extension
      const exprName = item.saved.replace(/\.[^.]+$/, '');
      // Path format: characters/{filename} (Pitfall #4, Research open question #3)
      selectedChar.value.expressions[exprName] = `characters/${item.saved}`;
    }
    script.pushState();
  }
  if (result.errors?.length > 0) {
    alert(`${result.errors.length} 个文件导入失败`);
  }

  // Reset input
  event.target.value = '';
}

// Rename expression (D-07)
function renameExpression(oldName, newName) {
  if (!selectedChar.value || !newName || newName === oldName) return;
  if (selectedChar.value.expressions[newName]) {
    alert('该表情名称已存在');
    return;
  }
  const path = selectedChar.value.expressions[oldName];
  delete selectedChar.value.expressions[oldName];
  selectedChar.value.expressions[newName] = path;
  script.pushState();
}

// Delete expression (only removes reference, not file on disk — per Research open question #1)
function deleteExpression(exprName) {
  if (!selectedChar.value) return;
  if (confirm(`确定要删除表情 "${exprName}" 吗？`)) {
    delete selectedChar.value.expressions[exprName];
    script.pushState();
  }
}
```

Template — full structure:
```html
<div class="character-editor">
  <!-- Sidebar -->
  <div class="sidebar">
    <div class="sidebar-header">
      <span class="sidebar-title">角色列表</span>
      <button class="add-btn" @click="addCharacter" aria-label="创建角色">+</button>
    </div>
    <div class="sidebar-list">
      <!-- Empty sidebar -->
      <div v-if="characterList.length === 0" class="sidebar-empty">
        <span>👤</span>
        <p>暂无角色</p>
        <p class="hint">点击上方 + 按钮创建</p>
      </div>
      <!-- Character items -->
      <div
        v-for="char in characterList"
        :key="char.id"
        class="char-item"
        :class="{ active: selectedId === char.id }"
        @click="selectCharacter(char.id)"
      >
        <div class="char-avatar">
          <img v-if="char.avatar" :src="char.avatar" :alt="char.name" />
          <span v-else class="avatar-fallback">👤</span>
        </div>
        <span class="char-name">{{ char.name }}</span>
      </div>
    </div>
    <div class="sidebar-footer">
      <button class="new-char-btn" @click="addCharacter">+ 新角色</button>
    </div>
  </div>

  <!-- Editor Pane -->
  <div class="editor-pane" v-if="selectedChar">
    <div class="form-group">
      <label>ID（键名）</label>
      <input type="text" :value="selectedId" disabled class="disabled-input" />
    </div>
    <div class="form-group">
      <label>显示名称</label>
      <input type="text" :value="selectedChar.name" @change="updateName($event.target.value)" />
    </div>
    <div class="form-group">
      <label>名称颜色</label>
      <div class="color-row">
        <input type="color" :value="selectedChar.color" @input="updateColor($event.target.value)" />
        <input type="text" :value="selectedChar.color" @change="updateColor($event.target.value)" />
      </div>
    </div>

    <!-- Delete character button -->
    <button class="delete-char-btn" @click="deleteCharacter(selectedId)">删除此角色</button>

    <!-- Expression Grid -->
    <div class="expressions-section">
      <div class="section-header">
        <h3>表情列表</h3>
        <button class="import-expr-btn" @click="importExpression">+ 导入表情</button>
      </div>

      <div v-if="expressions.length === 0" class="expr-empty">
        <span>🖼️</span>
        <p>该角色暂无表情图片</p>
        <p class="hint">点击"+ 导入表情"添加</p>
      </div>

      <div v-else class="expr-grid">
        <div
          class="expr-card"
          v-for="expr in expressions"
          :key="expr.name"
          @contextmenu.prevent="showExprMenu($event, expr.name)"
        >
          <div class="expr-thumbnail">
            <img :src="expr.src" :alt="expr.name" />
          </div>
          <div class="expr-name">
            <InlineEdit
              :ref="el => setExprEditRef(expr.name, el)"
              :value="expr.name"
              @save="renameExpression(expr.name, $event)"
            />
          </div>
        </div>
      </div>
    </div>

    <input
      ref="fileInputRef"
      type="file"
      multiple
      accept="image/*"
      style="display: none;"
      @change="handleExpressionFiles"
    />
  </div>

  <!-- No selection state -->
  <div class="editor-empty" v-else>
    <p>从左侧列表选择一个角色进行编辑，<br>或点击 + 创建新角色。</p>
  </div>

  <ContextMenu
    :visible="menuVisible"
    :x="menuX"
    :y="menuY"
    :items="menuItems"
    @select="onExprMenuAction"
    @close="menuVisible = false"
  />
</div>
```

Scoped CSS per UI-SPEC §2.5:
```css
.character-editor { display: flex; height: 100%; flex: 1; min-height: 0; }

/* Sidebar */
.sidebar { width: 240px; background: #252526; border-right: 1px solid #111; display: flex; flex-direction: column; }
.sidebar-header { padding: 16px; border-bottom: 1px solid #333; display: flex; justify-content: space-between; align-items: center; }
.sidebar-title { font-size: 14px; font-weight: 400; color: #ccc; }
.add-btn { background: #007acc; color: white; border: none; width: 24px; height: 24px; border-radius: 4px; cursor: pointer; font-size: 14px; display: flex; align-items: center; justify-content: center; }
.sidebar-list { flex: 1; overflow-y: auto; }
.sidebar-empty { display: flex; flex-direction: column; align-items: center; justify-content: center; padding: 40px 16px; color: #555; font-size: 12px; gap: 8px; }
.sidebar-empty span { font-size: 32px; }
.sidebar-empty p { margin: 0; }
.sidebar-empty .hint { font-size: 11px; }

.char-item { padding: 10px 12px; display: flex; align-items: center; gap: 10px; cursor: pointer; border-bottom: 1px solid #333; transition: background 150ms ease; }
.char-item:hover { background: #2a2d2e; }
.char-item.active { background: #37373d; border-left: 3px solid #007acc; }

.char-avatar { width: 36px; height: 36px; border-radius: 50%; overflow: hidden; background: #1e1e1e; flex-shrink: 0; display: flex; align-items: center; justify-content: center; }
.char-avatar img { width: 100%; height: 100%; object-fit: cover; object-position: top; }
.avatar-fallback { font-size: 18px; color: #555; }
.char-name { font-size: 14px; color: #ccc; overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }

.sidebar-footer { padding: 12px; border-top: 1px solid #333; }
.new-char-btn { background: transparent; border: 1px dashed #555; color: #888; padding: 8px; width: 100%; border-radius: 4px; cursor: pointer; font-size: 12px; }
.new-char-btn:hover { border-color: #007acc; color: #ccc; }

/* Editor pane */
.editor-pane { flex: 1; padding: 20px 40px; overflow-y: auto; }
.editor-empty { flex: 1; display: flex; align-items: center; justify-content: center; color: #888; font-size: 14px; text-align: center; line-height: 1.8; }

.form-group { margin-bottom: 20px; display: flex; flex-direction: column; gap: 8px; }
.form-group label { font-size: 12px; color: #aaa; }
.form-group input[type="text"] { background: #3c3c3c; border: 1px solid #555; color: #fff; padding: 8px 10px; border-radius: 4px; }
.disabled-input { opacity: 0.5; cursor: not-allowed; }
.color-row { display: flex; gap: 10px; align-items: center; }
.color-row input[type="color"] { width: 40px; height: 32px; border: none; cursor: pointer; background: transparent; }
.color-row input[type="text"] { flex: 1; background: #3c3c3c; border: 1px solid #555; color: #fff; padding: 8px 10px; border-radius: 4px; }

.delete-char-btn { background: transparent; border: 1px solid #a22; color: #e66; padding: 6px 16px; border-radius: 4px; cursor: pointer; font-size: 12px; margin-bottom: 20px; }
.delete-char-btn:hover { background: #a22; color: #fff; }

/* Expression section */
.expressions-section { margin-top: 24px; background: #1e1e1e; border: 1px solid #333; border-radius: 6px; padding: 20px; }
.section-header { display: flex; justify-content: space-between; align-items: center; margin-bottom: 16px; }
.section-header h3 { margin: 0; font-weight: 400; font-size: 16px; color: #ccc; }
.import-expr-btn { background: #0e633c; color: #fff; border: none; padding: 6px 16px; border-radius: 4px; cursor: pointer; font-size: 12px; }
.import-expr-btn:hover { background: #117748; }

.expr-empty { display: flex; flex-direction: column; align-items: center; justify-content: center; padding: 40px; color: #555; font-size: 12px; gap: 8px; }
.expr-empty span { font-size: 32px; }
.expr-empty p { margin: 0; }
.expr-empty .hint { font-size: 11px; }

.expr-grid { display: grid; grid-template-columns: repeat(auto-fill, minmax(100px, 1fr)); gap: 12px; }
.expr-card { background: #252526; border: 1px solid #333; border-radius: 6px; overflow: hidden; cursor: pointer; transition: border-color 200ms ease; }
.expr-card:hover { border-color: #007acc; }
.expr-thumbnail { height: 80px; background: #1e1e1e; display: flex; align-items: center; justify-content: center; padding: 4px; }
.expr-thumbnail img { max-width: 100%; max-height: 100%; object-fit: contain; }
.expr-name { padding: 6px 8px; font-size: 12px; text-align: center; white-space: nowrap; overflow: hidden; text-overflow: ellipsis; border-top: 1px solid #333; color: #ccc; }
```
  </action>
  <verify>
    <automated>npx vite build 2>&1 | Select-String -Pattern "error" -SimpleMatch</automated>
  </verify>
  <acceptance_criteria>
    - src/editor/components/resource-library/CharacterEditor.vue no longer contains "将在后续计划中实现" (scaffold removed)
    - CharacterEditor.vue contains `useScriptStore` and `useAssetStore` imports (dual store coordination)
    - CharacterEditor.vue contains `script.data.characters` access
    - CharacterEditor.vue contains `script.pushState()` calls (at least 4: addCharacter, deleteCharacter, updateName/updateColor, renameExpression, deleteExpression, handleExpressionFiles)
    - CharacterEditor.vue contains `prompt('请输入角色 ID` for new character creation
    - CharacterEditor.vue contains `object-fit: cover` and `object-position: top` for character avatar (D-04)
    - CharacterEditor.vue contains `.avatar-fallback` with `👤` emoji for characters without expressions
    - CharacterEditor.vue contains `assets.importAssets('characters'` for expression import (ASSET-09)
    - CharacterEditor.vue contains expression path construction: `characters/${item.saved}`
    - CharacterEditor.vue contains `@contextmenu.prevent` on expression cards
    - CharacterEditor.vue contains `confirm(` for both character deletion and expression deletion
    - CharacterEditor.vue contains delete confirmation text `该角色的所有表情数据也会被移除`
    - CharacterEditor.vue contains form labels: `ID（键名）`, `显示名称`, `名称颜色`, `表情列表`
    - CharacterEditor.vue contains empty states: `暂无角色`, `从左侧列表选择一个角色`, `该角色暂无表情图片`
    - CharacterEditor.vue CSS contains `.sidebar` with `width: 240px`
    - CharacterEditor.vue CSS contains `.expr-grid` with `minmax(100px, 1fr)`
    - `npx vite build` passes
  </acceptance_criteria>
  <done>CharacterEditor implements dual-panel layout: 240px sidebar with avatar list + editor pane with name/color/ID forms + expression thumbnail grid. Expression import via file picker (not path typing). Context menu rename/delete on expressions. All mutations call pushState() for undo. Avatar uses CSS object-position: top crop (D-04). New character uses prompt() for ID. Delete character warns about expression data removal.</done>
</task>

</tasks>

<verification>
- `npx vite build` passes with zero errors
- CharacterEditor coordinates both script store (character data) and asset store (file operations)
- Sidebar shows character avatars with top-cropped first expression (D-04)
- Expression images are imported via file picker, not manual path input (ASSET-09)
- Expression paths use `characters/{filename}` format matching asset:// protocol
- All character data mutations are followed by script.pushState() for undo support
- Context menu on expressions provides rename/delete (D-06)
</verification>

<success_criteria>
- Character sidebar shows list with avatar + name (ASSET-07, D-03)
- Avatar is first expression cropped from top (D-04), fallback 👤 emoji when no expressions
- Editor pane shows ID (disabled), name, color picker, expression grid (ASSET-07)
- Expression thumbnails display in grid per character (ASSET-08)
- Expression import via file picker button (ASSET-09, D-08)
- Right-click expression → rename/delete context menu (D-06)
- New character via prompt() dialog, delete with confirmation (D-14)
- All empty states render correct Chinese text (UI-SPEC §4.4, §4.5)
</success_criteria>

<output>
After completion, create `.planning/phases/07-asset-library-ui/07-04-SUMMARY.md`
</output>
