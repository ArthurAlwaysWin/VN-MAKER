---
phase: 35-chinese-localization
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/editor/components/DialogueBoxSettings.vue
  - src/editor/components/page-editor/PageInspector.vue
  - src/editor/views/SettingsDesigner.vue
  - src/editor/views/TitleDesigner.vue
  - src/editor/components/page-editor/AudioPicker.vue
  - src/editor/components/ExportModal.vue
  - src/editor/views/Scenes.vue
  - src/editor/views/Characters.vue
autonomous: true
requirements: [L10N-02, L10N-03, L10N-04, L10N-05, L10N-06, L10N-07]

must_haves:
  truths:
    - "所有字体选择器下拉框显示中文标签（无衬线体/衬线体/等宽字体）"
    - "转场效果下拉框显示中文（淡入淡出/左滑入/右滑入/无）"
    - "AudioPicker tab 显示 BGM 和 音效"
    - "ExportModal 格式切换按钮显示 网页版 和 桌面版"
    - "所有 X (px)/Y (px) 标签显示为 X坐标/Y坐标"
    - "Characters.vue 表情占位符为纯中文示例"
  artifacts:
    - path: "src/editor/components/DialogueBoxSettings.vue"
      provides: "中文字体标签"
      contains: "无衬线体"
    - path: "src/editor/components/page-editor/PageInspector.vue"
      provides: "中文转场选项 + 中文字体标签"
      contains: "淡入淡出"
    - path: "src/editor/views/SettingsDesigner.vue"
      provides: "中文字体标签 (3处)"
      contains: "无衬线体"
    - path: "src/editor/views/TitleDesigner.vue"
      provides: "中文字体标签 (2处)"
      contains: "无衬线体"
    - path: "src/editor/components/page-editor/AudioPicker.vue"
      provides: "SE → 音效 tab 标签"
      contains: "音效"
    - path: "src/editor/components/ExportModal.vue"
      provides: "Web → 网页版 按钮"
      contains: "网页版"
    - path: "src/editor/views/Scenes.vue"
      provides: "X坐标/Y坐标 标签"
      contains: "X坐标"
    - path: "src/editor/views/Characters.vue"
      provides: "纯中文表情占位符"
      contains: "如 微笑"
  key_links:
    - from: "src/editor/components/page-editor/PageInspector.vue"
      to: "page.transition.type"
      via: "select option display text only — value attributes unchanged"
      pattern: "value=\"fade\">淡入淡出"
    - from: "src/editor/components/ExportModal.vue"
      to: "format ref"
      via: "button display text only — format value stays 'web'"
      pattern: "format === 'web'.*网页版"
---

<objective>
将编辑器中所有剩余英文 UI 文本替换为中文：字体选择器标签、转场选项、AudioPicker tab、ExportModal 格式按钮、坐标标签、表情占位符。

Purpose: 完成 L10N-02 到 L10N-07 全部本地化需求，消灭编辑器中所有面向用户的英文文字。
Output: 8 个文件的 UI 文本全部中文化，底层 value/key 不变。
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-chinese-localization/35-CONTEXT.md

@src/editor/components/DialogueBoxSettings.vue
@src/editor/components/page-editor/PageInspector.vue
@src/editor/views/SettingsDesigner.vue
@src/editor/views/TitleDesigner.vue
@src/editor/components/page-editor/AudioPicker.vue
@src/editor/components/ExportModal.vue
@src/editor/views/Scenes.vue
@src/editor/views/Characters.vue
</context>

<tasks>

<task type="auto">
  <name>Task 1: Font selector labels 中文化 (4 files, 6+ locations)</name>
  <files>
    src/editor/components/DialogueBoxSettings.vue
    src/editor/components/page-editor/PageInspector.vue
    src/editor/views/SettingsDesigner.vue
    src/editor/views/TitleDesigner.vue
  </files>
  <read_first>
    src/editor/components/DialogueBoxSettings.vue
    src/editor/components/page-editor/PageInspector.vue
    src/editor/views/SettingsDesigner.vue
    src/editor/views/TitleDesigner.vue
  </read_first>
  <action>
**Per L10N-02 / D-01:** Change all font `<option>` display text and `systemFonts` array labels. Only change the display label — NEVER change the `value` attribute.

The standard Chinese labels are:
- `Sans Serif` → `无衬线体`
- `Serif` → `衬线体`
- `Monospace` → `等宽字体`
- `Noto Sans SC` → keep `Noto Sans SC` (proper font name)
- `Noto Serif SC` → keep `Noto Serif SC` (proper font name)

**File 1: DialogueBoxSettings.vue (lines 123-129)**

Change the `systemFonts` array from:
```js
const systemFonts = [
  { label: 'Noto Sans SC', value: "'Noto Sans SC', sans-serif" },
  { label: 'Noto Serif SC', value: "'Noto Serif SC', serif" },
  { label: 'Sans Serif', value: 'sans-serif' },
  { label: 'Serif', value: 'serif' },
  { label: 'Monospace', value: 'monospace' },
];
```
To:
```js
const systemFonts = [
  { label: 'Noto Sans SC', value: "'Noto Sans SC', sans-serif" },
  { label: 'Noto Serif SC', value: "'Noto Serif SC', serif" },
  { label: '无衬线体', value: 'sans-serif' },
  { label: '衬线体', value: 'serif' },
  { label: '等宽字体', value: 'monospace' },
];
```

**File 2: PageInspector.vue (lines 403-409)**

Change the `systemFonts` array from:
```js
const systemFonts = [
  { label: 'Noto Sans SC', value: "'Noto Sans SC', sans-serif" },
  { label: 'Noto Serif SC', value: "'Noto Serif SC', serif" },
  { label: 'Sans Serif', value: 'sans-serif' },
  { label: 'Serif', value: 'serif' },
  { label: 'Monospace', value: 'monospace' },
];
```
To:
```js
const systemFonts = [
  { label: 'Noto Sans SC', value: "'Noto Sans SC', sans-serif" },
  { label: 'Noto Serif SC', value: "'Noto Serif SC', serif" },
  { label: '无衬线体', value: 'sans-serif' },
  { label: '衬线体', value: 'serif' },
  { label: '等宽字体', value: 'monospace' },
];
```

**File 3: SettingsDesigner.vue — 3 inline `<select>` blocks**

There are 3 font select dropdowns (around lines 150-155, 202-207, 255-260). In each block, change the option display text:

```html
<!-- Before (each of 3 blocks) -->
<option value="sans-serif">Sans Serif</option>
<option value="'Noto Sans SC', sans-serif">Noto Sans SC</option>
<option value="'Noto Serif SC', serif">Noto Serif SC</option>
<option value="serif">Serif</option>
<option value="monospace">Monospace</option>
```

```html
<!-- After (each of 3 blocks) -->
<option value="sans-serif">无衬线体</option>
<option value="'Noto Sans SC', sans-serif">Noto Sans SC</option>
<option value="'Noto Serif SC', serif">Noto Serif SC</option>
<option value="serif">衬线体</option>
<option value="monospace">等宽字体</option>
```

Repeat for ALL 3 occurrences. Do NOT change any `value` attribute.

**File 4: TitleDesigner.vue — 2 inline `<select>` blocks**

There are 2 font select dropdowns (around lines 140-145 and 189-194). In each block:

```html
<!-- Before (each of 2 blocks) -->
<option value="sans-serif">Sans Serif</option>
<option value="'Noto Sans SC', sans-serif">Noto Sans SC</option>
<option value="'Noto Serif SC', serif">Noto Serif SC</option>
<option value="serif">Serif</option>
<option value="monospace">Monospace</option>
```

```html
<!-- After (each of 2 blocks) -->
<option value="sans-serif">无衬线体</option>
<option value="'Noto Sans SC', sans-serif">Noto Sans SC</option>
<option value="'Noto Serif SC', serif">Noto Serif SC</option>
<option value="serif">衬线体</option>
<option value="monospace">等宽字体</option>
```

Repeat for BOTH occurrences. Do NOT change any `value` attribute.
  </action>
  <verify>
    <automated>cd E:\projects\my-awesome-project && Select-String -Path "src/editor/components/DialogueBoxSettings.vue","src/editor/components/page-editor/PageInspector.vue","src/editor/views/SettingsDesigner.vue","src/editor/views/TitleDesigner.vue" -Pattern "Sans Serif|>Serif<|>Monospace<" | Measure-Object | Select-Object -ExpandProperty Count</automated>
  </verify>
  <acceptance_criteria>
    - grep for `>Sans Serif<` returns 0 matches across all 4 files (English label removed)
    - grep for `>Monospace<` returns 0 matches across all 4 files
    - DialogueBoxSettings.vue contains `label: '无衬线体'`
    - DialogueBoxSettings.vue contains `label: '等宽字体'`
    - PageInspector.vue contains `label: '无衬线体'`
    - PageInspector.vue contains `label: '等宽字体'`
    - SettingsDesigner.vue contains `>无衬线体</option>` (3 occurrences)
    - SettingsDesigner.vue contains `>等宽字体</option>` (3 occurrences)
    - TitleDesigner.vue contains `>无衬线体</option>` (2 occurrences)
    - TitleDesigner.vue contains `>等宽字体</option>` (2 occurrences)
    - No `value` attributes were changed (all values still english: `sans-serif`, `serif`, `monospace`, etc.)
  </acceptance_criteria>
  <done>All 6+ font selector locations across 4 files display Chinese labels. Value attributes unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Transition + AudioPicker + ExportModal + Coordinates + Characters 中文化</name>
  <files>
    src/editor/components/page-editor/PageInspector.vue
    src/editor/components/page-editor/AudioPicker.vue
    src/editor/components/ExportModal.vue
    src/editor/views/Scenes.vue
    src/editor/views/Characters.vue
  </files>
  <read_first>
    src/editor/components/page-editor/PageInspector.vue
    src/editor/components/page-editor/AudioPicker.vue
    src/editor/components/ExportModal.vue
    src/editor/views/Scenes.vue
    src/editor/views/Characters.vue
  </read_first>
  <action>
**A. PageInspector.vue — Transition options 中文化 (L10N-03, per D-03c)**

Around lines 26-29, change the transition `<option>` display text only (keep `value` unchanged):

```html
<!-- Before -->
<option value="fade">fade</option>
<option value="slide-left">slide-left</option>
<option value="slide-right">slide-right</option>
<option value="none">none</option>
```

```html
<!-- After -->
<option value="fade">淡入淡出</option>
<option value="slide-left">左滑入</option>
<option value="slide-right">右滑入</option>
<option value="none">无</option>
```

**B. AudioPicker.vue — SE tab label 中文化 (L10N-04, per D-02)**

Line 12, change the SE tab button text:

```html
<!-- Before -->
<button class="tab-btn" :class="{ active: activeTab === 'se' }" @click="switchTab('se')">SE</button>
```

```html
<!-- After -->
<button class="tab-btn" :class="{ active: activeTab === 'se' }" @click="switchTab('se')">音效</button>
```

Note: The BGM button on line 11 stays as `BGM` (per D-02: BGM 认知度高，保留).

**C. ExportModal.vue — Web button label 中文化 (L10N-05, per D-03d)**

Line 13, change the Web format button text:

```html
<!-- Before -->
<button
  :class="['format-btn', { active: format === 'web' }]"
  @click="format = 'web'"
>Web</button>
```

```html
<!-- After -->
<button
  :class="['format-btn', { active: format === 'web' }]"
  @click="format = 'web'"
>网页版</button>
```

Note: The `format` ref value remains `'web'` — only the display text changes. The `桌面版` button is already Chinese.

**D. Scenes.vue — Coordinate labels 中文化 (L10N-06, per D-03a)**

There are 3 pairs of X/Y coordinate labels. Change ALL 6 occurrences:

Around line 120: `<label>X (px)</label>` → `<label>X坐标</label>`
Around line 124: `<label>Y (px)</label>` → `<label>Y坐标</label>`

Around line 191: `<label>X (px)</label>` → `<label>X坐标</label>`
Around line 195: `<label>Y (px)</label>` → `<label>Y坐标</label>`

Around line 258: `<label>X (px)</label>` → `<label>X坐标</label>`
Around line 262: `<label>Y (px)</label>` → `<label>Y坐标</label>`

**E. Characters.vue — Expression placeholder 中文化 (L10N-07)**

Line 54, change the expression name placeholder:

```html
<!-- Before -->
placeholder="表情名称（如 smile）"
```

```html
<!-- After -->
placeholder="表情名称（如 微笑）"
```

The other placeholder on line 60 (`图片路径（如 characters/sakura_smile.png）`) stays as-is because it's a file path format example (per D-03b: 保留英文值格式).
  </action>
  <verify>
    <automated>cd E:\projects\my-awesome-project && Select-String -Path "src/editor/components/page-editor/PageInspector.vue" -Pattern ">fade<|>slide-left<|>slide-right<|>none<" | Measure-Object | Select-Object -ExpandProperty Count</automated>
  </verify>
  <acceptance_criteria>
    - PageInspector.vue contains `>淡入淡出</option>` and `>左滑入</option>` and `>右滑入</option>` and `>无</option>`
    - PageInspector.vue does NOT contain `>fade</option>` or `>slide-left</option>` or `>slide-right</option>` or `>none</option>`
    - PageInspector.vue transition option `value` attributes remain unchanged: `value="fade"`, `value="slide-left"`, `value="slide-right"`, `value="none"`
    - AudioPicker.vue contains `>音效</button>` (was SE)
    - AudioPicker.vue still contains `>BGM</button>` (unchanged)
    - ExportModal.vue contains `>网页版</button>` (was Web)
    - ExportModal.vue format button still uses `format = 'web'` (value unchanged)
    - Scenes.vue contains `<label>X坐标</label>` (3 occurrences)
    - Scenes.vue contains `<label>Y坐标</label>` (3 occurrences)
    - Scenes.vue does NOT contain `X (px)` or `Y (px)`
    - Characters.vue contains `placeholder="表情名称（如 微笑）"`
    - Characters.vue does NOT contain `如 smile`
  </acceptance_criteria>
  <done>All transition/audio/export/coordinate/placeholder text displays Chinese. No English-facing labels remain. All underlying values/keys unchanged.</done>
</task>

</tasks>

<verification>
1. Open page editor → transition dropdown shows 淡入淡出/左滑入/右滑入/无
2. Open AudioPicker → tabs show BGM and 音效
3. Open ExportModal → format buttons show 网页版 and 桌面版
4. Open Scenes editor → coordinate labels show X坐标/Y坐标
5. Open Characters → expression placeholder shows 表情名称（如 微笑）
6. Open any font dropdown → shows 无衬线体/衬线体/等宽字体
7. No JavaScript errors in console
</verification>

<success_criteria>
- Zero English-facing labels in font selectors, transition options, AudioPicker tabs, ExportModal buttons, coordinate labels, or expression placeholders
- All underlying data values (transition type, audio tab key, export format, etc.) remain English strings
- No JavaScript errors — all features function identically to before
</success_criteria>

<output>
After completion, create `.planning/phases/35-chinese-localization/35-02-SUMMARY.md`
</output>
