---
phase: 43-screenlayouts
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ui/SaveLoadScreen.js
autonomous: true
requirements:
  - SCREEN-01
  - COMPAT-02

must_haves:
  truths:
    - "SaveLoadScreen.setLayout(config) stores config; show() consumes it for rendering"
    - "Config with background/backdropBlur overrides screen background and blur"
    - "Config with header overrides title text, title color, header background image, header height"
    - "Config with slotGrid changes grid columns/rows/gap/position, dynamically computing slots-per-page and total pages"
    - "Config with slot overrides card background/border/borderRadius/emptyText/thumbnailRadius"
    - "Config with pagination overrides colors and supports dots style"
    - "setLayout(null) produces identical rendering to current hardcoded behavior (COMPAT-02)"
  artifacts:
    - path: "src/ui/SaveLoadScreen.js"
      provides: "Configurable 108-slot save/load screen with setLayout API"
      exports: ["SaveLoadScreen"]
      contains: "setLayout"
  key_links:
    - from: "SaveLoadScreen.setLayout"
      to: "this._layoutConfig"
      via: "stores config reference"
      pattern: "_layoutConfig"
    - from: "SaveLoadScreen._render"
      to: "this._layoutConfig"
      via: "branches rendering on config presence"
      pattern: "this\\._layoutConfig"
    - from: "SaveLoadScreen"
      to: "sanitizeCssValue"
      via: "guards all string config values before CSS"
      pattern: "sanitizeCssValue"
    - from: "SaveLoadScreen"
      to: "resolvePath"
      via: "resolves backgroundImage asset paths"
      pattern: "resolvePath"
---

<objective>
Add setLayout(config) to SaveLoadScreen, enabling configurable background, header, grid layout, slot styling, and pagination from ui.saveLoadScreen config. When config is null, rendering is identical to current hardcoded behavior (COMPAT-02).

Purpose: SCREEN-01 — make SaveLoadScreen appearance fully data-driven via script.json config.
Output: Modified src/ui/SaveLoadScreen.js with setLayout API and config-branched rendering.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/43-screenlayouts/43-CONTEXT.md

@src/ui/SaveLoadScreen.js
@src/ui/sanitize.js
@src/engine/assetPath.js
@src/ui/SettingsScreen.js (reference — setLayout pattern + background image layer pattern)

<interfaces>
<!-- Current SaveLoadScreen public API — preserve these signatures unchanged -->
From src/ui/SaveLoadScreen.js:

  export class SaveLoadScreen {
    constructor(container, saveManager)   // DO NOT change (per CONTEXT decision #1)
    show(mode = 'save', source = 'bar')  // calls _render(), shows screen
    hide(skipRoute = false)               // hides screen, optionally routes close
    // Callbacks: onSave, onLoad, onDelete, onClose
    // Private: _render(), _renderGrid(), _renderPagination(), _createSlotCard()
    // Private: _attachKeyboard(), _detachKeyboard(), _showConfirmation(), _clearConfirmation()
  }
  // Module constants: SLOTS_PER_PAGE = 9, TOTAL_PAGES = 12

<!-- Sanitization utilities — use for ALL config string/numeric values -->
From src/ui/sanitize.js:

  export function sanitizeCssValue(value)    // string|undefined — rejects CSS injection
  export function clampField(field, value)   // clamps: x[-300,1580] y[-300,1020] width[1,2560] height[1,1440] borderRadius[0,500] padding[0,200]
  export function clamp(value, min, max)     // generic numeric clamp

<!-- Asset path resolution — use for ALL image paths from config -->
From src/engine/assetPath.js:

  export function resolvePath(path)  // resolves asset path for current environment

<!-- Design spec schema (Section 5.1) — the config shape setLayout receives -->

  saveLoadScreen config shape:
    background: null               — CSS color string e.g. "rgba(0,0,0,0.85)"
    backdropBlur: 12               — number, pixels
    header.saveTitle: "存 档"       — save mode title text
    header.loadTitle: "読 档"       — load mode title text
    header.saveTitleColor: null     — CSS color
    header.loadTitleColor: null     — CSS color
    header.backgroundImage: null    — asset path
    header.height: 70              — pixels
    slotGrid.columns: 3            — grid columns (determines slotsPerPage with rows)
    slotGrid.rows: 3               — grid rows
    slotGrid.gap: 12               — grid gap px
    slotGrid.x: 60                 — absolute left px
    slotGrid.y: 90                 — absolute top px
    slotGrid.width: 1160           — grid container width px
    slotGrid.height: 540           — grid container height px
    slot.background: "rgba(30,30,50,0.6)" — CSS background
    slot.backgroundImage: null      — asset path for slot card bg
    slot.borderRadius: 6           — px
    slot.border: "1px solid rgba(255,255,255,0.1)" — CSS border shorthand
    slot.emptyText: "— 空 —"       — empty slot placeholder
    slot.thumbnailRadius: 4        — px
    pagination.style: "dots"        — "dots" = small circles; default/missing = numbered tabs
    pagination.activeColor: null    — CSS color for active page
    pagination.inactiveColor: null  — CSS color for inactive pages

<!-- Current CSS defaults that config inline styles override -->

  #save-load-screen: background var(--gm-panel-bg, rgba(10,10,20,0.95)); backdrop-filter blur(var(--gm-blur, 8px))
  .save-load-grid: grid repeat(3, 1fr); gap 16px; padding 24px 40px; flex 1
  .save-slot: background var(--gm-card-bg, rgba(30,30,50,0.6)); border-radius var(--gm-radius-lg, 6px)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add setLayout + config-branched background, header, and pagination</name>
  <files>src/ui/SaveLoadScreen.js</files>
  <action>
Per SCREEN-01 and COMPAT-02 (CONTEXT decisions #1-#4: setLayout method, store-on-set/apply-on-show, inline defaults, config branching).

1. ADD IMPORT after the existing resolvePath import:
   import { sanitizeCssValue, clampField, clamp } from './sanitize.js';

2. ADD FIELD in constructor (after this._cachedSlots = null):
   this._layoutConfig = null;

3. ADD setLayout() METHOD in Public API section (after hide()):
   setLayout(config) { this._layoutConfig = config || null; }
   Include JSDoc: @param {object|null} config — null preserves hardcoded defaults (COMPAT-02)

4. ADD COMPUTED GETTERS after setLayout, before the Render section comment:
   get _slotsPerPage() — returns (layoutConfig.slotGrid.columns||3)*(rows||3) or SLOTS_PER_PAGE
   get _totalPages() — returns Math.ceil(108 / this._slotsPerPage)

5. MODIFY _render():
   - At top: const cfg = this._layoutConfig;
   - Make title config-aware:
       const title = this.mode === 'save' ? (cfg?.header?.saveTitle ?? '存 档') : (cfg?.header?.loadTitle ?? '読 档');
   - Keep existing innerHTML template and close-button listener UNCHANGED
   - AFTER close-button addEventListener, add config block:
     if (cfg) {
       - Background: sanitizeCssValue(cfg.background) -> this.el.style.background
       - BackdropBlur: clamp(cfg.backdropBlur, 0, 50) -> this.el.style.backdropFilter = 'blur(Npx)'
       - Header bg image: sanitizeCssValue + resolvePath -> header.style.backgroundImage with cover/center
       - Header height: clampField('height', cfg.header.height) -> header.style.minHeight
       - Title color: mode-dependent saveTitleColor/loadTitleColor -> sanitizeCssValue -> titleEl.style.color
     }

6. MODIFY _renderPagination():
   - Replace TOTAL_PAGES with this._totalPages in for-loop
   - Inside loop, after creating btn, add pagination config:
     const pCfg = this._layoutConfig?.pagination;
     if (pCfg) {
       - If pCfg.style === 'dots': change className to 'page-dot' + active, clear textContent, set inline styles (width:10px, height:10px, borderRadius:50%, display:inline-block, margin:0 4px, padding:0, minWidth:auto, border:none)
       - Apply activeColor/inactiveColor: sanitizeCssValue -> btn.style.backgroundColor for active/inactive states
     }

7. MODIFY _attachKeyboard():
   - Replace TOTAL_PAGES with this._totalPages in the ArrowRight bounds check

COMPAT-02 GUARANTEE: When _layoutConfig is null:
- Title: ?? fallback = '存 档'/'読 档' (identical)
- if (cfg) block skipped — no style overrides
- _totalPages = Math.ceil(108/9) = 12 = TOTAL_PAGES
- Zero rendering difference
  </action>
  <verify>
    <automated>node --check src/ui/SaveLoadScreen.js</automated>
  </verify>
  <done>
    - setLayout(config) method exists, stores config
    - _render() applies background/header when config present
    - _renderPagination() supports dots style and custom colors
    - Computed _totalPages works correctly
    - Keyboard navigation respects computed _totalPages
    - Null config = identical rendering
  </done>
</task>

<task type="auto">
  <name>Task 2: Config-branched grid layout and slot styling</name>
  <files>src/ui/SaveLoadScreen.js</files>
  <action>
Continues from Task 1 — _renderGrid and _createSlotCard config branching.

1. MODIFY _renderGrid():
   - Replace slot range with computed values:
       const startSlot = (this._currentPage - 1) * this._slotsPerPage + 1;
       const endSlot = Math.min(this._currentPage * this._slotsPerPage, 108);
   - After grid.innerHTML = '', apply grid config:
     const gCfg = this._layoutConfig?.slotGrid;
     if (gCfg) {
       - gridTemplateColumns: 'repeat(' + (gCfg.columns||3) + ', 1fr)'
       - gridTemplateRows: 'repeat(' + (gCfg.rows||3) + ', 1fr)'
       - gap: clampField('padding', gCfg.gap) + 'px'
       - If position/size fields set: position absolute, left/top/width/height via clampField,
         flex:none, padding:0 (overrides CSS flex:1 and padding)
     }

2. MODIFY _createSlotCard():
   - At top: const sCfg = this._layoutConfig?.slot;

   - FOR EMPTY SLOTS: replace innerHTML with programmatic span:
       const span = document.createElement('span');
       span.className = 'save-slot-empty-text';
       span.textContent = sCfg?.emptyText ?? '— 空 —';
       slotEl.appendChild(span);

   - FOR OCCUPIED SLOTS: after innerHTML, apply thumbnailRadius:
       if (sCfg?.thumbnailRadius != null) {
         const thumb = slotEl.querySelector('.save-slot-thumb');
         if (thumb) thumb.style.borderRadius = clampField('borderRadius', sCfg.thumbnailRadius) + 'px';
       }

   - FOR ALL CARDS (before return slotEl): apply shared slot styling:
       if (sCfg) {
         - background: sanitizeCssValue(sCfg.background) -> slotEl.style.background
         - backgroundImage: sanitizeCssValue + resolvePath -> child div layer (position absolute, inset 0, zIndex 0, cover/center, borderRadius inherit, pointerEvents none); set slotEl position relative
         - borderRadius: clampField('borderRadius', sCfg.borderRadius) -> slotEl.style.borderRadius
         - border: sanitizeCssValue(sCfg.border) -> slotEl.style.border
       }

COMPAT-02 GUARANTEE: When _layoutConfig is null:
- _slotsPerPage = 9, same range as SLOTS_PER_PAGE
- gCfg undefined — no grid overrides
- sCfg undefined — no slot overrides
- emptyText: ?? '— 空 —' (identical)
- Only structural change: innerHTML to createElement for empty text (same DOM output)
  </action>
  <verify>
    <automated>node --check src/ui/SaveLoadScreen.js</automated>
  </verify>
  <done>
    - Grid responds to slotGrid config (columns/rows/gap/position/size)
    - Dynamic slots-per-page from columns x rows
    - Slots respond to slot config (background/border/radius)
    - Thumbnails respond to thumbnailRadius
    - Empty slots use configurable emptyText via textContent (XSS-safe)
    - Null config = identical rendering
  </done>
</task>

</tasks>

<verification>
1. node --check src/ui/SaveLoadScreen.js — no syntax errors
2. Class exports SaveLoadScreen with setLayout method
3. setLayout(null) then show('save') — identical to current
4. setLayout with config then show — config values applied
5. All strings through sanitizeCssValue, all numbers through clampField/clamp
6. All images through resolvePath
</verification>

<success_criteria>
- SaveLoadScreen has setLayout(config) method (SCREEN-01)
- Config drives: background, backdropBlur, header, slotGrid, slot, pagination
- setLayout(null) = zero visual change (COMPAT-02)
- All values sanitized
</success_criteria>

<output>
After completion, create .planning/phases/43-screenlayouts/43-01-SUMMARY.md
</output>
