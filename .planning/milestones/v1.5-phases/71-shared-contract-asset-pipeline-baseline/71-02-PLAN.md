---
phase: 71-shared-contract-asset-pipeline-baseline
plan: 02
type: execute
wave: 2
depends_on:
  - "71-01"
files_modified:
  - src/editor/components/theme/NineSliceModal.vue
  - src/engine/ThemeManager.js
  - tests/themeManagerUiImage.test.js
  - tests/uiImageFieldFlow.test.js
autonomous: true
requirements: [AST-01, AST-02, AST-05, AST-06]

must_haves:
  truths:
    - "NineSlice 的新图选择走标准 `assets/ui/` 资产通路，配置里只留下 canonical `ui/...` 路径，满足 D-01、D-02、D-03。"
    - "旧 nine-slice data URL 与旧路径仍能被读取并应用，不会因为 Phase 71 改写而崩掉，满足 D-04。"
    - "ThemeManager 会在真正使用 nine-slice 图片前先 resolve 相对路径，修复 research 已确认的断点，且只在显式重选后把 legacy 值改写成 canonical 路径，满足 D-05。"
  artifacts:
    - path: "src/editor/components/theme/NineSliceModal.vue"
      provides: "NineSlice 标准选图/清除入口"
      contains: "pickUiImage"
    - path: "src/engine/ThemeManager.js"
      provides: "NineSlice normal/hover/active 统一 resolvePath 读取"
      contains: "resolvePath"
    - path: "tests/themeManagerUiImage.test.js"
      provides: "NineSlice relative-path/data-url 兼容回归"
      min_lines: 80
  key_links:
    - from: "src/editor/components/theme/NineSliceModal.vue"
      to: "src/editor/utils/uiImageField.js"
      via: "modal 的选择/清除逻辑复用共享 helper"
      pattern: "pickUiImage|clearUiImage"
    - from: "src/engine/ThemeManager.js"
      to: "src/engine/assetPath.js"
      via: "nine-slice normal/hover/active 图片在生成 CSS 前统一 resolve"
      pattern: "resolvePath\\("
    - from: "src/editor/components/theme/NineSliceModal.vue"
      to: "src/editor/composables/useThemeEditor.js"
      via: "仍复用既有 theme preview/commit owner，不引入第二套预览链路"
      pattern: "sendThemeToPreview|commitTheme"
---

<objective>
打通主题 nine-slice 的标准写入与兼容读取。

Purpose: 先修掉 research 已定位的 ThemeManager 相对路径断点，再把 nine-slice 从历史 data URL 写法切到 Phase 71 的统一资产通路。
Output: 可写 canonical `ui/...`、可读 legacy data URL/旧路径的 nine-slice 流程与回归测试。
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/71-shared-contract-asset-pipeline-baseline/71-CONTEXT.md
@.planning/phases/71-shared-contract-asset-pipeline-baseline/71-RESEARCH.md
@src/editor/components/theme/NineSliceModal.vue
@src/engine/ThemeManager.js
@src/engine/assetPath.js
@src/editor/composables/useThemeEditor.js
@.planning/phases/71-shared-contract-asset-pipeline-baseline/71-01-PLAN.md

<interfaces>
Expected from src/editor/utils/uiImageField.js after 71-01:
```javascript
export async function pickUiImage(options) {}
export function clearUiImage(options) {}
export function getUiImageDisplayValue(value) {}
```

From src/engine/assetPath.js:
```javascript
export function resolvePath(path) {}
```

From src/editor/composables/useThemeEditor.js:
```javascript
function sendThemeToPreview() {}
function commitTheme() {}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 先写 nine-slice 读取红测并修复 ThemeManager 相对路径断点</name>
  <files>
    src/engine/ThemeManager.js
    tests/themeManagerUiImage.test.js
  </files>
  <behavior>
    - Test 1: canonical `ui/...` 的 nine-slice normal/hover/active 图片都会先经过 `resolvePath()` 再写入 CSS。
    - Test 2: 历史 data URL 仍被原样保留并可继续用于 border-image，不会被错误拼接成 `asset://data:...`。
    - Test 3: 未配置 hover/active 时不生成坏掉的 state rule，保持现有按钮行为稳定。
  </behavior>
  <action>先在 `tests/themeManagerUiImage.test.js` 写 RED，直接覆盖 research 已确认的 break：`ThemeManager` 当前没有在 nine-slice normal/hover/active 上 resolve 相对 UI 路径。然后在 `src/engine/ThemeManager.js` 引入 `resolvePath()`，对 `config.src`、`config.states.hover.src`、`config.states.active.src` 一律先 resolve，再生成 CSS；同时保持 data URL/HTTP 的 legacy pass-through 行为，不修改 nine-slice selector map，不扩面到 Phase 72-75 的 surface DOM rollout。</action>
  <verify>
    <automated>npx vitest run tests/themeManagerUiImage.test.js</automated>
  </verify>
  <done>ThemeManager 已能正确消费 canonical `ui/...` 与 legacy data URL，nine-slice 读取断点被 focused test 锁住。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 把 NineSliceModal 从 data URL 写法切到标准选图 helper</name>
  <files>
    src/editor/components/theme/NineSliceModal.vue
    tests/uiImageFieldFlow.test.js
  </files>
  <behavior>
    - Test 1: NineSliceModal 不再以 `FileReader.readAsDataURL()` 作为持久化主路径，而是复用 71-01 helper 走标准选图。
    - Test 2: 旧值显示/预览仍可读，但只有用户显式重选时才会把 legacy 值改写成 canonical `ui/...`。
    - Test 3: clear、preview、commit 仍复用既有 `useThemeEditor` owner，不新增第二套 preview state。
  </behavior>
  <action>扩展 `tests/uiImageFieldFlow.test.js` 或在同文件内新增 NineSliceModal wiring 覆盖，然后更新 `src/editor/components/theme/NineSliceModal.vue`：把 normal/hover/active 图片入口统一改为调用 `src/editor/utils/uiImageField.js` 的 helper，底层走 `selectAsset(['ui'])` 满足 D-01、D-02、D-03；保留 legacy 值显示与清空能力，但不做静默迁移，只有显式重选后才重写为 canonical `ui/...` 满足 D-04、D-05。显式避免：改 `.gmtheme`、新增假预览、或顺手做对话框图片层 DOM/名牌/装饰 rollout（这些留给 Phase 72）。</action>
  <verify>
    <automated>npx vitest run tests/uiImageFieldFlow.test.js tests/themeManagerUiImage.test.js tests/dialogueBoxNameplate.test.js &amp;&amp; npm run build</automated>
  </verify>
  <done>NineSliceModal 已接入标准 UI 图片选择通路，旧值仍可读，显式重选后才写成 canonical 路径，且对话框相关现有行为未被回归。</done>
</task>

</tasks>

<verification>
同时跑 nine-slice helper 流程、ThemeManager 路径解析与现有对话框相关回归，确认 “可写 canonical + 可读 legacy + 预览不坏” 三件事一起成立。
</verification>

<success_criteria>
- NineSliceModal 不再把 base64 data URL 当成新写入的标准格式。
- ThemeManager 能稳定消费 canonical 相对路径与 legacy data URL。
- 重选才改写、未重选不迁移的兼容边界被测试锁定。
</success_criteria>

<output>
After completion, create `.planning/phases/71-shared-contract-asset-pipeline-baseline/71-02-SUMMARY.md`
</output>
