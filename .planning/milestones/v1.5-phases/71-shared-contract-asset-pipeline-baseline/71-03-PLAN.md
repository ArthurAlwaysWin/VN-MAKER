---
phase: 71-shared-contract-asset-pipeline-baseline
plan: 03
type: execute
wave: 2
depends_on:
  - "71-01"
files_modified:
  - src/editor/components/layout/DecorationSection.vue
  - src/editor/components/layout/PanelBackgroundSection.vue
  - src/editor/components/layout/BacklogSection.vue
  - src/editor/components/layout/GameMenuSection.vue
  - src/editor/components/layout/SaveLoadSection.vue
  - src/engine/scanAssets.js
  - electron/exportGame.js
  - electron/exportDesktop.js
  - tests/screenUiImageEntryWiring.test.js
  - tests/scanAssets.test.js
  - tests/exportGame.test.js
  - tests/exportDesktop.test.js
autonomous: true
requirements: [AST-01, AST-05, AST-06]

must_haves:
  truths:
    - "screen/decor 现有图片字段改走统一选图 helper，legacy 值仍能显示与运行，只有显式重选才改写为 canonical `ui/...`，满足 D-01、D-03、D-05。"
    - "scan/export 通过 registry 集中识别 UI 图片字段，并在导出时复制 `assetDict.ui`，满足 D-10、D-11。"
    - "Phase 71 只建立资产通路与注册边界，不顺手做 surface DOM rollout 或 `.gmtheme` 格式升级，满足 D-12。"
  artifacts:
    - path: "tests/screenUiImageEntryWiring.test.js"
      provides: "screen/decor 标准选图接线回归"
      min_lines: 100
    - path: "src/engine/scanAssets.js"
      provides: "registry-driven UI image scan foundation with `ui` bucket"
      contains: "ui:"
    - path: "electron/exportGame.js"
      provides: "Web 导出复制 UI 图片 bucket"
      contains: "assetDict.ui"
    - path: "electron/exportDesktop.js"
      provides: "Desktop 导出复制 UI 图片 bucket"
      contains: "assetDict.ui"
  key_links:
    - from: "src/editor/components/layout/DecorationSection.vue"
      to: "src/editor/utils/uiImageField.js"
      via: "装饰图改为标准选图/清除流程"
      pattern: "pickUiImage|clearUiImage"
    - from: "src/engine/scanAssets.js"
      to: "src/shared/uiImageContract.js"
      via: "UI 图片 collector 走共享 registry，而不是继续堆散落 if"
      pattern: "UI_IMAGE_SCAN_REGISTRY|collect"
    - from: "electron/exportGame.js"
      to: "src/engine/scanAssets.js"
      via: "导出复制列表显式纳入 `assetDict.ui`"
      pattern: "assetDict\\.ui"
    - from: "electron/exportDesktop.js"
      to: "src/engine/scanAssets.js"
      via: "桌面导出复制列表显式纳入 `assetDict.ui`"
      pattern: "assetDict\\.ui"
---

<objective>
收口 screen/decor 旧写入口，并把 UI 图片纳入 scan/export 注册基线。

Purpose: 让 SaveLoad、Backlog、GameMenu、Settings 与页头装饰等现有入口都能遵守同一条 UI 图片规则，同时补齐导出不会漏图的前置门。
Output: screen/decor 标准选图接线、UI 图片 scan registry 与导出复制覆盖。
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/71-shared-contract-asset-pipeline-baseline/71-CONTEXT.md
@.planning/phases/71-shared-contract-asset-pipeline-baseline/71-RESEARCH.md
@src/editor/components/layout/DecorationSection.vue
@src/editor/components/layout/PanelBackgroundSection.vue
@src/editor/components/layout/BacklogSection.vue
@src/editor/components/layout/GameMenuSection.vue
@src/editor/components/layout/SaveLoadSection.vue
@src/engine/scanAssets.js
@electron/exportGame.js
@electron/exportDesktop.js
@.planning/phases/71-shared-contract-asset-pipeline-baseline/71-01-PLAN.md

<interfaces>
Expected from src/editor/utils/uiImageField.js after 71-01:
```javascript
export async function pickUiImage(options) {}
export function clearUiImage(options) {}
export function getUiImageDisplayValue(value) {}
```

From src/editor/composables/useScreenLayoutEditor.js:
```javascript
function getActiveScreenConfig() {}
function sendScreenLayoutToPreview(screenId) {}
function commitScreenLayout() {}
function setScreenField(field, value) {}
function setScreenNestedField(group, field, value) {}
```

Expected from src/shared/uiImageContract.js after 71-01:
```javascript
export const UI_IMAGE_SCAN_REGISTRY = [];
export function collectUiImagePaths(script, add) {}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 先写 screen/decor 接线红测并替换 legacy 文本写入口</name>
  <files>
    src/editor/components/layout/DecorationSection.vue
    src/editor/components/layout/PanelBackgroundSection.vue
    src/editor/components/layout/BacklogSection.vue
    src/editor/components/layout/GameMenuSection.vue
    src/editor/components/layout/SaveLoadSection.vue
    tests/screenUiImageEntryWiring.test.js
  </files>
  <behavior>
    - Test 1: DecorationSection、PanelBackgroundSection、BacklogSection、GameMenuSection、SaveLoadSection 的图片字段改为复用共享 helper，而不是继续把 `<input type="text">` 当主写入口。
    - Test 2: 这些 section 仍复用 `useScreenLayoutEditor` 的 preview/commit owner；非图片字段行为不被顺手改坏。
    - Test 3: legacy 路径值仍可展示，只有显式选择新图时才改写为 canonical `ui/...`。
  </behavior>
  <action>先在 `tests/screenUiImageEntryWiring.test.js` 写 RED，锁定 D-01、D-03、D-05、D-09 的接线边界，再改 5 个 section。对 `DecorationSection.vue` 的 `deco.src`、`PanelBackgroundSection.vue` 的 `settingsScreen.background`、`BacklogSection.vue` 的 `backgroundImage/header.backgroundImage`、`GameMenuSection.vue` 的 `backgroundImage`、`SaveLoadSection.vue` 的 `background/header.backgroundImage`，统一接入 71-01 的 helper，实现“选择图片 / 清除图片 + 只读展示当前值”的标准流程；继续调用 `sendScreenLayoutToPreview()` / `commitScreenLayout()`，不要扩到 screen 装饰层 DOM、本地假预览或 cursor/icon scope。</action>
  <verify>
    <automated>node --test tests/decorLayoutEditor.test.js &amp;&amp; npx vitest run tests/screenUiImageEntryWiring.test.js tests/saveLoadScreenLayout.test.js tests/backlogScreenLayout.test.js tests/gameMenuLayout.test.js</automated>
  </verify>
  <done>所有已知 screen/decor legacy 写入口都已切到标准选图 helper，直接影响的现有 layout 行为回归仍为绿色。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 先写 scan/export 红测并纳入 UI 图片 registry 与导出复制</name>
  <files>
    src/engine/scanAssets.js
    electron/exportGame.js
    electron/exportDesktop.js
    tests/scanAssets.test.js
    tests/exportGame.test.js
    tests/exportDesktop.test.js
  </files>
  <behavior>
    - Test 1: `scanAssets()` 在保持现有 5 类行为稳定的前提下新增 `ui` bucket，并通过 registry 收集当前 runtime 已支持的 UI 图片字段。
    - Test 2: data URL / http / https 仍被过滤；canonical `ui/...` 与 legacy 字符串路径会进入 `ui` bucket。
    - Test 3: `exportGame` / `exportDesktop` 会复制 `assetDict.ui`，缺失 UI 资源时同样走 warnings，不回退到漏图静默成功。
  </behavior>
  <action>按 D-07、D-10、D-11、D-12 更新 `src/engine/scanAssets.js`、`electron/exportGame.js`、`electron/exportDesktop.js`，并先扩展 `tests/scanAssets.test.js`、`tests/exportGame.test.js`、`tests/exportDesktop.test.js` 写 RED。`scanAssets()` 必须改为通过 71-01 共享 registry/helper 驱动 UI 图片收集，而不是继续在函数内部堆散落 `if (cfg.xxx)`；registry 首版至少覆盖本 phase touchpoint 与当前 runtime 已能 resolve 的 UI 图片字段。导出链路显式追加 `assetDict.ui` 到复制列表，但不要把 `.gmtheme` / `themePackager` 一起拖进来。</action>
  <verify>
    <automated>node --test tests/scanAssets.test.js tests/exportDesktop.test.js &amp;&amp; npx vitest run tests/exportGame.test.js tests/saveLoadScreenLayout.test.js tests/backlogScreenLayout.test.js tests/gameMenuLayout.test.js tests/screenUiImageEntryWiring.test.js &amp;&amp; npm run build</automated>
  </verify>
  <done>UI 图片已进入 registry-driven scan/export 基线，Web 与 Desktop 导出都不会漏掉 Phase 71 已接入的 UI 资源，且相关 layout/export 回归全部通过。</done>
</task>

</tasks>

<verification>
先验证 screen/decor 接线没有破坏现有 layout，再验证 scan/export 已把 UI 图片复制进导出产物，最后跑 build 确认整个基线能编译通过。
</verification>

<success_criteria>
- screen/decor 现有图片字段全部改走标准选图 helper，legacy 值仍可读、显式重选才改写。
- `scanAssets()` 有统一 `ui` bucket 和 registry 入口，后续 Phase 72-75 只需追加 collector。
- Web/Desktop 导出显式复制 UI 图片资产，不再出现“已写入 canonical 路径但导出漏图”的基线缺口。
</success_criteria>

<output>
After completion, create `.planning/phases/71-shared-contract-asset-pipeline-baseline/71-03-SUMMARY.md`
</output>
