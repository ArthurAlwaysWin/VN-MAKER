---
phase: 72-dialogue-box-picture-loop
plan: 02
type: execute
wave: 2
depends_on: [72-01]
files_modified:
  - src/ui/DialogueBox.js
  - src/style.css
  - src/engine/ThemeManager.js
  - tests/dialogueBoxUiSkin.test.js
  - tests/dialogueBoxNameplate.test.js
  - tests/themeManagerUiImage.test.js
autonomous: true
requirements: [DLG-01, DLG-02]

must_haves:
  truths:
    - "运行时可以显示对话框主框图片、名牌背景图和至少一层装饰图，而且主框图片 owner 仍是 `ui.theme.nineSlice.dialogueBox`。"
    - "名牌文字、正文、继续指示与 QAB 始终处在图片层之上；装饰图不会截走点击。"
    - "floating nameplate 在 nine-slice 主框开启时不会被根元素裁掉，缺图时会回退到当前 CSS 外观。"
  artifacts:
    - path: "src/ui/DialogueBox.js"
      provides: "Dialogue underlay DOM, image application logic, preview-safe render helper"
      contains: "dialogue-visual-underlay"
    - path: "src/style.css"
      provides: "Dialogue underlay/foreground z-index and pointer-event rules"
      contains: ".dialogue-name-plate"
    - path: "src/engine/ThemeManager.js"
      provides: "dialogue nine-slice overflow-safe rule generation"
      contains: "#dialogue-box"
    - path: "tests/dialogueBoxUiSkin.test.js"
      provides: "DLG-01/DLG-02 runtime layering regression coverage"
      min_lines: 120
  key_links:
    - from: "src/ui/DialogueBox.js"
      to: "src/style.css"
      via: "class names for underlay, nameplate art, decoration nodes, and foreground stack"
      pattern: "dialogue-visual-underlay|dialogue-nameplate|dialogue-decoration"
    - from: "src/engine/ThemeManager.js"
      to: "src/ui/DialogueBox.js"
      via: "dialogueBox nine-slice root must not clip floating nameplate art"
      pattern: "dialogueBox|overflow|isolation"
    - from: "src/ui/DialogueBox.js"
      to: "src/main.js"
      via: "public preview render helper used by iframe preview path"
      pattern: "renderPreviewLine"
---

<objective>
给 runtime 对话框增加可回退的图片底层，并锁定对话前景层级安全。

Purpose: 把 nameplate/decor 图片真正渲染到运行时 DOM/CSS，同时解决 floating nameplate 与 nine-slice overflow 的高风险断点，确保自动推进、继续指示和 QAB 不被图片破坏。
Output: `DialogueBox.js` underlay 实现、overflow-safe ThemeManager 规则、focused layering regressions。
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/72-dialogue-box-picture-loop/72-CONTEXT.md
@.planning/phases/72-dialogue-box-picture-loop/72-RESEARCH.md
@.planning/phases/72-dialogue-box-picture-loop/72-VALIDATION.md
@.planning/phases/71-shared-contract-asset-pipeline-baseline/71-02-SUMMARY.md
@src/ui/DialogueBox.js
@src/style.css
@src/engine/ThemeManager.js
@tests/dialogueBoxNameplate.test.js
@tests/themeManagerUiImage.test.js

<interfaces>
From src/ui/DialogueBox.js:
```javascript
export class DialogueBox {
  show(data) {}
  applyGlobalStyle(settings) {}
  setNameplateStyle(style) {}
  hide() {}
}
```

From src/engine/ThemeManager.js:
```javascript
export function applyNineSlice(themeData) {}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build dedicated runtime underlay nodes and preview-safe dialogue rendering</name>
  <read_first>
    @src/ui/DialogueBox.js
    @src/engine/ThemeManager.js
    @.planning/phases/72-dialogue-box-picture-loop/72-RESEARCH.md
    @tests/themeManagerUiImage.test.js
  </read_first>
  <files>
    src/ui/DialogueBox.js
    src/engine/ThemeManager.js
    tests/dialogueBoxUiSkin.test.js
    tests/themeManagerUiImage.test.js
  </files>
  <behavior>
    - Test 1: `applyGlobalStyle()` can consume `nameplateBackgroundImage` and `decorations[]`, render them into dedicated underlay nodes, and clear them when values are null/invalid.
    - Test 2: decorations use `pointer-events: none` and never become the click target for dialogue advance.
    - Test 3: a public preview helper renders a completed dialogue line with the indicator visible, without depending on private `_finishLine()` calls from `main.js`.
    - Test 4: when `ui.theme.nineSlice.dialogueBox` is active, floating nameplate art/text still remain visible above the frame.
  </behavior>
  <action>Per D-01, D-03, D-05, D-06, and D-07, refactor `src/ui/DialogueBox.js` so `#dialogue-box` owns a visual underlay instead of painting images directly on the root inline style. Create explicit DOM nodes with stable selectors/classes: a root underlay like `.dialogue-visual-underlay`, a nameplate art node under `.dialogue-name-plate`, and a decoration container whose children carry deterministic selectors such as `[data-dialogue-decoration-index]`. Resolve image sources through the runtime path helper already used by ThemeManager, but clear the node styles when the field is null/empty/legacy-invalid so CSS fallback survives. Add a public method named `renderPreviewLine({ speakerName, text, speakerColor })` that renders a fully completed line and visible indicator for iframe preview use. In `src/engine/ThemeManager.js`, keep `ui.theme.nineSlice.dialogueBox` as the only main-frame owner while changing the dialogueBox rule generation so it no longer clips floating nameplates; do not add any `ui.dialogueBox.frame*` field and do not rewrite the render stack away from DOM/CSS.</action>
  <acceptance_criteria>
    - `DialogueBox` exposes a public `renderPreviewLine()` method for preview mode.
    - Nameplate background and decoration art render in dedicated underlay nodes, not on `this.el.style.background`.
    - The generated nine-slice CSS for `#dialogue-box` no longer causes floating nameplate clipping.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/dialogueBoxUiSkin.test.js tests/themeManagerUiImage.test.js</automated>
  </verify>
  <done>The runtime can paint dialogue-specific images without breaking the locked main-frame owner or the preview-safe render path needed by ProjectSettings.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lock z-index, overflow, pointer-events, and fallback behavior in CSS/tests</name>
  <read_first>
    @src/style.css
    @src/ui/DialogueBox.js
    @tests/dialogueBoxNameplate.test.js
    @.planning/phases/72-dialogue-box-picture-loop/72-VALIDATION.md
  </read_first>
  <files>
    src/style.css
    tests/dialogueBoxNameplate.test.js
    tests/dialogueBoxUiSkin.test.js
  </files>
  <behavior>
    - Test 1: `.dialogue-name-plate`, `.dialogue-text-area`, `.dialogue-indicator`, and `#quick-action-bar` all stack above the visual underlay.
    - Test 2: floating, inline, and banner nameplates retain readable text and visible art when dialogue images are enabled.
    - Test 3: when dialogue image fields are cleared, the dialogue box falls back to the current CSS background rather than leaving broken inline URLs or blanked content.
  </behavior>
  <action>Per D-03, D-05, D-06, and D-07, extend the dialogue section in `src/style.css` using exact selectors already present in runtime: `#dialogue-box`, `.dialogue-name-plate`, `.dialogue-text-area`, `.dialogue-indicator`, and `#quick-action-bar`. Add z-index/position rules so the underlay stays at the bottom, nameplate/text/indicator/QAB stay above it, and all decoration-related nodes have `pointer-events: none`. Keep the current root background CSS as fallback for missing images. Update `tests/dialogueBoxNameplate.test.js` only where the new underlay structure affects assertions, and keep the focused regression surface centered on layering, clipping, and fallback rather than broad repo-wide UI behavior.</action>
  <acceptance_criteria>
    - The foreground selectors named in D-05 are explicitly above the art layer.
    - Decoration nodes cannot intercept clicks because CSS sets `pointer-events: none`.
    - Clearing image fields leaves the existing CSS dialogue background usable.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/dialogueBoxUiSkin.test.js tests/dialogueBoxNameplate.test.js tests/themeManagerUiImage.test.js</automated>
  </verify>
  <done>Runtime dialogue layering is deterministic and safe: art is underneath, interactions still work, floating nameplates survive nine-slice, and missing images fall back cleanly.</done>
</task>

</tasks>

<verification>
Run the focused runtime suite only: `tests/dialogueBoxUiSkin.test.js`, `tests/dialogueBoxNameplate.test.js`, and `tests/themeManagerUiImage.test.js`. Do not use repo-wide Vitest as a gate for this plan.
</verification>

<success_criteria>
- Dialogue-specific images render through dedicated DOM/CSS underlay layers.
- Floating nameplates remain visible when the main dialogue nine-slice is active.
- Text, continue indicator, and QAB are provably above art layers and stay clickable/readable.
</success_criteria>

<output>
After completion, create `.planning/phases/72-dialogue-box-picture-loop/72-02-SUMMARY.md`
</output>
