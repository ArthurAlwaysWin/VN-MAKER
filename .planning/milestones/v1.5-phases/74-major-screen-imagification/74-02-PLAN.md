---
phase: 74-major-screen-imagification
plan: 02
type: execute
wave: 2
depends_on:
  - 74-01
files_modified:
  - src/engine/ThemeManager.js
  - src/main.js
  - src/style.css
  - src/engine/SaveLoadScreen.js
  - src/engine/BacklogScreen.js
  - src/engine/GameMenuScreen.js
  - src/engine/SettingsScreen.js
  - tests/saveLoadScreenLayout.test.js
  - tests/backlogScreenLayout.test.js
  - tests/gameMenuLayout.test.js
  - tests/settingsStructured.test.js
  - tests/themeManagerUiImage.test.js
autonomous: true
requirements:
  - MSI-01
  - MSI-03
must_haves:
  truths:
    - "All four major screens render chrome.backgroundImage as a full-cover background layer and chrome.decorations as non-interactive overlay elements."
    - "GameMenu reads from the new chrome path with @deprecated legacy fallback — no visual regression for existing projects."
    - "Background and decoration rendering does not interfere with existing content, click targets, or scroll behavior."
  artifacts:
    - path: "src/engine/SaveLoadScreen.js"
      provides: "setLayout() chrome field consumption"
      contains: "chrome.backgroundImage, chrome.decorations"
    - path: "src/engine/BacklogScreen.js"
      provides: "setLayout() chrome field consumption"
      contains: "chrome.backgroundImage, chrome.decorations"
    - path: "src/engine/GameMenuScreen.js"
      provides: "setLayout() chrome field consumption with legacy fallback"
      contains: "chrome.backgroundImage"
    - path: "src/engine/SettingsScreen.js"
      provides: "setLayout() chrome field consumption"
      contains: "chrome.backgroundImage, chrome.decorations"
    - path: "tests/saveLoadScreenLayout.test.js"
      provides: "SaveLoad background + decoration rendering regression"
    - path: "tests/backlogScreenLayout.test.js"
      provides: "Backlog background + decoration rendering regression"
  key_links:
    - from: "src/main.js"
      to: "src/engine/ThemeManager.js"
      via: "applyScreenBackgrounds call site"
      pattern: "applyScreenBackgrounds"
    - from: "src/engine/SaveLoadScreen.js"
      to: "ui.saveLoadScreen.chrome"
      via: "setLayout()"
      pattern: "chrome\\.backgroundImage|chrome\\.decorations"
---

<objective>
Wire the four major screen runtimes to consume the chrome.backgroundImage and chrome.decorations[] contract fields, rendering them as visual layers without breaking existing content or interaction.

Purpose: close the runtime rendering gap — backgrounds appear as full-cover images, decorations as non-interactive overlays, and GameMenu migrates transparently.
Output: all four screens render chrome fields, decoration layers have pointerEvents:none, and regression tests cover the rendering pipeline.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/74-major-screen-imagification/74-CONTEXT.md
@.planning/phases/74-major-screen-imagification/74-DISCUSSION-LOG.md
@.planning/phases/74-major-screen-imagification/74-01-SUMMARY.md
@src/engine/ThemeManager.js
@src/engine/SaveLoadScreen.js
@src/engine/BacklogScreen.js
@src/engine/GameMenuScreen.js
@src/engine/SettingsScreen.js
@src/main.js
@src/style.css
@tests/saveLoadScreenLayout.test.js
@tests/backlogScreenLayout.test.js
@tests/gameMenuLayout.test.js
@tests/settingsStructured.test.js
@tests/themeManagerUiImage.test.js

<interfaces>
From src/engine/ThemeManager.js (74-01 output):
```js
export function applyScreenBackgrounds(container, uiData) {}
export const SCREEN_BACKGROUND_SELECTORS = { saveLoadScreen, backlogScreen, gameMenu, settingsScreen };
```

From runtime screens:
```js
screen.setLayout(config) — already handles screen-specific config fields
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire applyScreenBackgrounds into runtime and preview theme paths</name>
  <read_first>
    @src/engine/ThemeManager.js
    @src/main.js
    @src/style.css
    @tests/themeManagerUiImage.test.js
  </read_first>
  <files>src/main.js, src/style.css, tests/themeManagerUiImage.test.js</files>
  <behavior>
    - Test 1: `applyScreenBackgrounds()` is called during initial runtime load after `applyTheme()` and `applyNineSlice()`.
    - Test 2: `applyScreenBackgrounds()` is called during preview `update-theme` message handling.
    - Test 3: Screen background style tag injection does not remove or conflict with the nine-slice or button-family style tags.
  </behavior>
  <action>Wire `applyScreenBackgrounds()` into `src/main.js` at every point where `applyTheme()` and `applyNineSlice()` are already called: initial load, `applyPreviewScriptSnapshot()`, and the `update-theme` message handler. In `src/style.css`, add a base rule ensuring screen containers (`#save-load-screen`, `#backlog-screen`, `.game-menu-screen`, `#settings-screen`) have `position: relative` and `overflow: hidden` so background images fill correctly. Extend `tests/themeManagerUiImage.test.js` to verify call ordering and style tag independence.</action>
  <verify>
    <automated>npx vitest run tests/themeManagerUiImage.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `applyScreenBackgrounds` is called in all theme-application paths.
    - Screen background, nine-slice, and button-family style tags coexist.
    - Screen containers have proper positioning for background rendering.
  </acceptance_criteria>
  <done>Screen background CSS is injected alongside existing theme systems without conflicts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render chrome.decorations as non-interactive overlay layers in all four screens</name>
  <read_first>
    @src/engine/SaveLoadScreen.js
    @src/engine/BacklogScreen.js
    @src/engine/GameMenuScreen.js
    @src/engine/SettingsScreen.js
    @.planning/phases/74-major-screen-imagification/74-CONTEXT.md
    @tests/saveLoadScreenLayout.test.js
    @tests/backlogScreenLayout.test.js
    @tests/gameMenuLayout.test.js
    @tests/settingsStructured.test.js
  </read_first>
  <files>src/engine/SaveLoadScreen.js, src/engine/BacklogScreen.js, src/engine/GameMenuScreen.js, src/engine/SettingsScreen.js, src/style.css, tests/saveLoadScreenLayout.test.js, tests/backlogScreenLayout.test.js, tests/gameMenuLayout.test.js, tests/settingsStructured.test.js</files>
  <behavior>
    - Test 1: SaveLoad `setLayout()` with `chrome.decorations: [{src:'ui/sl/decor.png', x:10, y:20, width:100, height:50}]` creates positioned `<img>` elements inside the screen container with `pointer-events: none`.
    - Test 2: Backlog and GameMenu `setLayout()` render chrome.decorations the same way.
    - Test 3: Settings `setLayout()` renders `chrome.decorations` in addition to its existing `header.decorations` (both can coexist).
    - Test 4: Calling `setLayout()` again removes previous decoration elements before creating new ones (no accumulation).
    - Test 5: Empty or missing `chrome.decorations` array produces no decoration elements and does not error.
  </behavior>
  <action>Add a shared `renderDecorations(container, decorations)` helper (or inline in each screen) that iterates `chrome.decorations[]` and creates absolutely-positioned `<img class="screen-decoration">` elements with `pointer-events: none` set via CSS class. Each decoration element gets `left`, `top`, `width`, `height` from the decoration config and `object-fit: contain`. In each screen's `setLayout()`, call this helper with `config.chrome?.decorations`. For Settings, render both `config.chrome?.decorations` and the existing `config.header?.decorations` (the latter remains at header level). In `src/style.css`, add `.screen-decoration { position: absolute; pointer-events: none; }` base rules. Extend all four screen layout tests with decoration rendering assertions.</action>
  <verify>
    <automated>npx vitest run tests/saveLoadScreenLayout.test.js tests/backlogScreenLayout.test.js tests/gameMenuLayout.test.js tests/settingsStructured.test.js</automated>
  </verify>
  <acceptance_criteria>
    - All four screens render chrome.decorations as absolute-positioned, non-interactive overlays.
    - Settings supports both chrome.decorations and header.decorations without conflict.
    - Re-calling setLayout replaces decorations instead of accumulating.
    - Empty decorations arrays are handled gracefully.
  </acceptance_criteria>
  <done>All four major screens render decoration layers consistently, with pointer-events:none and proper lifecycle management.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement GameMenu chrome.backgroundImage with legacy fallback in setLayout</name>
  <read_first>
    @src/engine/GameMenuScreen.js
    @.planning/phases/74-major-screen-imagification/74-DISCUSSION-LOG.md
    @tests/gameMenuLayout.test.js
  </read_first>
  <files>src/engine/GameMenuScreen.js, tests/gameMenuLayout.test.js</files>
  <behavior>
    - Test 1: GameMenu `setLayout()` with `chrome.backgroundImage: 'ui/gm/bg.png'` resolves the image via `resolvePath()` and sets it as the background.
    - Test 2: GameMenu `setLayout()` without `chrome.backgroundImage` but with legacy `backgroundImage: 'ui/gm/old.png'` uses the legacy value as fallback.
    - Test 3: When both `chrome.backgroundImage` and legacy `backgroundImage` exist, chrome takes precedence.
    - Test 4: The fallback code path has a `@deprecated` comment noting "Remove in next major milestone — Phase 74 migration path".
  </behavior>
  <action>Update `GameMenuScreen.setLayout()` to read `config.chrome?.backgroundImage` first, then fall back to `config.backgroundImage` (legacy). The fallback must have an explicit `@deprecated` JSDoc or inline comment: `// @deprecated Remove in next major milestone — Phase 74 migration path`. Pass the resolved background image to the existing background rendering mechanism. Extend `tests/gameMenuLayout.test.js` with chrome precedence, legacy fallback, and both-present scenarios.</action>
  <verify>
    <automated>npx vitest run tests/gameMenuLayout.test.js</automated>
  </verify>
  <acceptance_criteria>
    - Chrome backgroundImage takes precedence over legacy path.
    - Legacy path works as fallback when chrome path is absent.
    - @deprecated comment is present on the fallback code.
  </acceptance_criteria>
  <done>GameMenu transparently migrates to chrome.backgroundImage while preserving backward compatibility with @deprecated annotation.</done>
</task>

</tasks>

<verification>
- `npx vitest run tests/themeManagerUiImage.test.js tests/saveLoadScreenLayout.test.js tests/backlogScreenLayout.test.js tests/gameMenuLayout.test.js tests/settingsStructured.test.js`
- `npm run build`
</verification>

<success_criteria>
- All four screens render chrome.backgroundImage as full-cover backgrounds.
- All four screens render chrome.decorations as non-interactive overlays.
- GameMenu legacy fallback works with @deprecated annotation.
- Existing screen functionality (content, scrolling, click targets) is unaffected.
</success_criteria>

<output>
After completion, create `.planning/phases/74-major-screen-imagification/74-02-SUMMARY.md`
</output>
