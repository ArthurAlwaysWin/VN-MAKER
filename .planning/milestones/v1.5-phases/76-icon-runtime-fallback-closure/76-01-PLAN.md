---
phase: 76-icon-runtime-fallback-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main.js
  - src/ui/QuickActionBar.js
  - src/style.css
  - tests/quickActionBarThemeIcon.test.js
  - tests/mainThemeIconRouting.test.js
  - tests/quickActionBarButtonFamily.test.js
autonomous: true
requirements:
  - ICO-01
  - AST-03
must_haves:
  truths:
    - "Users who configure `ui.theme.icons.qab` see that icon on the real QuickActionBar in preview and runtime instead of only hardcoded SVGs."
    - "When the `qab` slot is empty, QuickActionBar keeps the existing default SVG/state behavior."
    - "The same theme icon object reaches QuickActionBar during init, preview bootstrap, and `update-theme` refreshes."
  artifacts:
    - path: "src/ui/QuickActionBar.js"
      provides: "QAB themed icon consumer with default fallback path"
      contains: "setThemeIcons"
    - path: "src/main.js"
      provides: "QAB theme icon handoff in runtime + preview"
      contains: "quickBar.setThemeIcons"
    - path: "tests/quickActionBarThemeIcon.test.js"
      provides: "QAB icon rendering regression coverage"
    - path: "tests/mainThemeIconRouting.test.js"
      provides: "main.js source-pattern coverage for QAB icon routing"
  key_links:
    - from: "src/main.js"
      to: "src/ui/QuickActionBar.js"
      via: "quickBar.setThemeIcons(themeIcons || null)"
      pattern: "quickBar\\.setThemeIcons"
    - from: "src/ui/QuickActionBar.js"
      to: "src/ui/themeIconHelpers.js"
      via: "qab slot resolution"
      pattern: "resolveThemeIcon\\(.*'qab'"
---

<objective>
Close the missing runtime consumer for `ui.theme.icons.qab` so QAB icon configuration reaches the real QuickActionBar surface everywhere Phase 76 cares about.

Purpose: satisfy the audit blocker that preview/runtime parity breaks because QAB never consumes the locked icon slot.
Output: QAB runtime wiring in `main.js`, themed rendering in `QuickActionBar.js`, and focused regressions proving the slot is no longer editor/export-only.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v1.5-MILESTONE-AUDIT.md
@.planning/phases/75-cursor-icon-pipeline-closure/75-SUMMARY.md
@.planning/phases/76-icon-runtime-fallback-closure/76-CONTEXT.md
@src/main.js
@src/ui/QuickActionBar.js
@src/ui/themeIconHelpers.js
@src/shared/uiImageContract.js
@tests/quickActionBarButtonFamily.test.js

<decision_traceability>
- D-76-01: `ui.theme.icons.qab` must become a real runtime consumer, not just an editor/export slot.
- D-76-02: fix shared preview/runtime logic first; do not patch only one leg.
- D-76-03: reuse the existing theme/icon pipeline and do not create a second icon system or new slot family.
</decision_traceability>

<interfaces>
From src/ui/themeIconHelpers.js:
```js
export function resolveThemeIcon(icons, slotKey, fallbackText, cssClass = '') {}
export function hasThemeIcon(icons, slotKey) {}
```

From src/ui/QuickActionBar.js:
```js
setAutoActive(active) {}
setSkipActive(active) {}
setQuickLoadEnabled(enabled) {}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add real QAB themed-icon rendering with default SVG fallback</name>
  <read_first>
    @src/ui/QuickActionBar.js
    @src/ui/themeIconHelpers.js
    @src/style.css
    @tests/quickActionBarButtonFamily.test.js
  </read_first>
  <files>src/ui/QuickActionBar.js, src/style.css, tests/quickActionBarThemeIcon.test.js, tests/quickActionBarButtonFamily.test.js</files>
  <behavior>
    - Test 1: with `themeIcons.qab` configured, all 8 `.qab-btn` buttons render the themed icon instead of the current hardcoded SVG markup.
    - Test 2: with no `qab` icon configured, QuickActionBar keeps the current SVG fallback, `.active`, and `.disabled` behavior.
    - Test 3: QAB image sizing/alignment does not break the Phase 73 button-family underlay (`.qab-btn::before`) or click delegation.
  </behavior>
  <action>Update `src/ui/QuickActionBar.js` per D-76-01/D-76-03 so the component owns `_themeIcons`, exposes `setThemeIcons(icons)`, and renders the locked `qab` slot through the existing theme icon helper instead of inventing a new icon path. Keep the current hardcoded SVG set as the empty-slot fallback allowed by the Phase 76 discretion note. If the themed icon needs dedicated sizing, add the smallest possible CSS hook in `src/style.css` (for example a `.qab-theme-icon` rule) without disturbing `.qab-btn`, `.qab-btn::before`, `.active`, or `.disabled` semantics. Lock the behavior in a new `tests/quickActionBarThemeIcon.test.js` and refresh `tests/quickActionBarButtonFamily.test.js` so Phase 73 underlay coverage still proves QAB state classes survive the icon change.</action>
  <verify>
    <automated>npx vitest run tests/quickActionBarThemeIcon.test.js tests/quickActionBarButtonFamily.test.js</automated>
  </verify>
  <done>QuickActionBar can render the configured `ui.theme.icons.qab` asset on every button, while empty-slot behavior still falls back to the current SVG-based UI without layout or state regressions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire QAB theme icons through init, preview bootstrap, and live theme updates</name>
  <read_first>
    @src/main.js
    @src/ui/QuickActionBar.js
    @src/shared/uiImageContract.js
  </read_first>
  <files>src/main.js, tests/mainThemeIconRouting.test.js</files>
  <behavior>
    - Test 1: runtime init passes `engine.script.ui?.theme?.icons` to `quickBar.setThemeIcons(...)` alongside the existing SaveLoad/GameMenu/Backlog/Settings consumers.
    - Test 2: preview bootstrap (`applyPreviewScriptSnapshot`) also passes the same icon object to QuickActionBar.
    - Test 3: preview `update-theme` refreshes QAB icons with `themeIcons || null`, keeping parity with the other icon consumers and without adding a new preview message type.
  </behavior>
  <action>Add the missing `quickBar.setThemeIcons(...)` handoff in every existing icon-routing branch inside `src/main.js` per D-76-01/D-76-02: normal init, preview snapshot bootstrap, and the `update-theme` message handler. Do not touch scan/export collectors or invent a QAB-only transport — Phase 75 already froze `ui.theme.icons.qab` in `src/shared/uiImageContract.js`. Create `tests/mainThemeIconRouting.test.js` as a focused source-pattern regression instead of burying this check in unrelated config-routing coverage, and keep the assertions narrow to the three real routing sites the audit called out.</action>
  <verify>
    <automated>npx vitest run tests/mainThemeIconRouting.test.js tests/quickActionBarThemeIcon.test.js && node --test tests/scanAssets.test.js</automated>
  </verify>
  <done>`ui.theme.icons.qab` reaches QuickActionBar in runtime preview bootstrap, normal game startup, and live theme updates, so the QAB slot is no longer editor/export-only.</done>
</task>

</tasks>

<verification>
- `npx vitest run tests/quickActionBarThemeIcon.test.js tests/quickActionBarButtonFamily.test.js tests/mainThemeIconRouting.test.js`
- `node --test tests/scanAssets.test.js`
</verification>

<success_criteria>
- QAB has a real runtime consumer for the locked `qab` icon slot.
- Preview and runtime use the same `themeIcons` handoff path for QuickActionBar.
- Empty-slot fallback remains the current SVG-based QAB UI.
</success_criteria>

<output>
After completion, create `.planning/phases/76-icon-runtime-fallback-closure/76-01-SUMMARY.md`
</output>
