---
phase: 35-chinese-localization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/editor/components/theme/TokenAccordion.vue
  - src/editor/components/theme/ColorTokenRow.vue
  - src/editor/components/theme/FontTokenRow.vue
  - src/editor/components/theme/SliderTokenRow.vue
  - src/editor/components/theme/GradientTokenRow.vue
autonomous: true
requirements: [L10N-01, L10N-02]

must_haves:
  truths:
    - "主题编辑器中 41 个 token 全部显示 2-4 字中文名而非 CSS key"
    - "字体 token 下拉框显示中文标签（无衬线体/衬线体）而非英文（sans-serif/serif）"
  artifacts:
    - path: "src/editor/components/theme/TokenAccordion.vue"
      provides: "TOKEN_LABELS 映射对象 (41 entries)"
      contains: "TOKEN_LABELS"
    - path: "src/editor/components/theme/ColorTokenRow.vue"
      provides: "label prop 显示"
      contains: "label"
    - path: "src/editor/components/theme/FontTokenRow.vue"
      provides: "label prop 显示 + 中文字体标签"
      contains: "无衬线体"
    - path: "src/editor/components/theme/SliderTokenRow.vue"
      provides: "label prop 显示"
      contains: "label"
    - path: "src/editor/components/theme/GradientTokenRow.vue"
      provides: "label prop 显示"
      contains: "label"
  key_links:
    - from: "src/editor/components/theme/TokenAccordion.vue"
      to: "ColorTokenRow/FontTokenRow/SliderTokenRow/GradientTokenRow"
      via: ":label prop binding using TOKEN_LABELS[key]"
      pattern: ":label=\"TOKEN_LABELS\\[key\\]\""
---

<objective>
将主题编辑器中 41 个 CSS token 的显示名称从英文 key 替换为简洁中文名，并将 FontTokenRow 的系统字体下拉框标签中文化。

Purpose: 消除主题编辑器中最大面积的英文残留（41 个 token label），实现 L10N-01 和部分 L10N-02。
Output: TokenAccordion.vue 包含 TOKEN_LABELS 映射，4 个 token row 组件通过 label prop 显示中文名。
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-chinese-localization/35-CONTEXT.md

@src/editor/components/theme/TokenAccordion.vue
@src/editor/components/theme/ColorTokenRow.vue
@src/editor/components/theme/FontTokenRow.vue
@src/editor/components/theme/SliderTokenRow.vue
@src/editor/components/theme/GradientTokenRow.vue
@src/engine/tokens.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add TOKEN_LABELS map and pass label prop in TokenAccordion.vue</name>
  <files>src/editor/components/theme/TokenAccordion.vue</files>
  <read_first>
    src/editor/components/theme/TokenAccordion.vue
    src/engine/tokens.js
    .planning/phases/35-chinese-localization/35-CONTEXT.md
  </read_first>
  <action>
In TokenAccordion.vue `<script setup>` section, add a `TOKEN_LABELS` constant object after the `TOKEN_GROUPS` array (per D-01: 简洁派 2-4 字, 组名提供上下文). The full mapping:

```js
const TOKEN_LABELS = {
  // 🎨 核心色
  'primary': '主色',
  'primary-subtle': '淡主色',
  'danger': '危险色',
  'danger-hover': '悬停色',
  'accent': '强调色',
  'accent-border': '边框色',
  'shadow': '阴影',
  'title-glow': '标题光晕',
  'save-title': '存档标题',
  'load-title': '读档标题',
  // 📝 文字
  'text': '正文',
  'text-heading': '标题',
  'text-secondary': '次要',
  'text-muted': '弱化',
  'text-dim': '暗淡',
  'text-faint': '极淡',
  // 📏 边框
  'border': '边框',
  'border-hover': '悬停',
  'border-active': '激活',
  // 🖼️ 背景
  'dialogue-bg': '对话框',
  'panel-bg': '面板',
  'menu-bg': '菜单',
  'card-bg': '卡片',
  'card-bg-hover': '悬停卡片',
  'title-bg': '标题背景',
  'confirm-bg': '确认框',
  // 🔘 按钮
  'btn-bg': '底色',
  'btn-text': '文字',
  'btn-border': '边框',
  'btn-hover-bg': '悬停色',
  'btn-hover-text': '悬停文字',
  'btn-hover-border': '悬停边框',
  // 🔤 字体
  'font-body': '正文',
  'font-display': '标题',
  // ⭕ 圆角
  'radius': '小',
  'radius-lg': '大',
  // 🌫️ 模糊
  'blur': '模糊',
  // 🎛️ 控件
  'slider-track': '滑轨',
  'slider-thumb': '滑块',
  'scrollbar': '滚动条',
  // 💬 说话人
  'speaker-shadow': '阴影',
};
```

Then in the `<template>`, pass the label as a prop to each row component. Update each of the 4 component usages inside the `v-for` loop to add `:label="TOKEN_LABELS[key] || key"`:

Change:
```html
<ColorTokenRow
  v-if="getTokenType(key) === 'color-alpha'"
  :token-key="key"
  :has-alpha="true"
  :show-contrast="needsContrast(key)"
/>
```
To:
```html
<ColorTokenRow
  v-if="getTokenType(key) === 'color-alpha'"
  :token-key="key"
  :label="TOKEN_LABELS[key] || key"
  :has-alpha="true"
  :show-contrast="needsContrast(key)"
/>
```

Apply the same `:label="TOKEN_LABELS[key] || key"` prop to ALL 5 component usages in the template:
1. `ColorTokenRow` (color-alpha variant) — add `:label="TOKEN_LABELS[key] || key"`
2. `ColorTokenRow` (color variant) — add `:label="TOKEN_LABELS[key] || key"`
3. `FontTokenRow` — add `:label="TOKEN_LABELS[key] || key"`
4. `SliderTokenRow` — add `:label="TOKEN_LABELS[key] || key"`
5. `GradientTokenRow` — add `:label="TOKEN_LABELS[key] || key"`
  </action>
  <verify>
    <automated>cd E:\projects\my-awesome-project && Select-String -Path "src/editor/components/theme/TokenAccordion.vue" -Pattern "TOKEN_LABELS" | Measure-Object | Select-Object -ExpandProperty Count</automated>
  </verify>
  <acceptance_criteria>
    - TokenAccordion.vue contains `const TOKEN_LABELS = {`
    - TOKEN_LABELS has exactly 41 entries (one per token in DEFAULT_TOKENS)
    - TokenAccordion.vue contains `:label="TOKEN_LABELS[key] || key"` on all 5 row component usages
    - grep for `'primary': '主色'` returns a match in TokenAccordion.vue
    - grep for `'speaker-shadow': '阴影'` returns a match in TokenAccordion.vue
  </acceptance_criteria>
  <done>TokenAccordion.vue has TOKEN_LABELS with all 41 token mappings, passes :label prop to all row components</done>
</task>

<task type="auto">
  <name>Task 2: Update token row components to display label prop + FontTokenRow systemFonts 中文化</name>
  <files>
    src/editor/components/theme/ColorTokenRow.vue
    src/editor/components/theme/FontTokenRow.vue
    src/editor/components/theme/SliderTokenRow.vue
    src/editor/components/theme/GradientTokenRow.vue
  </files>
  <read_first>
    src/editor/components/theme/ColorTokenRow.vue
    src/editor/components/theme/FontTokenRow.vue
    src/editor/components/theme/SliderTokenRow.vue
    src/editor/components/theme/GradientTokenRow.vue
  </read_first>
  <action>
**A. Add `label` prop to all 4 row components and use it in the template.**

In each component's `defineProps()`, add `label: String`. Then change the template from `{{ tokenKey }}` to `{{ label || tokenKey }}`.

Specific changes per file:

**ColorTokenRow.vue:**
- In `defineProps`, add `label: String` (currently has tokenKey, hasAlpha, showContrast)
- Line 3: change `{{ tokenKey }}` to `{{ label || tokenKey }}`

**FontTokenRow.vue:**
- In `defineProps`, add `label: String` (currently has tokenKey)
- Line 3: change `{{ tokenKey }}` to `{{ label || tokenKey }}`

**SliderTokenRow.vue:**
- In `defineProps`, add `label: String` (currently has tokenKey)
- Line 3: change `{{ tokenKey }}` to `{{ label || tokenKey }}`

**GradientTokenRow.vue:**
- In `defineProps`, add `label: String` (currently has tokenKey)
- Line 3: change `{{ tokenKey }}` to `{{ label || tokenKey }}`

**B. FontTokenRow.vue: Change systemFonts labels to Chinese (per L10N-02).**

Change the `systemFonts` array from:
```js
const systemFonts = [
  { label: "'Noto Sans SC', sans-serif", value: "'Noto Sans SC', 'Segoe UI', 'Microsoft YaHei', sans-serif" },
  { label: "'Noto Serif SC', serif", value: "'Noto Serif SC', serif" },
  { label: 'sans-serif', value: 'sans-serif' },
  { label: 'serif', value: 'serif' },
];
```

To:
```js
const systemFonts = [
  { label: 'Noto Sans SC', value: "'Noto Sans SC', 'Segoe UI', 'Microsoft YaHei', sans-serif" },
  { label: 'Noto Serif SC', value: "'Noto Serif SC', serif" },
  { label: '无衬线体', value: 'sans-serif' },
  { label: '衬线体', value: 'serif' },
];
```
  </action>
  <verify>
    <automated>cd E:\projects\my-awesome-project && Select-String -Path "src/editor/components/theme/ColorTokenRow.vue","src/editor/components/theme/FontTokenRow.vue","src/editor/components/theme/SliderTokenRow.vue","src/editor/components/theme/GradientTokenRow.vue" -Pattern "label \|\| tokenKey" | Measure-Object | Select-Object -ExpandProperty Count</automated>
  </verify>
  <acceptance_criteria>
    - All 4 row components contain `{{ label || tokenKey }}` in their template
    - All 4 row components have `label: String` in defineProps
    - FontTokenRow.vue contains `label: '无衬线体'`
    - FontTokenRow.vue contains `label: '衬线体'`
    - FontTokenRow.vue does NOT contain `label: 'sans-serif'` (old English label)
    - FontTokenRow.vue does NOT contain `label: "'Noto Sans SC', sans-serif"` (old verbose label)
  </acceptance_criteria>
  <done>All 4 token row components display Chinese label via prop. FontTokenRow system font labels are Chinese.</done>
</task>

</tasks>

<verification>
1. Open theme editor — all 41 tokens should show 2-4 character Chinese names instead of CSS keys
2. Font token dropdown should show "Noto Sans SC", "Noto Serif SC", "无衬线体", "衬线体"
3. No token row should display raw English CSS key names
</verification>

<success_criteria>
- 41 token labels display Chinese names in the theme editor
- FontTokenRow dropdown shows Chinese font labels
- No English CSS key names visible in token rows
- No JavaScript errors in console
</success_criteria>

<output>
After completion, create `.planning/phases/35-chinese-localization/35-01-SUMMARY.md`
</output>
