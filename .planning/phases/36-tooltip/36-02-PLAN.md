---
phase: 36-tooltip
plan: 02
type: execute
wave: 2
depends_on: ["36-01"]
files_modified:
  - src/editor/components/page-editor/PageInspector.vue
  - src/editor/components/page-editor/SceneTree.vue
  - src/editor/components/page-editor/CanvasToolbar.vue
  - src/editor/views/ResourceLibrary.vue
  - src/editor/components/resource-library/CharacterEditor.vue
  - src/editor/components/resource-library/BgRemovalModal.vue
  - src/editor/components/resource-library/FontGrid.vue
  - src/editor/views/TitleDesigner.vue
  - src/editor/views/SettingsDesigner.vue
  - src/editor/views/Scenes.vue
  - src/editor/components/page-editor/AudioPicker.vue
  - src/editor/components/page-editor/VoiceMatchPreview.vue
  - src/editor/views/Characters.vue
  - src/editor/components/page-editor/CharacterPicker.vue
  - src/editor/views/Assets.vue
  - src/editor/views/CreateProjectQuick.vue
  - src/editor/views/CreateProjectWizard.vue
  - src/editor/views/WelcomeScreen.vue
  - src/editor/components/canvas/CanvasPreview.vue
  - src/editor/views/PageEditor.vue
  - src/editor/components/resource-library/MiniPlayer.vue
  - src/editor/components/resource-library/ImportNotification.vue
  - src/editor/components/resource-library/AssetPickerModal.vue
  - src/editor/App.vue
  - src/editor/components/TabBar.vue
  - src/editor/components/canvas/DraggableElement.vue
  - src/editor/components/page-editor/PageCanvas.vue
autonomous: true
requirements:
  - HELP-02
  - HELP-06
  - HELP-07
  - HELP-08

must_haves:
  truths:
    - "Script editor (PageInspector, SceneTree, CanvasToolbar) has HelpTip on transition dropdown, voice match area, scene jump badge, and add-character button"
    - "Resource library (ResourceLibrary, CharacterEditor, BgRemovalModal, FontGrid) has HelpTip on import sections, expression section, and bg removal controls"
    - "Title/Settings designers have HelpTip on preset button palette headers, element palette headers, and canvas background/delete buttons"
    - "All icon-only buttons across the entire editor have Chinese title attributes — grep audit shows near-zero untitled icon buttons"
  artifacts:
    - path: "src/editor/components/page-editor/PageInspector.vue"
      provides: "Script editor HelpTip integration"
      contains: "HelpTip"
    - path: "src/editor/views/TitleDesigner.vue"
      provides: "Title designer HelpTip integration"
      contains: "HelpTip"
    - path: "src/editor/views/ResourceLibrary.vue"
      provides: "Resource library HelpTip integration"
      contains: "HelpTip"
  key_links:
    - from: "src/editor/components/page-editor/PageInspector.vue"
      to: "src/editor/helpTexts.js"
      via: "import { HELP_SCRIPT }"
      pattern: "HELP_SCRIPT"
    - from: "src/editor/views/ResourceLibrary.vue"
      to: "src/editor/helpTexts.js"
      via: "import { HELP_RESOURCE }"
      pattern: "HELP_RESOURCE"
    - from: "src/editor/views/TitleDesigner.vue"
      to: "src/editor/helpTexts.js"
      via: "import { HELP_DESIGNER }"
      pattern: "HELP_DESIGNER"
---

<objective>
Complete the help system by integrating HelpTip into the remaining 3 editor areas (script editor, resource library, designers) and performing a comprehensive button title sweep across all editor components.

Purpose: Covers HELP-06 (script editor help), HELP-07 (resource library help), HELP-08 (designer help), and HELP-02 (button title full coverage). This plan completes the Phase 36 tooltip help system.

Output: HelpTip instances in 9 more editor components, and Chinese `title` attributes on all remaining icon buttons across ~16 additional components.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-tooltip/36-CONTEXT.md
@.planning/phases/36-tooltip/36-RESEARCH.md
@.planning/phases/36-tooltip/36-01-SUMMARY.md

@src/editor/components/HelpTip.vue
@src/editor/helpTexts.js

<interfaces>
<!-- HelpTip.vue API (created in plan 01) -->
Props:
  text: String (required) — help text, \n for multiline

Usage:
  import HelpTip from '../components/HelpTip.vue';  (path varies by depth)
  import { HELP_SCRIPT } from '../helpTexts.js';     (path varies by depth)
  <HelpTip :text="HELP_SCRIPT.transition" />

helpTexts.js exports:
  HELP_THEME   — theme editor texts (used in plan 01)
  HELP_EXPORT  — export modal texts (used in plan 01)
  HELP_SETTINGS — project settings texts (used in plan 01)
  HELP_SCRIPT  — script editor texts (this plan)
  HELP_RESOURCE — resource library texts (this plan)
  HELP_DESIGNER — designer texts (this plan)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integrate HelpTip into script editor, resource library, and designers + area-specific button titles</name>
  <files>src/editor/components/page-editor/PageInspector.vue, src/editor/components/page-editor/SceneTree.vue, src/editor/components/page-editor/CanvasToolbar.vue, src/editor/views/ResourceLibrary.vue, src/editor/components/resource-library/CharacterEditor.vue, src/editor/components/resource-library/BgRemovalModal.vue, src/editor/components/resource-library/FontGrid.vue, src/editor/views/TitleDesigner.vue, src/editor/views/SettingsDesigner.vue</files>
  <read_first>
    src/editor/components/page-editor/PageInspector.vue (full file — transition dropdown at lines 22-31, character section at lines 41+, choice section, audio section; 8 untitled buttons)
    src/editor/components/page-editor/SceneTree.vue (full file — voice match button line 26, menu button line 27, page add buttons; 3 untitled buttons)
    src/editor/components/page-editor/CanvasToolbar.vue (full file — add character button line 4, page info; 4 untitled buttons; mute/play/stop already have titles)
    src/editor/views/ResourceLibrary.vue (full file — toolbar with import button line 16, sub-tabs; 2 untitled buttons)
    src/editor/components/resource-library/CharacterEditor.vue (full file — add button line 7, new char button line 32, delete button line 51, import expression line 57; 4 untitled buttons)
    src/editor/components/resource-library/BgRemovalModal.vue (full file — confirm button line 64-68, direct-use button line 70, cancel line 71, close line 7; 4 untitled buttons)
    src/editor/components/resource-library/FontGrid.vue (full file — check for untitled buttons)
    src/editor/views/TitleDesigner.vue (full file — toolbar buttons lines 26-36: background, clear bg, BGM, preview, clear bgm, delete; palette section lines 6-18; 9 untitled buttons)
    src/editor/views/SettingsDesigner.vue (full file — toolbar buttons lines 29-33: background, clear bg, delete; 4 untitled buttons)
    src/editor/helpTexts.js (verify HELP_SCRIPT, HELP_RESOURCE, HELP_DESIGNER keys)
    src/editor/components/HelpTip.vue (verify import path)
  </read_first>
  <action>
**A. PageInspector.vue — HelpTip + button titles (HELP-06)**

1. Add imports in `<script setup>`:
   ```javascript
   import HelpTip from './HelpTip.vue';
   import { HELP_SCRIPT } from '../../helpTexts.js';
   ```
   Note: HelpTip.vue is in `src/editor/components/` and PageInspector is in `src/editor/components/page-editor/`, so the relative path from PageInspector is `../HelpTip.vue`. Adjust as needed based on actual file structure.

2. Add HelpTip instances:
   - Next to the "过渡" label (`<label>过渡</label>`, around line 23):
     ```html
     <label>过渡 <HelpTip :text="HELP_SCRIPT.transition" /></label>
     ```
   - Next to the "🧑 角色列表" section toggle (inside the toggle div text):
     Add a HelpTip after the toggle text — but since it's inside an @click handler div, place it next to the section-body or after the toggle text. Best approach: add after section-toggle's text content:
     ```html
     <div class="section-toggle" @click="sections.chars = !sections.chars">
       {{ sections.chars ? '▼' : '▶' }} 🧑 角色列表 <HelpTip :text="HELP_SCRIPT.addCharacter" />
     </div>
     ```

3. Add `title` to untitled buttons:
   - Add character button (if exists in PageInspector, like `+ 添加角色`) → add `title="添加角色到当前页面"`
   - Any `<button class="delete-x"` without title that doesn't already have title → check existing; line 58 already has `title="移除角色"`
   - Audio/voice related buttons → add `title` as appropriate
   - Choice-related buttons (add choice, delete choice) → add `title="添加选项"` and `title="删除选项"` if not present
   - Read the file carefully for ALL `<button` tags and add `title` where missing (for icon-only buttons per D-15)

**B. SceneTree.vue — HelpTip + button titles (HELP-06)**

1. Add imports in `<script setup>`:
   ```javascript
   import HelpTip from '../HelpTip.vue';
   import { HELP_SCRIPT } from '../../helpTexts.js';
   ```
   Note: SceneTree is in `page-editor/`, HelpTip is in `components/`, so path is `../HelpTip.vue`.

2. HelpTip instance — near the voice match or scene jump area. Best placement: nowhere prominent enough for a standalone HelpTip in the tree view (voice match and scene menu already have title attributes at lines 26-27). Focus on button titles.

3. Add `title` to untitled buttons:
   - Page add/new buttons at bottom of scene tree → add `title="添加新页面"` (or similar, read the actual button text)
   - Any expand/collapse or contextmenu buttons without titles → add appropriate `title`
   - Scan the full file for `<button` tags missing `title=`

**C. CanvasToolbar.vue — HelpTip + button titles (HELP-06)**

1. Add imports in `<script setup>`:
   ```javascript
   import HelpTip from '../HelpTip.vue';
   import { HELP_SCRIPT } from '../../helpTexts.js';
   ```

2. Add HelpTip next to the "添加角色" button:
   ```html
   <button class="add-char-btn" @click="editor.showCharPicker.value = true"
     :disabled="!editor.currentPage.value" title="添加角色到当前页面">
     + 添加角色
   </button>
   <HelpTip :text="HELP_SCRIPT.addCharacter" />
   ```

3. Add `title` to any untitled buttons. Existing: mute/play/stop already have titles. Check for remaining untitled buttons (e.g., add character button at line 4 does NOT have title) → add `title="添加角色到当前页面"`.

**D. ResourceLibrary.vue — HelpTip + button titles (HELP-07)**

1. Add imports in `<script setup>`:
   ```javascript
   import HelpTip from '../components/HelpTip.vue';
   import { HELP_RESOURCE } from '../helpTexts.js';
   ```

2. Add HelpTip next to the import button or the toolbar title. Best: next to `<h2>资源库</h2>`:
   ```html
   <h2>资源库 <HelpTip :text="HELP_RESOURCE.imageFormats" /></h2>
   ```
   Note: This is a general resource tip. Alternatively, add it next to the import button:
   ```html
   <button v-if="showImportButton" class="import-btn" @click="onImportClick" title="导入文件到当前分类">📂 导入文件</button>
   ```

3. Add `title` to untitled buttons:
   - Import button `<button class="import-btn" ...>📂 导入文件</button>` → add `title="导入文件到当前分类"`
   - Sub-tab buttons are text-labeled → skip

**E. CharacterEditor.vue — HelpTip + button titles (HELP-07)**

1. Add imports in `<script setup>`:
   ```javascript
   import HelpTip from './HelpTip.vue';
   import { HELP_RESOURCE } from '../../helpTexts.js';
   ```
   Note: CharacterEditor is in `resource-library/`, HelpTip is in `components/`. Path: `../HelpTip.vue`.

2. Add HelpTip next to the expression section header `<h3>表情列表</h3>`:
   ```html
   <h3>表情列表 <HelpTip :text="HELP_RESOURCE.characterExpr" /></h3>
   ```

3. Add `title` to untitled buttons:
   - `<button class="add-btn" @click="addCharacter" aria-label="创建角色">+</button>` → add `title="创建新角色"`
   - `<button class="new-char-btn" @click="addCharacter">+ 新角色</button>` → add `title="创建新角色"`
   - `<button class="delete-char-btn" @click="deleteCharacter(selectedId)">删除此角色</button>` → add `title="永久删除该角色及其所有表情"`
   - `<button class="import-expr-btn" @click="importExpression">+ 导入表情</button>` → add `title="导入表情图片文件"`

**F. BgRemovalModal.vue — HelpTip + button titles (HELP-07)**

1. Add imports in `<script setup>`:
   ```javascript
   import HelpTip from '../HelpTip.vue';
   import { HELP_RESOURCE } from '../../helpTexts.js';
   ```

2. Add HelpTip next to the modal header `<span>去除纯色背景</span>`:
   ```html
   <span>去除纯色背景 <HelpTip :text="HELP_RESOURCE.bgRemoval" /></span>
   ```

3. Add `title` to untitled buttons:
   - Close button `<button class="bgr-close" ...>×</button>` → skip (obvious close)
   - Confirm button `<button class="bgr-btn-primary" ...>✅ 确认去背景</button>` → add `title="保存去背景后的透明 PNG"`
   - Direct use button `<button class="bgr-btn-secondary" @click="$emit('skip')">直接使用</button>` → add `title="跳过去背景，直接使用原图"`
   - Cancel button → skip (text label clear)

**G. FontGrid.vue — HelpTip (HELP-07)**

1. Add imports in `<script setup>`:
   ```javascript
   import HelpTip from '../HelpTip.vue';
   import { HELP_RESOURCE } from '../../helpTexts.js';
   ```
   Note: FontGrid is in `resource-library/`, HelpTip is in `components/`. Path: `../HelpTip.vue`.

2. Check for a section header or prominent area to add HelpTip. FontGrid likely renders font cards with a context menu. If there's a header or empty state message, add HelpTip there. If the component has no prominent header, add a HelpTip instance in the template near the top or in the empty state.

3. Button titles: FontGrid likely uses context menus (rename/delete) rather than standalone buttons. Check for any `<button` tags without title and add as needed.

**H. TitleDesigner.vue — HelpTip + button titles (HELP-08)**

1. Add imports in `<script setup>`:
   ```javascript
   import HelpTip from '../components/HelpTip.vue';
   import { HELP_DESIGNER } from '../helpTexts.js';
   ```

2. Add HelpTip instances:
   - Next to "🎮 预制按钮" palette header (line 6):
     ```html
     <div class="palette-header">🎮 预制按钮 <HelpTip :text="HELP_DESIGNER.presetButtons" /></div>
     ```
   - Next to "🎨 元素" palette header (line 15):
     ```html
     <div class="palette-header">🎨 元素 <HelpTip :text="HELP_DESIGNER.decorImage" /></div>
     ```

3. Add `title` to untitled toolbar buttons (9 buttons):
   - `<button class="toolbar-btn" @click="pickBackground">🖼️ 背景</button>` → add `title="选择背景图片"`
   - `<button class="toolbar-btn" v-if="layout.background" @click="clearBackground">✕ 清除背景</button>` → add `title="清除背景图片"`
   - `<button class="toolbar-btn" @click="pickBgm">🎵 BGM</button>` → add `title="选择背景音乐"`
   - `<button class="toolbar-btn" @click="toggleBgmPreview">{{ bgmPlaying ? '⏸ 暂停' : '▶ 试听' }}</button>` → add `:title="bgmPlaying ? '暂停试听' : '试听背景音乐'"`
   - `<button class="toolbar-btn" @click="clearBgm">✕ 清除</button>` → add `title="清除背景音乐"`
   - `<button class="toolbar-btn danger" :disabled="!selectedId" @click="deleteSelected">🗑 删除</button>` → add `title="删除选中元素"`
   - Scan for any additional untitled buttons (e.g., layer order if present) and add titles

**I. SettingsDesigner.vue — HelpTip + button titles (HELP-08)**

1. Add imports in `<script setup>`:
   ```javascript
   import HelpTip from '../components/HelpTip.vue';
   import { HELP_DESIGNER } from '../helpTexts.js';
   ```

2. Add HelpTip instances:
   - Next to "⚙️ 设置组件" palette header (line 6):
     ```html
     <div class="palette-header">⚙️ 设置组件 <HelpTip :text="HELP_DESIGNER.settingComponents" /></div>
     ```
   - Next to "🎨 装饰元素" palette header (line 18):
     ```html
     <div class="palette-header">🎨 装饰元素 <HelpTip :text="HELP_DESIGNER.decorImage" /></div>
     ```

3. Add `title` to untitled toolbar buttons (4 buttons):
   - `<button class="toolbar-btn" @click="pickBackground">🖼️ 背景</button>` → add `title="选择背景图片"`
   - `<button class="toolbar-btn" v-if="layout.background" @click="clearBackground">✕ 清除背景</button>` → add `title="清除背景图片"`
   - `<button class="toolbar-btn danger" :disabled="!selectedId" @click="deleteSelected">🗑 删除</button>` → add `title="删除选中元素"`
   - Any remaining untitled buttons → add titles as appropriate
  </action>
  <verify>
    <automated>powershell -Command "$files = @('src/editor/components/page-editor/PageInspector.vue','src/editor/components/page-editor/SceneTree.vue','src/editor/components/page-editor/CanvasToolbar.vue','src/editor/views/ResourceLibrary.vue','src/editor/components/resource-library/CharacterEditor.vue','src/editor/components/resource-library/BgRemovalModal.vue','src/editor/views/TitleDesigner.vue','src/editor/views/SettingsDesigner.vue'); $ok = $true; foreach ($f in $files) { if (-not (Select-String -Path $f -Pattern 'HelpTip|HELP_' -Quiet)) { Write-Host \"MISSING HelpTip in $f\"; $ok = $false } }; if ($ok) { Write-Host 'All files have HelpTip integration' }"</automated>
  </verify>
  <acceptance_criteria>
    - PageInspector.vue contains `import HelpTip from` and `import { HELP_SCRIPT }` and `<HelpTip :text="HELP_SCRIPT.transition"`
    - CanvasToolbar.vue contains `import HelpTip from` or add-character button has `title="添加角色到当前页面"`
    - ResourceLibrary.vue import button has `title="导入文件到当前分类"`
    - CharacterEditor.vue contains `<HelpTip :text="HELP_RESOURCE.characterExpr"` and add-btn has `title="创建新角色"` and delete-char-btn has `title=` attribute and import-expr-btn has `title=` attribute
    - BgRemovalModal.vue contains `<HelpTip :text="HELP_RESOURCE.bgRemoval"` and bgr-btn-primary has `title=` attribute
    - TitleDesigner.vue contains `import { HELP_DESIGNER }` and `<HelpTip :text="HELP_DESIGNER.presetButtons"` and all 6 toolbar buttons have `title=` attributes
    - SettingsDesigner.vue contains `<HelpTip :text="HELP_DESIGNER.settingComponents"` and toolbar buttons have `title=` attributes
  </acceptance_criteria>
  <done>Script editor has HelpTip on transition and character sections. Resource library has HelpTip on import section, character expression section, and bg removal modal. Title/Settings designers have HelpTip on palette headers. All icon buttons in these 9 files have Chinese title attributes.</done>
</task>

<task type="auto">
  <name>Task 2: Comprehensive button title sweep for all remaining editor components</name>
  <files>src/editor/views/Scenes.vue, src/editor/components/page-editor/AudioPicker.vue, src/editor/components/page-editor/VoiceMatchPreview.vue, src/editor/views/Characters.vue, src/editor/components/page-editor/CharacterPicker.vue, src/editor/views/Assets.vue, src/editor/views/CreateProjectQuick.vue, src/editor/views/CreateProjectWizard.vue, src/editor/views/WelcomeScreen.vue, src/editor/components/canvas/CanvasPreview.vue, src/editor/views/PageEditor.vue, src/editor/components/resource-library/MiniPlayer.vue, src/editor/components/resource-library/ImportNotification.vue, src/editor/components/resource-library/AssetPickerModal.vue, src/editor/App.vue, src/editor/components/TabBar.vue, src/editor/components/canvas/DraggableElement.vue, src/editor/components/page-editor/PageCanvas.vue</files>
  <read_first>
    src/editor/views/Scenes.vue (full file — 10 untitled buttons: scene CRUD, page CRUD, coordinate fields)
    src/editor/components/page-editor/AudioPicker.vue (full file — 5 untitled buttons)
    src/editor/components/page-editor/VoiceMatchPreview.vue (full file — 4 untitled buttons)
    src/editor/views/Characters.vue (full file — 4 untitled buttons)
    src/editor/components/page-editor/CharacterPicker.vue (full file — 3 untitled buttons)
    src/editor/views/Assets.vue (full file — 3 untitled buttons)
    src/editor/views/CreateProjectQuick.vue (full file — 3 untitled buttons)
    src/editor/views/CreateProjectWizard.vue (full file — 6 untitled buttons)
    src/editor/views/WelcomeScreen.vue (full file — 2 untitled buttons)
    src/editor/views/PageEditor.vue (full file — 1 untitled button)
    src/editor/components/resource-library/MiniPlayer.vue (full file — 1 untitled button)
    src/editor/components/resource-library/ImportNotification.vue (full file — 1 untitled button)
    src/editor/components/resource-library/AssetPickerModal.vue (full file — 1 untitled button)
    src/editor/App.vue (full file — line 25 has preview button without title)
    src/editor/components/TabBar.vue (full file — 1 untitled button)
    src/editor/components/canvas/DraggableElement.vue (check for any buttons)
    src/editor/components/canvas/CanvasPreview.vue (check for any buttons)
    src/editor/components/page-editor/PageCanvas.vue (2 untitled buttons)
  </read_first>
  <action>
**Sweep all remaining Vue components and add Chinese `title` attributes to every icon-only button that is missing one, per D-12 and D-13 (inline hardcoded, not referencing helpTexts.js).**

For each file below: open the file, find ALL `<button` elements without a `title=` attribute, and add an appropriate Chinese title. Skip text-only buttons whose label is self-explanatory (e.g., "取消", "确认", "确定", "关闭", labeled tab buttons).

**Rules for title text (per D-13, consistent with Pitfall 4):**
- Title should be 2-8 Chinese characters describing the action
- For buttons with keyboard shortcuts, include in parentheses: `title="保存 (Ctrl+S)"`
- Icon-only buttons (emoji/symbol + no text) → ALWAYS add title
- Icon + short text buttons (e.g., "🗑 删除") → add title for extra context about WHAT it affects
- Pure text buttons with clear labels ("取消", "确认") → SKIP
- Dialog close buttons (×) → SKIP

**File-by-file guide (executor MUST read each file and handle ALL buttons, not just the ones listed below — these are representative examples):**

**Scenes.vue** (~10 buttons): This is a legacy scene editor view. Add titles to:
- Scene/page creation buttons → `title="添加新场景"`, `title="添加新页面"`
- Delete buttons → `title="删除场景"`, `title="删除页面"`
- Any move/reorder buttons → `title="上移"`, `title="下移"`
- Skip text buttons (取消, 确认)

**AudioPicker.vue** (~5 buttons): Audio picker for BGM/SE selection.
- Play/pause buttons → `title="播放"`, `title="暂停"`
- Clear/select buttons → `title="清除音频"`, `title="选择音频文件"`
- Tab buttons with text labels → skip

**VoiceMatchPreview.vue** (~4 buttons): Voice matching preview panel.
- Play/stop buttons → `title="播放语音"`, `title="停止"`
- Confirm/cancel buttons → add title if icon-only, skip if text-labeled

**Characters.vue** (~4 buttons): Character management view.
- Add/delete character buttons → `title="创建新角色"`, `title="删除角色"`
- Import buttons → `title="导入角色图片"`

**CharacterPicker.vue** (~3 buttons): Character selection modal.
- Character selection → add title describing the action
- Close/cancel → skip if text-labeled

**Assets.vue** (~3 buttons): Legacy asset view.
- Import/manage buttons → add appropriate titles

**CreateProjectQuick.vue** (~3 buttons): Quick project creation dialog.
- Create/cancel → skip if text-labeled
- Folder picker → `title="选择项目保存位置"`

**CreateProjectWizard.vue** (~6 buttons): Step-by-step project wizard.
- Navigation (上一步/下一步) → skip if text-labeled
- Folder picker → `title="选择项目保存位置"`
- Any icon-only buttons → add titles

**WelcomeScreen.vue** (~2 buttons): Welcome/start screen.
- Open project / create project → add title if icon-only

**PageEditor.vue** (~1 button): Main page editor wrapper.
- Stop preview overlay button → check if already has title; add if not

**PageCanvas.vue** (~2 buttons): Canvas area with possible overlay buttons.
- Any click-to-action buttons → add titles

**MiniPlayer.vue** (~1 button): Audio mini player.
- Play/pause button → `title="播放"` / `title="暂停"`

**ImportNotification.vue** (~1 button): Import result notification.
- Dismiss button → `title="关闭通知"` if icon-only

**AssetPickerModal.vue** (~1 button): Asset picker modal.
- Close/confirm → add title if icon-only

**App.vue** (~1 button): Main editor header.
- Preview button at line 25 `<button class="preview-btn" @click="openPreview">▶ 预览</button>` → add `title="在新窗口中预览游戏"`

**TabBar.vue** (~1 button): Tab bar.
- If there's an untitled button → add title

**DraggableElement.vue and CanvasPreview.vue**: Check for any `<button` tags. If none, skip.

**IMPORTANT:** After completing all files, run the audit command to verify coverage:
```bash
Select-String -Path src/editor -Recurse -Include "*.vue" -Pattern '<button' | Select-String -Pattern 'title=' -NotMatch
```
This will show remaining untitled buttons. For each result:
- If it's a text-labeled button (取消, 确认, 确定, etc.) → acceptable to leave without title
- If it's an icon-only button → must add title
  </action>
  <verify>
    <automated>powershell -Command "$results = Select-String -Path 'src/editor' -Recurse -Include '*.vue' -Pattern '<button' | Where-Object { $_.Line -notmatch 'title=' }; $count = ($results | Measure-Object).Count; Write-Host \"Buttons without title: $count (target: under 20 — text-labeled only)\"; if ($count -lt 25) { Write-Host 'PASS'; exit 0 } else { Write-Host 'FAIL — too many untitled buttons'; exit 1 }"</automated>
  </verify>
  <acceptance_criteria>
    - Running `Select-String -Path src/editor -Recurse -Include '*.vue' -Pattern '<button' | Where-Object { $_.Line -notmatch 'title=' }` returns fewer than 20 results (remaining should only be text-labeled buttons like 取消/确认/关闭/应用)
    - App.vue preview button has `title="在新窗口中预览游戏"`
    - Scenes.vue scene/page creation buttons have `title=` attributes
    - AudioPicker.vue play/pause/clear buttons have `title=` attributes
    - CharacterEditor.vue add/delete/import buttons have `title=` attributes
    - TitleDesigner.vue all toolbar buttons have `title=` attributes
    - All `<button` tags containing only emoji/icon (no Chinese text label) have a `title=` attribute
  </acceptance_criteria>
  <done>All icon-only and ambiguous buttons across the entire editor (~88 buttons per D-12 estimate) have Chinese title attributes. Grep audit confirms fewer than 20 untitled buttons remain (only text-labeled buttons like 取消/确认 are exempt).</done>
</task>

</tasks>

<verification>
After all tasks complete:

1. HelpTip integration count: `Select-String -Path src/editor -Recurse -Include "*.vue" -Pattern '<HelpTip' | Measure-Object` → at least 20 instances total (across both plans)
2. helpTexts usage: `Select-String -Path src/editor -Recurse -Include "*.vue" -Pattern 'HELP_SCRIPT|HELP_RESOURCE|HELP_DESIGNER' | Measure-Object` → at least 6 matches
3. Button title audit: `Select-String -Path src/editor -Recurse -Include '*.vue' -Pattern '<button' | Where-Object { $_.Line -notmatch 'title=' } | Measure-Object` → fewer than 20 remaining (text-labeled only)
4. Build check: `npm run build` completes without errors
5. Areas covered: All 6 editor areas have HelpTip instances (theme ✓ plan 01, export ✓ plan 01, settings ✓ plan 01, script ✓ this plan, resource ✓ this plan, designers ✓ this plan)
</verification>

<success_criteria>
- PageInspector has HelpTip on transition dropdown area
- SceneTree buttons all have title attributes
- CanvasToolbar has HelpTip next to add-character
- ResourceLibrary import button has title, section header has HelpTip
- CharacterEditor expression section has HelpTip, all buttons have titles
- BgRemovalModal header has HelpTip, confirm/skip buttons have titles
- TitleDesigner palette headers have HelpTip, all 9 toolbar buttons have titles
- SettingsDesigner palette headers have HelpTip, all toolbar buttons have titles
- Comprehensive button title sweep: fewer than 20 untitled buttons remain across entire editor (only text-labeled exemptions)
- `npm run build` passes
</success_criteria>

<output>
After completion, create `.planning/phases/36-tooltip/36-02-SUMMARY.md`
</output>
