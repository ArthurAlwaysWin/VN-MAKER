# 59-01 Plan — Title Page Preview

## Goal
Add iframe-based engine preview to TitleDesigner so users see the actual rendered title page.

## Tasks

### Task 1: Engine — add titleScreen to postMessage handlers + re-render fix
**Files**: `src/main.js`, `src/ui/TitleScreen.js`
**Changes**:
1. `TitleScreen.js` — add `isVisible` getter + re-render on `setLayout()` if visible
2. `main.js` — add `case 'titleScreen'` to `update-screen-layout` switch
3. `main.js` — add `case 'titleScreen'` to `show-screen` switch

### Task 2: Composable — useTitlePreview.js
**Files**: `src/editor/composables/useTitlePreview.js`
**Creates**: Lightweight composable managing iframe ref, engine readiness, debounced postMessage.
- `iframeRef`, `isEngineReady`
- `sendTitleLayoutToPreview()` — debounced 200ms, sends `update-screen-layout` + `show-screen`
- `startEngine()` — sends start message with full script snapshot
- `onEngineMessage()` — handles 'ready' handshake
- `cleanup()` — removes listener + clears timer

### Task 3: TitleDesigner.vue — iframe preview panel + toggle
**Files**: `src/editor/views/TitleDesigner.vue`
**Changes**:
1. Add toggle button in toolbar: "🔍 预览" / "✏️ 编辑"
2. Add `showPreview` ref (default false)
3. Add iframe element (shown when showPreview=true, canvas hidden)
4. Import + use `useTitlePreview`
5. Watch `layout` deep → `sendTitleLayoutToPreview()` for auto-sync

## Wave: 1 (all sequential — engine → composable → editor)

## Verification
- Build passes
- 259+ existing tests pass
- Engine iframe shows title screen when toggle is clicked
