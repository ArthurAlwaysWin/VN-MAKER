---
phase: 62-character-preset-runtime-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/shared/cinematicContract.js
  - src/engine/ScriptEngine.js
  - tests/characterAnimationContract.test.js
autonomous: true
requirements: [ANIM-01, ANIM-02]

must_haves:
  truths:
    - "Page characters carry one normalized preset animation value into runtime page-entry contracts."
    - "`none` and missing animation values behave as no-op defaults without breaking legacy pages or unknown-value preservation."
    - "Dialogue-time `set_expression` updates never retrigger character motion; animation is emitted only from page render entry."
  artifacts:
    - path: "src/shared/cinematicContract.js"
      provides: "Shared character animation defaults and supported preset metadata"
      contains: "DEFAULT_CHARACTER_ANIMATION"
    - path: "src/engine/ScriptEngine.js"
      provides: "show_character payloads that include normalized page-entry animation values"
      contains: "animation:"
    - path: "tests/characterAnimationContract.test.js"
      provides: "Focused regression coverage for preset normalization and ScriptEngine emission boundaries"
      min_lines: 60
  key_links:
    - from: "src/shared/cinematicContract.js"
      to: "src/engine/ScriptEngine.js"
      via: "Normalized character animation helper consumed during _renderPage()"
      pattern: "getCharacterAnimationValue|KNOWN_CHARACTER_ANIMATIONS"
    - from: "src/engine/ScriptEngine.js"
      to: "src/main.js"
      via: "show_character remains the only runtime contract carrying page-entry animation"
      pattern: "emit\\('show_character'"
    - from: "src/engine/ScriptEngine.js"
      to: "src/ui/CharacterLayer.js"
      via: "set_expression stays expression-only so motion playback ownership remains in CharacterLayer"
      pattern: "emit\\('set_expression'"
---

<objective>
Freeze the runtime character-animation contract so page characters can carry stable preset names into runtime without moving playback ownership out of CharacterLayer.

Purpose: Land ANIM-01 and the contract half of ANIM-02 before playback code starts touching DOM/CSS.
Output: Shared preset/default helpers, ScriptEngine page-entry animation emission, and focused regression tests guarding the boundary.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/62-character-preset-runtime-foundation/62-CONTEXT.md
@.planning/phases/61-contract-freeze-visual-ownership/61-02-SUMMARY.md
@.planning/milestones/v1.0-phases/38-expression-crossfade/38-CONTEXT.md
@.planning/milestones/v1.0-phases/39-expression-state-management/39-CONTEXT.md
@docs/superpowers/specs/2026-04-21-v1.4-cinematic-upgrade-design.md
@src/shared/cinematicContract.js
@src/engine/ScriptEngine.js

<interfaces>
From src/shared/cinematicContract.js:
```javascript
export const DEFAULT_CHARACTER_ANIMATION = 'none';

export function getCharacterAnimationValue(animation) {
  if (typeof animation === 'string' && animation.trim()) {
    return animation;
  }
  return DEFAULT_CHARACTER_ANIMATION;
}
```

From src/engine/ScriptEngine.js:
```javascript
this.emit('show_character', {
  id: char.id,
  expression: validExpr,
  position: char.position || 'center',
  x: char.x,
  y: char.y,
  scale: char.scale ?? 1,
  transition: wasVisible ? 'none' : 'fade',
  duration: wasVisible ? 0 : 500,
  image: expressions[validExpr] || '',
});

this.emit('set_expression', {
  id: dlg.speaker,
  expression: validExpr,
  image: expressions[validExpr] || '',
});
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing contract tests for preset normalization and page-entry emission</name>
  <files>
    tests/characterAnimationContract.test.js
  </files>
  <behavior>
    - Test 1: supported preset metadata includes exactly `fade-in`, `slide-in-left`, `slide-in-right`, `shake`, `nod`, `breathe`, and `bounce`, while `none`/missing normalize to no animation.
    - Test 2: `ScriptEngine._renderPage()` emits normalized `animation` on `show_character` for each page character, preserving per-character values such as `breathe` and `slide-in-left`.
    - Test 3: dialogue-time `set_expression` does not emit animation or otherwise retrigger page-entry motion.
  </behavior>
  <action>Add RED coverage first for D-01, D-02, D-04, D-05, D-06, D-10, and D-11. Use the existing compatibility-helper route in `src/shared/cinematicContract.js` rather than inventing a second normalization path. Keep the tests phase-bounded: no editor UI, no preview replay API, no camera effects, and no new transition work.</action>
  <verify>
    <automated>npx vitest run tests/characterAnimationContract.test.js</automated>
  </verify>
  <done>Focused tests exist, encode the locked animation contract, and fail because the current runtime does not yet emit normalized page-entry animation metadata.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement shared preset helpers and ScriptEngine animation emission</name>
  <files>
    src/shared/cinematicContract.js
    src/engine/ScriptEngine.js
    tests/characterAnimationContract.test.js
  </files>
  <behavior>
    - Page characters can carry one supported preset name into runtime via `show_character.animation`.
    - Missing/blank animation values normalize to `none` and stay safe for old pages.
    - `set_expression` remains expression-only and never becomes an ad hoc motion trigger.
  </behavior>
  <action>Expand `src/shared/cinematicContract.js` to export the locked preset list per D-01 (`fade-in`, `slide-in-left`, `slide-in-right`, `shake`, `nod`, `breathe`, `bounce`) while preserving the existing compatibility-helper route from Phase 61. Keep the contract preset-only per D-03: no custom parameters, amplitudes, easing controls, or composed expressions. Normalize `undefined`, empty strings, and `none` to the D-02 no-animation default, but do not coerce unknown raw strings away in page payload ownership. Update `ScriptEngine._renderPage()` so every emitted `show_character` contract carries the normalized page-entry animation and only page-entry animation per D-04 and D-10. Keep `set_expression` expression-only per D-05, preserve Phase 38 crossfade and Phase 39 expression inheritance behavior per D-11, and do not add editor controls, preview APIs, camera logic, or runtime dependencies.</action>
  <verify>
    <automated>npx vitest run tests/characterAnimationContract.test.js &amp;&amp; npm run build</automated>
  </verify>
  <done>`src/shared/cinematicContract.js` exposes stable runtime preset/default helpers, `ScriptEngine` emits normalized page-entry animation values without taking over playback, the new tests pass, and the app still builds.</done>
</task>

</tasks>

<verification>
Run the focused contract suite, then build the app to confirm the added animation metadata does not break the existing runtime/editor bundle.
</verification>

<success_criteria>
- `show_character` payloads include a normalized `animation` field for page-entry rendering.
- The supported runtime preset set matches the seven locked presets, while `none`/missing remain safe no-op defaults.
- `set_expression` remains expression-only, preserving Phase 38/39 behavior and preventing dialogue-time motion retriggers.
</success_criteria>

<output>
After completion, create `.planning/phases/62-character-preset-runtime-foundation/62-01-SUMMARY.md`
</output>
