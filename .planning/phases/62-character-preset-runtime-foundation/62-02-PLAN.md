---
phase: 62-character-preset-runtime-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - "62-01"
files_modified:
  - src/ui/CharacterLayer.js
  - src/style.css
  - tests/characterMotionPlayback.test.js
autonomous: true
requirements: [ANIM-02, ANIM-03]

must_haves:
  truths:
    - "Players see the configured preset motion only when the page renders in, on the `.character-motion` node instead of the layout node."
    - "One-shot presets clean themselves after playback and can replay cleanly on later page entries."
    - "The `breathe` loop stays active only for the current page and is removed on hide, replace, clear, reset, or rapid page changes."
  artifacts:
    - path: "src/ui/CharacterLayer.js"
      provides: "Preset registry, playback, replay, and cleanup ownership for character motion"
      contains: "character-motion"
    - path: "src/style.css"
      provides: "Motion-only keyframes/classes for the seven locked presets"
      contains: "@keyframes"
    - path: "tests/characterMotionPlayback.test.js"
      provides: "jsdom regression coverage for motion playback and cleanup lifecycle"
      min_lines: 90
  key_links:
    - from: "src/ui/CharacterLayer.js"
      to: "src/style.css"
      via: "Preset names resolve to motion-only CSS classes and keyframes"
      pattern: "fade-in|slide-in-left|shake|breathe|bounce"
    - from: "src/engine/ScriptEngine.js"
      to: "src/ui/CharacterLayer.js"
      via: "show_character.animation drives page-entry playback; set_expression does not"
      pattern: "animation:"
    - from: "src/main.js"
      to: "src/ui/CharacterLayer.js"
      via: "replay/title/preview-stop routes already rely on characters.clear() for cleanup"
      pattern: "characters\\.clear\\(\\)"
---

<objective>
Implement CharacterLayer-owned preset motion playback and cleanup so page-entry animations are visible, replayable, and clean under rapid navigation.

Purpose: Finish ANIM-02 and ANIM-03 on the runtime side without bleeding into editor controls, preview replay APIs, or camera/transition work.
Output: CharacterLayer motion registry/playback lifecycle, motion-only CSS keyframes, and focused jsdom regression coverage.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/62-character-preset-runtime-foundation/62-CONTEXT.md
@.planning/phases/61-contract-freeze-visual-ownership/61-01-SUMMARY.md
@.planning/milestones/v1.0-phases/38-expression-crossfade/38-CONTEXT.md
@.planning/milestones/v1.0-phases/39-expression-state-management/39-CONTEXT.md
@docs/superpowers/specs/2026-04-21-v1.4-cinematic-upgrade-design.md
@src/main.js
@src/ui/CharacterLayer.js
@src/style.css
@.planning/phases/62-character-preset-runtime-foundation/62-01-SUMMARY.md

<interfaces>
From src/main.js:
```javascript
engine.on('show_character', (data) => {
  if (skipMode) {
    characters.show({ ...data, duration: 0, transition: 'none', skip: true });
    return;
  }
  characters.show(data);
});

engine.on('set_expression', (data) => {
  if (skipMode) {
    characters.setExpression({ ...data, skip: true });
    return;
  }
  characters.setExpression(data);
});

function replayCurrentPage() {
  characters.clear();
  background.clear();
  engine.resetRenderState();
  engine.renderCurrentPage();
}
```

From src/ui/CharacterLayer.js:
```javascript
export class CharacterLayer {
  show(data) {}
  hide(data) {}
  setExpression(data) {}
  clear() {}
}
```

From src/style.css:
```css
.character-sprite {
  transition-property: opacity, transform;
}

.character-motion {
  position: absolute;
  inset: 0;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing playback and cleanup regression tests for CharacterLayer motion</name>
  <files>
    tests/characterMotionPlayback.test.js
  </files>
  <behavior>
    - Test 1: page-entry `show()` applies each locked preset to `.character-motion`, never `.character-sprite`, and `none`/unknown values produce no motion class.
    - Test 2: one-shot presets (`fade-in`, `slide-in-left`, `slide-in-right`, `shake`, `nod`, `bounce`) self-clean after playback and can replay cleanly on a later `show()` call for the same character.
    - Test 3: `breathe` persists as the only loop until `hide()`, replacement `show()`, or `clear()` is called, and `setExpression()` does not retrigger or disturb motion state.
  </behavior>
  <action>Add RED jsdom coverage for D-01, D-02, D-05, D-06, D-07, D-08, D-09, D-12, D-13, and D-14. Model rapid page changes by calling `show()`/`hide()`/`clear()` in quick succession so the tests prove stale motion classes and inline styles cannot leak between page renders. Keep the suite runtime-only: no editor UI, no replay API, no camera effects, no new page transitions.</action>
  <verify>
    <automated>npx vitest run tests/characterMotionPlayback.test.js</automated>
  </verify>
  <done>Focused playback/cleanup tests exist, encode the locked lifecycle rules, and fail because CharacterLayer does not yet own preset playback or cleanup.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement CharacterLayer preset registry, motion classes, and lifecycle cleanup</name>
  <files>
    src/ui/CharacterLayer.js
    src/style.css
    tests/characterMotionPlayback.test.js
  </files>
  <behavior>
    - All seven locked presets play from `.character-motion` on page entry only.
    - One-shots self-clean and replay cleanly; `breathe` loops until an explicit runtime cleanup path removes it.
    - Expression crossfades remain independent from motion playback.
  </behavior>
  <action>In `src/ui/CharacterLayer.js`, add a preset registry owned by CharacterLayer per D-10 and D-01 for `fade-in`, `slide-in-left`, `slide-in-right`, `shake`, `nod`, `breathe`, and `bounce`. Resolve `none`, missing, or unsupported values to no-op per D-02 and Phase 61 compatibility rules. Apply motion only to `.character-motion` per D-09; do not move layout/scale/enter ownership off `.character-sprite`. Implement replay-safe cleanup for one-shots per D-07 and D-14, and loop cleanup for `breathe` on hide, replacement, clear, render reset, and rapid page changes per D-06, D-08, D-12, and D-13. Preserve Phase 38 crossfade ownership and Phase 39 expression inheritance by keeping `setExpression()` expression-only per D-05 and D-11. In `src/style.css`, add motion-only keyframes/classes for the seven presets without touching new runtime dependencies or container transition ownership.</action>
  <verify>
    <automated>npx vitest run tests/characterMotionPlayback.test.js &amp;&amp; npm run build</automated>
  </verify>
  <done>CharacterLayer owns preset playback/replay/cleanup on `.character-motion`, one-shots self-clean, `breathe` is the only loop and cleans on all required exits, the focused jsdom suite passes, and the app still builds.</done>
</task>

</tasks>

<verification>
Run the motion lifecycle suite, then build the app to confirm the new CSS keyframes and CharacterLayer state tracking do not break runtime bundling.
</verification>

<success_criteria>
- The runtime supports exactly the seven locked presets on `.character-motion` and treats `none`/missing as no animation.
- One-shot presets remove their motion state after playback and can replay on the next page entry.
- `breathe` persists only while the character remains active on the current page and is removed by hide/replace/clear/reset flows.
</success_criteria>

<output>
After completion, create `.planning/phases/62-character-preset-runtime-foundation/62-02-SUMMARY.md`
</output>
