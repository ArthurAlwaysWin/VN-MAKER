---
phase: 63-camera-runtime-shared-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/shared/cinematicContract.js
  - src/engine/ScriptEngine.js
  - tests/cameraContract.test.js
autonomous: true
requirements: [CAM-01, CAM-02, CAM-03]

must_haves:
  truths:
    - "Each page can carry at most one page-level camera contract into runtime as a page_enter payload or null default."
    - "The runtime contract exposes the locked camera fields (`effect`, `durationMs`, `intensity`, `direction`, `trigger`) while preserving unknown effect strings unchanged."
    - "Camera triggering remains page-enter-only; character and dialogue events do not gain camera playback responsibility."
  artifacts:
    - path: "src/shared/cinematicContract.js"
      provides: "Shared camera defaults, supported effect metadata, and compatibility helpers"
      contains: "KNOWN_CAMERA_EFFECTS"
    - path: "src/engine/ScriptEngine.js"
      provides: "page_enter payloads with normalized camera contract data and untouched page payload ownership"
      contains: "camera:"
    - path: "tests/cameraContract.test.js"
      provides: "Focused regression coverage for camera contract defaults, page_enter emission, and unknown-effect preservation"
      min_lines: 80
  key_links:
    - from: "src/shared/cinematicContract.js"
      to: "src/engine/ScriptEngine.js"
      via: "Shared page-camera helper used during _renderPage()"
      pattern: "getPageCameraContract|KNOWN_CAMERA_EFFECTS"
    - from: "src/engine/ScriptEngine.js"
      to: "src/main.js"
      via: "page_enter remains the only trigger surface carrying camera runtime data"
      pattern: "emit\\('page_enter'"
    - from: "src/engine/ScriptEngine.js"
      to: "src/ui/CameraController.js"
      via: "CameraController will consume emitted page camera contracts while raw page payload stays preserved"
      pattern: "camera:"
---

<objective>
Freeze the page-camera runtime contract before playback work starts so Phase 63 has one authoritative shape for page_enter-triggered camera effects.

Purpose: Land the contract half of CAM-01, CAM-02, and CAM-03 without violating the ScriptEngine emitter-only boundary.
Output: Shared camera compatibility helpers, page_enter camera emission in ScriptEngine, and focused regression tests.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/63-camera-runtime-shared-cleanup/63-CONTEXT.md
@.planning/phases/61-contract-freeze-visual-ownership/61-02-SUMMARY.md
@.planning/phases/62-character-preset-runtime-foundation/62-01-SUMMARY.md
@docs/superpowers/specs/2026-04-21-v1.4-cinematic-upgrade-design.md
@src/shared/cinematicContract.js
@src/engine/ScriptEngine.js

<interfaces>
From src/shared/cinematicContract.js:
```javascript
export const DEFAULT_PAGE_CAMERA = null;

export function copyPageCinematicFields(sourcePage, targetPage) {
  targetPage.camera = cloneValue(sourcePage.camera ?? DEFAULT_PAGE_CAMERA);
  targetPage.transition = cloneValue(sourcePage.transition) ?? { type: 'fade', duration: 800 };
}
```

From src/engine/ScriptEngine.js:
```javascript
this.emit('page_enter', {
  sceneId: this.currentScene,
  pageIndex: this.pageIndex,
  page,
});

this.emit('show_character', {
  id: char.id,
  animation: getCharacterAnimationValue(char.animation),
  transition: wasVisible ? 'none' : 'fade',
});
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing camera-contract tests for defaults, page_enter payloads, and compatibility</name>
  <files>
    tests/cameraContract.test.js
  </files>
  <behavior>
    - Test 1: shared helpers expose exactly `shake`, `zoom`, `pan`, and `flash`, default missing page camera to `null`, and force runtime trigger semantics to `onEnter`.
    - Test 2: `ScriptEngine._renderPage()` emits camera data on `page_enter` only, keeping `show_character`, `dialogue`, and `set_expression` free of camera playback fields.
    - Test 3: unknown non-empty `camera.effect` strings survive byte-for-byte in emitted runtime payload ownership while runtime-facing helpers still let later playback layers safe no-op.
    - Test 4: `durationMs`, `intensity`, and `direction` survive emission untouched for supported effects, with `zoom`/`flash` not requiring `direction`.
    - Test 5: shared metadata/helpers explicitly lock the per-effect direction contract: `shake` only allows `horizontal|vertical|both`, `pan` only allows `left|right|up|down`, and `zoom`/`flash` expose no direction contract.
  </behavior>
  <action>Add RED coverage first for D-04, D-07, D-08, D-09, D-13, and the Phase 61 emitter-only boundary: camera triggers must come only from `page_enter`, raw page ownership must remain intact, and unknown effect strings must not be rewritten away. Reference the locked spec per D-07/D-08/D-09: support only `shake`, `zoom`, `pan`, `flash`, preserve `durationMs`, `intensity`, `direction`, fix runtime trigger semantics to `onEnter`, and lock the per-effect direction semantics (`shake: horizontal|vertical|both`, `pan: left|right|up|down`, `zoom/flash: no direction`). Explicitly avoid editor UI, preview replay API work, transition expansion, or any new dependency.</action>
  <verify>
    <automated>npx vitest run tests/cameraContract.test.js</automated>
  </verify>
  <done>Focused contract tests exist, encode the locked camera payload rules plus unknown-effect compatibility, and fail because the current runtime does not yet emit a stable camera contract on `page_enter`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement shared camera helpers and ScriptEngine page_enter emission</name>
  <files>
    src/shared/cinematicContract.js
    src/engine/ScriptEngine.js
    tests/cameraContract.test.js
  </files>
  <behavior>
    - Page camera defaults remain `null`, while non-null camera objects reach runtime as one page-level contract.
    - Supported effects are discoverable through shared helpers, but unknown effect strings remain unchanged for compatibility.
    - `trigger` is normalized to `onEnter` for runtime use without moving camera playback into ScriptEngine.
    - Shared camera metadata locks which effects accept `direction`, and which direction values are valid, so later playback cannot drift from spec.
    - Character/dialogue events stay camera-free so CameraController can remain the only playback owner later.
  </behavior>
  <action>Extend `src/shared/cinematicContract.js` per D-07, D-08, D-09, and D-13 with explicit camera exports for the four locked effects (`shake`, `zoom`, `pan`, `flash`), the fixed runtime trigger `onEnter`, effect metadata that locks the direction contract (`shake: horizontal|vertical|both`, `pan: left|right|up|down`, `zoom/flash: no direction`), and a helper such as `getPageCameraContract(camera)` that clones incoming page data, returns `null` for missing camera, preserves unknown non-empty `effect` values unchanged, and does not invent any new fields. Update `src/engine/ScriptEngine.js` per D-04 so `_renderPage()` emits the normalized camera contract on `page_enter` while keeping `page` itself untouched, leaving `show_character`, `dialogue`, `set_expression`, and other events unchanged to preserve D-01/D-03 ownership boundaries from prior phases. Do not add playback code, editor controls, iframe single-effect preview APIs, transition sequencing changes, or new runtime dependencies.</action>
  <verify>
    <automated>npx vitest run tests/cameraContract.test.js &amp;&amp; npm run build</automated>
  </verify>
  <done>`src/shared/cinematicContract.js` exports stable camera contract helpers, `ScriptEngine` emits camera data only through `page_enter`, unknown effect values remain preserved, the focused suite passes, and the app still builds.</done>
</task>

</tasks>

<verification>
Run the focused contract suite, then build the app to confirm the new camera metadata does not break the existing runtime/editor bundle.
</verification>

<success_criteria>
- Runtime-facing shared helpers know the four locked camera effects and the fixed `onEnter` trigger without mutating raw page payloads.
- `page_enter` carries page camera data for later playback, while `show_character`, `dialogue`, and `set_expression` remain camera-free.
- Unknown `camera.effect` values survive contract emission unchanged so later runtime playback can safe no-op without losing future compatibility.
</success_criteria>

<output>
After completion, create `.planning/phases/63-camera-runtime-shared-cleanup/63-01-SUMMARY.md`
</output>
