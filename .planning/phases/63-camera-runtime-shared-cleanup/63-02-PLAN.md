---
phase: 63-camera-runtime-shared-cleanup
plan: 02
type: execute
wave: 2
depends_on:
  - "63-01"
files_modified:
  - src/ui/CameraController.js
  - src/style.css
  - src/main.js
  - tests/cameraRuntimePlayback.test.js
  - tests/cameraCleanupWiring.test.js
autonomous: true
requirements: [CAM-02, CAM-03, CAM-04]

must_haves:
  truths:
    - "Players see at most one page-level camera effect active on `#stage-layer`, triggered once when a page enters."
    - "`shake`, `zoom`, and `pan` move only the stage visuals, while `flash` uses a stage-local overlay that does not cover dialogue or UI overlays."
    - "Replay, load, return-to-title, preview stop/end, and other existing reset paths clear stage transforms, filters, timers, and flash state deterministically."
  artifacts:
    - path: "src/ui/CameraController.js"
      provides: "Dedicated page-camera owner with single-active playback and cleanup"
      exports: ["CameraController"]
    - path: "src/style.css"
      provides: "Stage-layer camera classes, CSS variables, and stage-local flash overlay styling"
      contains: "camera-flash-overlay"
    - path: "src/main.js"
      provides: "Page-enter camera trigger wiring plus shared cleanup calls in runtime reset paths"
      contains: "camera.clear()"
    - path: "tests/cameraRuntimePlayback.test.js"
      provides: "jsdom regression coverage for CameraController playback, flash scoping, and single-active cleanup"
      min_lines: 100
    - path: "tests/cameraCleanupWiring.test.js"
      provides: "Regression coverage for shared cleanup wiring in main.js reset paths"
      min_lines: 60
  key_links:
    - from: "src/main.js"
      to: "src/ui/CameraController.js"
      via: "page_enter playback and replay/load/title/end/preview cleanup calls"
      pattern: "camera\\.(play|clear)"
    - from: "src/ui/CameraController.js"
      to: "src/style.css"
      via: "Stage camera effect classes/CSS variables and stage-local flash overlay styling"
      pattern: "camera-|flash-overlay"
    - from: "src/ui/CameraController.js"
      to: "#stage-layer"
      via: "Dedicated owner binds only to the reserved stage scope"
      pattern: "stage-layer"
---

<objective>
Implement CameraController-owned playback and wire deterministic shared cleanup into the existing runtime reset paths.

Purpose: Finish CAM-04 and the runtime side of CAM-02/CAM-03 without leaking camera ownership into BackgroundLayer, CharacterLayer, or ScriptEngine.
Output: New `CameraController`, stage-local camera CSS/flash overlay, main.js orchestration wiring, and focused regression coverage.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/63-camera-runtime-shared-cleanup/63-CONTEXT.md
@.planning/phases/61-contract-freeze-visual-ownership/61-01-SUMMARY.md
@.planning/phases/62-character-preset-runtime-foundation/62-02-SUMMARY.md
@docs/superpowers/specs/2026-04-21-v1.4-cinematic-upgrade-design.md
@src/main.js
@src/style.css
@src/shared/cinematicContract.js

<interfaces>
From src/main.js:
```javascript
const stageLayer = document.getElementById('stage-layer');

function replayCurrentPage() {
  characters.clear();
  background.clear();
  engine.resetRenderState();
  engine.renderCurrentPage();
}

engine.on('end', () => {
  if (engine._previewMode) {
    dialogueBox.hide();
    audio.stopVoice();
    window.parent.postMessage({ type: 'ended' }, '*');
    return;
  }

  setTimeout(() => {
    engine.resetRenderState();
    characters.clear();
    background.clear();
    showTitle();
  }, 2000);
});
```

From planned 63-01 output in src/shared/cinematicContract.js:
```javascript
export const KNOWN_CAMERA_EFFECTS = ['shake', 'zoom', 'pan', 'flash'];
export const DEFAULT_CAMERA_TRIGGER = 'onEnter';
export function getPageCameraContract(camera) {}
export function isKnownCameraEffect(effect) {}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing playback and cleanup tests for CameraController and main reset wiring</name>
  <files>
    tests/cameraRuntimePlayback.test.js
    tests/cameraCleanupWiring.test.js
  </files>
  <behavior>
    - Test 1: `CameraController` binds only to `#stage-layer`, plays `shake`, `zoom`, `pan`, and `flash`, and keeps flash overlay DOM inside the stage rather than `#ui-overlay`.
    - Test 2: starting a new camera effect clears the previous one first, unknown effect values safe no-op without mutating the incoming payload, and skip/rapid re-entry cleanup leaves no stale transform/filter/overlay state.
    - Test 3: `main.js` calls camera cleanup from `replayCurrentPage()`, load/quick-load replays, preview restart/start recovery, return-to-title, preview stop, preview end, and normal end/title-reset paths alongside the existing character/background cleanup routes.
  </behavior>
  <action>Add RED coverage for D-01, D-02, D-03, D-04, D-05, D-06, D-10, D-11, D-13, D-14, and D-15. Use jsdom for controller DOM assertions and a focused source-or-behavior regression for `main.js` cleanup routing so the tests prove stage-only ownership, single-active playback, stage-local flash, and deterministic cleanup across replay/load/title/preview stop/preview end/preview restart flows. Keep the suite phase-bounded: no editor camera UI, no iframe single-effect preview API, no transition expansion, no Phase 67 regression matrix, and no new dependency.</action>
  <verify>
    <automated>npx vitest run tests/cameraRuntimePlayback.test.js tests/cameraCleanupWiring.test.js</automated>
  </verify>
  <done>Focused playback/cleanup tests exist, encode the locked runtime behavior, and fail because CameraController plus main cleanup wiring do not exist yet.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement CameraController playback, stage-local flash, and shared cleanup orchestration</name>
  <files>
    src/ui/CameraController.js
    src/style.css
    src/main.js
    tests/cameraRuntimePlayback.test.js
    tests/cameraCleanupWiring.test.js
  </files>
  <behavior>
    - Camera playback is owned by a dedicated `CameraController`, triggered once per `page_enter`, and limited to one active page-level effect at a time.
    - `shake`, `zoom`, and `pan` animate `#stage-layer` itself; `flash` uses a dedicated overlay appended inside `#stage-layer`.
    - Cleanup clears timers, classes, inline styles/CSS variables, filters, and flash state on every required reset path and before new playback starts, including preview restart/start recovery.
  </behavior>
  <action>Create `src/ui/CameraController.js` per D-01 and D-02 as the sole page-camera owner bound only to `#stage-layer`. Give it explicit `play(camera, options = {})` and `clear()` ownership so D-05 and D-06 are enforced: before any new effect, clear the old one; do not queue, chain, or trigger from character/dialogue events. In `play()`, support only `shake`, `zoom`, `pan`, and `flash` per D-07, using `durationMs`, `intensity`, and `direction` per D-08 while treating `zoom`/`flash` direction as ignored. Implement `shake`/`zoom`/`pan` directly on `#stage-layer` per D-10 and a stage-local flash overlay per D-11; use CSS classes/variables in `src/style.css` per D-12 and do not add dependencies. Unknown `camera.effect` values must safe no-op without mutating input per D-13. For D-14, let skip/rapid re-entry prefer 0ms or immediate cleanup rather than guaranteed visible playback. In `src/main.js`, instantiate the controller once, trigger it only from `engine.on('page_enter', ...)` per D-04, and call `camera.clear()` from every existing reset path called out in D-03/D-15: `replayCurrentPage()`, load and quick-load via replay, preview restart/start recovery paths, return-to-title, preview stop, preview end, and normal end/title-reset flow, alongside existing `characters.clear()`, `background.clear()`, and `engine.resetRenderState()` orchestration. Do not move ownership into ScriptEngine, BackgroundLayer, CharacterLayer, or UI overlay code.</action>
  <verify>
    <automated>npx vitest run tests/cameraContract.test.js tests/cameraRuntimePlayback.test.js tests/cameraCleanupWiring.test.js tests/characterMotionPlayback.test.js tests/stageLayerOwnership.test.js &amp;&amp; npm run build</automated>
  </verify>
  <done>`CameraController` exists as the only page-camera owner, all four locked effects run stage-locally with single-active cleanup, main.js clears camera state across the required reset paths, focused regression suites pass, and the app still builds.</done>
</task>

</tasks>

<verification>
Run the focused camera contract/playback/cleanup suites plus the existing stage/character ownership regressions, then build the app to confirm the new controller and CSS integrate cleanly.
</verification>

<success_criteria>
- A page camera effect triggers once from `page_enter`, never from dialogue or character events, and no second page-level effect stacks on top of an uncleared first one.
- `flash` remains visually scoped to the stage layer instead of `#ui-overlay`, while `shake`/`zoom`/`pan` affect only the reserved stage scope.
- Replay, load, quick-load, preview restart/start recovery, title return, preview stop/end, and normal end leave no leftover camera classes, transforms, filters, or flash overlay state.
</success_criteria>

<output>
After completion, create `.planning/phases/63-camera-runtime-shared-cleanup/63-02-SUMMARY.md`
</output>
