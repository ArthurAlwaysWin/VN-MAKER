---
phase: 64-background-transition-expansion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/shared/cinematicContract.js
  - src/ui/BackgroundLayer.js
  - src/style.css
  - tests/cinematicContractCompatibility.test.js
  - tests/backgroundLayerTransitions.test.js
autonomous: true
requirements: [TRAN-01, TRAN-02]

must_haves:
  truths:
    - "Creators can choose the locked transition set `none`, `fade`, `slide-left`, `slide-right`, `dissolve`, `wipe`, `scale`, and `blur` without losing unknown saved values."
    - "Background transitions stay owned only by `BackgroundLayer`, run through the existing dual-layer DOM structure, and visibly distinguish the new effects from legacy `fade`/`slide-*` behavior."
    - "When a transition finishes or is interrupted, background layers return to a clean stable state with no leftover temporary class, filter, transform, or hidden stale image."
  artifacts:
    - path: "src/shared/cinematicContract.js"
      provides: "Shared transition registry, UI labels, unknown-option preservation, and runtime fallback helpers"
      contains: "scale"
    - path: "src/ui/BackgroundLayer.js"
      provides: "Sole background-transition owner with completion/cancel-safe transition handling"
      exports: ["BackgroundLayer"]
    - path: "src/style.css"
      provides: "CSS-only background transition classes/keyframes for dissolve, wipe, scale, and blur"
      contains: "bg-transition"
    - path: "tests/backgroundLayerTransitions.test.js"
      provides: "jsdom regression coverage for BackgroundLayer playback, cleanup, and interruption semantics"
      min_lines: 100
  key_links:
    - from: "src/shared/cinematicContract.js"
      to: "src/editor/components/page-editor/PageInspector.vue"
      via: "getTransitionUiOptions() drives the page transition select"
      pattern: "getTransitionUiOptions"
    - from: "src/shared/cinematicContract.js"
      to: "src/engine/ScriptEngine.js"
      via: "getRuntimeTransitionType() preserves unknown payloads while runtime falls back safely"
      pattern: "getRuntimeTransitionType"
    - from: "src/ui/BackgroundLayer.js"
      to: "src/style.css"
      via: "BackgroundLayer applies CSS classes/variables on the existing dual-layer DOM"
      pattern: "bg-transition|bg-image-layer"
---

<objective>
Expand the shared transition contract and the BackgroundLayer owner so Phase 64 gains expressive CSS-only page transitions without breaking compatibility.

Purpose: Land the transition catalog and owner-side cleanup foundation first so later sequencing work can wait on a stable background completion signal instead of duplicating timing guesses.
Output: Updated transition metadata, a completion-aware `BackgroundLayer`, new background transition CSS, and focused regression coverage.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/64-background-transition-expansion/64-CONTEXT.md
@.planning/phases/64-background-transition-expansion/64-RESEARCH.md
@.planning/phases/61-contract-freeze-visual-ownership/61-01-SUMMARY.md
@.planning/phases/63-camera-runtime-shared-cleanup/63-02-SUMMARY.md
@docs/superpowers/specs/2026-04-21-v1.4-cinematic-upgrade-design.md
@src/shared/cinematicContract.js
@src/ui/BackgroundLayer.js
@src/style.css
@src/editor/components/page-editor/PageInspector.vue
@tests/cinematicContractCompatibility.test.js

<interfaces>
From src/shared/cinematicContract.js:
```javascript
const KNOWN_TRANSITION_OPTIONS = [
  { value: 'fade', label: '淡入淡出', known: true },
  { value: 'slide-left', label: '左滑入', known: true },
  { value: 'slide-right', label: '右滑入', known: true },
  { value: 'none', label: '无', known: true },
];

export function getTransitionUiOptions(currentType) {}
export function getRuntimeTransitionType(type) {}
```

From src/ui/BackgroundLayer.js:
```javascript
export class BackgroundLayer {
  setBackground(data) {}
  clear() {}
}
```

From src/editor/components/page-editor/PageInspector.vue:
```javascript
const transitionOptions = computed(() => getTransitionUiOptions(page.value?.transition?.type));
const selectedTransitionType = computed(() => page.value?.transition?.type || 'fade');
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing contract and BackgroundLayer owner tests for the expanded transition set</name>
  <files>
    tests/cinematicContractCompatibility.test.js
    tests/backgroundLayerTransitions.test.js
  </files>
  <behavior>
    - Test 1: shared transition helpers expose exactly `none`, `fade`, `slide-left`, `slide-right`, `dissolve`, `wipe`, `scale`, and `blur`; `scale` is the internal enum; unknown values still surface as `未知转场：...`; and runtime fallback for unknown values stays `fade`.
    - Test 2: `BackgroundLayer` remains the only background-transition owner on the existing dual-layer DOM and can play `fade`, `dissolve`, `wipe`, `scale`, `blur`, `slide-left`, `slide-right`, `none`, and cut/immediate paths without a second controller.
    - Test 3: finishing, interrupting, or clearing a transition removes temporary classes, filters, transforms, timers, and stale outgoing imagery so the stable background state is clean.
  </behavior>
  <action>Add RED coverage for D-01, D-04, D-05, D-06, D-07, D-08, D-09, D-10, D-15, and D-16. Extend `tests/cinematicContractCompatibility.test.js` for the locked transition set, `scale` naming, UI labels, unknown-option preservation, and runtime fallback. Create `tests/backgroundLayerTransitions.test.js` with jsdom coverage for the existing `layerA/layerB + active` structure per D-08, including completion/interrupt behavior so later sequencing can trust a real owner signal. Keep this phase-bounded: no iframe single-effect preview API, no new editor mode, no full Phase 67 regression matrix, and no new dependency.</action>
  <verify>
    <automated>npx vitest run tests/cinematicContractCompatibility.test.js tests/backgroundLayerTransitions.test.js</automated>
  </verify>
  <done>The focused contract/owner tests exist, encode the locked Phase 64 behavior, and fail because the shared transition set and BackgroundLayer owner are not yet expressive or cleanup-safe enough.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the shared transition registry and a completion-aware BackgroundLayer owner</name>
  <files>
    src/shared/cinematicContract.js
    src/ui/BackgroundLayer.js
    src/style.css
    tests/cinematicContractCompatibility.test.js
    tests/backgroundLayerTransitions.test.js
  </files>
  <behavior>
    - The shared contract advertises the locked eight transition values, preserves unknown saved values for editor/store round-trips, and still falls back to `fade` only at runtime consumption.
    - `BackgroundLayer` owns all background transition playback on the existing dual-layer DOM and exposes a concrete completion signal that resolves on finish or safe interruption.
    - `dissolve`, `wipe`, `scale`, and `blur` are implemented with CSS/DOM only, are visibly distinct from legacy behavior, and leave no stable-state pollution after cleanup.
  </behavior>
  <action>Update `src/shared/cinematicContract.js` per D-04, D-05, D-15, and D-16 so the known transition list becomes `none`, `fade`, `slide-left`, `slide-right`, `dissolve`, `wipe`, `scale`, and `blur`, with Chinese UI labels and `scale` as the only internal enum for the zoom-like transition. Preserve unknown values unchanged in UI helpers and store ownership; keep runtime fallback at `getRuntimeTransitionType()` only. Rework `src/ui/BackgroundLayer.js` per D-01, D-06, D-07, D-08, D-09, and D-10 so it remains the sole owner, still uses `layerA/layerB + active`, and makes `setBackground(data)` return a promise that resolves when the transition has settled or has been safely cancelled/replaced. Add cancel-safe cleanup helpers so `clear()` and rapid replacement remove temporary class names, `filter`, `transform`, and stale outgoing imagery. In `src/style.css`, add CSS-only classes/keyframes/variables for `dissolve`, `wipe`, `scale`, and `blur`, keeping `wipe` preset-only per D-06 and making `dissolve` perceptibly different from `fade` per D-09 without introducing canvas, shaders, or third-party animation libraries. Do not move transition ownership into `ScriptEngine`, `main.js`, `CameraController`, or editor code.</action>
  <verify>
    <automated>npx vitest run tests/cinematicContractCompatibility.test.js tests/backgroundLayerTransitions.test.js &amp;&amp; node --test tests/scriptEngine.test.js &amp;&amp; npm run build</automated>
  </verify>
  <done>The shared transition contract exposes the locked Phase 64 set, unknown transition values still survive compatibility paths, `BackgroundLayer` can play and clean up the new CSS-only effects as the sole owner, and the focused suites plus build all pass.</done>
</task>

</tasks>

<verification>
Run the compatibility suite, the new BackgroundLayer jsdom suite, the existing ScriptEngine compatibility regression, and a production build to prove the contract and owner foundation integrate cleanly.
</verification>

<success_criteria>
- The editor/runtime shared helper layer knows the full locked Phase 64 transition set and still preserves unknown values byte-for-byte until runtime playback.
- `BackgroundLayer` alone can play `dissolve`, `wipe`, `scale`, and `blur` on the existing dual-layer DOM without introducing a second transition controller or new dependency.
- Transition completion, interruption, and clear paths leave no lingering background class, filter, transform, timer, or stale outgoing image state.
</success_criteria>

<output>
After completion, create `.planning/phases/64-background-transition-expansion/64-01-SUMMARY.md`
</output>
