---
phase: 64-background-transition-expansion
plan: 02
type: execute
wave: 2
depends_on:
  - "64-01"
files_modified:
  - src/main.js
  - tests/backgroundTransitionWiring.test.js
autonomous: true
requirements: [TRAN-04]

must_haves:
  truths:
    - "Players do not see character entry, camera playback, or dialogue/choice UI start before the background transition has finished or safe-cut."
    - "Skip, 0ms, and unchanged-background pages still advance deterministically without hanging the page-enter flow."
    - "Replay, load, title return, preview start/stop, and end flows clear any pending background-transition gate so no deferred events leak into the next page."
  artifacts:
    - path: "src/main.js"
      provides: "Concrete background-transition gate and deferred page-enter fan-out in the runtime orchestration layer"
      contains: "pendingPageEnter"
    - path: "tests/backgroundTransitionWiring.test.js"
      provides: "Regression coverage for deferred page-enter release, immediate-cut paths, and gate cleanup wiring"
      min_lines: 80
  key_links:
    - from: "src/main.js"
      to: "src/ui/BackgroundLayer.js"
      via: "Awaited transition completion signal before releasing page-enter fan-out"
      pattern: "background\\.setBackground|flushPageTransitionGate"
    - from: "src/main.js"
      to: "src/ui/CharacterLayer.js"
      via: "Queued `show_character` / `set_expression` release after the background gate flushes"
      pattern: "pendingCharacterEvents|characters\\.(show|setExpression)"
    - from: "src/main.js"
      to: "src/ui/CameraController.js"
      via: "Delayed page_enter side effects trigger camera only after background transition completion"
      pattern: "camera\\.(play|clear)"
---

<objective>
Gate page-enter orchestration behind background transition completion so Phase 64 sequencing stays stable without breaking existing reset flows.

Purpose: Finish TRAN-04 by making `main.js` concretely delay character/camera/dialogue entry until the background owner reports completion, while still safe-cutting on skip and cleanup paths.
Output: A deferred page-enter gate in `main.js` plus regression coverage for sequencing and cleanup routing.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/64-background-transition-expansion/64-CONTEXT.md
@.planning/phases/64-background-transition-expansion/64-RESEARCH.md
@docs/superpowers/specs/2026-04-21-v1.4-cinematic-upgrade-design.md
@src/main.js
@src/ui/BackgroundLayer.js
@tests/scriptEngine.test.js

<interfaces>
From src/main.js:
```javascript
engine.on('show_character', (data) => {
  if (skipMode) {
    characters.show({ ...data, duration: 0, transition: 'none', skip: true });
    return;
  }
  characters.show(data);
});

engine.on('set_background', (data) => {
  if (skipMode) {
    background.setBackground({ ...data, duration: 0, transition: 'cut' });
    return;
  }
  background.setBackground(data);
});

engine.on('page_enter', (data) => {
  if (skipMode) {
    camera.clear();
  } else {
    camera.play(data.camera);
  }
});

function replayCurrentPage() {
  camera.clear();
  characters.clear();
  background.clear();
  engine.resetRenderState();
  engine.renderCurrentPage();
}
```

From src/ui/BackgroundLayer.js after 64-01:
```javascript
export class BackgroundLayer {
  setBackground(data) {} // returns a completion promise
  clear() {}
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing sequencing and gate-cleanup regressions for deferred page-enter fan-out</name>
  <files>
    tests/backgroundTransitionWiring.test.js
  </files>
  <behavior>
    - Test 1: `main.js` opens a concrete gate on `page_enter`, queues new-page `show_character`, `set_expression`, `dialogue`, and `choice` work, and releases them only after `BackgroundLayer` reports completion.
    - Test 2: skip/cut, `duration: 0`, and no-background-change pages flush immediately and do not deadlock the render cycle.
    - Test 3: replay, load/quick-load via replay, return-to-title, preview start, preview stop, preview end, and normal end cancel pending gate state before clearing background/character/camera owners.
  </behavior>
  <action>Add RED coverage for D-03, D-11, D-12, D-13, and D-14 around a concrete deferred fan-out design in `main.js`. Make the test assert that `hide_character` remains part of old-page exit while new-page `show_character`, `set_expression`, camera/read-history page-enter side effects, and dialogue/choice display wait for the background owner signal. Keep the suite phase-bounded: no iframe single-effect preview API, no new editor mode, no full Phase 67 flow matrix, and no attempt to move sequencing into `ScriptEngine`.</action>
  <verify>
    <automated>npx vitest run tests/backgroundTransitionWiring.test.js</automated>
  </verify>
  <done>The sequencing regression exists, encodes the concrete gate/fan-out behavior Phase 64 needs, and fails because `main.js` still releases page-enter effects immediately.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the main.js background gate and reset-safe deferred page-enter release</name>
  <files>
    src/main.js
    tests/backgroundTransitionWiring.test.js
  </files>
  <behavior>
    - The runtime orchestration layer waits for the background owner to finish before starting character entry, camera playback, and dialogue/choice UI for the new page.
    - Fast paths (`skip`, cut, `duration: 0`, or unchanged background) release immediately without leaving queued state behind.
    - Reset paths cancel any in-flight gate before clearing owners so no deferred work leaks into replay, load, preview, title, or end transitions.
  </behavior>
  <action>Implement an explicit deferred fan-out in `src/main.js` per D-03, D-11, D-12, D-13, and D-14. Add concrete gate state such as `pendingPageEnter`, `pendingCharacterEvents`, `pendingUiEvent`, a render token, and `flush/cancel` helpers so sequencing is deterministic rather than timer-guessing. On `engine.on('page_enter')`, capture metadata and open the gate instead of immediately calling `camera.play()` or read-history logic. On `engine.on('set_background')`, call the `BackgroundLayer` promise API from 64-01, forcing `{ duration: 0, transition: 'cut' }` during skip per D-13, and flush the gate only when that promise resolves. Queue new-page `show_character`, `set_expression`, `dialogue`, and `choice` while the gate is open; keep `hide_character` immediate so old-page exit still happens before the background transition. If no `set_background` event arrives during the render turn because the background is unchanged, flush on the same turn's microtask/frame boundary so pages do not hang. When flushing, release the new-page work in stable order: character entry first, then delayed page-enter side effects (camera + read-history/skip-read-only logic), then dialogue/choice display so D-12 is satisfied. Cancel pending gate state before `replayCurrentPage()`, load/quick-load replay, return-to-title, preview `start` reset, preview `stop`, preview end, and normal end/title-reset cleanup. Do not move orchestration into `ScriptEngine`, do not add a second transition owner, and do not broaden scope into Phase 65 preview replay APIs or Phase 67's full regression matrix.</action>
  <verify>
    <automated>npx vitest run tests/backgroundTransitionWiring.test.js tests/backgroundLayerTransitions.test.js tests/cameraCleanupWiring.test.js &amp;&amp; npm run build</automated>
  </verify>
  <done>`main.js` contains a concrete background-transition gate that defers new-page fan-out until background completion, immediate-cut paths still progress safely, reset paths cancel queued work, and the sequencing/build regressions all pass.</done>
</task>

</tasks>

<verification>
Run the new sequencing regression together with the background-owner and camera cleanup guardrails, then build the app to confirm the deferred gate integrates with existing runtime orchestration.
</verification>

<success_criteria>
- Character entry, camera playback, and dialogue/choice UI no longer race ahead of the background transition.
- Skip/cut/unchanged-background pages complete immediately instead of waiting on a nonexistent transition.
- Replay, load, preview, title return, and end cleanup paths cannot leak deferred page-enter work into the next render.
</success_criteria>

<output>
After completion, create `.planning/phases/64-background-transition-expansion/64-02-SUMMARY.md`
</output>
