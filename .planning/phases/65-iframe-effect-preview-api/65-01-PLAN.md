---
phase: 65-iframe-effect-preview-api
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/editor/composables/usePageEditor.js
  - src/editor/views/PageEditor.vue
  - src/editor/components/page-editor/CanvasToolbar.vue
  - tests/pageEditorEffectPreviewState.test.js
autonomous: true
requirements: [ANIM-04, PREV-02, PREV-03]

must_haves:
  truths:
    - "Editor state can start a single character, camera, or transition effect preview request without entering the full play session path."
    - "Unavailable preview actions expose explicit disabled reasons before any iframe message is sent."
    - "Effect-preview terminal results update busy/failure state and return the editor out of temporary effect preview mode without breaking Phase 14 full-play semantics."
  artifacts:
    - path: "src/editor/composables/usePageEditor.js"
      provides: "Effect preview session owner with request bookkeeping, preflight reasons, and shared request builders"
      contains: "previewSessionType"
    - path: "src/editor/views/PageEditor.vue"
      provides: "Iframe/read-only routing that distinguishes full play from effect preview sessions"
      contains: "previewSessionType"
    - path: "src/editor/components/page-editor/CanvasToolbar.vue"
      provides: "Toolbar behavior that preserves full-play controls while reflecting effect-preview session state"
      contains: "previewSessionType"
    - path: "tests/pageEditorEffectPreviewState.test.js"
      provides: "Vitest coverage for preflight reasons, request/result bookkeeping, and session-type restore rules"
      min_lines: 120
  key_links:
    - from: "src/editor/composables/usePageEditor.js"
      to: "src/main.js"
      via: "postMessage `preview-effect` / `preview-effect-stop` envelope"
      pattern: "preview-effect"
    - from: "src/editor/views/PageEditor.vue"
      to: "src/editor/composables/usePageEditor.js"
      via: "iframe visibility and stop affordances depend on `previewSessionType` instead of a single play-only flag"
      pattern: "previewSessionType|isPreviewMode"
    - from: "src/editor/components/page-editor/CanvasToolbar.vue"
      to: "src/editor/composables/usePageEditor.js"
      via: "Toolbar labels and stop button stay tied to the correct preview session owner"
      pattern: "startPreview|stopPreview|previewSessionType"
---

<objective>
Create the editor-side effect preview contract so Phase 65 has one reusable request/state owner before runtime replay wiring lands.

Purpose: Lock the request lifecycle, disabled reasons, and play-vs-effect session split early so Phase 66 can add buttons without reopening protocol semantics.
Output: A tested `usePageEditor` effect-preview state owner plus minimal PageEditor/toolbar session plumbing that preserves existing full-play behavior.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/65-iframe-effect-preview-api/65-CONTEXT.md
@.planning/phases/65-iframe-effect-preview-api/65-RESEARCH.md
@.planning/phases/64-background-transition-expansion/64-02-SUMMARY.md
@docs/superpowers/specs/2026-04-21-v1.4-cinematic-upgrade-design.md
@src/editor/composables/usePageEditor.js
@src/editor/views/PageEditor.vue
@src/editor/components/page-editor/CanvasToolbar.vue

<interfaces>
From src/editor/composables/usePageEditor.js:
```javascript
const isPreviewMode = ref(false);
const isMuted = ref(false);
const isEngineReady = ref(false);
const previewIframeRef = ref(null);

function startPreview() {}
function stopPreview() {}
function toggleMute() {}
function onEngineMessage(event) {}
```

From src/editor/views/PageEditor.vue:
```vue
<PageCanvas v-show="!editor.isPreviewMode.value" />
<iframe v-show="editor.isPreviewMode.value" :ref="onIframeRef" class="preview-iframe" src="/index.html"></iframe>
<button v-if="editor.isPreviewMode.value" class="preview-overlay-stop" @click="editor.stopPreview()">■ 停止</button>
```

From src/editor/components/page-editor/CanvasToolbar.vue:
```vue
<template v-if="!editor.isPreviewMode.value">
  <button class="toolbar-btn play-btn" :disabled="!editor.currentPage.value || !editor.isEngineReady.value" @click="editor.startPreview()">▶ 试玩</button>
</template>
<template v-else>
  <span class="toolbar-info preview-label">🎮 试玩中</span>
  <button class="toolbar-btn stop-btn" @click="editor.stopPreview()">■ 停止</button>
</template>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing editor-state tests for effect preview requests, disabled reasons, and terminal result handling</name>
  <files>
    tests/pageEditorEffectPreviewState.test.js
  </files>
  <behavior>
    - Test 1: preflight rejects `engine-not-ready`, `no-page-selected`, `missing-character-animation`, `missing-camera-config`, and `missing-transition-config` before any iframe `postMessage`.
    - Test 2: `previewEffect` sends one D-01/D-02 request envelope with `type`, `requestId`, `effectKind`, `sceneId`, `pageIndex`, `script`, and effect payload, then enters busy effect-preview state only after the request is issued.
    - Test 3: `preview-effect-result` messages update busy/current-request/last-result fields for `accepted`, `completed`, `cancelled`, `rejected`, and `failed`, keeping D-03 bookkeeping in `usePageEditor.js`.
    - Test 4: full-play sessions (`previewSessionType === 'play'`) keep existing stop/overlay semantics, while effect sessions auto-exit back to the canvas on terminal results instead of pretending to be full test play.
  </behavior>
  <action>Per D-01, D-02, D-03, D-10, and D-11, create `tests/pageEditorEffectPreviewState.test.js` as a focused Vitest suite around `createPageEditor()`. Mock the iframe `contentWindow.postMessage`, ready handshake, and result messages. Encode the future-facing contract now: a shared `preview-effect` request family, a sibling `preview-effect-stop` cancel message, explicit reason codes, and a `previewSessionType` split (`play` vs `effect`) so Phase 14 full preview keeps working. Keep this task phase-bounded: do not add PageInspector buttons, do not add button copy/layout work from Phase 66, and do not broaden into runtime restore implementation yet.</action>
  <verify>
    <automated>npx vitest run tests/pageEditorEffectPreviewState.test.js</automated>
  </verify>
  <done>The new editor-state suite exists, documents the locked Phase 65 request/result contract, and fails because `usePageEditor` still only knows `start` / `stop` / `mute` full-play behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement editor-side effect preview session plumbing without expanding PageInspector UX</name>
  <files>
    src/editor/composables/usePageEditor.js
    src/editor/views/PageEditor.vue
    src/editor/components/page-editor/CanvasToolbar.vue
    tests/pageEditorEffectPreviewState.test.js
  </files>
  <behavior>
    - `usePageEditor` owns D-03 request bookkeeping: current request, busy state, last terminal result, and distinct preflight failure reasons.
    - Effect preview requests deep-clone the current `script.data`, attach the selected scene/page identity, and leave runtime authority to later Plan 65-02.
    - Existing full test play still uses `start` / `stop` / `mute`, while effect preview uses `previewSessionType === 'effect'` so stop/read-only/iframe visibility no longer assume “试玩中” means only full play.
  </behavior>
  <action>Implement the editor-side owner per D-01, D-02, D-03, D-06, D-10, and D-11 inside `src/editor/composables/usePageEditor.js`. Add a `previewSessionType` ref (`null | 'play' | 'effect'`), a shared `previewEffect(request)` entrypoint, and convenience helpers for character/camera/transition payload construction that Phase 66 buttons can call later without new state machines. `previewEffect` must deep-clone the current script snapshot exactly as `startPreview()` already does, attach `sceneId`/`pageIndex`, and record reason-coded preflight failures before sending anything. Add `stopActiveEffectPreview()` that posts `preview-effect-stop` for the active request, and update `onEngineMessage()` so `preview-effect-result` drives busy/terminal state plus auto-exit for effect sessions only. In `PageEditor.vue` and `CanvasToolbar.vue`, switch any play-only assumptions to `previewSessionType` so full-play controls remain Phase 14-compatible while effect preview can temporarily show the iframe and stop affordance without adding new inspector controls. Explicitly avoid Phase 66 work: no new PageInspector buttons, layouts, labels, or expanded cinematic config UI.</action>
  <verify>
    <automated>npx vitest run tests/pageEditorEffectPreviewState.test.js &amp;&amp; npm run build</automated>
  </verify>
  <done>`usePageEditor` exposes a stable effect-preview request/result contract with explicit reasons, PageEditor/toolbar preserve existing full-play behavior, and the focused suite plus build pass without any premature Phase 66 UX expansion.</done>
</task>

</tasks>

<verification>
Run the new editor-state suite and a production build to prove the composable contract and minimal session plumbing compile cleanly before runtime replay work starts.
</verification>

<success_criteria>
- Phase 65 has one editor-owned effect-preview API instead of ad hoc button-local state.
- Disabled reasons are explicit and traceable before runtime executes anything.
- Full test play keeps Phase 14 semantics while effect preview gets its own session type and terminal-result handling.
</success_criteria>

<output>
After completion, create `.planning/phases/65-iframe-effect-preview-api/65-01-SUMMARY.md`
</output>
