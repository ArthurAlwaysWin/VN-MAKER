---
phase: 65-iframe-effect-preview-api
plan: 02
type: execute
wave: 2
depends_on:
  - "65-01"
files_modified:
  - src/main.js
  - src/ui/BackgroundLayer.js
  - src/style.css
  - tests/iframeEffectPreviewWiring.test.js
  - tests/backgroundTransitionPreview.test.js
  - tests/backgroundTransitionWiring.test.js
  - tests/cameraCleanupWiring.test.js
autonomous: true
requirements: [ANIM-04, TRAN-03, PREV-02, PREV-03]

must_haves:
  truths:
    - "Runtime accepts one shared effect-preview protocol for character, camera, and transition requests and returns explicit accepted/terminal results with reason codes."
    - "Single-effect previews execute through CharacterLayer, CameraController, and BackgroundLayer only, with a global single-instance lock and no real page navigation for transition replay."
    - "When an effect preview completes, is cancelled, is superseded, or fails, the iframe returns to the pre-preview scene/page state with Phase 64 gate cleanup intact."
  artifacts:
    - path: "src/main.js"
      provides: "Effect preview protocol handlers, preview lock, snapshot restore helper, and owner-backed replay orchestration"
      contains: "preview-effect"
    - path: "src/ui/BackgroundLayer.js"
      provides: "Same-page transition preview support that stays inside BackgroundLayer ownership"
      contains: "previewVariant"
    - path: "src/style.css"
      provides: "Runtime-only same-page transition preview styling without polluting stable background state"
      contains: "bg-preview"
    - path: "tests/iframeEffectPreviewWiring.test.js"
      provides: "Protocol-level regression coverage for request/result statuses, restore wiring, and preview lock semantics"
      min_lines: 140
    - path: "tests/backgroundTransitionPreview.test.js"
      provides: "BackgroundLayer coverage for same-page transition preview playback and cleanup"
      min_lines: 100
  key_links:
    - from: "src/main.js"
      to: "src/engine/ScriptEngine.js"
      via: "Captured snapshots restored through `getState()` / `restoreState()` / `resetRenderState()` / `renderCurrentPage()`"
      pattern: "getState|restoreState|resetRenderState|renderCurrentPage"
    - from: "src/main.js"
      to: "src/ui/BackgroundLayer.js"
      via: "Transition preview waits on BackgroundLayer completion and preserves Phase 64 gate semantics"
      pattern: "setBackground|cancelPageTransitionGate"
    - from: "src/main.js"
      to: "src/editor/composables/usePageEditor.js"
      via: "Runtime posts `preview-effect-result` statuses consumed by editor request bookkeeping"
      pattern: "postMessage\\(\\{ type: 'preview-effect-result'"
---

<objective>
Implement the runtime-backed effect replay API so character animation, camera effect, and transition preview run through the iframe owners and always restore the selected page state afterward.

Purpose: Finish the Phase 65 runtime half by reusing existing owner boundaries and Phase 64 cleanup/gate rules instead of inventing a second preview engine.
Output: Shared runtime protocol handling in `main.js`, restore-safe replay orchestration, same-page transition preview support in `BackgroundLayer`, and focused regression coverage.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/65-iframe-effect-preview-api/65-CONTEXT.md
@.planning/phases/65-iframe-effect-preview-api/65-RESEARCH.md
@.planning/phases/63-camera-runtime-shared-cleanup/63-02-SUMMARY.md
@.planning/phases/64-background-transition-expansion/64-01-SUMMARY.md
@.planning/phases/64-background-transition-expansion/64-02-SUMMARY.md
@docs/superpowers/specs/2026-04-21-v1.4-cinematic-upgrade-design.md
@src/main.js
@src/engine/ScriptEngine.js
@src/ui/BackgroundLayer.js
@src/ui/CharacterLayer.js
@src/ui/CameraController.js
@tests/backgroundTransitionWiring.test.js
@tests/cameraCleanupWiring.test.js

<interfaces>
From src/main.js:
```javascript
function replayCurrentPage() {
  cancelPageTransitionGate();
  camera.clear();
  characters.clear();
  background.clear();
  engine.resetRenderState();
  engine.renderCurrentPage();
}

function initPreview() {
  function handlePreviewMessage(e) {
    switch (msg.type) {
      case 'start': {}
      case 'stop': {}
      case 'mute': {}
    }
  }
}
```

From src/engine/ScriptEngine.js:
```javascript
getState() {}
restoreState(state) {}
resetRenderState() {}
renderCurrentPage() {}
```

From src/ui/BackgroundLayer.js:
```javascript
export class BackgroundLayer {
  setBackground(data) {} // returns Promise that resolves when the transition settles
  clear() {}
}
```

From src/ui/CharacterLayer.js and src/ui/CameraController.js:
```javascript
characters.show(data);
characters.clear();
camera.play(cameraConfig, options = {});
camera.clear();
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing runtime protocol and same-page transition preview regressions</name>
  <files>
    tests/iframeEffectPreviewWiring.test.js
    tests/backgroundTransitionPreview.test.js
    tests/backgroundTransitionWiring.test.js
    tests/cameraCleanupWiring.test.js
  </files>
  <behavior>
    - Test 1: `main.js` accepts a shared `preview-effect` message, emits `preview-effect-result` for `accepted`, `completed`, `cancelled`, `rejected`, and `failed`, and distinguishes explicit reasons such as `unsupported-effect`, `preview-busy`, `restore-failed`, and `runtime-error`.
    - Test 2: new preview requests obey the D-06 global lock by cancelling/restoring the old request before accepting the replacement request, while stop/supersede/error paths reuse the same restore helper.
    - Test 3: character preview replays only via `CharacterLayer`, camera preview only via `CameraController`, and transition preview only via `BackgroundLayer` without mutating scene/page identity or bypassing Phase 64 gate cleanup.
    - Test 4: same-page transition preview remains visible when outgoing/incoming background are identical, then clears all temporary preview classes/state after completion or interruption.
  </behavior>
  <action>Create RED coverage for D-04 through D-12 in `tests/iframeEffectPreviewWiring.test.js` and `tests/backgroundTransitionPreview.test.js`. Extend `tests/backgroundTransitionWiring.test.js` and `tests/cameraCleanupWiring.test.js` to pin the new preview restore/cancel paths next to the existing replay/title/preview cleanup routes, preserving Phase 64 sequencing guarantees. Make the runtime tests assert that every request carries a fresh script snapshot from Plan 65-01 instead of relying on stale iframe state, that unsupported unknown enums fail explicitly rather than silently falling back, and that transition preview never performs real navigation. Keep scope tight: no PageInspector UX, no effect queue, and no Phase 67 full regression matrix.</action>
  <verify>
    <automated>npx vitest run tests/iframeEffectPreviewWiring.test.js tests/backgroundTransitionPreview.test.js tests/backgroundTransitionWiring.test.js tests/cameraCleanupWiring.test.js</automated>
  </verify>
  <done>The runtime regression suites exist, encode the shared preview protocol plus restore invariants, and fail because `main.js` still only supports full-play preview commands.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the shared runtime effect preview protocol, restore helper, and same-page transition replay</name>
  <files>
    src/main.js
    src/ui/BackgroundLayer.js
    src/style.css
    tests/iframeEffectPreviewWiring.test.js
    tests/backgroundTransitionPreview.test.js
    tests/backgroundTransitionWiring.test.js
    tests/cameraCleanupWiring.test.js
  </files>
  <behavior>
    - `main.js` uses one D-01/D-02 request family for all effect kinds, keeps one active preview lock, and posts explicit result messages that Plan 65-01 can consume directly.
    - Runtime captures a pre-preview snapshot, replays the requested effect through the existing owner only, and restores the selected scene/page state through one shared helper on complete/cancel/error/supersede.
    - Transition preview uses a runtime-only same-page variant inside `BackgroundLayer`, respects Phase 64 completion/gate semantics, and never mutates `script.data`, navigation state, or editor-owned page objects.
  </behavior>
  <action>Implement the runtime half per D-01 through D-12 in `src/main.js`, `src/ui/BackgroundLayer.js`, and `src/style.css`. In `main.js`, extend `initPreview()` with `preview-effect` and `preview-effect-stop` handling, plus a small set of helpers: one for applying the incoming script/theme/layout snapshot exactly like `start`, one for establishing the selected scene/page baseline from `sceneId` + `pageIndex`, one `restorePreviewSnapshot(snapshot)` helper that reuses `cancelPageTransitionGate()`, `camera.clear()`, `audio.stopVoice()`, `characters.clear()`, `background.clear()`, `engine.restoreState()`, `engine.resetRenderState()`, and `engine.renderCurrentPage()` per D-05, and one active-preview record enforcing the D-06 global lock. Use `preview-busy` only when a second request arrives while restore is already in flight; otherwise cancel the old preview with reason detail `superseded`, restore it, then accept the new request. For character preview, replay motion through `CharacterLayer.show()` on the already-rendered selected page using the request payload; for camera preview, call `CameraController.play()` with the effect payload; for transition preview, call `BackgroundLayer.setBackground()` with a runtime-only `previewVariant: 'same-page'`, await its completion promise, and keep `engine.currentScene` / `engine.pageIndex` unchanged per D-07 and D-08. In `BackgroundLayer` and `src/style.css`, add only the minimum preview-variant support needed to keep identical-background transition previews visually distinct while fully cleaning temporary preview classes and variables afterward; do not bypass the existing sole-owner logic or Phase 64 gate semantics per D-09. If an animation/camera/transition enum is unknown, return explicit `unsupported-effect` per D-12 instead of faking success with a fallback effect. Finish by ensuring stop, supersede, caught runtime errors, and restore failures all post explicit terminal results and leave the iframe on the stable selected page.</action>
  <verify>
    <automated>npx vitest run tests/iframeEffectPreviewWiring.test.js tests/backgroundTransitionPreview.test.js tests/backgroundTransitionWiring.test.js tests/cameraCleanupWiring.test.js &amp;&amp; node --test tests/scriptEngine.test.js &amp;&amp; npm run build</automated>
  </verify>
  <done>The iframe runtime replays character, camera, and transition effects through the existing owners with explicit result messages, same-page transition preview works without real navigation, restore is correct on every terminal path, and the focused suites plus build all pass.</done>
</task>

</tasks>

<verification>
Run the new runtime protocol suite, the same-page transition suite, the existing Phase 64 cleanup guardrails, the ScriptEngine regression suite, and a production build to prove effect replay integrates without breaking owner or gate boundaries.
</verification>

<success_criteria>
- Character, camera, and transition effect replay all use the same runtime message family and explicit reason codes.
- Transition preview stays inside BackgroundLayer ownership and does not mutate real navigation state.
- Every terminal path restores the selected page render cleanly with Phase 64 gate cleanup still intact.
</success_criteria>

<output>
After completion, create `.planning/phases/65-iframe-effect-preview-api/65-02-SUMMARY.md`
</output>
