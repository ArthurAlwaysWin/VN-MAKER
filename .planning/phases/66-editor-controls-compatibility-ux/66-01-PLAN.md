---
phase: 66-editor-controls-compatibility-ux
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/shared/cinematicContract.js
  - src/editor/composables/usePageEditor.js
  - tests/cinematicContractCompatibility.test.js
  - tests/pageEditorEffectPreviewState.test.js
autonomous: true
requirements: [PREV-01]

must_haves:
  truths:
    - "The existing PageInspector flow has one shared unknown-safe option contract for character animation, page camera, and page transition values instead of hard-coded per-component enums."
    - "Preview buttons for character, camera, and transition can consume scoped busy, disabled, and result state without showing the last camera failure under the character block or vice versa."
    - "Unknown non-empty cinematic values remain representable in the editor contract so opening or duplicating a page does not silently coerce them to a supported preset."
  artifacts:
    - path: "src/shared/cinematicContract.js"
      provides: "Shared editor/runtime cinematic UI option helpers for animation, camera effect, intensity, and direction compatibility"
      exports: ["getCharacterAnimationUiOptions", "getCameraEffectUiOptions", "getCameraDirectionUiOptions"]
    - path: "src/editor/composables/usePageEditor.js"
      provides: "Scoped preview-state helper for PageInspector blocks using the existing Phase 65 effect preview refs"
      contains: "getEffectPreviewUiState"
    - path: "tests/cinematicContractCompatibility.test.js"
      provides: "Regression coverage for unknown-safe animation/camera/transition UI helpers"
      min_lines: 120
    - path: "tests/pageEditorEffectPreviewState.test.js"
      provides: "Regression coverage for effect-kind-scoped preview status consumption"
      min_lines: 140
  key_links:
    - from: "src/shared/cinematicContract.js"
      to: "src/editor/components/page-editor/PageInspector.vue"
      via: "Shared helper exports drive the three cinematic selects without duplicating enums"
      pattern: "getCharacterAnimationUiOptions|getCameraEffectUiOptions|getCameraDirectionUiOptions"
    - from: "src/editor/composables/usePageEditor.js"
      to: "src/editor/components/page-editor/PageInspector.vue"
      via: "Scoped preview helper surfaces busy/disabled/result state from the existing Phase 65 refs"
      pattern: "getEffectPreviewUiState"
    - from: "src/editor/composables/usePageEditor.js"
      to: "src/main.js"
      via: "No protocol drift: editor still posts `preview-effect` and consumes `preview-effect-result`"
      pattern: "preview-effect|preview-effect-result"
---

<objective>
Add the shared editor-side compatibility and preview-state interfaces that Phase 66 PageInspector controls will consume.

Purpose: Lock the unknown-safe select helpers and scoped preview-state contract before touching PageInspector so all three cinematic surfaces reuse one source of truth per D-05, D-12, D-14, and D-15.
Output: Shared cinematic UI helper exports, scoped preview-state helpers in `usePageEditor.js`, and focused regression coverage.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/66-editor-controls-compatibility-ux/66-CONTEXT.md
@.planning/phases/66-editor-controls-compatibility-ux/66-RESEARCH.md
@.planning/phases/65-iframe-effect-preview-api/65-01-SUMMARY.md
@.planning/phases/65-iframe-effect-preview-api/65-02-SUMMARY.md
@src/shared/cinematicContract.js
@src/editor/composables/usePageEditor.js
@tests/cinematicContractCompatibility.test.js
@tests/pageEditorEffectPreviewState.test.js

<interfaces>
From src/shared/cinematicContract.js:
```javascript
export const DEFAULT_CHARACTER_ANIMATION = 'none';
export const KNOWN_CHARACTER_ANIMATIONS = [
  'fade-in',
  'slide-in-left',
  'slide-in-right',
  'shake',
  'nod',
  'breathe',
  'bounce',
];

export const KNOWN_CAMERA_EFFECTS = ['shake', 'zoom', 'pan', 'flash'];
export const CAMERA_EFFECT_DIRECTION_OPTIONS = {
  shake: ['horizontal', 'vertical', 'both'],
  zoom: null,
  pan: ['left', 'right', 'up', 'down'],
  flash: null,
};

export function getTransitionUiOptions(currentType) {}
```

From src/editor/composables/usePageEditor.js:
```javascript
const isEffectPreviewBusy = ref(false);
const activeEffectPreviewRequest = ref(null);
const lastEffectPreviewResult = ref(null);
const previewDisabledReason = ref(null);

function previewCharacterEffect(payload) {}
function previewCameraEffect(payload) {}
function previewTransitionEffect(payload) {}
```

Expected from this plan for downstream use:
```javascript
export function getCharacterAnimationUiOptions(currentAnimation) {}
export function getCameraEffectUiOptions(currentEffect) {}
export function getCameraDirectionUiOptions(effect, currentDirection) {}

function getEffectPreviewUiState(effectKind) {}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing helper and preview-state regressions for all three cinematic surfaces</name>
  <files>
    tests/cinematicContractCompatibility.test.js
    tests/pageEditorEffectPreviewState.test.js
  </files>
  <behavior>
    - Test 1: animation UI helpers expose `none` plus the locked preset list, append `未知动画：{value}` for unsupported current values, and never coerce unknown saved values to a supported preset.
    - Test 2: camera UI helpers expose an explicit empty/"无" choice, the locked effect list, fixed intensity options, directional options only for `shake` and `pan`, and append explicit unknown current values instead of hiding them.
    - Test 3: transition compatibility coverage stays intact while the new animation/camera helpers follow the same unknown-safe pattern as `getTransitionUiOptions()`.
    - Test 4: `usePageEditor.js` exposes a scoped helper that reports busy, disabled, and terminal result state only for the matching effect kind, so the last camera rejection does not show up under the character animation block.
  </behavior>
  <action>Extend RED coverage for D-05, D-06, D-07, D-08, D-09, D-12, D-14, D-15, and D-16 before implementation. In `tests/cinematicContractCompatibility.test.js`, add helper expectations for character animation and page camera UI options, including explicit unknown labels and a no-camera option that maps to raw `page.camera = null` rather than inventing a persisted `none` camera effect. In `tests/pageEditorEffectPreviewState.test.js`, add assertions for a new scoped preview helper driven by existing Phase 65 refs: it should key busy state from `activeEffectPreviewRequest`, key terminal result state from `lastEffectPreviewResult`, and only surface `previewDisabledReason` for the last attempted effect kind. Keep scope tight: no PageInspector template edits yet, no CanvasToolbar changes, no fake preview path, and no new preview protocol.</action>
  <verify>
    <automated>npx vitest run tests/cinematicContractCompatibility.test.js tests/pageEditorEffectPreviewState.test.js</automated>
  </verify>
  <done>The focused regression suites encode the Phase 66 editor helper contract and fail because the shared cinematic helpers and scoped preview-state interface do not exist yet.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement shared unknown-safe cinematic UI helpers and scoped effect preview state</name>
  <files>
    src/shared/cinematicContract.js
    src/editor/composables/usePageEditor.js
    tests/cinematicContractCompatibility.test.js
    tests/pageEditorEffectPreviewState.test.js
  </files>
  <behavior>
    - All three PageInspector surfaces can source their select options from one shared contract module without losing unknown values.
    - Camera UI metadata reflects the locked editor model: optional page camera, `low|medium|high` intensity, and direction only for `shake`/`pan`.
    - `usePageEditor.js` keeps the existing Phase 65 preview protocol untouched while exposing a thin derived helper for effect-kind-scoped UI consumption.
  </behavior>
  <action>Implement the Phase 66 foundation in `src/shared/cinematicContract.js` and `src/editor/composables/usePageEditor.js`. In `src/shared/cinematicContract.js`, add explicit editor helpers for D-05 through D-09 and D-14 through D-15: `getCharacterAnimationUiOptions(currentAnimation)` with `none` + the locked preset list per D-06; `getCameraEffectUiOptions(currentEffect)` with an explicit empty/no-camera choice plus `shake|zoom|pan|flash`; stable Chinese labels for camera intensity options; and `getCameraDirectionUiOptions(effect, currentDirection)` that only returns values for `shake`/`pan`, returns no selectable directions for `zoom`/`flash` per D-09, and appends explicit unknown current values instead of silently coercing them. Do not use `getPageCameraContract()` as an editor write-path helper because research already identified that as a silent-normalization risk. In `src/editor/composables/usePageEditor.js`, add a derived helper such as `getEffectPreviewUiState(effectKind)` plus the minimum bookkeeping needed to scope the existing `isEffectPreviewBusy`, `previewDisabledReason`, `activeEffectPreviewRequest`, and `lastEffectPreviewResult` by effect kind per D-12 and D-16. Keep the Phase 65 transport intact: still post `preview-effect`, still consume `preview-effect-result`, still rely on the existing readonly preview session, and do not add per-block preview state machines or any new editing mode.</action>
  <verify>
    <automated>npx vitest run tests/cinematicContractCompatibility.test.js tests/pageEditorEffectPreviewState.test.js &amp;&amp; npm run build</automated>
  </verify>
  <done>`src/shared/cinematicContract.js` exposes the shared unknown-safe editor helpers, `usePageEditor.js` provides scoped preview-state consumption for PageInspector, the focused suites pass, and the editor still builds cleanly.</done>
</task>

</tasks>

<verification>
Run the compatibility suite and the existing Phase 65 editor preview-state suite together, then build the app so the new helper exports do not break the editor bundle.
</verification>

<success_criteria>
- One shared helper layer now covers animation, camera, and transition compatibility UX instead of leaving PageInspector to hard-code extra enums.
- Preview UI consumers can ask for effect-kind-scoped busy/disabled/result state without inventing their own state machine.
- Unknown non-empty cinematic values stay representable end-to-end for later PageInspector wiring.
</success_criteria>

<output>
After completion, create `.planning/phases/66-editor-controls-compatibility-ux/66-01-SUMMARY.md`
</output>
