---
phase: 66-editor-controls-compatibility-ux
plan: 02
type: execute
wave: 2
depends_on:
  - "66-01"
files_modified:
  - src/editor/components/page-editor/PageInspector.vue
  - src/editor/helpTexts.js
  - tests/pageInspectorCinematicControls.test.js
autonomous: true
requirements: [PREV-01]

must_haves:
  truths:
    - "In the existing PageInspector flow, creators can choose a character animation from the selected character row without entering a separate mode, per D-01 and D-02."
    - "In the existing PageInspector flow, creators can configure page camera effect, duration, intensity, and direction from the page-properties area, with direction only when the current effect uses it, per D-03 and D-07 through D-09."
    - "Character animation, page camera, and page transition each expose an in-place iframe preview button with explicit disabled/busy/failure feedback and unknown-value-safe display, per D-10 through D-16."
  artifacts:
    - path: "src/editor/components/page-editor/PageInspector.vue"
      provides: "PageInspector-native cinematic controls and preview buttons for character animation, page camera, and transition"
      contains: "previewCharacterEffect"
    - path: "src/editor/helpTexts.js"
      provides: "Chinese HelpTip copy for animation, camera, and cinematic preview guidance"
      contains: "charAnimation"
    - path: "tests/pageInspectorCinematicControls.test.js"
      provides: "Focused regression coverage for PageInspector cinematic control wiring and phase-boundary guardrails"
      min_lines: 100
  key_links:
    - from: "src/editor/components/page-editor/PageInspector.vue"
      to: "src/shared/cinematicContract.js"
      via: "Character/camera/transition select options and compatibility labels come from the shared helper exports"
      pattern: "getCharacterAnimationUiOptions|getCameraEffectUiOptions|getTransitionUiOptions"
    - from: "src/editor/components/page-editor/PageInspector.vue"
      to: "src/editor/composables/usePageEditor.js"
      via: "Preview buttons call the existing effect-preview methods and consume the scoped preview-state helper"
      pattern: "previewCharacterEffect|previewCameraEffect|previewTransitionEffect|getEffectPreviewUiState"
    - from: "src/editor/components/page-editor/PageInspector.vue"
      to: "src/editor/helpTexts.js"
      via: "HelpTip copy stays centralized instead of inline prose"
      pattern: "HELP_SCRIPT"
---

<objective>
Wire all Phase 66 cinematic controls directly into the existing PageInspector flow with explicit compatibility and preview feedback.

Purpose: Finish PREV-01 by making animation, camera, and transition configuration feel like a natural extension of the existing page editor instead of a second workflow.
Output: Updated `PageInspector.vue`, new HelpTip copy, and focused wiring regressions for the three cinematic surfaces.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/66-editor-controls-compatibility-ux/66-CONTEXT.md
@.planning/phases/66-editor-controls-compatibility-ux/66-RESEARCH.md
@.planning/phases/65-iframe-effect-preview-api/65-01-SUMMARY.md
@.planning/phases/65-iframe-effect-preview-api/65-02-SUMMARY.md
@src/editor/components/page-editor/PageInspector.vue
@src/editor/composables/usePageEditor.js
@src/shared/cinematicContract.js
@src/editor/helpTexts.js

<interfaces>
From src/editor/components/page-editor/PageInspector.vue:
```vue
<select :value="selectedTransitionType" @change="setTransitionType($event.target.value)" class="field-input">
  <option v-for="option in transitionOptions" :key="option.value" :value="option.value">
    {{ option.label }}
  </option>
</select>

<div v-if="editor.selectedCharIndex.value === idx" class="char-scale-row" @click.stop>
  <input type="range" min="0.2" max="3" step="0.1" :value="char.scale || 1" />
</div>
```

From src/editor/composables/usePageEditor.js after 66-01:
```javascript
function previewCharacterEffect(payload) {}
function previewCameraEffect(payload) {}
function previewTransitionEffect(payload) {}
function getEffectPreviewUiState(effectKind) {}
```

From src/shared/cinematicContract.js after 66-01:
```javascript
export function getCharacterAnimationUiOptions(currentAnimation) {}
export function getCameraEffectUiOptions(currentEffect) {}
export function getCameraDirectionUiOptions(effect, currentDirection) {}
export function getTransitionUiOptions(currentType) {}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing PageInspector cinematic control wiring regressions</name>
  <files>
    tests/pageInspectorCinematicControls.test.js
  </files>
  <behavior>
    - Test 1: `PageInspector.vue` imports and uses the shared cinematic UI helpers rather than hard-coded animation/camera arrays.
    - Test 2: the selected character row contains an animation select plus a local replay button; the page-properties block contains a camera section and a transition replay button; no cinematic preview entrypoint is moved into `CanvasToolbar.vue`.
    - Test 3: camera direction controls are conditionally rendered only for `shake` and `pan`, while duration is clamped through editor-side setters and changes commit through `script.pushState()`.
    - Test 4: the component consumes scoped preview-state helpers and centralized `HELP_SCRIPT` keys for inline status/help copy instead of inventing a second preview state machine or modal workflow.
  </behavior>
  <action>Create RED coverage in `tests/pageInspectorCinematicControls.test.js` for D-01 through D-18. A string/structure regression is acceptable here: assert that `PageInspector.vue` imports the new Phase 66 helper exports, wires `previewCharacterEffect`, `previewCameraEffect`, and `previewTransitionEffect` inside the existing inspector, adds a camera block inside 页面属性, keeps transition editing in the current location per D-04, and does not touch `CanvasToolbar.vue` for effect preview entrypoints per D-11. Also assert that `src/editor/helpTexts.js` gains dedicated cinematic help keys so copy remains centralized.</action>
  <verify>
    <automated>npx vitest run tests/pageInspectorCinematicControls.test.js</automated>
  </verify>
  <done>The wiring regression file exists, encodes the locked Phase 66 placement and compatibility rules, and fails because PageInspector still lacks cinematic controls and inline preview UX.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement PageInspector-native character, camera, and transition cinematic controls</name>
  <files>
    src/editor/components/page-editor/PageInspector.vue
    src/editor/helpTexts.js
    tests/pageInspectorCinematicControls.test.js
  </files>
  <behavior>
    - The current PageInspector flow exposes animation, camera, and transition controls in place with no new mode, drawer, or fake preview surface.
    - Unknown animation/camera/transition values remain visible and selectable as explicit compatibility options until the creator intentionally changes them.
    - Preview buttons stay next to their owning controls and show clear disabled, busy, and terminal result feedback sourced from the existing Phase 65 preview state.
  </behavior>
  <action>Update `src/editor/components/page-editor/PageInspector.vue` to finish PREV-01 per D-01 through D-18. In the selected character row, add a character animation select that uses `getCharacterAnimationUiOptions(char.animation)` per D-02 and D-06, commits raw `page.characters[idx].animation` values with `script.pushState()`, and places a nearby replay button that calls `editor.previewCharacterEffect({ characterId: char.id, animation: char.animation })` per D-10 and D-11. In the existing 页面属性 section, keep transition editing where it already lives per D-04, add a transition replay button there, and add a page-camera block below it that edits raw `page.camera` directly per D-03 and D-07: effect select with an explicit no-camera option, `durationMs` number field clamped to `100-2000` per D-08, intensity select with `low|medium|high`, and conditional direction select from `getCameraDirectionUiOptions()` only for `shake` and `pan` per D-09. Use the scoped helper from `editor.getEffectPreviewUiState(effectKind)` to render subdued inline status text for busy, disabled, unsupported, rejected, failed, completed, or cancelled states without cross-talk between blocks per D-12 and D-16. Add centralized Chinese help copy in `src/editor/helpTexts.js` for character animation, page camera, and preview guidance, then reuse `HelpTip` next to the new labels. Explicitly avoid: changing `CanvasToolbar.vue`, calling `getPageCameraContract()` on editor writes, adding fake canvas playback, adding a new cinematic mode, or expanding into the Phase 67 cleanup matrix.</action>
  <verify>
    <automated>npx vitest run tests/pageInspectorCinematicControls.test.js &amp;&amp; npm run build</automated>
  </verify>
  <done>PageInspector now lets creators configure and preview character animation, page camera, and page transition in place with explicit compatibility UX, the focused regression passes, and the editor still builds.</done>
</task>

</tasks>

<verification>
Run the new PageInspector wiring regression and a production build to prove the inspector renders with the Phase 66 controls without breaking the editor bundle.
</verification>

<success_criteria>
- Character animation, page camera, and transition controls all live inside the existing PageInspector flow and nowhere else.
- Preview buttons sit next to their owning controls and consume the existing runtime-backed preview state with explicit feedback.
- Unknown cinematic values remain visible and round-trip safe until the user intentionally replaces them.
</success_criteria>

<output>
After completion, create `.planning/phases/66-editor-controls-compatibility-ux/66-02-SUMMARY.md`
</output>
