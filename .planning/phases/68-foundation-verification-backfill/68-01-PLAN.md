---
phase: 68-foundation-verification-backfill
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/61-contract-freeze-visual-ownership/61-VALIDATION.md
  - .planning/phases/61-contract-freeze-visual-ownership/61-VERIFICATION.md
  - .planning/phases/62-character-preset-runtime-foundation/62-VALIDATION.md
  - .planning/phases/62-character-preset-runtime-foundation/62-VERIFICATION.md
autonomous: true
gap_closure: true
requirements: [CAM-05, PREV-04, ANIM-01, ANIM-02, ANIM-03]

must_haves:
  truths:
    - "Phase 61 has auditable verification evidence for CAM-05 and PREV-04 instead of remaining orphaned in the milestone audit."
    - "Phase 62 has auditable verification evidence for ANIM-01, ANIM-02, and ANIM-03 instead of relying only on SUMMARY claims."
    - "Nyquist coverage for Phases 61 and 62 exists as current VALIDATION.md artifacts with commands that match the shipped focused suites."
  artifacts:
    - path: ".planning/phases/61-contract-freeze-visual-ownership/61-VALIDATION.md"
      provides: "Nyquist validation coverage for CAM-05 and PREV-04 using current focused automation commands"
      contains: "## Phase Requirements → Test Map"
    - path: ".planning/phases/61-contract-freeze-visual-ownership/61-VERIFICATION.md"
      provides: "Auditable Phase 61 verification table linking requirements, summary evidence, and rerun results"
      contains: "CAM-05"
    - path: ".planning/phases/62-character-preset-runtime-foundation/62-VALIDATION.md"
      provides: "Nyquist validation coverage for ANIM-01, ANIM-02, and ANIM-03"
      contains: "ANIM-01"
    - path: ".planning/phases/62-character-preset-runtime-foundation/62-VERIFICATION.md"
      provides: "Auditable Phase 62 verification table linking requirements, summary evidence, and rerun results"
      contains: "ANIM-03"
  key_links:
    - from: ".planning/phases/61-contract-freeze-visual-ownership/61-VERIFICATION.md"
      to: ".planning/phases/61-contract-freeze-visual-ownership/61-01-SUMMARY.md"
      via: "Requirement coverage and evidence table for CAM-05"
      pattern: "61-01-SUMMARY|CAM-05|#stage-layer"
    - from: ".planning/phases/61-contract-freeze-visual-ownership/61-VERIFICATION.md"
      to: ".planning/phases/61-contract-freeze-visual-ownership/61-02-SUMMARY.md"
      via: "Requirement coverage and evidence table for PREV-04"
      pattern: "61-02-SUMMARY|PREV-04|unknown"
    - from: ".planning/phases/62-character-preset-runtime-foundation/62-VERIFICATION.md"
      to: ".planning/phases/62-character-preset-runtime-foundation/62-01-SUMMARY.md"
      via: "Requirement coverage and evidence table for ANIM-01 and ANIM-02 contract coverage"
      pattern: "62-01-SUMMARY|ANIM-01|ANIM-02|show_character"
    - from: ".planning/phases/62-character-preset-runtime-foundation/62-VERIFICATION.md"
      to: ".planning/phases/62-character-preset-runtime-foundation/62-02-SUMMARY.md"
      via: "Requirement coverage and evidence table for ANIM-02 and ANIM-03 playback and cleanup evidence"
      pattern: "62-02-SUMMARY|ANIM-03|CharacterLayer|breathe"
---

<objective>
Backfill the missing validation and verification artifacts for the v1.4 foundation phases that established stage ownership and character animation runtime behavior.

Purpose: Close the Phase 61 and Phase 62 orphaned audit gaps without reopening feature scope or deferred tech debt.
Output: Current `61-VALIDATION.md`, `61-VERIFICATION.md`, `62-VALIDATION.md`, and `62-VERIFICATION.md` files that `/gsd-execute-phase` can produce directly.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/v1.4-v1.4-MILESTONE-AUDIT.md
@.planning/phases/61-contract-freeze-visual-ownership/61-01-SUMMARY.md
@.planning/phases/61-contract-freeze-visual-ownership/61-02-SUMMARY.md
@.planning/phases/62-character-preset-runtime-foundation/62-01-SUMMARY.md
@.planning/phases/62-character-preset-runtime-foundation/62-02-SUMMARY.md
@.planning/phases/64-background-transition-expansion/64-VALIDATION.md
@.planning/phases/65-iframe-effect-preview-api/65-VALIDATION.md
@.planning/phases/66-editor-controls-compatibility-ux/66-VALIDATION.md
@.planning/phases/67-integration-regression-gate/67-VALIDATION.md
@.planning/phases/15-voice-engine-foundation/15-VERIFICATION.md
@.planning/phases/46-data-persistence-editor-shell/46-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Phase 61 validation and verification evidence for CAM-05 and PREV-04</name>
  <files>
    .planning/phases/61-contract-freeze-visual-ownership/61-VALIDATION.md
    .planning/phases/61-contract-freeze-visual-ownership/61-VERIFICATION.md
  </files>
  <read_first>
    .planning/REQUIREMENTS.md
    .planning/v1.4-v1.4-MILESTONE-AUDIT.md
    .planning/phases/61-contract-freeze-visual-ownership/61-01-SUMMARY.md
    .planning/phases/61-contract-freeze-visual-ownership/61-02-SUMMARY.md
    .planning/phases/64-background-transition-expansion/64-VALIDATION.md
    .planning/phases/65-iframe-effect-preview-api/65-VALIDATION.md
    .planning/phases/15-voice-engine-foundation/15-VERIFICATION.md
    .planning/phases/46-data-persistence-editor-shell/46-VERIFICATION.md
  </read_first>
  <action>
    Create `61-VALIDATION.md` and `61-VERIFICATION.md` as documentation-only backfill artifacts for CAM-05 and PREV-04. Do not edit any runtime, editor, or test code.

    For `61-VALIDATION.md`, follow the structure used by `64-VALIDATION.md` through `67-VALIDATION.md`, but tailor the requirement map to Phase 61 only:
    - CAM-05 must point to the focused ownership suites from `61-01-SUMMARY.md`: `npx vitest run tests/stageLayerOwnership.test.js tests/exportGame.test.js`.
    - PREV-04 must point to the focused compatibility suites from `61-02-SUMMARY.md`: `npx vitest run tests/cinematicContractCompatibility.test.js`, `node --test tests/scriptEngine.test.js`, and `npm run build`.
    - Add a concise sampling section and planned-artifact section that names the already-existing test files and explains that Phase 68 is refreshing Nyquist coverage, not creating new tests.

    For `61-VERIFICATION.md`, follow the table-driven evidence style used in `15-VERIFICATION.md` and `46-VERIFICATION.md`:
    - Add Observable Truths, Required Artifacts, Key Link Verification, Behavioral Spot-Checks, and Requirements Coverage sections.
    - Explicitly map CAM-05 to the stage-only ownership chain (`index.html`, `src/main.js`, `src/style.css`, `src/ui/CharacterLayer.js`, `electron/exportGame.js`, `tests/stageLayerOwnership.test.js`, `tests/exportGame.test.js`) using `61-01-SUMMARY.md`.
    - Explicitly map PREV-04 to unknown enum preservation and runtime-safe fallback (`src/shared/cinematicContract.js`, `src/editor/stores/script.js`, `src/editor/components/page-editor/PageInspector.vue`, `src/engine/ScriptEngine.js`, `tests/cinematicContractCompatibility.test.js`, `tests/scriptEngine.test.js`) using `61-02-SUMMARY.md`.
    - Re-run the focused commands above and record pass/fail evidence in the verification report instead of copying stale claims.
    - Call out that the milestone audit orphan reason is resolved by the new verification artifact, but do not edit `.planning/v1.4-v1.4-MILESTONE-AUDIT.md` itself in this plan.

    Keep scope bounded to Phase 61 evidence only. Do not pull in Phase 64-67 requirements, preview preflight deduplication, or repo-wide deferred Vitest failures.
  </action>
  <verify>
    <automated>npx vitest run tests/stageLayerOwnership.test.js tests/exportGame.test.js tests/cinematicContractCompatibility.test.js && node --test tests/scriptEngine.test.js && npm run build && node -e "const fs=require('fs'); const files=['.planning/phases/61-contract-freeze-visual-ownership/61-VALIDATION.md','.planning/phases/61-contract-freeze-visual-ownership/61-VERIFICATION.md']; for (const file of files) { if (!fs.existsSync(file)) { console.error('Missing', file); process.exit(1); } } const validation=fs.readFileSync(files[0],'utf8'); const verification=fs.readFileSync(files[1],'utf8'); for (const token of ['CAM-05','PREV-04','## Phase Requirements → Test Map']) { if (!validation.includes(token) && token !== '## Phase Requirements → Test Map') { console.error('Validation missing', token); process.exit(1); } } for (const token of ['CAM-05','PREV-04','61-01-SUMMARY','61-02-SUMMARY','## Requirements Coverage']) { if (!verification.includes(token)) { console.error('Verification missing', token); process.exit(1); } } console.log('Phase 61 evidence docs present and keyed to both requirements');"</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/61-contract-freeze-visual-ownership/61-VALIDATION.md` exists and contains a requirement-to-test table for exactly CAM-05 and PREV-04.
    - `61-VALIDATION.md` names the focused commands already proven in the Phase 61 summaries instead of inventing new broad suites.
    - `.planning/phases/61-contract-freeze-visual-ownership/61-VERIFICATION.md` exists and contains explicit evidence tables for CAM-05 and PREV-04 with links back to `61-01-SUMMARY.md` and `61-02-SUMMARY.md`.
    - `61-VERIFICATION.md` records actual rerun evidence for the focused suites and build command.
    - No non-doc files are modified.
  </acceptance_criteria>
  <done>Phase 61 no longer lacks Nyquist coverage or a phase-level verification artifact; CAM-05 and PREV-04 can be audited from REQUIREMENTS mapping + SUMMARY evidence + VERIFICATION tables.</done>
</task>

<task type="auto">
  <name>Task 2: Create Phase 62 validation and verification evidence for ANIM-01, ANIM-02, and ANIM-03</name>
  <files>
    .planning/phases/62-character-preset-runtime-foundation/62-VALIDATION.md
    .planning/phases/62-character-preset-runtime-foundation/62-VERIFICATION.md
  </files>
  <read_first>
    .planning/REQUIREMENTS.md
    .planning/v1.4-v1.4-MILESTONE-AUDIT.md
    .planning/phases/62-character-preset-runtime-foundation/62-01-SUMMARY.md
    .planning/phases/62-character-preset-runtime-foundation/62-02-SUMMARY.md
    .planning/phases/64-background-transition-expansion/64-VALIDATION.md
    .planning/phases/67-integration-regression-gate/67-VALIDATION.md
    .planning/phases/15-voice-engine-foundation/15-VERIFICATION.md
    .planning/phases/46-data-persistence-editor-shell/46-VERIFICATION.md
  </read_first>
  <action>
    Create `62-VALIDATION.md` and `62-VERIFICATION.md` as auditable backfill artifacts for ANIM-01, ANIM-02, and ANIM-03. Keep the work docs-only; do not change `src/` or `tests/`.

    For `62-VALIDATION.md`, mirror the phase-validation format already used in Phases 64-67:
    - ANIM-01 and ANIM-02 must reference the contract-focused suite from `62-01-SUMMARY.md`: `npx vitest run tests/characterAnimationContract.test.js`.
    - ANIM-02 and ANIM-03 must reference the playback lifecycle suites from `62-02-SUMMARY.md`: `npx vitest run tests/characterMotionPlayback.test.js tests/characterAnimationContract.test.js tests/cinematicContractCompatibility.test.js tests/stageLayerOwnership.test.js`, `node --test tests/scriptEngine.test.js`, and `npm run build`.
    - Make the planned artifact list explicitly reuse the shipped suites and note that no new animation dependencies are added.

    For `62-VERIFICATION.md`, use the same evidence-report pattern as the existing verification examples:
    - Build Observable Truths that match ANIM-01, ANIM-02, and ANIM-03 from `.planning/REQUIREMENTS.md`.
    - In Required Artifacts and Key Link Verification, anchor the contract side to `src/shared/cinematicContract.js` and `src/engine/ScriptEngine.js` using `62-01-SUMMARY.md`.
    - Anchor the runtime lifecycle side to `src/ui/CharacterLayer.js`, `src/style.css`, `tests/characterMotionPlayback.test.js`, and `tests/characterAnimationContract.test.js` using `62-02-SUMMARY.md`.
    - Re-run the focused commands and write their actual outcomes into Behavioral Spot-Checks / Requirements Coverage.
    - Make the report explicit that unsupported unknown animation enums remain preserved/no-op safe, but do not drift into Phase 65 preview behavior or other future-scope discussions.

    Keep the narrative narrowly about shipped Phase 62 behavior. Do not add preview, camera, transition, or deferred tech-debt material.
  </action>
  <verify>
    <automated>npx vitest run tests/characterAnimationContract.test.js tests/characterMotionPlayback.test.js tests/cinematicContractCompatibility.test.js tests/stageLayerOwnership.test.js && node --test tests/scriptEngine.test.js && npm run build && node -e "const fs=require('fs'); const files=['.planning/phases/62-character-preset-runtime-foundation/62-VALIDATION.md','.planning/phases/62-character-preset-runtime-foundation/62-VERIFICATION.md']; for (const file of files) { if (!fs.existsSync(file)) { console.error('Missing', file); process.exit(1); } } const validation=fs.readFileSync(files[0],'utf8'); const verification=fs.readFileSync(files[1],'utf8'); for (const token of ['ANIM-01','ANIM-02','ANIM-03']) { if (!validation.includes(token)) { console.error('Validation missing', token); process.exit(1); } if (!verification.includes(token)) { console.error('Verification missing', token); process.exit(1); } } for (const token of ['62-01-SUMMARY','62-02-SUMMARY','## Requirements Coverage']) { if (!verification.includes(token)) { console.error('Verification missing', token); process.exit(1); } } console.log('Phase 62 evidence docs present and keyed to all requirements');"</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/62-character-preset-runtime-foundation/62-VALIDATION.md` exists and includes a requirement map for ANIM-01, ANIM-02, and ANIM-03.
    - `.planning/phases/62-character-preset-runtime-foundation/62-VERIFICATION.md` exists and includes evidence tables, artifact tables, and requirement coverage rows for all three animation requirements.
    - The verification report cites both `62-01-SUMMARY.md` and `62-02-SUMMARY.md` as evidence sources instead of relying on unexpanded summary frontmatter.
    - The document set stays foundation-only and does not mention deferred repo-wide failures except to note they are out of scope if needed.
    - No non-doc files are modified.
  </acceptance_criteria>
  <done>Phase 62 has refreshed Nyquist coverage and an auditable verification report that closes the ANIM-01/02/03 orphaned requirement gap.</done>
</task>

</tasks>

<verification>
Re-run the exact focused Phase 61 and Phase 62 suites named in the new validation files, then confirm the verification reports capture requirement tables, evidence references, and real command outcomes for all five covered requirement IDs.
</verification>

<success_criteria>
- Phase 61 has both `61-VALIDATION.md` and `61-VERIFICATION.md` covering CAM-05 and PREV-04 with auditable evidence.
- Phase 62 has both `62-VALIDATION.md` and `62-VERIFICATION.md` covering ANIM-01, ANIM-02, and ANIM-03 with auditable evidence.
- The work remains docs-only and does not reopen deferred tech debt or unrelated full-suite cleanup.
</success_criteria>

<output>
After completion, create `.planning/phases/68-foundation-verification-backfill/68-01-SUMMARY.md`
</output>
