---
phase: 71-shared-contract-asset-pipeline-baseline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/shared/uiImageContract.js
  - src/editor/utils/uiImageField.js
  - tests/uiImageContract.test.js
  - tests/uiImageFieldFlow.test.js
autonomous: true
requirements: [AST-01, AST-02, AST-05, AST-06]

must_haves:
  truths:
    - "编辑器侧有一条统一的 UI 图片选取/清除 contract，后续 surface 不再各自发明写法，满足 D-01、D-03、D-07。"
    - "legacy 路径与旧 data URL 可以被识别并保留，只有显式重选才进入 canonical `ui/...` 写法，满足 D-04、D-05。"
    - "scan/export 与 editor/runtime 可以共享同一组 root key、path 规则与 registry 入口，满足 D-08、D-10、D-11。"
  artifacts:
    - path: "src/shared/uiImageContract.js"
      provides: "UI 图片 canonical/legacy 判定、schema root 常量、scan registry 入口"
      contains: "UI_THEME_ROOT"
    - path: "src/editor/utils/uiImageField.js"
      provides: "标准选图/清除 helper，复用 assets store 与现有 preview owner"
      contains: "pickUiImage"
    - path: "tests/uiImageContract.test.js"
      provides: "共享契约纯函数回归"
      min_lines: 80
    - path: "tests/uiImageFieldFlow.test.js"
      provides: "标准选图与显式改写流程回归"
      min_lines: 100
  key_links:
    - from: "src/editor/utils/uiImageField.js"
      to: "src/editor/stores/assets.js"
      via: "统一调用 `selectAsset(['ui'])`"
      pattern: "selectAsset\\(\\['ui'\\]\\)"
    - from: "src/editor/utils/uiImageField.js"
      to: "src/shared/uiImageContract.js"
      via: "helper 使用共享 canonical/legacy 判定，而不是自己猜测格式"
      pattern: "classifyUiImageValue|normalizeUiImageSelection"
    - from: "src/shared/uiImageContract.js"
      to: "src/engine/scanAssets.js"
      via: "后续 plan 直接消费 registry/collector exports"
      pattern: "UI_IMAGE_SCAN_REGISTRY|collect"
---

<objective>
冻结 Phase 71 的共享 UI 图片契约与 editor 侧标准选图 helper。

Purpose: 先把 path 规则、legacy 判定、slot root 与标准写入口钉死，再让后续 NineSlice、screen chrome、scan/export 都接同一条资产通路。
Output: 新的 `uiImageContract`/`uiImageField` 基线与对应回归测试。
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/71-shared-contract-asset-pipeline-baseline/71-CONTEXT.md
@.planning/phases/71-shared-contract-asset-pipeline-baseline/71-RESEARCH.md
@src/editor/stores/assets.js
@src/editor/composables/useThemeEditor.js
@src/editor/composables/useScreenLayoutEditor.js

<interfaces>
From src/editor/stores/assets.js:
```javascript
export const useAssetStore = defineStore('assets', () => {
  async function selectAsset(types) {}
  async function importAssets(category, filePaths) {}
});
```

From src/editor/composables/useThemeEditor.js:
```javascript
function sendThemeToPreview() {}
function commitTheme() {}
```

From src/editor/composables/useScreenLayoutEditor.js:
```javascript
function sendScreenLayoutToPreview(screenId) {}
function commitScreenLayout() {}
function setScreenField(field, value) {}
function setScreenNestedField(group, field, value) {}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 先写共享契约红测并实现 canonical/legacy/registry 基线</name>
  <files>
    src/shared/uiImageContract.js
    tests/uiImageContract.test.js
  </files>
  <behavior>
    - Test 1: canonical 新写法只接受项目 `assets/` 根下的 `ui/...` 相对路径；`assets/ui/...`、绝对路径、空白字符串都不会被当作 canonical 成功值。
    - Test 2: legacy 判定能区分空值、canonical 路径、旧路径字符串、nine-slice 历史 data URL，且不会做静默迁移。
    - Test 3: contract 导出 `ui.theme`、`ui.dialogueBox`、`ui.<screen>.chrome` 所需 root/registry 常量，供后续 editor/runtime/export 复用。
  </behavior>
  <action>按 D-01、D-04、D-05、D-07、D-08、D-10、D-11 新建 `src/shared/uiImageContract.js`，并先在 `tests/uiImageContract.test.js` 写 RED 用例。contract 至少导出：canonical path helper（例如 `normalizeUiImageSelection()` / `isCanonicalUiImagePath()`）、legacy 分类 helper（显式覆盖 nine-slice data URL）、schema root 常量（`UI_THEME_ROOT`、`UI_DIALOGUE_ROOT`、screen roots），以及 scan/export 将消费的 registry 入口。这里不要实现任何 surface DOM rollout，不要改 `.gmtheme`，也不要把 registry 设计成散落的字符串常量表。</action>
  <verify>
    <automated>node --test tests/uiImageContract.test.js</automated>
  </verify>
  <done>`src/shared/uiImageContract.js` 成为唯一共享入口，测试证明 canonical/legacy/registry 规则已冻结，且没有任何静默迁移副作用。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 先写标准选图流程红测并实现 editor 通用 helper</name>
  <files>
    src/editor/utils/uiImageField.js
    tests/uiImageFieldFlow.test.js
  </files>
  <behavior>
    - Test 1: helper 统一通过 `selectAsset(['ui'])` 获取结果，取消选择时不改写当前值。
    - Test 2: legacy 值只有在用户显式重选后才会被改写成 canonical `ui/...` 路径。
    - Test 3: helper 同时支持 preview callback 与 clear callback，便于 theme owner 与 screen owner 复用而不新增第二套资产流。
    - Test 4: helper 不使用 `FileReader.readAsDataURL()`、不接受自由文本路径作为主写入口，满足 D-03。
    - Test 5: canonical `ui/...` 值经过真实 owner 的保存 / 重新读取 round-trip 后仍保持 canonical 形式，legacy 值在未显式重选时不会被偷偷改写。
  </behavior>
  <action>按 D-01、D-02、D-03、D-05、D-09，在 `tests/uiImageFieldFlow.test.js` 先写 RED 覆盖，再实现 `src/editor/utils/uiImageField.js`。helper 需要直接复用 `useAssetStore().selectAsset(['ui'])` 的既有 PNG/WebP/JPEG 过滤能力，不新增 IPC、不新建本地文件复制逻辑；同时提供可组合的 `pickUiImage`、`clearUiImage`、`getUiImageDisplayValue` 一类 API，让 NineSliceModal 与 screen section 只传 setter/preview/commit 回调即可接入。测试里要显式覆盖一次“标准选图 → canonical `ui/...` 写入 → 通过真实 owner 保存 / 重新读取 → 值仍为 canonical”的 round-trip，以及“legacy 值仅在显式重选后才被改写”的边界。明确避免：自由文本路径继续做主入口、静默把 legacy 值批量改写、或发明 editor-only 假预览 owner。</action>
  <verify>
    <automated>npx vitest run tests/uiImageFieldFlow.test.js</automated>
  </verify>
  <done>通用 helper 已锁定标准选图、显式改写与 canonical round-trip 规则，后续 surface 只需接线即可复用 Phase 71 contract。</done>
</task>

</tasks>

<verification>
先跑共享契约纯函数测试，再跑 editor 侧标准选图 helper 流程测试，确保 Phase 71 的基础 contract 在改 UI surface 前已经可执行、可复用。
</verification>

<success_criteria>
- `src/shared/uiImageContract.js` 成为 canonical path、legacy 判定、schema root、scan registry 的唯一共享入口。
- `src/editor/utils/uiImageField.js` 统一封装标准选图/清除流程，后续 surface 不再自己读文件或手写路径。
- canonical `ui/...` 值经过保存 / 重新读取后仍保持稳定，legacy 值在未显式重选时不会被静默改写。
- 所有规则都以 focused test 锁定，后续计划可以直接依赖这些接口实现。
</success_criteria>

<output>
After completion, create `.planning/phases/71-shared-contract-asset-pipeline-baseline/71-01-SUMMARY.md`
</output>
