---
phase: 72-dialogue-box-picture-loop
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/editor/stores/script.js
  - src/shared/uiImageContract.js
  - tests/dialogueBoxSchemaFlow.test.js
  - tests/scanAssets.test.js
autonomous: true
requirements: [DLG-01]

must_haves:
  truths:
    - "对话框专属图片字段只落在 `ui.dialogueBox`，而主框图片 owner 仍然只有 `ui.theme.nineSlice.dialogueBox`。"
    - "名牌背景图与装饰图的 canonical `ui/...` 路径会进入 `scanAssets()` 的 `ui` bucket，导出链路不会漏掉 Phase 72 新字段。"
    - "旧项目未配置这些字段时，`ui.dialogueBox` 仍可安全读取并保留现有字体/名牌配置。"
  artifacts:
    - path: "src/editor/stores/script.js"
      provides: "带 dialogue 图片字段的 `ui.dialogueBox` 默认 schema"
      contains: "nameplateBackgroundImage"
    - path: "src/shared/uiImageContract.js"
      provides: "Dialogue box UI image collector"
      contains: "ui.dialogueBox"
    - path: "tests/dialogueBoxSchemaFlow.test.js"
      provides: "dialogueBox schema regression coverage"
      min_lines: 80
    - path: "tests/scanAssets.test.js"
      provides: "Phase 72 dialogue image scan coverage"
      min_lines: 200
  key_links:
    - from: "src/editor/stores/script.js"
      to: "src/editor/components/DialogueBoxSettings.vue"
      via: "getDialogueBox()/updateDialogueBox() owner chain"
      pattern: "getDialogueBox|updateDialogueBox"
    - from: "src/shared/uiImageContract.js"
      to: "src/engine/scanAssets.js"
      via: "collectUiImagePaths() adds dialogue image paths to ui bucket"
      pattern: "collectUiImagePaths|nameplateBackgroundImage|decorations"
---

<objective>
冻结 Phase 72 的 `ui.dialogueBox` 图片 schema 与 scan/export collector 基线。

Purpose: 先把 dialogue-box 专属字段、owner 边界和 canonical `ui/...` 收集规则钉死，再让 runtime 和 editor 在同一份 contract 上实现。
Output: `ui.dialogueBox` 新字段默认值、collector 扩展、focused schema/scan 回归测试。
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/72-dialogue-box-picture-loop/72-CONTEXT.md
@.planning/phases/72-dialogue-box-picture-loop/72-RESEARCH.md
@.planning/phases/72-dialogue-box-picture-loop/72-VALIDATION.md
@.planning/phases/71-shared-contract-asset-pipeline-baseline/71-01-SUMMARY.md
@.planning/phases/71-shared-contract-asset-pipeline-baseline/71-03-SUMMARY.md
@src/editor/stores/script.js
@src/shared/uiImageContract.js
@src/engine/scanAssets.js
@tests/scanAssets.test.js

<interfaces>
From src/editor/stores/script.js:
```javascript
function getDialogueBox() {}
function updateDialogueBox(dialogueBox) {}
```

From src/shared/uiImageContract.js:
```javascript
export const UI_DIALOGUE_BOX_ROOT = 'ui.dialogueBox';
export function normalizeUiImageSelection(value) {}
export function collectUiImagePaths(script, add, registry) {}
```

From src/engine/scanAssets.js:
```javascript
export function scanAssets(script) {}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Freeze `ui.dialogueBox` image defaults on the Pinia owner</name>
  <read_first>
    @src/editor/stores/script.js
    @.planning/phases/72-dialogue-box-picture-loop/72-CONTEXT.md
    @.planning/phases/72-dialogue-box-picture-loop/72-RESEARCH.md
  </read_first>
  <files>
    src/editor/stores/script.js
    tests/dialogueBoxSchemaFlow.test.js
  </files>
  <behavior>
    - Test 1: `getDialogueBox()` keeps existing typography fields and returns `nameplateStyle`, `nameplateBackgroundImage`, and `decorations` defaults for legacy projects.
    - Test 2: `updateDialogueBox()` preserves a decoration row shaped as `{ src, x, y, width, height }` without dropping existing dialogue fields.
    - Test 3: missing Phase 72 fields are initialized in place instead of replacing the entire dialogueBox object.
  </behavior>
  <action>Per D-02, D-03, D-04, and D-07, extend `src/editor/stores/script.js` so `getDialogueBox()` guarantees these exact fields on `data.value.ui.dialogueBox`: `fontSize`, `fontFamily`, `textColor`, `nameplateFontSize`, `nameplateFontFamily`, `nameplateColor`, `nameplateStyle` (default `'inline'`), `nameplateBackgroundImage` (default `null`), and `decorations` (default `[]`). Keep `updateDialogueBox(dialogueBox)` as the only whole-object persistence entry point; do not create a second dialogue image store, do not move the main frame image under `ui.dialogueBox`, and do not silently rewrite legacy non-canonical image strings on load. Add `tests/dialogueBoxSchemaFlow.test.js` first, then implement until it passes.</action>
  <acceptance_criteria>
    - `script.getDialogueBox()` returns Phase 72 fields for both new and legacy project data.
    - `ui.theme.nineSlice.dialogueBox` is not duplicated under `ui.dialogueBox`.
    - Decoration rows round-trip with exact keys `src`, `x`, `y`, `width`, and `height`.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/dialogueBoxSchemaFlow.test.js</automated>
  </verify>
  <done>`ui.dialogueBox` is the single owner for dialogue-specific image fields, and legacy projects can load the expanded object without losing prior dialogue settings.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Register dialogue image collectors and lock scan coverage</name>
  <read_first>
    @src/shared/uiImageContract.js
    @src/engine/scanAssets.js
    @tests/scanAssets.test.js
    @.planning/phases/71-shared-contract-asset-pipeline-baseline/71-03-SUMMARY.md
  </read_first>
  <files>
    src/shared/uiImageContract.js
    tests/scanAssets.test.js
  </files>
  <behavior>
    - Test 1: canonical `ui/...` values in `ui.dialogueBox.nameplateBackgroundImage` and `ui.dialogueBox.decorations[].src` land in the `ui` bucket.
    - Test 2: empty, null, and legacy non-canonical values are ignored by the UI collector instead of being reclassified as canonical assets.
    - Test 3: `ui.theme.nineSlice.dialogueBox` remains the only main-frame image owner; collector changes do not invent a second frame field.
  </behavior>
  <action>Per D-01, D-02, and D-04, extend `src/shared/uiImageContract.js` by adding a dedicated collector for `script.ui?.dialogueBox?.nameplateBackgroundImage` plus every `script.ui?.dialogueBox?.decorations?.[i]?.src`. Route both through the existing canonical-path helper so only `ui/...` values are collected. Then update the `tests/scanAssets.test.js` fixture and expected `ui` array with exact dialogue paths such as `ui/dialogue/nameplate.webp` and `ui/dialogue/decor-flower.webp`. Do not edit `src/engine/scanAssets.js` conditionals directly for dialogue-only special cases; keep the Phase 71 registry extension pattern.</action>
  <acceptance_criteria>
    - `collectUiImagePaths()` covers `nameplateBackgroundImage` and `decorations[].src`.
    - `scanAssets(fullScript).ui` includes the new dialogue paths in sorted order.
    - No code path adds `ui.dialogueBox.frameImage`, `backgroundImage`, or any second main-frame owner field.
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/scanAssets.test.js</automated>
  </verify>
  <done>The Phase 72 dialogue image fields are now part of the shared UI asset registry, so runtime preview and export can consume the same canonical `ui/...` sources.</done>
</task>

</tasks>

<verification>
Run the new schema regression first, then the shared asset scanner regression. This plan is complete only when both the store owner and the collector registry agree on the Phase 72 field shape.
</verification>

<success_criteria>
- `ui.dialogueBox` exposes dialogue-only image fields without stealing ownership from `ui.theme.nineSlice.dialogueBox`.
- `scanAssets()` collects canonical dialogue nameplate/decor images through the shared registry.
- Legacy projects keep working because missing fields are defaulted safely and non-canonical values are not auto-migrated.
</success_criteria>

<output>
After completion, create `.planning/phases/72-dialogue-box-picture-loop/72-01-SUMMARY.md`
</output>
