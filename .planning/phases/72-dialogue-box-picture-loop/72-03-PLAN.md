---
phase: 72-dialogue-box-picture-loop
plan: 03
type: execute
wave: 3
depends_on: [72-01, 72-02]
files_modified:
  - src/editor/components/DialogueBoxSettings.vue
  - src/editor/views/ProjectSettings.vue
  - src/main.js
  - tests/uiImageFieldFlow.test.js
  - tests/dialogueBoxPreviewWiring.test.js
autonomous: true
requirements: [DLG-01, DLG-03]

must_haves:
  truths:
    - "编辑器可以为名牌背景和至少一层装饰图执行标准选图/清空，并立即把结果发到 runtime-backed iframe。"
    - "ProjectSettings 右侧 iframe 能稳定显示一条真实运行时对话样例，而不是依赖本地 mini preview。"
    - "对话框预览消息通路稳定，切换/清空图片配置后能立刻看到真实 runtime 结果。"
  artifacts:
    - path: "src/editor/components/DialogueBoxSettings.vue"
      provides: "dialogue image fields, decoration list controls, canonical picker wiring"
      contains: "pickUiImage"
    - path: "src/editor/views/ProjectSettings.vue"
      provides: "dialogue preview owner for iframe"
      contains: "show-dialogue-preview"
    - path: "src/main.js"
      provides: "preview message handler that renders a stable runtime dialogue sample"
      contains: "case 'show-dialogue-preview'"
    - path: "tests/dialogueBoxPreviewWiring.test.js"
      provides: "DLG-03 preview owner/message regression coverage"
      min_lines: 100
  key_links:
    - from: "src/editor/components/DialogueBoxSettings.vue"
      to: "src/editor/utils/uiImageField.js"
      via: "pickUiImage()/clearUiImage() for all canonical image writes"
      pattern: "pickUiImage|clearUiImage"
    - from: "src/editor/views/ProjectSettings.vue"
      to: "src/main.js"
      via: "postMessage type `show-dialogue-preview`"
      pattern: "show-dialogue-preview"
    - from: "src/main.js"
      to: "src/ui/DialogueBox.js"
      via: "runtime preview uses `renderPreviewLine()` instead of Vue-side fake markup"
      pattern: "renderPreviewLine"
---

<objective>
把 dialogue 图片字段接到 editor surface 与 runtime-backed iframe preview 上。

Purpose: 用户需要在真正的运行时里确认图片层、文本层和继续指示没有被遮挡；因此本 plan 负责 canonical 写入口、ProjectSettings 预览 owner，以及稳定的 dialogue preview message path。
Output: DialogueBoxSettings 图片表单、ProjectSettings 预览 owner、`main.js` 预览消息处理、focused preview wiring tests。
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/72-dialogue-box-picture-loop/72-CONTEXT.md
@.planning/phases/72-dialogue-box-picture-loop/72-RESEARCH.md
@.planning/phases/72-dialogue-box-picture-loop/72-VALIDATION.md
@src/editor/components/DialogueBoxSettings.vue
@src/editor/views/ProjectSettings.vue
@src/editor/composables/useThemeEditor.js
@src/editor/utils/uiImageField.js
@src/main.js
@tests/uiImageFieldFlow.test.js

<interfaces>
From src/editor/utils/uiImageField.js:
```javascript
export async function pickUiImage(options) {}
export function clearUiImage(options) {}
export function getUiImageDisplayValue(value) {}
```

From src/editor/composables/useThemeEditor.js:
```javascript
function flushPreview() {}
function startEngine() {}
function onEngineMessage(event) {}
```

From src/main.js preview transport:
```javascript
case 'start':
case 'update-theme':
case 'show-screen':
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reuse Phase 71 helpers for dialogue image fields and route every change to the iframe owner</name>
  <read_first>
    @src/editor/components/DialogueBoxSettings.vue
    @src/editor/views/ProjectSettings.vue
    @src/editor/utils/uiImageField.js
    @src/editor/composables/useThemeEditor.js
  </read_first>
  <files>
    src/editor/components/DialogueBoxSettings.vue
    src/editor/views/ProjectSettings.vue
    tests/uiImageFieldFlow.test.js
  </files>
  <behavior>
    - Test 1: DialogueBoxSettings source uses `pickUiImage()` and `clearUiImage()` for `nameplateBackgroundImage` and decoration `src`, not `FileReader` or freeform text writes.
    - Test 2: ProjectSettings owns the iframe preview trigger and emits a dedicated `show-dialogue-preview` message after preview state is flushed.
    - Test 3: users can add/remove at least one decoration row with exact editable fields `src`, `x`, `y`, `width`, and `height`.
  </behavior>
  <action>Per D-02, D-03, D-04, D-08, D-09, and D-10, extend `src/editor/components/DialogueBoxSettings.vue` with these exact fields under the existing dialogue settings owner: a nameplate background image control bound to `settings.nameplateBackgroundImage`; a decoration list bound to `settings.decorations`; and per-row numeric inputs for `x`, `y`, `width`, and `height`. Every image mutation must call `pickUiImage()` / `clearUiImage()` from `src/editor/utils/uiImageField.js`, writing canonical `ui/...` values back through `script.updateDialogueBox()`. In `src/editor/views/ProjectSettings.vue`, keep the right-hand iframe as the only truth source by owning a provided callback that (1) ensures preview state is started/flushed through `themeEditor`, then (2) posts `{ type: 'show-dialogue-preview', speakerName: '预览角色', text: '这是一段用于检查对话框图片层、文字层和继续指示的稳定示例台词。' }` to the iframe. Keep the local mini preview only as a form aid; do not turn it into the completion signal, and do not touch `tests/mainConfigRouting.test.js`.</action>
  <acceptance_criteria>
    - DialogueBoxSettings writes `ui.dialogueBox.nameplateBackgroundImage` and `ui.dialogueBox.decorations[n].src` through the shared helper only.
    - ProjectSettings becomes the single owner that sends `show-dialogue-preview` to the iframe.
    - At least one decoration row can be created, edited, cleared, and removed from the form without inventing a drag canvas.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/uiImageFieldFlow.test.js</automated>
  </verify>
  <done>The editor surface now exposes the Phase 72 dialogue image fields with canonical write semantics and sends every committed change to the runtime-backed iframe owner.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add a stable runtime dialogue preview message path and focused preview regressions</name>
  <read_first>
    @src/main.js
    @src/editor/views/ProjectSettings.vue
    @.planning/phases/72-dialogue-box-picture-loop/72-VALIDATION.md
    @tests/uiImageFieldFlow.test.js
  </read_first>
  <files>
    src/main.js
    tests/dialogueBoxPreviewWiring.test.js
  </files>
  <behavior>
    - Test 1: preview mode handles a dedicated `show-dialogue-preview` message.
    - Test 2: the message path hides other screens, stops auto/skip side effects, and renders a stable runtime sample through `dialogueBox.renderPreviewLine(...)`.
    - Test 3: changing dialogue settings in ProjectSettings uses the new preview message path instead of depending on the project’s first scene.
  </behavior>
  <action>Per D-08, D-09, and D-10, extend the `initPreview()` message switch in `src/main.js` with a new `case 'show-dialogue-preview':`. In that branch, keep the existing runtime/iframe architecture: stop auto and skip timers, hide choice/menu/screens if needed, leave the current preview snapshot loaded, re-apply `dialogueBox.applyGlobalStyle(engine.script.ui?.dialogueBox)` and `dialogueBox.setNameplateStyle(...)`, then call the new public `dialogueBox.renderPreviewLine({ speakerName: msg.speakerName, text: msg.text })`. Add `tests/dialogueBoxPreviewWiring.test.js` as the focused gate for ProjectSettings → iframe → runtime routing. Do not add a fake Vue dialogue renderer, do not depend on `tests/mainConfigRouting.test.js`, and do not widen the gate to unrelated preview behaviors.</action>
  <acceptance_criteria>
    - `src/main.js` contains a dedicated `show-dialogue-preview` preview case.
    - The runtime preview sample is driven by the real `DialogueBox` class, not editor-side markup.
    - Focused tests cover the new message path without making stale repo-wide preview tests a blocker.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/dialogueBoxPreviewWiring.test.js tests/uiImageFieldFlow.test.js</automated>
  </verify>
  <done>Dialogue image edits can now be verified in the right-side iframe through a stable runtime preview path that always renders the same sample speaker/text.</done>
</task>

</tasks>

<verification>
Run the focused preview suite and then the final phase gate:

- `npx vitest run tests/dialogueBoxPreviewWiring.test.js tests/uiImageFieldFlow.test.js`
- `node --test tests/scanAssets.test.js && npx vitest run tests/dialogueBoxSchemaFlow.test.js tests/dialogueBoxNameplate.test.js tests/themeManagerUiImage.test.js tests/uiImageFieldFlow.test.js tests/dialogueBoxUiSkin.test.js tests/dialogueBoxPreviewWiring.test.js && npm run build`
</verification>

<success_criteria>
- DialogueBoxSettings exposes canonical image pickers for nameplate art and decoration rows.
- ProjectSettings owns a stable `show-dialogue-preview` message path into the iframe.
- The runtime-backed iframe immediately reflects dialogue image changes using a fixed sample dialogue, without relying on the local mini preview.
</success_criteria>

<output>
After completion, create `.planning/phases/72-dialogue-box-picture-loop/72-03-SUMMARY.md`
</output>
