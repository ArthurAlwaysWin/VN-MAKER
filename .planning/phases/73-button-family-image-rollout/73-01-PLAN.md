---
phase: 73-button-family-image-rollout
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/shared/uiImageContract.js
  - src/engine/ThemeManager.js
  - tests/uiImageContract.test.js
  - tests/scanAssets.test.js
  - tests/themeManagerUiImage.test.js
autonomous: true
requirements:
  - BTN-01
  - BTN-02
must_haves:
  truths:
    - "Theme data accepts the five frozen button families only under ui.theme.buttonFamilies."
    - "Canonical UI asset scanning includes every configured button-family state image."
    - "ThemeManager owns one centralized selector registry for button-family state CSS generation."
  artifacts:
    - path: "src/shared/uiImageContract.js"
      provides: "buttonFamilies contract shape and scan collector"
      contains: "ui.theme.buttonFamilies"
    - path: "src/engine/ThemeManager.js"
      provides: "button-family selector registry and CSS builder entry points"
      contains: "gameMenuButton, qab, closeButton, pageTabPager, settingsTab"
    - path: "tests/uiImageContract.test.js"
      provides: "contract + collector regression coverage"
    - path: "tests/themeManagerUiImage.test.js"
      provides: "button-family CSS generation regression coverage"
  key_links:
    - from: "src/shared/uiImageContract.js"
      to: "src/engine/scanAssets.js"
      via: "UI_IMAGE_SCAN_REGISTRY collector"
      pattern: "collectUiImagePaths"
    - from: "src/engine/ThemeManager.js"
      to: "ui.theme.buttonFamilies"
      via: "centralized selector registry"
      pattern: "gameMenuButton|qab|closeButton|pageTabPager|settingsTab"
---

<objective>
Freeze the shared button-family contract and the centralized ThemeManager registry that every later runtime/editor task will target.

Purpose: lock D-01/D-02/D-03/D-04/D-06/D-07 into concrete keys and selectors before any DOM or editor wiring starts.
Output: canonical `ui.theme.buttonFamilies` schema, scan coverage, and tested registry/build helpers for all five families.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/73-button-family-image-rollout/73-CONTEXT.md
@.planning/phases/73-button-family-image-rollout/73-RESEARCH.md
@.planning/phases/73-button-family-image-rollout/73-VALIDATION.md
@.planning/phases/71-shared-contract-asset-pipeline-baseline/71-03-SUMMARY.md
@src/shared/uiImageContract.js
@src/engine/ThemeManager.js
@src/engine/scanAssets.js
@tests/uiImageContract.test.js
@tests/scanAssets.test.js
@tests/themeManagerUiImage.test.js

<interfaces>
From src/shared/uiImageContract.js:
```js
export const UI_THEME_ROOT = 'ui.theme';
export const UI_IMAGE_SCAN_REGISTRY = [/* collectors */];
export function collectUiImagePaths(script, add, registry = UI_IMAGE_SCAN_REGISTRY) {}
```

From src/engine/ThemeManager.js:
```js
export function applyTheme(container, themeData) {}
export function applyNineSlice(themeData) {}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define the canonical button-family contract and collector</name>
  <read_first>
    @.planning/phases/73-button-family-image-rollout/73-CONTEXT.md
    @.planning/phases/73-button-family-image-rollout/73-RESEARCH.md
    @src/shared/uiImageContract.js
    @src/engine/scanAssets.js
    @tests/uiImageContract.test.js
    @tests/scanAssets.test.js
  </read_first>
  <files>src/shared/uiImageContract.js, tests/uiImageContract.test.js, tests/scanAssets.test.js</files>
  <behavior>
    - Test 1: `collectUiImagePaths()` adds canonical `ui.theme.buttonFamilies.gameMenuButton|qab|closeButton` `normal/hover/pressed` paths and ignores empty / legacy-path values.
    - Test 2: `collectUiImagePaths()` adds canonical `pageTabPager|settingsTab` `normal/hover/pressed/selected` paths.
    - Test 3: `scanAssets()` includes the new button-family image paths in the `ui` bucket without changing existing ordering/dedup semantics.
  </behavior>
  <action>Update `src/shared/uiImageContract.js` so `ui.theme.buttonFamilies` becomes the only Phase 73 contract owner per D-01, using exactly these keys: `gameMenuButton`, `qab`, `closeButton`, `pageTabPager`, `settingsTab`. Collect only the frozen families from D-02; map 3-state families to `normal/hover/pressed` and 4-state families to `normal/hover/pressed/selected` per D-03/D-04. Keep collection canonical-path-only, add no per-screen roots, and extend `tests/uiImageContract.test.js` plus `tests/scanAssets.test.js` with exact coverage for each new state key so risk 5 (`uiImageContract` scan/export coverage) is closed.</action>
  <verify>
    <automated>node --test tests/uiImageContract.test.js tests/scanAssets.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `ui.theme.buttonFamilies` is the only new Phase 73 schema root.
    - Only the five locked families are collected.
    - Canonical `ui/...` button-family paths appear in the `ui` scan bucket.
  </acceptance_criteria>
  <done>`uiImageContract` and `scanAssets` tests prove the five-family contract is canonical, scan-visible, and legacy-safe.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Freeze the ThemeManager selector registry and CSS-builder interface</name>
  <read_first>
    @.planning/phases/73-button-family-image-rollout/73-RESEARCH.md
    @src/engine/ThemeManager.js
    @tests/themeManagerUiImage.test.js
  </read_first>
  <files>src/engine/ThemeManager.js, tests/themeManagerUiImage.test.js</files>
  <behavior>
    - Test 1: ThemeManager resolves normal/hover/pressed imagery for `.game-menu-button`, `.qab-btn`, and the close-family selector list.
    - Test 2: ThemeManager emits selected-state rules for `.page-tab`, `.page-dot`, `.settings-tab-btn`, and `.gm-tab` via existing `.active` semantics.
    - Test 3: Missing optional state images do not emit broken rules or clear CSS fallback behavior.
  </behavior>
  <action>Add a centralized button-family registry to `src/engine/ThemeManager.js` per D-06/D-07, with these exact selector groups: `gameMenuButton -> ['.game-menu-button']`, `qab -> ['.qab-btn']`, `closeButton -> ['.save-load-close', '.backlog-close', '.settings-close', '.settings-structured-close', '.settings-structured-footer-close', '.settings-custom-close']`, `pageTabPager -> ['.page-tab', '.page-dot']`, `settingsTab -> ['.settings-tab-btn', '.gm-tab']`. Introduce a dedicated CSS-builder/apply entry point for button-family underlays (separate style tag from nine-slice) but do not wire runtime call sites yet. Extend `tests/themeManagerUiImage.test.js` to assert registry coverage, resolved paths, close-family multi-selector output, and selected-state `.active` mapping for the 4-state families.</action>
  <verify>
    <automated>npx vitest run tests/themeManagerUiImage.test.js</automated>
  </verify>
  <acceptance_criteria>
    - ThemeManager has one button-family registry covering all five families and no extra selectors.
    - The close-family selector list includes the dedicated structured/custom settings hooks expected by later plans.
    - Selected-state CSS is defined through `.active` semantics, not a new state machine.
  </acceptance_criteria>
  <done>ThemeManager exposes a tested, centralized registry/build path that downstream runtime and editor work can implement against without redefining selectors.</done>
</task>

</tasks>

<verification>
Run the foundation gates before any runtime/editor plan:

- `node --test tests/uiImageContract.test.js tests/scanAssets.test.js`
- `npx vitest run tests/themeManagerUiImage.test.js`
</verification>

<success_criteria>
- `ui.theme.buttonFamilies` exists with the five locked family keys only.
- Scan coverage includes every canonical button-family state image path.
- ThemeManager owns the full selector registry needed by later plans.
</success_criteria>

<output>
After completion, create `.planning/phases/73-button-family-image-rollout/73-01-SUMMARY.md`
</output>
