---
phase: 73-button-family-image-rollout
plan: 02
type: execute
wave: 2
depends_on:
  - 73-01
files_modified:
  - src/engine/ThemeManager.js
  - src/main.js
  - src/style.css
  - src/ui/SettingsScreen.js
  - src/ui/widgets/TabWidget.js
  - tests/themeManagerUiImage.test.js
  - tests/settingsStructured.test.js
  - tests/saveLoadScreenLayout.test.js
  - tests/gameMenuLayout.test.js
  - tests/configurableTabs.test.js
  - tests/quickActionBarButtonFamily.test.js
autonomous: true
requirements:
  - BTN-01
  - BTN-02
  - BTN-03
must_haves:
  truths:
    - "The five frozen button families render image underlays in real runtime DOM without replacing text, SVGs, or click targets."
    - "Selected-state imagery appears anywhere existing `.active` tabs/pagers are rendered, including widget-style `gm-tab` buttons."
    - "Close-family imagery targets only actual close/back surfaces, not generic structured footer buttons."
  artifacts:
    - path: "src/engine/ThemeManager.js"
      provides: "runtime button-family CSS injection"
      contains: "style tag for button families"
    - path: "src/ui/widgets/TabWidget.js"
      provides: "gm-tab `.active` parity hook"
      contains: "classList.toggle('active'"
    - path: "src/ui/SettingsScreen.js"
      provides: "dedicated structured/custom close selectors"
      contains: "settings-structured-footer-close"
    - path: "tests/quickActionBarButtonFamily.test.js"
      provides: "QAB active/disabled/SVG regression"
  key_links:
    - from: "src/main.js"
      to: "src/engine/ThemeManager.js"
      via: "initial theme apply + preview update-theme path"
      pattern: "applyTheme\\(|update-theme"
    - from: "src/ui/widgets/TabWidget.js"
      to: "ThemeManager settingsTab selectors"
      via: ".gm-tab.active"
      pattern: "gm-tab"
    - from: "src/ui/SettingsScreen.js"
      to: "ThemeManager closeButton selectors"
      via: "settings-structured-footer-close|settings-custom-close"
      pattern: "action === 'close'"
---

<objective>
Normalize the runtime DOM hooks and inject the shared button-family image CSS across real game surfaces without breaking existing semantics.

Purpose: close the five runtime risks from CONTEXT/RESEARCH — gm-tab parity, selector-safe close hooks, default vs config-driven paths, QAB semantics, and underlay-only rendering.
Output: button-family CSS injection wired into runtime/preview, selector-safe DOM hooks, and regression coverage for all target surfaces.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/73-button-family-image-rollout/73-CONTEXT.md
@.planning/phases/73-button-family-image-rollout/73-RESEARCH.md
@.planning/phases/73-button-family-image-rollout/73-VALIDATION.md
@.planning/phases/72-dialogue-box-picture-loop/72-02-SUMMARY.md
@src/engine/ThemeManager.js
@src/main.js
@src/style.css
@src/ui/SettingsScreen.js
@src/ui/widgets/TabWidget.js
@src/ui/GameMenu.js
@src/ui/QuickActionBar.js
@src/ui/SaveLoadScreen.js
@src/ui/BacklogScreen.js
@tests/themeManagerUiImage.test.js
@tests/settingsStructured.test.js
@tests/saveLoadScreenLayout.test.js
@tests/gameMenuLayout.test.js
@tests/configurableTabs.test.js

<interfaces>
From src/main.js:
```js
applyTheme(gameContainer, engine.script.ui?.theme);
applyNineSlice(engine.script.ui?.theme);
window.addEventListener('message', handlePreviewMessage);
```

From src/ui/widgets/TabWidget.js:
```js
export function createTabBar(labels, config, onSelect) {
  return { el, setActive };
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Normalize selected and close-role hooks before skinning</name>
  <read_first>
    @src/ui/widgets/TabWidget.js
    @src/ui/SettingsScreen.js
    @tests/configurableTabs.test.js
    @tests/settingsStructured.test.js
  </read_first>
  <files>src/ui/widgets/TabWidget.js, src/ui/SettingsScreen.js, tests/configurableTabs.test.js, tests/settingsStructured.test.js</files>
  <behavior>
    - Test 1: `createTabBar().setActive()` toggles `.active` on `.gm-tab` so widget-style settings tabs expose the same selected semantics as `.settings-tab-btn`.
    - Test 2: only footer buttons with `action === 'close'` receive the new close-family hook; `title`/`reset` footer buttons keep `.settings-structured-footer-btn` only.
    - Test 3: custom settings buttons with `elem.action === 'close'` expose a dedicated close-family hook for config-driven DOM coverage.
  </behavior>
  <action>Update `src/ui/widgets/TabWidget.js` so every `.gm-tab` adds/removes `.active` inside `setActive()` per D-04 and risk 1; keep existing `activeBackgroundImage` behavior intact. Update `src/ui/SettingsScreen.js` so structured footer close buttons get a dedicated class exactly named `.settings-structured-footer-close` and custom-layout close buttons get `.settings-custom-close`; do not skin generic `.settings-structured-footer-btn` and do not tag title/reset buttons, closing risk 2 while covering config-driven DOM paths for risk 3. Extend `tests/configurableTabs.test.js` and `tests/settingsStructured.test.js` with exact assertions for `.active` parity and selector-safe close hooks.</action>
  <verify>
    <automated>npx vitest run tests/settingsStructured.test.js && node --test tests/configurableTabs.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `.gm-tab` selected state is discoverable through `.active`.
    - Structured footer close surfaces have a dedicated hook distinct from generic footer buttons.
    - Config-driven close buttons are hookable without widening scope to unrelated buttons.
  </acceptance_criteria>
  <done>Runtime DOM now exposes the exact `.active` and close-role selectors that ThemeManager can target safely across default and config-driven settings surfaces.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire button-family CSS injection through runtime and lock semantic regressions</name>
  <read_first>
    @src/engine/ThemeManager.js
    @src/main.js
    @src/style.css
    @src/ui/QuickActionBar.js
    @src/ui/GameMenu.js
    @src/ui/SaveLoadScreen.js
    @src/ui/BacklogScreen.js
    @tests/themeManagerUiImage.test.js
    @tests/gameMenuLayout.test.js
    @tests/saveLoadScreenLayout.test.js
  </read_first>
  <files>src/engine/ThemeManager.js, src/main.js, src/style.css, tests/themeManagerUiImage.test.js, tests/gameMenuLayout.test.js, tests/saveLoadScreenLayout.test.js, tests/quickActionBarButtonFamily.test.js</files>
  <behavior>
    - Test 1: runtime theme application injects button-family CSS for `.game-menu-button`, `.qab-btn`, `.save-load-close`, `.backlog-close`, `.settings-close`, `.settings-structured-close`, `.settings-structured-footer-close`, `.settings-custom-close`, `.page-tab`, `.page-dot`, `.settings-tab-btn`, and `.gm-tab`.
    - Test 2: QAB `.active` / `.disabled` classes and SVG icons remain untouched while underlay CSS is present.
    - Test 3: default and config-driven render paths still expose the same family selectors for game menu, save/load pager, and close surfaces.
  </behavior>
  <action>Finish the ThemeManager implementation from 73-01 by generating a dedicated `galgame-button-families` style tag that applies image underlays/backgrounds only per D-06/D-08/D-09. In `src/engine/ThemeManager.js`, emit CSS for `normal`, `:hover`, `:active`, and `.active` (`selected`) states using the locked family keys; keep `.active` and `.disabled` semantics on QAB unchanged per D-03 and risk 4, and order rules so selected tabs/pagers still show selected art. In `src/main.js`, call the new button-family apply function everywhere theme updates already happen (`applyPreviewScriptSnapshot`, initial runtime load, and `update-theme`). In `src/style.css`, add any required stacking/position helpers so button art stays below labels/SVGs and does not intercept clicks; do not replace existing text/SVG DOM or hit targets. Extend `tests/themeManagerUiImage.test.js`, `tests/gameMenuLayout.test.js`, and `tests/saveLoadScreenLayout.test.js`, and add `tests/quickActionBarButtonFamily.test.js` so risks 3 and 4 are explicitly covered.</action>
  <verify>
    <automated>npx vitest run tests/themeManagerUiImage.test.js tests/gameMenuLayout.test.js tests/saveLoadScreenLayout.test.js tests/quickActionBarButtonFamily.test.js</automated>
  </verify>
  <acceptance_criteria>
    - Runtime and preview theme updates both inject button-family CSS.
    - All five families skin real runtime selectors without replacing text/SVG content.
    - QAB active/disabled behavior, selected tab semantics, and default/config-driven DOM paths remain intact.
  </acceptance_criteria>
  <done>The engine applies button-family imagery centrally across real runtime surfaces while preserving readability, alignment, and click behavior for BTN-01/BTN-02/BTN-03.</done>
</task>

</tasks>

<verification>
- `npx vitest run tests/themeManagerUiImage.test.js tests/gameMenuLayout.test.js tests/saveLoadScreenLayout.test.js tests/settingsStructured.test.js tests/quickActionBarButtonFamily.test.js`
- `node --test tests/configurableTabs.test.js`
</verification>

<success_criteria>
- Theme updates inject button-family CSS in both runtime and preview paths.
- `gm-tab` selected imagery uses `.active` parity.
- Structured settings close hooks are selector-safe and do not catch non-close footer buttons.
</success_criteria>

<output>
After completion, create `.planning/phases/73-button-family-image-rollout/73-02-SUMMARY.md`
</output>
