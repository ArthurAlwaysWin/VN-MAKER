---
phase: 73-button-family-image-rollout
plan: 03
type: execute
wave: 2
depends_on:
  - 73-01
files_modified:
  - src/editor/components/theme/ButtonFamilyImageSettings.vue
  - src/editor/views/ProjectSettings.vue
  - src/editor/stores/script.js
  - tests/uiImageFieldFlow.test.js
  - tests/buttonFamilyPreviewWiring.test.js
autonomous: true
requirements:
  - BTN-01
  - BTN-02
  - BTN-03
must_haves:
  truths:
    - "Users edit all button-family image states from one global theme surface instead of per-screen forms."
    - "Preview controls route each family to an existing runtime-backed owner: game menu, save/load, settings/backlog, or dialogue/QAB."
    - "Editing or clearing button-family images persists under ui.theme.buttonFamilies and immediately posts update-theme to the iframe."
  artifacts:
    - path: "src/editor/components/theme/ButtonFamilyImageSettings.vue"
      provides: "five-family image state editor"
      contains: "gameMenuButton, qab, closeButton, pageTabPager, settingsTab"
    - path: "src/editor/views/ProjectSettings.vue"
      provides: "preview target routing for button families"
      contains: "show-screen|show-dialogue-preview"
    - path: "tests/buttonFamilyPreviewWiring.test.js"
      provides: "editor preview routing regression coverage"
  key_links:
    - from: "src/editor/components/theme/ButtonFamilyImageSettings.vue"
      to: "src/editor/stores/script.js"
      via: "theme.buttonFamilies mutations + commit"
      pattern: "updateTheme"
    - from: "src/editor/views/ProjectSettings.vue"
      to: "src/main.js"
      via: "postMessage update-theme + show-screen/show-dialogue-preview"
      pattern: "postMessage"
---

<objective>
Expose the frozen button-family fields in the global theme editor and reuse existing runtime-backed preview owners for each family.

Purpose: implement D-10/D-11 without creating per-screen forms or a fake preview sandbox.
Output: centralized button-family image UI, persisted `ui.theme.buttonFamilies` values, and preview-routing tests for game menu, save/load, settings/backlog, and QAB surfaces.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/73-button-family-image-rollout/73-CONTEXT.md
@.planning/phases/73-button-family-image-rollout/73-RESEARCH.md
@.planning/phases/72-dialogue-box-picture-loop/72-03-SUMMARY.md
@src/editor/views/ProjectSettings.vue
@src/editor/composables/useThemeEditor.js
@src/editor/components/DialogueBoxSettings.vue
@src/editor/utils/uiImageField.js
@src/editor/stores/script.js
@tests/uiImageFieldFlow.test.js

<interfaces>
From src/editor/utils/uiImageField.js:
```js
export async function pickUiImage({ setValue, preview, commit }) {}
export function clearUiImage({ setValue, preview, commit }) {}
```

From src/editor/views/ProjectSettings.vue:
```js
themeEditor.startEngine();
themeEditor.flushPreview();
iframe.contentWindow.postMessage({ type: 'show-screen', screenId }, '*');
iframe.contentWindow.postMessage({ type: 'show-dialogue-preview', ...sample }, '*');
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the centralized button-family editor surface under ui.theme</name>
  <read_first>
    @src/editor/views/ProjectSettings.vue
    @src/editor/components/DialogueBoxSettings.vue
    @src/editor/utils/uiImageField.js
    @src/editor/stores/script.js
    @tests/uiImageFieldFlow.test.js
  </read_first>
  <files>src/editor/components/theme/ButtonFamilyImageSettings.vue, src/editor/views/ProjectSettings.vue, src/editor/stores/script.js, tests/uiImageFieldFlow.test.js</files>
  <behavior>
    - Test 1: the new editor surface writes only `ui.theme.buttonFamilies.gameMenuButton|qab|closeButton|pageTabPager|settingsTab` state keys.
    - Test 2: every image field uses `pickUiImage` / `clearUiImage`; no freeform path input or FileReader fallback is reintroduced.
    - Test 3: clearing or selecting a field commits through the theme owner and immediately triggers iframe theme preview updates.
  </behavior>
  <action>Create `src/editor/components/theme/ButtonFamilyImageSettings.vue` with five family cards only per D-02: `gameMenuButton`, `qab`, `closeButton`, `pageTabPager`, `settingsTab`. Expose exactly `normal/hover/pressed` for the first three families and `normal/hover/pressed/selected` for the tab families per D-03/D-04. Mount the component in `src/editor/views/ProjectSettings.vue` inside the global theme section (not in screen-specific editors) per D-10. Update `src/editor/stores/script.js` only as needed to ensure `getTheme()`/`updateTheme()` can persist a stable `buttonFamilies` object without changing ownership. Extend `tests/uiImageFieldFlow.test.js` so the new component is locked to shared picker helpers and to `ui.theme.buttonFamilies`, with no editor-only sandbox or deferred extra families.</action>
  <verify>
    <automated>npx vitest run tests/uiImageFieldFlow.test.js</automated>
  </verify>
  <acceptance_criteria>
    - All button-family fields live in one global theme surface.
    - The editor writes only the locked five-family keys under `ui.theme.buttonFamilies`.
    - Picker/clear flows reuse the shared canonical UI image helpers.
  </acceptance_criteria>
  <done>Users can configure every Phase 73 button-family image state from one centralized theme panel backed by the canonical theme store.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reuse runtime-backed preview owners for each family</name>
  <read_first>
    @src/editor/views/ProjectSettings.vue
    @src/editor/composables/useThemeEditor.js
    @src/main.js
    @tests/uiImageFieldFlow.test.js
  </read_first>
  <files>src/editor/views/ProjectSettings.vue, tests/uiImageFieldFlow.test.js, tests/buttonFamilyPreviewWiring.test.js</files>
  <behavior>
    - Test 1: preview actions route `gameMenuButton` to `show-screen: gameMenu`, `pageTabPager` to `show-screen: saveLoadScreen`, `settingsTab` and `closeButton` to `show-screen: settingsScreen` / `backlogScreen`, and `qab` to `show-dialogue-preview`.
    - Test 2: each preview action sends `update-theme` before switching targets so newly selected assets are visible in the existing iframe owner.
    - Test 3: no new preview message type or local button sandbox is introduced.
  </behavior>
  <action>Extend `src/editor/views/ProjectSettings.vue` with button-family preview callbacks that reuse current runtime-backed owners per D-11: `gameMenuButton -> show-screen(gameMenu)`, `pageTabPager -> show-screen(saveLoadScreen)`, `settingsTab -> show-screen(settingsScreen)`, `closeButton -> show-screen(settingsScreen)` plus a secondary backlog target option if needed for `.backlog-close`, and `qab -> show-dialogue-preview` using the existing dialogue sample path. Keep preview ownership in ProjectSettings; do not add new iframe message types if `show-screen` / `show-dialogue-preview` already cover the target. Add `tests/buttonFamilyPreviewWiring.test.js` and extend `tests/uiImageFieldFlow.test.js` to assert postMessage sequencing (`start` if needed, `update-theme`, then the existing preview target message) and the absence of any editor-only sandbox code.</action>
  <verify>
    <automated>npx vitest run tests/uiImageFieldFlow.test.js tests/buttonFamilyPreviewWiring.test.js</automated>
  </verify>
  <acceptance_criteria>
    - Preview controls switch the iframe to existing runtime owners only.
    - Theme updates are flushed before each preview target switch.
    - No new sandbox/preview renderer is added.
  </acceptance_criteria>
  <done>Button-family edits can be previewed through the real runtime surfaces required by D-11, using the existing iframe transport only.</done>
</task>

</tasks>

<verification>
- `npx vitest run tests/uiImageFieldFlow.test.js tests/buttonFamilyPreviewWiring.test.js`
- After wave completion, run the focused phase suite from `73-VALIDATION.md`.
</verification>

<success_criteria>
- The editor exposes one centralized button-family image surface under `ui.theme`.
- Preview routing uses existing runtime-backed owners and no new sandbox.
- Button-family picker changes immediately reach the iframe via `update-theme`.
</success_criteria>

<output>
After completion, create `.planning/phases/73-button-family-image-rollout/73-03-SUMMARY.md`
</output>
