---
phase: 74-major-screen-imagification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/shared/uiImageContract.js
  - src/engine/ThemeManager.js
  - src/engine/scanAssets.js
  - tests/uiImageContract.test.js
  - tests/scanAssets.test.js
  - tests/themeManagerUiImage.test.js
autonomous: true
requirements:
  - MSI-01
  - MSI-02
must_haves:
  truths:
    - "All four major screens expose chrome.backgroundImage and chrome.decorations[] at the canonical contract level, with GameMenu migrating from its legacy backgroundImage root."
    - "Every configured background or decoration image is recognized by the scan/export pipeline so assets never disappear outside the editor."
    - "GameMenu legacy path fallback is annotated @deprecated with a clear removal milestone."
  artifacts:
    - path: "src/shared/uiImageContract.js"
      provides: "screen chrome contract fields and scan collectors"
      contains: "chrome.backgroundImage, chrome.decorations"
    - path: "src/engine/ThemeManager.js"
      provides: "screen background CSS builder entry points"
      contains: "screenBackgroundSelectors, applyScreenBackgrounds"
    - path: "tests/uiImageContract.test.js"
      provides: "contract + collector regression coverage"
    - path: "tests/themeManagerUiImage.test.js"
      provides: "screen background CSS generation regression coverage"
  key_links:
    - from: "src/shared/uiImageContract.js"
      to: "src/engine/scanAssets.js"
      via: "UI_IMAGE_SCAN_REGISTRY collector"
      pattern: "collectUiImagePaths"
    - from: "src/engine/ThemeManager.js"
      to: "ui.xxxScreen.chrome"
      via: "screen background selectors"
      pattern: "saveLoadScreen|backlogScreen|gameMenu|settingsScreen"
---

<objective>
Freeze the shared screen chrome contract for background images and decorations, add the GameMenu legacy migration path, and extend the scan/ThemeManager pipeline to recognize the new fields.

Purpose: lock the canonical `ui.xxxScreen.chrome.backgroundImage` + `chrome.decorations[]` schema before any runtime or editor wiring starts.
Output: canonical chrome schema for all 4 screens, GameMenu @deprecated fallback, scan coverage, and tested ThemeManager builder helpers.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/74-major-screen-imagification/74-CONTEXT.md
@.planning/phases/74-major-screen-imagification/74-DISCUSSION-LOG.md
@.planning/phases/73-button-family-image-rollout/73-01-SUMMARY.md
@src/shared/uiImageContract.js
@src/engine/ThemeManager.js
@src/engine/scanAssets.js
@tests/uiImageContract.test.js
@tests/scanAssets.test.js
@tests/themeManagerUiImage.test.js

<interfaces>
From src/shared/uiImageContract.js:
```js
export const UI_SCREEN_CHROME_ROOTS = Object.freeze({
  saveLoadScreen: 'ui.saveLoadScreen.chrome',
  backlogScreen:  'ui.backlogScreen.chrome',
  gameMenu:       'ui.gameMenu.chrome',
  settingsScreen: 'ui.settingsScreen.chrome',
});
export const UI_IMAGE_SCAN_REGISTRY = [/* collectors */];
export function collectUiImagePaths(script, add, registry = UI_IMAGE_SCAN_REGISTRY) {}
```

From src/engine/ThemeManager.js:
```js
export function applyTheme(container, themeData) {}
export function applyNineSlice(themeData) {}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend the screen chrome collector for backgroundImage and decorations</name>
  <read_first>
    @.planning/phases/74-major-screen-imagification/74-CONTEXT.md
    @src/shared/uiImageContract.js
    @src/engine/scanAssets.js
    @tests/uiImageContract.test.js
    @tests/scanAssets.test.js
  </read_first>
  <files>src/shared/uiImageContract.js, tests/uiImageContract.test.js, tests/scanAssets.test.js</files>
  <behavior>
    - Test 1: `collectUiImagePaths()` collects `ui.saveLoadScreen.chrome.backgroundImage` and all `ui.saveLoadScreen.chrome.decorations[].src` when present.
    - Test 2: Same for backlogScreen, gameMenu chrome, and settingsScreen chrome.
    - Test 3: `collectUiImagePaths()` still collects the legacy `ui.gameMenu.backgroundImage` path (backward compat during migration).
    - Test 4: `collectUiImagePaths()` collects settings `ui.settingsScreen.header.decorations[].src` and `ui.settingsScreen.header.backgroundImage` (existing behavior preserved).
    - Test 5: Null/undefined chrome fields produce no scan entries.
  </behavior>
  <action>Update `collectScreenChromeUiImages()` in `src/shared/uiImageContract.js` so it also scans the new `.chrome.backgroundImage` and `.chrome.decorations[].src` paths for all four screens (saveLoadScreen, backlogScreen, gameMenu, settingsScreen). The legacy paths (`saveLoadScreen.background`, `backlogScreen.backgroundImage`, `gameMenu.backgroundImage`, `settingsScreen.header.backgroundImage`, `settingsScreen.header.decorations`) must remain in the collector for backward compatibility. Extend `tests/uiImageContract.test.js` and `tests/scanAssets.test.js` with exact coverage for each new chrome path and decoration entry.</action>
  <verify>
    <automated>node --test tests/uiImageContract.test.js tests/scanAssets.test.js</automated>
  </verify>
  <acceptance_criteria>
    - All four screen chrome `.backgroundImage` paths are scanned.
    - All four screen chrome `.decorations[].src` paths are scanned.
    - Legacy paths remain scanned for backward compatibility.
    - Null/empty chrome fields produce no entries.
  </acceptance_criteria>
  <done>`uiImageContract` and `scanAssets` tests prove the four-screen chrome contract is scan-visible and backward-compatible.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add ThemeManager screen background builder and GameMenu deprecated fallback</name>
  <read_first>
    @src/engine/ThemeManager.js
    @tests/themeManagerUiImage.test.js
    @.planning/phases/74-major-screen-imagification/74-DISCUSSION-LOG.md
  </read_first>
  <files>src/engine/ThemeManager.js, tests/themeManagerUiImage.test.js</files>
  <behavior>
    - Test 1: `applyScreenBackgrounds()` emits CSS for `.save-load-screen` with `background-image` from `ui.saveLoadScreen.chrome.backgroundImage` using `object-fit: cover`.
    - Test 2: Same for `.backlog-screen`, `.game-menu-screen`, and `#settings-screen`.
    - Test 3: When `ui.gameMenu.chrome.backgroundImage` is absent but `ui.gameMenu.backgroundImage` (legacy) is present, the legacy value is used as fallback.
    - Test 4: When both chrome and legacy paths exist for gameMenu, chrome path takes precedence.
    - Test 5: `applyScreenBackgrounds()` creates a dedicated style tag `galgame-screen-backgrounds` and removes any previous instance before re-injecting.
  </behavior>
  <action>Add a `SCREEN_BACKGROUND_SELECTORS` registry to `src/engine/ThemeManager.js` mapping screen IDs to CSS selectors: `saveLoadScreen -> ['.save-load-screen']`, `backlogScreen -> ['.backlog-screen']`, `gameMenu -> ['.game-menu-screen']`, `settingsScreen -> ['#settings-screen']`. Add `applyScreenBackgrounds(container, uiData)` that reads `ui.xxxScreen.chrome.backgroundImage` for each screen and injects a `galgame-screen-backgrounds` style tag with `background-image` + `object-fit: cover` rules. For gameMenu, implement the legacy fallback: read `ui.gameMenu.chrome.backgroundImage` first, fall back to `ui.gameMenu.backgroundImage` with an explicit `@deprecated` JSDoc comment noting "Remove in next major milestone — Phase 74 migration path". Extend `tests/themeManagerUiImage.test.js` with coverage for all four screen backgrounds, gameMenu fallback precedence, and style tag lifecycle.</action>
  <verify>
    <automated>npx vitest run tests/themeManagerUiImage.test.js</automated>
  </verify>
  <acceptance_criteria>
    - ThemeManager can emit background CSS for all four major screens.
    - GameMenu reads chrome path first, then falls back to legacy path.
    - Legacy fallback is annotated @deprecated with removal milestone.
    - Style tag is properly managed (remove-then-inject).
  </acceptance_criteria>
  <done>ThemeManager owns the tested screen-background builder that downstream runtime and editor work can consume without redefining selectors or fallback logic.</done>
</task>

</tasks>

<verification>
Run the foundation gates before any runtime/editor plan:

- `node --test tests/uiImageContract.test.js tests/scanAssets.test.js`
- `npx vitest run tests/themeManagerUiImage.test.js`
</verification>

<success_criteria>
- All four screen chrome `.backgroundImage` and `.decorations[]` paths are scan-visible.
- GameMenu legacy fallback is @deprecated with a removal note.
- ThemeManager can build background CSS for all four screens.
</success_criteria>

<output>
After completion, create `.planning/phases/74-major-screen-imagification/74-01-SUMMARY.md`
</output>
