---
phase: 74-major-screen-imagification
plan: 03
type: execute
wave: 2
depends_on:
  - 74-01
files_modified:
  - src/editor/components/layout/MajorScreenImageSettings.vue
  - src/editor/components/layout/SaveLoadSection.vue
  - src/editor/components/layout/BacklogSection.vue
  - src/editor/components/layout/GameMenuSection.vue
  - src/editor/components/layout/DecorationSection.vue
  - src/editor/views/SaveLoadEditor.vue
  - src/editor/views/BacklogEditor.vue
  - src/editor/views/GameMenuEditor.vue
  - src/editor/stores/script.js
  - tests/majorScreenImageSettings.test.js
  - tests/uiImageFieldFlow.test.js
autonomous: true
requirements:
  - MSI-01
  - MSI-02
  - MSI-04
must_haves:
  truths:
    - "Users configure background images and decorations for all four major screens from the existing per-screen editor panels."
    - "Decoration layers exceeding 3 trigger a soft performance hint in the editor UI."
    - "All image fields use the shared pickUiImage/clearUiImage helpers — no freeform path input."
    - "Changes immediately propagate to the iframe preview via the existing update-screen-layout postMessage path."
  artifacts:
    - path: "src/editor/components/layout/MajorScreenImageSettings.vue"
      provides: "reusable background + decoration config panel for SaveLoad/Backlog/GameMenu"
      contains: "chrome.backgroundImage, chrome.decorations, performance hint"
    - path: "src/editor/components/layout/SaveLoadSection.vue"
      provides: "SaveLoad chrome integration"
      contains: "MajorScreenImageSettings"
    - path: "src/editor/components/layout/BacklogSection.vue"
      provides: "Backlog chrome integration"
      contains: "MajorScreenImageSettings"
    - path: "src/editor/components/layout/GameMenuSection.vue"
      provides: "GameMenu chrome integration"
      contains: "MajorScreenImageSettings"
    - path: "tests/majorScreenImageSettings.test.js"
      provides: "editor component regression coverage"
  key_links:
    - from: "src/editor/components/layout/MajorScreenImageSettings.vue"
      to: "src/editor/utils/uiImageField.js"
      via: "pickUiImage / clearUiImage"
      pattern: "pickUiImage|clearUiImage"
    - from: "src/editor/components/layout/MajorScreenImageSettings.vue"
      to: "src/editor/composables/useScreenLayoutEditor.js"
      via: "chrome field mutations + sendScreenLayoutToPreview"
      pattern: "setScreenNestedField|sendScreenLayoutToPreview"
---

<objective>
Expose the chrome.backgroundImage and chrome.decorations[] fields in the per-screen editor panels with a shared MajorScreenImageSettings component, and ensure preview propagation works for all four screens.

Purpose: implement the user-facing configuration surface for MSI-01/MSI-02/MSI-04 without creating per-screen forms or custom preview sandboxes.
Output: reusable MajorScreenImageSettings.vue component, soft performance hint for >3 decorations, and iframe preview integration via existing postMessage paths.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/74-major-screen-imagification/74-CONTEXT.md
@.planning/phases/74-major-screen-imagification/74-DISCUSSION-LOG.md
@.planning/phases/74-major-screen-imagification/74-01-SUMMARY.md
@.planning/phases/74-major-screen-imagification/74-02-SUMMARY.md
@src/editor/components/layout/SaveLoadSection.vue
@src/editor/components/layout/BacklogSection.vue
@src/editor/components/layout/GameMenuSection.vue
@src/editor/components/layout/DecorationSection.vue
@src/editor/components/theme/ButtonFamilyImageSettings.vue
@src/editor/utils/uiImageField.js
@src/editor/composables/useScreenLayoutEditor.js
@src/editor/stores/script.js
@tests/uiImageFieldFlow.test.js

<interfaces>
From src/editor/utils/uiImageField.js:
```js
export async function pickUiImage({ setValue, preview, commit }) {}
export function clearUiImage({ setValue, preview, commit }) {}
```

From src/editor/composables/useScreenLayoutEditor.js:
```js
const editor = {
  setScreenNestedField(group, field, value),
  commitScreenLayout(),
  sendScreenLayoutToPreview(),
  getActiveScreenConfig(),
};
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create MajorScreenImageSettings.vue shared component</name>
  <read_first>
    @src/editor/components/theme/ButtonFamilyImageSettings.vue
    @src/editor/components/layout/DecorationSection.vue
    @src/editor/utils/uiImageField.js
    @src/editor/composables/useScreenLayoutEditor.js
    @tests/uiImageFieldFlow.test.js
  </read_first>
  <files>src/editor/components/layout/MajorScreenImageSettings.vue, tests/majorScreenImageSettings.test.js, tests/uiImageFieldFlow.test.js</files>
  <behavior>
    - Test 1: The component renders a background image field that calls `pickUiImage` / `clearUiImage` for `chrome.backgroundImage`.
    - Test 2: The component renders a decoration list with add/remove controls; each decoration has src (via pickUiImage), x, y, width, height inputs.
    - Test 3: When decorations.length > 3, a soft performance hint text "装饰层较多可能影响性能" is visible.
    - Test 4: When decorations.length <= 3, the performance hint is hidden.
    - Test 5: All image fields use `pickUiImage` / `clearUiImage` — no freeform path input or FileReader.
    - Test 6: Field changes trigger `setScreenNestedField('chrome', ...)` and `sendScreenLayoutToPreview()`.
  </behavior>
  <action>Create `src/editor/components/layout/MajorScreenImageSettings.vue` as a reusable component that accepts no props (injects `useScreenLayoutEditor()` directly). It renders: (1) a "背景图" section with a single image field for `chrome.backgroundImage`, using `pickUiImage`/`clearUiImage` helpers; (2) a "装饰层" section with a list of decoration items, each having `src` (image picker), `x`, `y`, `width`, `height` numeric inputs. Add/remove decoration buttons. When `decorations.length > 3`, show a small hint: "装饰层较多可能影响性能" in muted color. All mutations go through `editor.setScreenNestedField('chrome', ...)` and `editor.sendScreenLayoutToPreview()`. Style consistently with existing Section components. Create `tests/majorScreenImageSettings.test.js` and extend `tests/uiImageFieldFlow.test.js`.</action>
  <verify>
    <automated>npx vitest run tests/majorScreenImageSettings.test.js tests/uiImageFieldFlow.test.js</automated>
  </verify>
  <acceptance_criteria>
    - Background image field uses pickUiImage/clearUiImage exclusively.
    - Decoration list supports add/remove with position/size inputs.
    - Performance hint appears at >3 decorations, hidden at <=3.
    - All changes propagate via useScreenLayoutEditor.
  </acceptance_criteria>
  <done>A reusable MajorScreenImageSettings component exists, tested and styled, ready for embedding in per-screen sections.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Embed MajorScreenImageSettings in SaveLoad/Backlog/GameMenu sections and adapt Settings DecorationSection</name>
  <read_first>
    @src/editor/components/layout/SaveLoadSection.vue
    @src/editor/components/layout/BacklogSection.vue
    @src/editor/components/layout/GameMenuSection.vue
    @src/editor/components/layout/DecorationSection.vue
    @src/editor/views/SaveLoadEditor.vue
    @src/editor/views/BacklogEditor.vue
    @src/editor/views/GameMenuEditor.vue
  </read_first>
  <files>src/editor/components/layout/SaveLoadSection.vue, src/editor/components/layout/BacklogSection.vue, src/editor/components/layout/GameMenuSection.vue, src/editor/components/layout/DecorationSection.vue, tests/majorScreenImageSettings.test.js</files>
  <behavior>
    - Test 1: SaveLoadSection renders MajorScreenImageSettings as a collapsible sub-section.
    - Test 2: BacklogSection and GameMenuSection render MajorScreenImageSettings similarly.
    - Test 3: Settings DecorationSection now also supports chrome.decorations (in addition to existing header.decorations).
    - Test 4: Configuring a background image in any section triggers `update-screen-layout` postMessage to the iframe.
  </behavior>
  <action>Import and embed `MajorScreenImageSettings` inside `SaveLoadSection.vue`, `BacklogSection.vue`, and `GameMenuSection.vue` as a collapsible "图片与装饰" section. For `DecorationSection.vue` (used by Settings), extend it to also support `chrome.backgroundImage` and `chrome.decorations` alongside the existing `header.backgroundImage` and `header.decorations`. The chrome fields should appear as a separate collapsible sub-section labeled "屏幕背景与装饰" to distinguish from header-level decorations. Extend `tests/majorScreenImageSettings.test.js` with integration assertions for each section.</action>
  <verify>
    <automated>npx vitest run tests/majorScreenImageSettings.test.js</automated>
  </verify>
  <acceptance_criteria>
    - All three non-Settings sections show MajorScreenImageSettings.
    - Settings DecorationSection supports chrome-level fields in addition to header-level.
    - Preview propagation works for all four screens.
  </acceptance_criteria>
  <done>Users can configure background images and decorations for all four major screens from the existing editor panels, with live iframe preview.</done>
</task>

</tasks>

<verification>
- `npx vitest run tests/majorScreenImageSettings.test.js tests/uiImageFieldFlow.test.js`
- `npm run build`
- Manual: open editor, navigate to each screen section, configure a background image and decoration, verify iframe preview updates.
</verification>

<success_criteria>
- MajorScreenImageSettings is embedded in SaveLoad/Backlog/GameMenu sections.
- Settings supports chrome-level decorations in addition to header-level.
- Performance hint appears for >3 decorations.
- Preview updates propagate immediately for all four screens.
</success_criteria>

<output>
After completion, create `.planning/phases/74-major-screen-imagification/74-03-SUMMARY.md`
</output>
