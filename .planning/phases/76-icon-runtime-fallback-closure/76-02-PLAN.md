---
phase: 76-icon-runtime-fallback-closure
plan: 02
type: execute
wave: 2
depends_on:
  - 76-01
files_modified:
  - src/ui/themeIconHelpers.js
  - src/ui/GameMenu.js
  - src/ui/SaveLoadScreen.js
  - src/ui/BacklogScreen.js
  - src/ui/SettingsScreen.js
  - src/ui/QuickActionBar.js
  - src/style.css
  - tests/themeIconHelpers.test.js
  - tests/gameMenuLayout.test.js
  - tests/saveLoadScreenLayout.test.js
  - tests/backlogScreenLayout.test.js
  - tests/settingsStructured.test.js
  - tests/quickActionBarThemeIcon.test.js
autonomous: true
requirements:
  - ICO-01
  - AST-04
must_haves:
  truths:
    - "Missing, deleted, or invalid themed icon assets fall back to default icon/text behavior instead of leaving broken `<img>` output."
    - "The fallback contract behaves the same for `gameMenu`, `qab`, `close`, and `voiceReplay` consumers."
    - "Preview/runtime/export stay on the same icon contract because runtime recovery happens inside the existing shared theme icon path."
  artifacts:
    - path: "src/ui/themeIconHelpers.js"
      provides: "shared broken-asset fallback contract for themed icons"
      contains: "resolveThemeIcon"
    - path: "src/ui/GameMenu.js"
      provides: "game menu themed icon consumer with asset-failure recovery"
      contains: "gameMenu"
    - path: "src/ui/SaveLoadScreen.js"
      provides: "close icon fallback recovery on save/load"
      contains: "save-load-close"
    - path: "src/ui/BacklogScreen.js"
      provides: "close + voice replay icon fallback recovery"
      contains: "backlog-close"
    - path: "src/ui/SettingsScreen.js"
      provides: "default, structured, and custom close icon fallback recovery"
      contains: "settings-structured-close"
    - path: "tests/themeIconHelpers.test.js"
      provides: "shared helper regression coverage for broken asset fallback"
  key_links:
    - from: "src/ui/themeIconHelpers.js"
      to: "src/ui/GameMenu.js|src/ui/SaveLoadScreen.js|src/ui/BacklogScreen.js|src/ui/SettingsScreen.js|src/ui/QuickActionBar.js"
      via: "shared themed icon markup + fallback hook"
      pattern: "resolveThemeIcon|attach"
    - from: "src/shared/uiImageContract.js"
      to: "tests/scanAssets.test.js"
      via: "existing icon scan/export contract remains unchanged"
      pattern: "UI_ICON_SLOT_KEYS"
---

<objective>
Finish the audit closure by making themed icon fallback resilient to bad assets everywhere the Phase 75 icon contract is consumed.

Purpose: close AST-04 and the remaining ICO-01 gap so invalid icon paths recover to default visuals instead of broken `<img>` behavior.
Output: one shared broken-asset fallback contract adopted by all current icon consumers, plus focused regressions proving all four locked icon slots recover cleanly.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v1.5-MILESTONE-AUDIT.md
@.planning/phases/75-cursor-icon-pipeline-closure/75-SUMMARY.md
@.planning/phases/76-icon-runtime-fallback-closure/76-CONTEXT.md
@src/ui/themeIconHelpers.js
@src/ui/GameMenu.js
@src/ui/SaveLoadScreen.js
@src/ui/BacklogScreen.js
@src/ui/SettingsScreen.js
@src/ui/QuickActionBar.js
@src/shared/uiImageContract.js
@tests/themeIconHelpers.test.js
@tests/gameMenuLayout.test.js
@tests/saveLoadScreenLayout.test.js
@tests/backlogScreenLayout.test.js
@tests/settingsStructured.test.js

<decision_traceability>
- D-76-01: use the existing `ui.theme.icons` slots only; do not add new icon slots.
- D-76-02: fallback must recover not just empty config, but also bad/missing asset paths.
- D-76-03: fix the shared theme icon path reused by preview/runtime/export; do not create a second icon system.
</decision_traceability>

<interfaces>
From src/ui/themeIconHelpers.js:
```js
export function resolveThemeIcon(icons, slotKey, fallbackText, cssClass = '') {}
export function hasThemeIcon(icons, slotKey) {}
```

From existing consumers:
```js
setThemeIcons(icons) {}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Upgrade the shared theme icon helper to handle broken assets</name>
  <read_first>
    @src/ui/themeIconHelpers.js
    @tests/themeIconHelpers.test.js
    @tests/quickActionBarThemeIcon.test.js
  </read_first>
  <files>src/ui/themeIconHelpers.js, src/ui/QuickActionBar.js, src/style.css, tests/themeIconHelpers.test.js, tests/quickActionBarThemeIcon.test.js</files>
  <behavior>
    - Test 1: a configured icon path still resolves through `resolvePath()` and renders the themed icon normally.
    - Test 2: when the themed asset errors, the helper swaps back to fallback content instead of leaving a broken `<img>` in place.
    - Test 3: QAB keeps its D-76-01 SVG fallback on asset failure, not an empty button or broken image icon.
  </behavior>
  <action>Refactor `src/ui/themeIconHelpers.js` per D-76-02/D-76-03 into one reusable contract that can survive image load failure for all theme-icon consumers. The simplest acceptable shape is a helper that emits deterministic themed-icon markup plus one shared post-render/bind step that replaces failed images with the provided fallback content; do not rely on per-consumer bespoke fixes. Update `src/ui/QuickActionBar.js` only as needed to consume the new helper contract without abandoning the Plan 01 SVG fallback. If helper changes require CSS, keep it shared and minimal in `src/style.css` so icon/text fallback states remain aligned with existing buttons. Extend `tests/themeIconHelpers.test.js` and `tests/quickActionBarThemeIcon.test.js` to simulate image failure and prove the helper no longer leaves broken `<img>` output behind.</action>
  <verify>
    <automated>npx vitest run tests/themeIconHelpers.test.js tests/quickActionBarThemeIcon.test.js</automated>
  </verify>
  <done>The shared theme icon helper can recover from image load failures, and QuickActionBar inherits that recovery without introducing a QAB-only icon pipeline.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Adopt the fallback contract in game menu, save/load, backlog, and settings consumers</name>
  <read_first>
    @src/ui/GameMenu.js
    @src/ui/SaveLoadScreen.js
    @src/ui/BacklogScreen.js
    @src/ui/SettingsScreen.js
    @tests/gameMenuLayout.test.js
    @tests/saveLoadScreenLayout.test.js
    @tests/backlogScreenLayout.test.js
    @tests/settingsStructured.test.js
  </read_first>
  <files>src/ui/GameMenu.js, src/ui/SaveLoadScreen.js, src/ui/BacklogScreen.js, src/ui/SettingsScreen.js, tests/gameMenuLayout.test.js, tests/saveLoadScreenLayout.test.js, tests/backlogScreenLayout.test.js, tests/settingsStructured.test.js</files>
  <behavior>
    - Test 1: `gameMenu` keeps per-button icons highest priority, but falls back from a broken theme icon to text-only/default button content instead of a broken image.
    - Test 2: `close` consumers in SaveLoad, Backlog, and Settings recover to their existing fallback text/icon strings after image error.
    - Test 3: `voiceReplay` in Backlog recovers to `▶` / `■` behavior after image error and does not strand a broken icon while toggling playback state.
  </behavior>
  <action>Apply the shared fallback hook from Task 1 to every existing theme icon consumer per D-76-01/D-76-02: `src/ui/GameMenu.js` (`gameMenu` slot), `src/ui/SaveLoadScreen.js`, `src/ui/BacklogScreen.js`, and `src/ui/SettingsScreen.js` (`close` / `voiceReplay` slots). Preserve existing precedence and labels: GameMenu button-specific icons still beat theme icons, SaveLoad/Backlog close buttons still recover to `返回`, Settings close buttons still recover to `×` or `返回` according to their current surface, and Backlog voice replay still toggles between `▶` and `■`. Extend the layout/runtime tests named above so they dispatch image-error scenarios and assert that no broken `<img>` remains in the DOM after fallback recovery.</action>
  <verify>
    <automated>npx vitest run tests/gameMenuLayout.test.js tests/saveLoadScreenLayout.test.js tests/backlogScreenLayout.test.js tests/settingsStructured.test.js tests/themeIconHelpers.test.js tests/quickActionBarThemeIcon.test.js</automated>
  </verify>
  <done>All existing theme icon consumers recover from bad assets to their preexisting default visuals, so broken icon files no longer degrade the runtime UI.</done>
</task>

</tasks>

<verification>
- `npx vitest run tests/themeIconHelpers.test.js tests/quickActionBarThemeIcon.test.js tests/gameMenuLayout.test.js tests/saveLoadScreenLayout.test.js tests/backlogScreenLayout.test.js tests/settingsStructured.test.js`
- `node --test tests/scanAssets.test.js tests/exportGame.test.js tests/exportDesktop.test.js`
- Capture the focused pass results in the phase summary so Phase 76 has auditable evidence for the reopened icon/runtime blockers without backfilling Phases 71/72/74/75.
</verification>

<success_criteria>
- Bad or deleted theme icon assets no longer leave broken `<img>` output on any locked Phase 75 icon slot.
- `gameMenu`, `qab`, `close`, and `voiceReplay` all use the same shared fallback contract.
- Existing scan/export coverage stays green, proving Phase 76 fixed runtime parity without forking the asset pipeline.
</success_criteria>

<output>
After completion, create `.planning/phases/76-icon-runtime-fallback-closure/76-02-SUMMARY.md`
</output>
