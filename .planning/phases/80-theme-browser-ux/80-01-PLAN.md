---
phase: 80-theme-browser-ux
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/editor/services/themeBrowser.js
  - src/editor/services/themePackageImport.js
  - tests/themeBrowserService.test.js
  - tests/themePackageImportUx.test.js
autonomous: true
requirements:
  - BRW-01
  - BRW-02
  - BRW-03
must_haves:
  truths:
    - "Built-in themes and imported preflight results normalize into one browser item shape before UI rendering."
    - "The browser can tell whether a theme is available, applied, or compatibility-only by deriving state from script.data.ui.theme.packageMeta plus preflight metadata."
    - "A freshly imported theme can appear in the browser immediately without inventing a persistent imported-theme registry."
  artifacts:
    - path: "src/editor/services/themeBrowser.js"
      provides: "Normalization, filtering, lifecycle, preview fallback, and apply-impact helpers for unified browser items"
    - path: "src/editor/services/themePackageImport.js"
      provides: "Preflight import result extended with browserEntry data for session-scoped imported items"
    - path: "tests/themeBrowserService.test.js"
      provides: "Focused coverage for item normalization, state derivation, static preview fallback, and apply-impact messaging"
    - path: "tests/themePackageImportUx.test.js"
      provides: "Regression coverage for imported browserEntry and legacy-partial inspect-only semantics"
  key_links:
    - from: "src/editor/services/themeBrowser.js"
      to: "src/editor/stores/script.js"
      via: "script.data.ui.theme.packageMeta lifecycle derivation"
      pattern: "packageMeta"
    - from: "src/editor/services/themePackageImport.js"
      to: "src/editor/services/themeBrowser.js"
      via: "browserEntry creation from preflight metadata"
      pattern: "browserEntry"
    - from: "src/editor/services/themeBrowser.js"
      to: "src/shared/themePackageContract.js"
      via: "coverage/missingCoverage normalization"
      pattern: "FULL_THEME_COVERAGE_KEYS"
---

<objective>
Build the thin browser-data layer for Phase 80 so the UI can render built-in themes, imported themes, and legacy-partial themes through one normalized item model.

Purpose: Satisfy BRW-01/02/03 without reopening Phase 78/79 package mechanics or adding a second truth source.
Output: `themeBrowser.js` helpers, session-scoped imported browser entries, and focused tests that lock the Phase 80 data contract.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/80-theme-browser-ux/80-CONTEXT.md
@.planning/phases/80-theme-browser-ux/80-RESEARCH.md
@docs/superpowers/specs/2026-04-27-phase-80-theme-browser-design.md
@docs/superpowers/plans/2026-04-27-phase-80-theme-browser-implementation.md
@.planning/phases/78-theme-package-contract-compatibility-boundaries/78-02-SUMMARY.md
@.planning/phases/79-unified-install-apply-export-pipeline/79-02-SUMMARY.md
@src/editor/services/themePackageImport.js
@src/editor/services/themePackageInstall.js
@src/editor/builtinThemes.js
@src/shared/themePackageContract.js
@src/editor/stores/script.js
@tests/themePackageImportUx.test.js

<interfaces>
From `src/editor/services/themePackageImport.js`:
```javascript
export function createThemePackageImportSummary(preflight = {}) { ... }
export async function preflightThemePackageImport({ ipcRenderer, scriptStore } = {}) { ... }
```

From `src/editor/services/themePackageInstall.js`:
```javascript
export async function installAndApplyThemePackage({
  ipcRenderer,
  scriptStore,
  projectStore,
  assetStore,
  source,
  filePath,
  themeId,
} = {}) { ... }
```

From `src/shared/themePackageContract.js`:
```javascript
export const FULL_THEME_COVERAGE_KEYS = Object.freeze([
  'theme',
  'widgetStyles',
  'dialogueBox',
  'saveLoadScreen',
  'backlogScreen',
  'gameMenu',
  'settingsScreen',
]);
export function getThemePackageAssetRoot(themeId) { ... }
```

From `src/editor/stores/script.js`:
```javascript
function applyThemeBundle(bundleConfig, packageMeta) {
  const nextTheme = JSON.parse(JSON.stringify(bundleConfig?.theme ?? {}));
  if (packageMeta) {
    nextTheme.packageMeta = JSON.parse(JSON.stringify(packageMeta));
  }
  data.value.ui.theme = nextTheme;
}
```

From `src/editor/builtinThemes.js`:
```javascript
export const BUILTIN_THEMES = [
  { id, name, description, primaryColor, tokens, widgetStyles, screens },
];
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock the unified browser item contract and imported-session semantics</name>
  <files>tests/themeBrowserService.test.js, tests/themePackageImportUx.test.js</files>
  <behavior>
    - Test 1: `buildThemeBrowserItems()` returns one shared item shape with `source`, `mode`, `lifecycle`, `coverage`, `missingCoverage`, `applyImpact`, and `preview` for both built-in and imported entries (per D-04, D-06).
    - Test 2: lifecycle derives from `script.data.ui.theme.packageMeta` only, including `builtin` and persisted `file` sources mapped to browser labels without mutating persisted schema (per D-05, D-11).
    - Test 3: imported preflight results expose a `browserEntry` for immediate browser refresh/selection, but there is no persisted imported inventory or registry requirement after reopen (research finding, per D-07, D-15).
    - Test 4: legacy-partial entries remain inspect-only and never report Apply eligibility; preview assertions stay static/fallback-only and never reference iframe/live preview (per D-09, D-12).
    - Test 5: apply-impact assertions describe coverage overlap or first-write text, not filesystem overwrite counts (per D-13).
  </behavior>
  <action>Write focused Vitest coverage first. Create `tests/themeBrowserService.test.js` as the Wave 0 scaffold for normalization/filtering/lifecycle/preview fallback/apply-impact rules. Extend `tests/themePackageImportUx.test.js` so `preflightThemePackageImport()` must return `browserEntry` metadata and preserve legacy-partial inspect-only behavior. In test names and expectations, explicitly lock D-04/D-05/D-06/D-07/D-09/D-11/D-12/D-13/D-15 and keep Phase 80 thin by asserting session-scoped imported entries only — do not add a persisted browser inventory, registry file, or reopen-time imported cache.</action>
  <verify>
    <automated>npx vitest run tests/themeBrowserService.test.js tests/themePackageImportUx.test.js</automated>
  </verify>
  <done>The repo contains failing-first tests that describe the normalized item shape, real applied-state derivation, legacy-partial inspect-only behavior, static preview policy, and session-scoped imported entry flow.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the thin normalization helpers and imported browser-entry bridge</name>
  <files>src/editor/services/themeBrowser.js, src/editor/services/themePackageImport.js, tests/themeBrowserService.test.js, tests/themePackageImportUx.test.js</files>
  <behavior>
    - Test 1: helper exports are pure and deterministic for the same inputs.
    - Test 2: built-in items get stable author/version/preview fallbacks without requiring `builtinThemes.js` manifest expansion unless tests prove it is necessary.
    - Test 3: imported items reuse preflight metadata for coverage/missingCoverage and browser labels, while Apply eligibility stays driven by `mode/status`, not by file counts.
  </behavior>
  <action>Create `src/editor/services/themeBrowser.js` with explicit named exports for item normalization, filtering, preview fallback, and `computeThemeApplyImpact()`; keep the module pure and renderer-safe. Derive `lifecycle` from `script.data.ui.theme.packageMeta` only (per D-05), normalize persisted `source: 'file'` into UI label `imported` without changing stored schema (research constraint), and reuse Phase 78/79 metadata for `coverage`, `missingCoverage`, namespace text, and counts (per D-07). Extend `src/editor/services/themePackageImport.js` so successful preflight returns `browserEntry` data for immediate in-browser insertion/selection (per D-15), while legacy-partial packages remain inspect-only with no full-theme CTA (per D-09). Use stable static preview fallback data only (per D-12), compute user-facing overwrite impact from coverage overlap or first-write language instead of namespace overwrite counts (per D-13), and return conservative reason text when metadata is incomplete or apply is unavailable (per D-14). Do not modify `installAndApplyThemePackage()`, do not add any registry persistence, and do not introduce a second apply path (per D-03).</action>
  <verify>
    <automated>npx vitest run tests/themeBrowserService.test.js tests/themePackageImportUx.test.js</automated>
  </verify>
  <done>`themeBrowser.js` exists with named exports consumed by later UI work, imported preflight results include `browserEntry`, lifecycle stays tied to `packageMeta`, legacy-partial remains inspect-only, and the focused helper/import tests pass.</done>
</task>

</tasks>

<verification>
Focused gate for this plan:
- `npx vitest run tests/themeBrowserService.test.js tests/themePackageImportUx.test.js`
- Confirm no new persisted imported-theme inventory file/module was introduced.
</verification>

<success_criteria>
- Built-in and imported themes can be represented by the same item contract before any UI work starts.
- Applied-state derivation depends only on `script.data.ui.theme.packageMeta`.
- Imported themes appear immediately after preflight via session-scoped `browserEntry`, while legacy-partial themes stay inspect-only.
- No live iframe preview or second install/apply path is introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/80-theme-browser-ux/80-01-SUMMARY.md`
</output>
