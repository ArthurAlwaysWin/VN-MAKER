---
phase: 80-theme-browser-ux
plan: 02
type: execute
wave: 2
depends_on:
  - 80-01
files_modified:
  - src/editor/components/theme/ThemeBrowserModal.vue
  - src/editor/views/ProjectSettings.vue
  - src/editor/helpTexts.js
  - tests/themeBrowserModal.test.js
  - tests/themeBrowserRouting.test.js
autonomous: true
requirements:
  - BRW-01
  - BRW-02
  - BRW-03
must_haves:
  truths:
    - "Project Settings opens one unified theme browser instead of split preset/package entry points."
    - "The browser shows static preview cards plus detail-panel coverage and overwrite explanations before apply."
    - "Users can distinguish built-in, imported, currently applied, and compatibility-only partial themes from the browser state and CTA rules."
  artifacts:
    - path: "src/editor/components/theme/ThemeBrowserModal.vue"
      provides: "Unified four-region browser with toolbar import, filter rail, card list, and detail panel"
    - path: "src/editor/views/ProjectSettings.vue"
      provides: "Single browser entry wired from Project Settings"
    - path: "tests/themeBrowserModal.test.js"
      provides: "Regression coverage for browser layout, state badges, detail content, and no-live-preview policy"
    - path: "tests/themeBrowserRouting.test.js"
      provides: "Regression coverage that Project Settings routes theme browsing through the unified browser only"
  key_links:
    - from: "src/editor/views/ProjectSettings.vue"
      to: "src/editor/components/theme/ThemeBrowserModal.vue"
      via: "single toolbar entry and modal close wiring"
      pattern: "ThemeBrowserModal"
    - from: "src/editor/components/theme/ThemeBrowserModal.vue"
      to: "src/editor/services/themeBrowser.js"
      via: "normalized items, filtering, and impact/detail rendering"
      pattern: "buildThemeBrowserItems|filterThemeBrowserItems|computeThemeApplyImpact"
    - from: "src/editor/components/theme/ThemeBrowserModal.vue"
      to: "src/editor/services/themePackageInstall.js"
      via: "shared apply orchestration for full themes only"
      pattern: "installAndApplyThemePackage"
---

<objective>
Ship the unified Phase 80 browser surface in Project Settings so users can browse, inspect, import, and apply themes through one modal without split UX or preview-scope drift.

Purpose: Complete BRW-01/02/03 at the UI layer while preserving the Phase 79 install/apply path and the no-live-preview boundary.
Output: A single `ThemeBrowserModal`, Project Settings routing updates, help copy alignment, and focused UI regression tests.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/80-theme-browser-ux/80-CONTEXT.md
@.planning/phases/80-theme-browser-ux/80-RESEARCH.md
@docs/superpowers/specs/2026-04-27-phase-80-theme-browser-design.md
@docs/superpowers/plans/2026-04-27-phase-80-theme-browser-implementation.md
@src/editor/views/ProjectSettings.vue
@src/editor/components/theme/ThemePackageModal.vue
@src/editor/components/theme/PresetModal.vue
@src/editor/services/themePackageInstall.js
@src/editor/helpTexts.js

<interfaces>
Planned contract from `80-01`:
```javascript
export function buildThemeBrowserItems({ builtins, importedEntries, scriptData } = {}) { ... }
export function filterThemeBrowserItems(items, filters = {}) { ... }
export function computeThemeApplyImpact(item, scriptData = {}) { ... }
export function resolveThemeBrowserPreview(item) { ... }
```

Existing apply/import boundaries:
```javascript
export async function preflightThemePackageImport({ ipcRenderer, scriptStore } = {}) { ... }
export async function installAndApplyThemePackage({
  ipcRenderer,
  scriptStore,
  projectStore,
  assetStore,
  source,
  filePath,
  themeId,
} = {}) { ... }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock unified browser UI behavior with focused routing and modal tests</name>
  <files>tests/themeBrowserModal.test.js, tests/themeBrowserRouting.test.js</files>
  <behavior>
    - Test 1: `ProjectSettings.vue` routes theme browsing through one `ThemeBrowserModal` entry instead of split `PresetModal` + `ThemePackageModal` buttons/modals (per D-01).
    - Test 2: the browser source/layout reflects the locked four-region structure: toolbar import, filter rail, card list, and detail panel (per D-02).
    - Test 3: cards/details render the required state semantics and CTA rules for `内置可用`, `已导入`, `当前已应用`, and `兼容导入 / 部分主题`; the applied item hides the primary Apply CTA and legacy-partial remains inspect-only (per D-08, D-09, D-10, D-11).
    - Test 4: browser copy and source assertions explicitly reject iframe/live-preview behavior and require static preview/fallback language plus pre-apply impact explanation (per D-12, D-13).
    - Test 5: import-failure assertions preserve filter/search/selection/detail state and show inline feedback near the toolbar import action (per D-14, D-15).
  </behavior>
  <action>Create focused tests before implementation. Add `tests/themeBrowserModal.test.js` for layout, badges, detail-panel explanations, static preview boundary, and inline import feedback behavior. Add `tests/themeBrowserRouting.test.js` for Project Settings entry replacement and no-split-modal regressions. Make assertions trace back to D-01/D-02/D-08/D-09/D-10/D-11/D-12/D-13/D-14/D-15, and keep scope limited to browser UX only — no golden theme production, registry persistence, or new preview system (per D-03).</action>
  <verify>
    <automated>npx vitest run tests/themeBrowserModal.test.js tests/themeBrowserRouting.test.js</automated>
  </verify>
  <done>Focused UI tests exist and fail-first on missing unified browser layout, missing state semantics, or any attempt to route Phase 80 through split modals/live preview.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the unified browser modal and Project Settings wiring</name>
  <files>src/editor/components/theme/ThemeBrowserModal.vue, src/editor/views/ProjectSettings.vue, src/editor/helpTexts.js, tests/themeBrowserModal.test.js, tests/themeBrowserRouting.test.js</files>
  <behavior>
    - Test 1: the modal composes from normalized items instead of separate built-in/imported rendering branches.
    - Test 2: full built-in and full imported items apply through the existing shared install/apply service only.
    - Test 3: import success refreshes and selects the new item in-browser, while import failure leaves existing browser context intact.
  </behavior>
  <action>Create `src/editor/components/theme/ThemeBrowserModal.vue` as the single Phase 80 browser surface and wire it from `ProjectSettings.vue` as the only theme-browsing entry (per D-01). Implement the locked four-region layout from D-02: top toolbar with import, left-side filters, center card list, right-side detail panel. Drive all rendering from normalized items supplied by `themeBrowser.js` (per D-04, D-06), show static preview/fallback only with no iframe/live preview for unopened themes (per D-12), and keep scope to browse/understand/select without rewriting package mechanics or content production (per D-03). Use `preflightThemePackageImport()` for toolbar import and preserve filter/search/selection/detail state on failure while showing inline feedback near the import control; on success, append/select the returned `browserEntry` immediately (per D-15). Use `installAndApplyThemePackage()` unchanged for full built-in/imported Apply CTA handling (per D-08), keep legacy-partial items inspect-only with coverage/missing coverage explanation and no whole-theme CTA (per D-09), derive `当前已应用` only from `script.data.ui.theme.packageMeta` and hide the primary Apply CTA for that item (per D-05, D-10, D-11), and show conservative reason/impact text when metadata is incomplete or apply is unavailable (per D-13, D-14). Update `src/editor/helpTexts.js` only as needed to keep browser/help copy aligned with the new unified entry.</action>
  <verify>
    <automated>npx vitest run tests/themeBrowserService.test.js tests/themeBrowserModal.test.js tests/themeBrowserRouting.test.js tests/themePackageImportUx.test.js tests/themePackageInstallFlow.test.js tests/scriptThemeApply.test.js</automated>
  </verify>
  <done>Project Settings opens one unified browser; cards and details show the required preview/status/coverage/impact information; import feedback stays inside the browser; full themes apply through the shared Phase 79 path; legacy-partial stays inspect-only.</done>
</task>

</tasks>

<verification>
Overall Phase 80 UI gate:
- `npx vitest run tests/themeBrowserService.test.js tests/themeBrowserModal.test.js tests/themeBrowserRouting.test.js tests/themePackageImportUx.test.js tests/themePackageInstallFlow.test.js tests/scriptThemeApply.test.js`
- `npm run build`
</verification>

<success_criteria>
- Project Settings no longer exposes split preset/package theme browsing entry points.
- The browser shows one normalized list of built-in, imported, applied, and compatibility-only items with static previews and coverage/impact details.
- Import success refreshes/selects the new item in-place; import failure preserves current browser context and shows inline feedback.
- All Apply actions still flow through the shared Phase 79 install/apply path, and no live iframe preview or second truth source is introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/80-theme-browser-ux/80-02-SUMMARY.md`
</output>
