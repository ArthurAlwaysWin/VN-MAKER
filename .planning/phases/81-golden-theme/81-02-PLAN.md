---
phase: 81-golden-theme
plan: '02'
type: execute
wave: 2
depends_on:
  - '81-01'
files_modified:
  - src/editor/builtinThemes.js
  - electron/themePackageInstaller.js
  - src/editor/services/themeBrowser.js
  - src/editor/services/themePackageImport.js
  - src/editor/components/theme/ThemeBrowserModal.vue
  - tests/themePackageInstaller.test.js
  - tests/themePackageInstallFlow.test.js
  - tests/scriptThemeApply.test.js
  - tests/themeBrowserService.test.js
  - tests/themePackageImportUx.test.js
  - tests/themeBrowserModal.test.js
autonomous: true
requirements:
  - THM-01
must_haves:
  truths:
    - "Built-in `wafuu` is presented and applied as the titleScreen-inclusive golden theme sample."
    - "The unified browser and import summaries describe full-theme completeness in 8-surface language, including titleScreen."
    - "Applying `wafuu` still goes through the shared install/apply path rather than a browser-only or title-only shortcut."
  artifacts:
    - path: src/editor/builtinThemes.js
      provides: "wafuu golden baseline including titleScreen data"
      contains: "id: 'wafuu'"
    - path: src/editor/services/themeBrowser.js
      provides: "titleScreen-aware coverage labels and apply-impact copy"
      contains: "COVERAGE_LABELS"
    - path: src/editor/components/theme/ThemeBrowserModal.vue
      provides: "browser UI copy that reflects titleScreen completeness"
      contains: "覆盖范围"
  key_links:
    - from: src/editor/builtinThemes.js
      to: electron/themePackageInstaller.js
      via: "createBuiltInThemeUi()/install-theme-package"
      pattern: "titleScreen"
    - from: src/editor/services/themeBrowser.js
      to: src/editor/components/theme/ThemeBrowserModal.vue
      via: "coverageLabels/missingCoverageLabels"
      pattern: "标题界面"
    - from: src/editor/services/themePackageImport.js
      to: src/editor/components/theme/ThemeBrowserModal.vue
      via: "preflight summary and imported browserEntry copy"
      pattern: "标题界面"
---

<objective>
Promote built-in `wafuu` into the shipped golden theme sample so the same browser/install/apply pipeline now exposes and applies a titleScreen-inclusive full theme.

Purpose: satisfy D-04, D-05, D-09, and D-11 by turning `wafuu` into the actual Phase 81 acceptance baseline instead of leaving titleScreen as an unowned gap.
Output: `wafuu` golden baseline data plus browser/import UX that clearly shows titleScreen as part of full-theme completeness.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/81-golden-theme/81-CONTEXT.md
@.planning/phases/81-golden-theme/81-RESEARCH.md
@.planning/phases/80-theme-browser-ux/80-CONTEXT.md
@.planning/phases/80-theme-browser-ux/80-VERIFICATION.md
@src/editor/builtinThemes.js
@src/editor/services/themeBrowser.js
@src/editor/services/themePackageImport.js
@src/editor/components/theme/ThemeBrowserModal.vue

<interfaces>
From `src/editor/builtinThemes.js`:
```javascript
{
  id: 'wafuu',
  tokens: { ... },
  widgetStyles: { ... },
  screens: {
    saveLoadScreen: { ... },
    backlogScreen: { ... },
    gameMenu: { ... },
    settingsScreen: { ... },
  },
}
```

From `src/editor/services/themeBrowser.js`:
```javascript
const COVERAGE_LABELS = Object.freeze({
  theme: '主题基础',
  widgetStyles: '通用控件',
  dialogueBox: '对话框',
  saveLoadScreen: '存档/读档',
  backlogScreen: '回看界面',
  gameMenu: '游戏菜单',
  settingsScreen: '设置界面',
});
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Promote `wafuu` to a titleScreen-inclusive golden built-in theme</name>
  <files>src/editor/builtinThemes.js, electron/themePackageInstaller.js, tests/themePackageInstaller.test.js, tests/themePackageInstallFlow.test.js, tests/scriptThemeApply.test.js</files>
  <behavior>
    - Test 1: built-in `wafuu` install returns a full bundle that includes `titleScreen`.
    - Test 2: applying built-in `wafuu` changes title visuals through the shared install/apply path.
    - Test 3: if `wafuu` introduces canonical title image refs, those refs are materialized into the project namespace before apply.
  </behavior>
  <action>Per D-04 and D-05, extend `wafuu` in `builtinThemes.js` so it contains a real title baseline that matches its dialogue/save-load/backlog/game-menu/settings visual language. Route that title data through the same installed bundle shape created in Plan 81-01; do not add a second built-in title apply path. If the uplift uses canonical `ui/themes/wafuu/...` images, update the built-in install branch to copy those app-bundled assets into the project-local namespace before apply/export so D-10 save/reopen/export parity still holds. Keep Phase 82 out of scope: do not raise the completeness bar for the other built-in themes in this plan.</action>
  <verify>
    <automated>npx vitest run tests/themePackageInstaller.test.js tests/themePackageInstallFlow.test.js tests/scriptThemeApply.test.js</automated>
  </verify>
  <done>`wafuu` can be installed/applied as the titleScreen-inclusive golden theme using the same shared pipeline as imported full themes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update unified browser and import summaries so full-theme completeness explicitly includes titleScreen</name>
  <files>src/editor/services/themeBrowser.js, src/editor/services/themePackageImport.js, src/editor/components/theme/ThemeBrowserModal.vue, tests/themeBrowserService.test.js, tests/themePackageImportUx.test.js, tests/themeBrowserModal.test.js</files>
  <behavior>
    - Test 1: browser coverage and apply-impact labels include `标题界面`.
    - Test 2: built-in `wafuu` and imported ready packages both describe completeness with titleScreen included.
    - Test 3: the unified browser remains static-preview-only and keeps the existing built-in/imported/applied/legacy-partial lifecycle semantics.
  </behavior>
  <action>Per D-09 and D-12, update the shared browser/import label maps, detail copy, and any relevant modal badges so “完整主题” now means the titleScreen-inclusive 8-surface contract. Keep Phase 80’s single-browser rules intact: no split modal, no live iframe preview, no browser-only applied-state registry. `ThemeBrowserModal.vue` should continue consuming normalized browser items; only adjust text, labels, and coverage presentation needed to make titleScreen visible in the browser parity story.</action>
  <verify>
    <automated>npx vitest run tests/themeBrowserService.test.js tests/themePackageImportUx.test.js tests/themeBrowserModal.test.js</automated>
  </verify>
  <done>The browser/import UX clearly communicates that the golden full-theme sample includes titleScreen, without regressing Phase 80 status semantics or preview boundaries.</done>
</task>

</tasks>

<verification>
After both tasks, rerun both automated commands. Confirm the browser still uses one modal and that `wafuu` remains the only Phase 81 golden baseline.
</verification>

<success_criteria>
- `wafuu` carries titleScreen baseline data through the shared install/apply path.
- The unified browser and import summaries show `标题界面` as part of full-theme completeness.
- No new title-only UI entry point or Phase 82 multi-theme work appears.
</success_criteria>

<output>
After completion, create `.planning/phases/81-golden-theme/81-02-SUMMARY.md`
</output>
