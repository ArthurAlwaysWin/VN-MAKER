---
phase: 81-golden-theme
plan: '03'
type: execute
wave: 3
depends_on:
  - '81-01'
  - '81-02'
files_modified:
  - tests/themeGoldenAcceptance.test.js
  - tests/themePackageExporter.test.js
  - tests/themePackageRoundTrip.test.js
  - tests/themeBrowserService.test.js
  - electron/themePackageExporter.js
  - electron/themePackageInstaller.js
  - src/editor/stores/script.js
  - src/editor/services/themeBrowser.js
autonomous: false
requirements:
  - THM-01
must_haves:
  truths:
    - "Golden `wafuu` survives apply, save/reopen, playtest, exported output, export, and reimport without losing titleScreen visuals."
    - "Reopened/exported/reimported projects still describe the applied package as a full theme that includes titleScreen."
    - "Human verification can confirm titleScreen and the other non-content UI surfaces read as one coherent visual system while titleScreen.bgm stays unchanged."
  artifacts:
    - path: tests/themeGoldenAcceptance.test.js
      provides: "focused end-to-end parity evidence for titleScreen-inclusive golden theme"
      contains: "save/reopen/playtest/exported-output/export/reimport/browser parity"
    - path: tests/themePackageRoundTrip.test.js
      provides: "round-trip assertions for titleScreen assets and snapshot"
      contains: "titleScreen"
    - path: tests/themeBrowserService.test.js
      provides: "reopen/browser parity assertions with titleScreen coverage"
      contains: "标题界面"
  key_links:
    - from: src/editor/stores/script.js
      to: electron/themePackageExporter.js
      via: "persisted script.json snapshot after apply/save/reopen"
      pattern: "titleScreen"
    - from: electron/themePackageExporter.js
      to: tests/themePackageRoundTrip.test.js
      via: "exported `.gmtheme` parsed back into full theme metadata"
      pattern: "titleScreen"
    - from: src/editor/services/themeBrowser.js
      to: tests/themeGoldenAcceptance.test.js
      via: "reopened browser coverage for applied package"
      pattern: "FULL_THEME_COVERAGE_KEYS"
---

<objective>
Prove the titleScreen-inclusive golden `wafuu` theme meets Phase 81 acceptance by locking save/reopen/playtest/exported-output/export/reimport/browser parity and then performing one final human verification pass.

Purpose: satisfy D-10, D-11, and D-12 with focused evidence that the shipped golden theme behaves consistently across the whole existing delivery chain.
Output: end-to-end automated acceptance coverage plus one blocking human verification checklist for visual coherence and titleScreen.bgm preservation.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/81-golden-theme/81-CONTEXT.md
@.planning/phases/81-golden-theme/81-RESEARCH.md
@.planning/phases/81-golden-theme/81-VALIDATION.md
@src/editor/services/themeBrowser.js
@electron/themePackageExporter.js
@src/editor/stores/script.js
@tests/themePackageExporter.test.js
@tests/themePackageRoundTrip.test.js
@tests/themeBrowserService.test.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add focused golden-theme parity evidence for titleScreen across save/reopen/playtest/exported-output/export/reimport/browser flows</name>
  <files>tests/themeGoldenAcceptance.test.js, tests/themePackageExporter.test.js, tests/themePackageRoundTrip.test.js, tests/themeBrowserService.test.js, electron/themePackageExporter.js, electron/themePackageInstaller.js, src/editor/stores/script.js, src/editor/services/themeBrowser.js</files>
  <behavior>
    - Test 1: after full-theme apply, serializing and reloading script data preserves title visuals and original `titleScreen.bgm`.
    - Test 2: playtest/runtime and exported output both render the themed `titleScreen` visuals while keeping `titleScreen.bgm` project-owned.
    - Test 3: exporting then reparsing/reimporting the golden theme preserves title snapshot data and title asset manifest files.
    - Test 4: browser reconstruction after reopen still reports the applied package as full coverage, including `标题界面`.
  </behavior>
  <action>Create a dedicated focused acceptance regression that stitches together the exact THM-01 story from D-10: apply the title-inclusive golden theme, persist/reload the script snapshot, verify runtime playtest behavior, verify exported output/runtime behavior, export a `.gmtheme`, parse/reimport it, and confirm title visuals plus browser coverage remain stable while `titleScreen.bgm` stays on the project side. Reuse the existing exporter/installer/browser/store surfaces instead of inventing a new test-only pipeline. If fully automated playtest/exported-output proof is not practical with current harnesses, keep the persistence/export/reimport/browser portions automated and add an explicit blocking human verification step for playtest/exported-output parity instead of silently omitting that acceptance path. If this exposes drift, make only the minimal fixes inside the already touched Phase 81 files listed here.</action>
  <verify>
    <automated>npx vitest run tests/themePackageContract.test.js tests/themePackager.test.js tests/themePackagePreflight.test.js tests/themePackageInstaller.test.js tests/themePackageInstallFlow.test.js tests/scriptThemeApply.test.js tests/themePackageExporter.test.js tests/themePackageRoundTrip.test.js tests/themeBrowserService.test.js tests/themePackageImportUx.test.js tests/themeGoldenAcceptance.test.js && npm run build</automated>
  </verify>
  <done>There is a focused proof that golden `wafuu` keeps titleScreen parity across apply, save/reopen, playtest, exported output, export, reimport, and browser reconstruction, with any non-automatable runtime checks explicitly escalated to the blocking human gate.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human acceptance check for titleScreen-inclusive golden theme coherence</name>
  <what-built>Full Phase 81 golden theme behavior through the unified browser, shared install/apply/export/import pipeline, and titleScreen visual-only ownership.</what-built>
  <action>Use the shipped Phase 81 implementation from Tasks 81-01 and 81-02 exactly as built; do not hand-edit project files during verification. This checkpoint exists only to confirm the human-visible acceptance bar in D-03 and D-11 after the automated parity evidence is already green.</action>
  <how-to-verify>
    1. Open Project Settings → `🎭 主题浏览器`, select built-in `wafuu`, and apply it.
    2. Inspect the title screen, dialogue, save/load, backlog, game menu, and settings surfaces. Confirm they read as one coherent visual system rather than isolated image swaps.
    3. Before saving, note the current `ui.titleScreen.bgm` value. Save the project, close/reopen it, and confirm title visuals persist while the BGM value is unchanged.
    4. Launch a runtime playtest from the themed project and confirm the title screen visuals match the applied theme while `ui.titleScreen.bgm` still follows the project value rather than the theme.
    5. Export a runnable build/output from the themed project, open it, and confirm the title screen visuals still match the golden theme and the BGM behavior remains project-owned.
    6. Export the current theme package, import it into a fresh project through the same browser/import flow, apply it, and confirm the browser still presents the theme as a full package whose coverage includes titleScreen.
  </how-to-verify>
  <verify>
    <automated>npx vitest run tests/themePackageContract.test.js tests/themePackager.test.js tests/themePackagePreflight.test.js tests/themePackageInstaller.test.js tests/themePackageInstallFlow.test.js tests/scriptThemeApply.test.js tests/themePackageExporter.test.js tests/themePackageRoundTrip.test.js tests/themeBrowserService.test.js tests/themePackageImportUx.test.js tests/themeGoldenAcceptance.test.js && npm run build</automated>
  </verify>
  <done>Human verification confirms visual coherence across title/dialogue/save-load/backlog/game-menu/settings and confirms `titleScreen.bgm` stayed untouched through save/reopen/playtest/exported-output/export/reimport.</done>
  <resume-signal>Type "approved" if all checks pass, or describe the exact mismatch (surface, step, expected, actual).</resume-signal>
</task>

</tasks>

<verification>
Task 1 must pass before the human checkpoint. Do not skip the build step, and do not replace the manual check with screenshots alone.
</verification>

<success_criteria>
- Automated parity evidence plus the blocking human gate cover titleScreen across save/reopen/playtest/exported-output/export/reimport/browser flows.
- Human verification confirms visual coherence across title/dialogue/save-load/backlog/game-menu/settings.
- `titleScreen.bgm` remains project-owned throughout the full acceptance path.
</success_criteria>

<output>
After completion, create `.planning/phases/81-golden-theme/81-03-SUMMARY.md`
</output>
