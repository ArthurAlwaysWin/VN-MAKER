---
phase: 82-4
plan: '01'
type: execute
wave: 1
depends_on: []
files_modified:
  - src/editor/builtinThemes.js
  - src/editor/builtinThemeAssets.js
  - electron/themePackageInstaller.js
  - src/editor/services/themeBrowser.js
  - tests/builtinThemeCompleteness.test.js
  - tests/builtinThemeVisualContract.test.js
  - tests/themeBrowserService.test.js
  - tests/themePackageInstaller.test.js
autonomous: true
requirements:
  - THM-02
  - THM-03
must_haves:
  truths:
    - "Theme browser truth is backed by explicit built-in data instead of assuming every built-in is already full."
    - "Built-in themes can materialize declared bundled assets into project-local `assets/ui/themes/<id>/...` before apply/export parity depends on them."
    - "The shipped roster has an enforceable 8-surface completeness gate and explicit visual-signature metadata before theme art rollout continues."
  artifacts:
    - path: tests/builtinThemeCompleteness.test.js
      provides: "explicit 5-theme completeness gate over the 8-surface contract"
      contains: "FULL_THEME_COVERAGE_KEYS"
    - path: tests/builtinThemeVisualContract.test.js
      provides: "preview and visual-signature gate for shipped built-ins"
      contains: "visualSignature"
    - path: src/editor/builtinThemeAssets.js
      provides: "single bundled-asset registry for built-in theme install materialization"
      contains: "ui/themes/"
    - path: electron/themePackageInstaller.js
      provides: "shared built-in/imported install path with project-local file writes"
      contains: "source === 'builtin'"
  key_links:
    - from: src/editor/builtinThemes.js
      to: src/editor/services/themeBrowser.js
      via: "explicit coverage + preview metadata consumed by browser normalization"
      pattern: "coverage|preview|visualSignature"
    - from: src/editor/builtinThemeAssets.js
      to: electron/themePackageInstaller.js
      via: "bundled source file -> canonical project-local target copy"
      pattern: "ui/themes/"
    - from: tests/builtinThemeCompleteness.test.js
      to: src/editor/builtinThemes.js
      via: "meaningful 8-surface assertions for all 5 shipped themes"
      pattern: "default|wafuu|modern-sky|fantasy-dark|minimal-white"
---

<objective>
Establish truthful built-in completeness, explicit visual-signature contracts, and shared built-in asset materialization before authoring the remaining four shipped themes.

Purpose: close the research-confirmed truth gap first so Phase 82 builds on verified completeness instead of browser theater.
Output: built-in completeness tests, visual-contract tests, honest browser normalization, and a shared built-in asset registry/installer path.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/82-4/82-CONTEXT.md
@.planning/phases/82-4/82-RESEARCH.md
@.planning/phases/81-golden-theme/81-03-SUMMARY.md
@src/editor/builtinThemes.js
@src/shared/themePackageContract.js
@src/editor/services/themeBrowser.js
@electron/themePackageInstaller.js
@tests/themeBrowserService.test.js
@tests/themePackageInstaller.test.js

<interfaces>
From `src/shared/themePackageContract.js`:
```javascript
export const FULL_THEME_COVERAGE_KEYS = Object.freeze([
  'theme',
  'widgetStyles',
  'dialogueBox',
  'saveLoadScreen',
  'backlogScreen',
  'gameMenu',
  'settingsScreen',
  'titleScreen',
]);
```

From `electron/themePackageInstaller.js`:
```javascript
return {
  success: true,
  bundle: normalizeInstalledBundle(...),
  packageMeta: {
    source: 'builtin',
    themeId: theme.id,
    mode: 'full',
    assetRoot: `ui/themes/${theme.id}/`,
  },
};
```

Target built-in manifest contract for downstream plans:
```javascript
{
  id: 'default',
  preview: { mode: 'asset', src: '/builtin-themes/default/preview.svg', background: '#...', accent: '#...' },
  visualSignature: {
    materialLanguage: '...',
    contourLanguage: '...',
    styleDirection: '...',
    requiredTells: {
      titleScreen: '...',
      dialogueBox: '...',
      buttonFamily: '...',
      majorScreenChrome: '...',
    },
  },
  coverage: [...FULL_THEME_COVERAGE_KEYS],
  ui: {
    theme: { ... },
    widgetStyles: { ... },
    dialogueBox: { ... },
    saveLoadScreen: { ... },
    backlogScreen: { ... },
    gameMenu: { ... },
    settingsScreen: { ... },
    titleScreen: { ... },
  },
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add explicit built-in completeness and visual-contract gates, then make browser truth follow them</name>
  <read_first>@.planning/phases/82-4/82-CONTEXT.md, @.planning/phases/82-4/82-RESEARCH.md, @src/editor/builtinThemes.js, @src/shared/themePackageContract.js, @src/editor/services/themeBrowser.js, @tests/themeBrowserService.test.js, @tests/themePackageInstaller.test.js</read_first>
  <files>tests/builtinThemeCompleteness.test.js, tests/builtinThemeVisualContract.test.js, tests/themeBrowserService.test.js, src/editor/builtinThemes.js, src/editor/services/themeBrowser.js</files>
  <behavior>
    - Test 1: `default`, `wafuu`, `modern-sky`, `fantasy-dark`, and `minimal-white` each fail unless they expose meaningful data for every key in `FULL_THEME_COVERAGE_KEYS`.
    - Test 2: each built-in fails unless it declares explicit `preview` metadata plus `visualSignature.materialLanguage`, `visualSignature.contourLanguage`, `visualSignature.styleDirection`, and four `requiredTells`.
    - Test 3: browser normalization stops granting built-ins implicit `FULL_THEME_COVERAGE_KEYS` fallback and instead reflects explicit built-in coverage truth.
  </behavior>
  <acceptance_criteria>
    - `tests/builtinThemeCompleteness.test.js` exists and iterates exactly the 5 shipped built-in IDs.
    - `tests/builtinThemeVisualContract.test.js` fails if any built-in lacks preview metadata or visual-signature fields.
    - `tests/themeBrowserService.test.js` no longer treats built-in fullness as implicit fallback behavior.
    - `src/editor/builtinThemes.js` makes `wafuu.dialogueBox` explicit so the baseline also satisfies D-02 and D-04 instead of relying on a soft exception.
  </acceptance_criteria>
  <action>Write the new completeness and visual-contract tests first, then update `src/editor/builtinThemes.js` and `src/editor/services/themeBrowser.js` to satisfy them. Per D-03, keep `BUILTIN_THEMES` as the single built-in source; do not create a second browser registry. Per D-04, make full coverage explicit in manifest data rather than inferred from browser defaults. Per D-07, D-08, and D-09, add explicit `preview` and `visualSignature` metadata for all 5 shipped themes, including locked role descriptions and required tells for `titleScreen`, `dialogueBox`, one button-family motif, and one major-screen chrome motif. Per D-02, patch the built-in `wafuu` baseline so `dialogueBox` is explicitly non-empty. Per D-05, keep browser behavior static-preview-only and reuse the existing normalization/apply-impact path instead of inventing a built-in-only browser shortcut.</action>
  <verify>
    <automated>npx vitest run tests/builtinThemeCompleteness.test.js tests/builtinThemeVisualContract.test.js tests/themeBrowserService.test.js</automated>
  </verify>
  <done>Built-in completeness and differentiation have explicit failing gates, `wafuu` no longer depends on a dialogueBox exception, and browser truth now reflects actual built-in data rather than a blanket full-theme assumption.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the shared built-in bundled-asset registry and project-local materialization path</name>
  <read_first>@.planning/phases/82-4/82-CONTEXT.md, @.planning/phases/82-4/82-RESEARCH.md, @electron/themePackageInstaller.js, @src/editor/builtinThemes.js, @tests/themePackageInstaller.test.js, @tests/scriptThemeApply.test.js</read_first>
  <files>src/editor/builtinThemeAssets.js, electron/themePackageInstaller.js, tests/themePackageInstaller.test.js, src/editor/builtinThemes.js</files>
  <behavior>
    - Test 1: built-in themes with declared bundled files are copied into `assets/ui/themes/<id>/...` before install returns success.
    - Test 2: built-in themes with no declared bundled files still return the same `bundle + packageMeta` shape and do not break renderer apply.
    - Test 3: the bundled-asset registry maps app-bundled source files to canonical `ui/themes/<id>/...` targets without creating a second built-in apply pipeline.
  </behavior>
  <acceptance_criteria>
    - `src/editor/builtinThemeAssets.js` exists and exports one registry keyed by built-in theme ID.
    - `tests/themePackageInstaller.test.js` covers both a declared-file copy path and a no-files built-in path.
    - `electron/themePackageInstaller.js` still returns `{ success, bundle, packageMeta }` for both `source: 'builtin'` and `source: 'file'`.
    - Built-in target paths stay canonical under `ui/themes/<themeId>/...`.
  </acceptance_criteria>
  <action>Create `src/editor/builtinThemeAssets.js` as the single Phase 82 bundled-asset registry. Use source files under `public/builtin-themes/<themeId>/...` and target paths under canonical `ui/themes/<themeId>/...`; this is the shared source of truth for all later theme asset authoring. Update `electron/themePackageInstaller.js` so the built-in branch resolves the declared bundled files, copies them into `assets/<targetPath>`, and only then returns the existing normalized `bundle + packageMeta` contract. Keep `source: 'builtin'`, `assetRoot`, `applyThemeBundle()`, export, and import semantics unchanged per D-05 and D-12. Do not invent a built-in-only exporter, browser-only asset path, or renderer-side file write shortcut. This task is infrastructure only: land the materialization path and registry shape, but leave actual theme art files for Plans 82-02 and 82-03.</action>
  <verify>
    <automated>npx vitest run tests/themePackageInstaller.test.js tests/scriptThemeApply.test.js</automated>
  </verify>
  <done>Built-in image themes now have one shared, project-local install path ready for Phase 82 asset rollout, with canonical namespace safety preserved.</done>
</task>

</tasks>

<verification>
Run both automated commands after Task 2. The browser must remain one static-preview surface, and built-in installs must still terminate in the same renderer `applyThemeBundle()` semantics as imported packages.
</verification>

<success_criteria>
- Built-in completeness is enforced by new tests instead of browser assumptions.
- The shipped roster exposes explicit preview/signature metadata that later plans can fill with final authored data.
- Built-in canonical asset refs can be materialized into project-local files through the shared installer path before any image-heavy theme rollout lands.
</success_criteria>

<output>
After completion, create `.planning/phases/82-4/82-01-SUMMARY.md`
</output>
