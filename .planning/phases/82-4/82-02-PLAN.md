---
phase: 82-4
plan: '02'
type: execute
wave: 2
depends_on:
  - '82-01'
files_modified:
  - src/editor/builtinThemes.js
  - src/editor/builtinThemeAssets.js
  - public/builtin-themes/default/preview.svg
  - public/builtin-themes/default/dialogue-nameplate.svg
  - public/builtin-themes/default/screen-chrome.svg
  - public/builtin-themes/default/title-background.svg
  - public/builtin-themes/default/title-mark.svg
  - public/builtin-themes/modern-sky/preview.svg
  - public/builtin-themes/modern-sky/dialogue-nameplate.svg
  - public/builtin-themes/modern-sky/screen-chrome.svg
  - public/builtin-themes/modern-sky/title-background.svg
  - public/builtin-themes/modern-sky/title-mark.svg
autonomous: true
requirements:
  - THM-02
  - THM-03
must_haves:
  truths:
    - "The built-in `default` theme is a shipped full theme, not an empty engine-state placeholder."
    - "The built-in `modern-sky` theme reads as a distinct airy glass-panel system across title/dialogue/button families/major screens."
    - "Both themes install through the shared built-in pipeline with canonical project-local assets and explicit 8-surface ownership."
  artifacts:
    - path: src/editor/builtinThemes.js
      provides: "full `default` and `modern-sky` built-in theme definitions with explicit UI ownership"
      contains: "default"
    - path: src/editor/builtinThemeAssets.js
      provides: "bundled asset mappings for `default` and `modern-sky`"
      contains: "public/builtin-themes/"
    - path: public/builtin-themes/default/preview.svg
      provides: "static browser preview for `default`"
      contains: "<svg"
    - path: public/builtin-themes/modern-sky/preview.svg
      provides: "static browser preview for `modern-sky`"
      contains: "<svg"
  key_links:
    - from: src/editor/builtinThemeAssets.js
      to: electron/themePackageInstaller.js
      via: "bundled file copy into canonical project asset namespace"
      pattern: "ui/themes/default|ui/themes/modern-sky"
    - from: src/editor/builtinThemes.js
      to: src/editor/services/themeBrowser.js
      via: "explicit preview + coverage + visualSignature metadata"
      pattern: "preview|visualSignature"
    - from: src/editor/builtinThemes.js
      to: tests/builtinThemeCompleteness.test.js
      via: "explicit 8-surface manifest ownership"
      pattern: "dialogueBox|titleScreen"
---

<objective>
Promote `default` and `modern-sky` from partial built-ins to shipped full themes with explicit assets, preview metadata, and differentiated 8-surface UI ownership.

Purpose: satisfy the first half of THM-02/03 by turning two partial built-ins into real shipped themes without breaking the shared install/apply/export/browser path.
Output: authored `default` and `modern-sky` theme assets plus explicit built-in manifest/asset-registry entries that pass the new completeness and visual-contract gates.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/82-4/82-CONTEXT.md
@.planning/phases/82-4/82-RESEARCH.md
@src/editor/builtinThemes.js
@src/editor/builtinThemeAssets.js
@tests/builtinThemeCompleteness.test.js
@tests/builtinThemeVisualContract.test.js
@tests/themePackageInstaller.test.js

<interfaces>
Use the Phase 82 built-in manifest contract from Plan 82-01:
```javascript
{
  id,
  preview,
  visualSignature,
  coverage: [...FULL_THEME_COVERAGE_KEYS],
  ui: {
    theme,
    widgetStyles,
    dialogueBox,
    saveLoadScreen,
    backlogScreen,
    gameMenu,
    settingsScreen,
    titleScreen,
  },
}
```

Use the built-in asset registry shape from `src/editor/builtinThemeAssets.js`:
```javascript
{
  default: [
    { sourcePath: 'public/builtin-themes/default/dialogue-nameplate.svg', targetPath: 'ui/themes/default/dialogue/nameplate.svg' },
  ],
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Promote `default` into the polished neutral shipped baseline</name>
  <read_first>@.planning/phases/82-4/82-CONTEXT.md, @.planning/phases/82-4/82-RESEARCH.md, @src/editor/builtinThemes.js, @src/editor/builtinThemeAssets.js, @tests/builtinThemeCompleteness.test.js, @tests/builtinThemeVisualContract.test.js</read_first>
  <files>src/editor/builtinThemes.js, src/editor/builtinThemeAssets.js, public/builtin-themes/default/preview.svg, public/builtin-themes/default/dialogue-nameplate.svg, public/builtin-themes/default/screen-chrome.svg, public/builtin-themes/default/title-background.svg, public/builtin-themes/default/title-mark.svg</files>
  <acceptance_criteria>
    - `default` has explicit non-empty data for `theme`, `widgetStyles`, `dialogueBox`, `saveLoadScreen`, `backlogScreen`, `gameMenu`, `settingsScreen`, and `titleScreen`.
    - `default.preview.src` is `/builtin-themes/default/preview.svg`, and `default.visualSignature.styleDirection` describes the polished neutral baseline role from D-08.
    - `src/editor/builtinThemeAssets.js` maps the `default` bundled files to canonical targets under `ui/themes/default/`.
    - `default` no longer reads as an empty override theme in tests or browser metadata.
  </acceptance_criteria>
  <action>Per D-01, D-04, and D-08, turn `default` into the shipped polished neutral baseline instead of “engine default / empty overrides”. Author exactly these bundled files: `public/builtin-themes/default/preview.svg`, `dialogue-nameplate.svg`, `screen-chrome.svg`, `title-background.svg`, and `title-mark.svg`. In `src/editor/builtinThemes.js`, fill the explicit `ui` surfaces introduced in 82-01 so `default` has a restrained rounded-rectangle contour system, polished neutral materials, a clean title masthead, understated dialogue/nameplate treatment, pragmatic button-family styling, and consistent save/backlog/game-menu/settings chrome. In `src/editor/builtinThemeAssets.js`, map the non-preview files to canonical targets `ui/themes/default/dialogue/nameplate.svg`, `ui/themes/default/screens/chrome.svg`, `ui/themes/default/title/background.svg`, and `ui/themes/default/title/mark.svg`. Reuse only structural coordinates when helpful; do not recolor `wafuu` art or copy its ornamental silhouette, per D-10 and D-11.</action>
  <verify>
    <automated>npx vitest run tests/builtinThemeCompleteness.test.js tests/builtinThemeVisualContract.test.js tests/themePackageInstaller.test.js tests/scriptThemeApply.test.js tests/themeBrowserService.test.js</automated>
  </verify>
  <done>`default` is a real shipped full theme with explicit preview/signature metadata, canonical bundled assets, and coherent neutral styling across all required surfaces.</done>
</task>

<task type="auto">
  <name>Task 2: Promote `modern-sky` into the airy glass-panel shipped theme</name>
  <read_first>@.planning/phases/82-4/82-CONTEXT.md, @.planning/phases/82-4/82-RESEARCH.md, @src/editor/builtinThemes.js, @src/editor/builtinThemeAssets.js, @tests/builtinThemeCompleteness.test.js, @tests/builtinThemeVisualContract.test.js</read_first>
  <files>src/editor/builtinThemes.js, src/editor/builtinThemeAssets.js, public/builtin-themes/modern-sky/preview.svg, public/builtin-themes/modern-sky/dialogue-nameplate.svg, public/builtin-themes/modern-sky/screen-chrome.svg, public/builtin-themes/modern-sky/title-background.svg, public/builtin-themes/modern-sky/title-mark.svg</files>
  <acceptance_criteria>
    - `modern-sky` has explicit non-empty data for all 8 coverage surfaces.
    - `modern-sky.preview.src` is `/builtin-themes/modern-sky/preview.svg`, and `visualSignature` names airy glass / slim-radii / floating-card direction per D-08.
    - `src/editor/builtinThemeAssets.js` maps the `modern-sky` non-preview files to canonical targets under `ui/themes/modern-sky/`.
    - `modern-sky` includes a distinct title motif, dialogue-box motif, button-family motif, and major-screen chrome motif instead of token-only recolors.
  </acceptance_criteria>
  <action>Per D-01, D-04, D-07, D-08, and D-09, promote `modern-sky` into the airy modern / glass-panel theme. Author exactly these bundled files: `public/builtin-themes/modern-sky/preview.svg`, `dialogue-nameplate.svg`, `screen-chrome.svg`, `title-background.svg`, and `title-mark.svg`. In `src/editor/builtinThemes.js`, give `modern-sky` explicit `ui` ownership for all 8 surfaces with translucent acrylic material language, slim radii, soft separators, floating-card button treatment, a glassy title hero, and major-screen chrome that feels lighter and more layered than `default`. Preserve the current dual-column settings intent where it helps, but extend the same direction into dialogue/title/save/backlog/game menu so the theme reads as one system. Map non-preview files in `src/editor/builtinThemeAssets.js` to canonical `ui/themes/modern-sky/...` targets. Do not clone `default` or `wafuu` layouts with a hue swap; the difference must be visible in material, contour, and overall direction per D-07.</action>
  <verify>
    <automated>npx vitest run tests/builtinThemeCompleteness.test.js tests/builtinThemeVisualContract.test.js tests/themePackageInstaller.test.js tests/scriptThemeApply.test.js tests/themeBrowserService.test.js</automated>
  </verify>
  <done>`modern-sky` becomes a full shipped theme with canonical assets and unmistakable airy glass-panel language across title/dialogue/buttons/major screens.</done>
</task>

</tasks>

<verification>
After both tasks, rerun the automated command once more. Both `default` and `modern-sky` must satisfy the explicit completeness and visual-contract gates introduced in 82-01.
</verification>

<success_criteria>
- `default` is no longer an empty baseline placeholder.
- `modern-sky` is a fully owned shipped theme with distinct material and contour language.
- Both themes remain on the shared built-in installer/export/browser pipeline and use canonical project-local asset paths.
</success_criteria>

<output>
After completion, create `.planning/phases/82-4/82-02-SUMMARY.md`
</output>
