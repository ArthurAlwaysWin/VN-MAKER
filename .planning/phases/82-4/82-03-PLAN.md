---
phase: 82-4
plan: '03'
type: execute
wave: 3
depends_on:
  - '82-01'
  - '82-02'
files_modified:
  - src/editor/builtinThemes.js
  - src/editor/builtinThemeAssets.js
  - public/builtin-themes/fantasy-dark/preview.svg
  - public/builtin-themes/fantasy-dark/dialogue-nameplate.svg
  - public/builtin-themes/fantasy-dark/screen-chrome.svg
  - public/builtin-themes/fantasy-dark/title-background.svg
  - public/builtin-themes/fantasy-dark/title-mark.svg
  - public/builtin-themes/minimal-white/preview.svg
  - public/builtin-themes/minimal-white/dialogue-nameplate.svg
  - public/builtin-themes/minimal-white/screen-chrome.svg
  - public/builtin-themes/minimal-white/title-background.svg
  - public/builtin-themes/minimal-white/title-mark.svg
autonomous: true
requirements:
  - THM-02
  - THM-03
must_haves:
  truths:
    - "The built-in `fantasy-dark` theme is a shipped full theme with unmistakable ornate dark-fantasy styling across title/dialogue/buttons/major screens."
    - "The built-in `minimal-white` theme is a shipped full theme with bright editorial minimal styling across all 8 surfaces."
    - "Both themes install through the same shared built-in pipeline with canonical project-local assets and explicit preview/signature data."
  artifacts:
    - path: src/editor/builtinThemes.js
      provides: "full `fantasy-dark` and `minimal-white` built-in theme definitions"
      contains: "fantasy-dark"
    - path: src/editor/builtinThemeAssets.js
      provides: "bundled asset mappings for `fantasy-dark` and `minimal-white`"
      contains: "ui/themes/fantasy-dark"
    - path: public/builtin-themes/fantasy-dark/preview.svg
      provides: "static browser preview for `fantasy-dark`"
      contains: "<svg"
    - path: public/builtin-themes/minimal-white/preview.svg
      provides: "static browser preview for `minimal-white`"
      contains: "<svg"
  key_links:
    - from: src/editor/builtinThemeAssets.js
      to: electron/themePackageInstaller.js
      via: "bundled file copy into canonical project asset namespace"
      pattern: "ui/themes/fantasy-dark|ui/themes/minimal-white"
    - from: src/editor/builtinThemes.js
      to: src/editor/services/themeBrowser.js
      via: "explicit preview + coverage + visualSignature metadata"
      pattern: "fantasy-dark|minimal-white"
    - from: src/editor/builtinThemes.js
      to: tests/builtinThemeVisualContract.test.js
      via: "distinct material/contour/style assertions"
      pattern: "materialLanguage|contourLanguage|styleDirection"
---

<objective>
Promote `fantasy-dark` and `minimal-white` into the final two shipped full themes with explicit assets, preview metadata, and clearly different visual languages.

Purpose: finish the remaining content-production half of THM-02/03 so all four non-golden built-ins become real shipped themes, not partial variants.
Output: authored `fantasy-dark` and `minimal-white` theme assets plus explicit manifest/asset-registry entries that satisfy the shared completeness and visual-contract gates.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/82-4/82-CONTEXT.md
@.planning/phases/82-4/82-RESEARCH.md
@src/editor/builtinThemes.js
@src/editor/builtinThemeAssets.js
@tests/builtinThemeCompleteness.test.js
@tests/builtinThemeVisualContract.test.js
@tests/themePackageInstaller.test.js

<interfaces>
Use the same explicit built-in manifest and asset-registry contracts established in Plan 82-01 and already exercised by Plan 82-02.

Per-theme visual-signature fields must remain concrete:
```javascript
visualSignature: {
  materialLanguage: '...',
  contourLanguage: '...',
  styleDirection: '...',
  requiredTells: {
    titleScreen: '...',
    dialogueBox: '...',
    buttonFamily: '...',
    majorScreenChrome: '...',
  },
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Promote `fantasy-dark` into the ornate dark-fantasy shipped theme</name>
  <read_first>@.planning/phases/82-4/82-CONTEXT.md, @.planning/phases/82-4/82-RESEARCH.md, @src/editor/builtinThemes.js, @src/editor/builtinThemeAssets.js, @tests/builtinThemeCompleteness.test.js, @tests/builtinThemeVisualContract.test.js</read_first>
  <files>src/editor/builtinThemes.js, src/editor/builtinThemeAssets.js, public/builtin-themes/fantasy-dark/preview.svg, public/builtin-themes/fantasy-dark/dialogue-nameplate.svg, public/builtin-themes/fantasy-dark/screen-chrome.svg, public/builtin-themes/fantasy-dark/title-background.svg, public/builtin-themes/fantasy-dark/title-mark.svg</files>
  <acceptance_criteria>
    - `fantasy-dark` has explicit non-empty data for all 8 coverage surfaces.
    - `fantasy-dark.preview.src` is `/builtin-themes/fantasy-dark/preview.svg`, and `visualSignature` names ornate dark-fantasy material/contour/style direction per D-08.
    - `src/editor/builtinThemeAssets.js` maps the `fantasy-dark` non-preview files to canonical targets under `ui/themes/fantasy-dark/`.
    - `fantasy-dark` includes a distinct crest/title motif, dialogue-box motif, button-family motif, and major-screen chrome motif that are not interchangeable with `wafuu` or `modern-sky`.
  </acceptance_criteria>
  <action>Per D-01, D-04, D-07, D-08, and D-09, promote `fantasy-dark` into the ornate dark-fantasy shipped theme. Author exactly these bundled files: `public/builtin-themes/fantasy-dark/preview.svg`, `dialogue-nameplate.svg`, `screen-chrome.svg`, `title-background.svg`, and `title-mark.svg`. In `src/editor/builtinThemes.js`, give the theme explicit `ui` ownership for all 8 surfaces with lacquer/metal/parchment material cues, pointed or framed contours, a crest-like title treatment, decorated dialogue plate, and more ceremonial chrome for save/backlog/game menu/settings. Keep the styling visually darker and more ornate than `wafuu`; do not repurpose `wafuu` as a tinted base. Map non-preview files to canonical `ui/themes/fantasy-dark/...` targets in `src/editor/builtinThemeAssets.js`.</action>
  <verify>
    <automated>npx vitest run tests/builtinThemeCompleteness.test.js tests/builtinThemeVisualContract.test.js tests/themePackageInstaller.test.js tests/scriptThemeApply.test.js tests/themeBrowserService.test.js</automated>
  </verify>
  <done>`fantasy-dark` becomes a fully owned shipped theme with canonical assets and a clear ornate dark-fantasy visual identity.</done>
</task>

<task type="auto">
  <name>Task 2: Promote `minimal-white` into the bright editorial shipped theme</name>
  <read_first>@.planning/phases/82-4/82-CONTEXT.md, @.planning/phases/82-4/82-RESEARCH.md, @src/editor/builtinThemes.js, @src/editor/builtinThemeAssets.js, @tests/builtinThemeCompleteness.test.js, @tests/builtinThemeVisualContract.test.js</read_first>
  <files>src/editor/builtinThemes.js, src/editor/builtinThemeAssets.js, public/builtin-themes/minimal-white/preview.svg, public/builtin-themes/minimal-white/dialogue-nameplate.svg, public/builtin-themes/minimal-white/screen-chrome.svg, public/builtin-themes/minimal-white/title-background.svg, public/builtin-themes/minimal-white/title-mark.svg</files>
  <acceptance_criteria>
    - `minimal-white` has explicit non-empty data for all 8 coverage surfaces, including the currently missing `settingsScreen` and `titleScreen`.
    - `minimal-white.preview.src` is `/builtin-themes/minimal-white/preview.svg`, and `visualSignature` names bright minimal editorial material/contour/style direction per D-08.
    - `src/editor/builtinThemeAssets.js` maps the `minimal-white` non-preview files to canonical targets under `ui/themes/minimal-white/`.
    - `minimal-white` includes a typography-led title motif, quiet dialogue-box motif, button-family motif, and sparse major-screen chrome motif that are visibly distinct from the other four shipped themes.
  </acceptance_criteria>
  <action>Per D-01, D-04, D-07, D-08, and D-09, promote `minimal-white` into the bright minimal editorial theme. Author exactly these bundled files: `public/builtin-themes/minimal-white/preview.svg`, `dialogue-nameplate.svg`, `screen-chrome.svg`, `title-background.svg`, and `title-mark.svg`. In `src/editor/builtinThemes.js`, fill all 8 explicit `ui` surfaces with paper/editorial card materials, flat edges or thin rules, generous whitespace, quiet button-family treatment, and a typography-led title. Keep it bright, sparse, and intentional rather than merely “light mode default”. Reuse only structural coordinates if needed; do not clone `default` with inverted colors. Map non-preview files to canonical `ui/themes/minimal-white/...` targets in `src/editor/builtinThemeAssets.js`.</action>
  <verify>
    <automated>npx vitest run tests/builtinThemeCompleteness.test.js tests/builtinThemeVisualContract.test.js tests/themePackageInstaller.test.js tests/scriptThemeApply.test.js tests/themeBrowserService.test.js</automated>
  </verify>
  <done>`minimal-white` becomes a fully owned shipped theme with canonical assets and a clear bright editorial visual identity.</done>
</task>

</tasks>

<verification>
After both tasks, rerun the automated command once more. `fantasy-dark` and `minimal-white` must both satisfy the explicit completeness and visual-contract gates introduced in 82-01.
</verification>

<success_criteria>
- `fantasy-dark` and `minimal-white` are both explicit 8-surface shipped themes.
- Their material language, contour language, and style direction are clearly distinct from each other and from `default`/`modern-sky`/`wafuu`.
- Both remain on the shared built-in installer/export/browser pipeline with canonical project-local asset paths.
</success_criteria>

<output>
After completion, create `.planning/phases/82-4/82-03-SUMMARY.md`
</output>
