---
phase: 82-4
plan: '04'
type: execute
wave: 4
depends_on:
  - '82-02'
  - '82-03'
files_modified:
  - tests/builtinThemeAcceptance.test.js
  - tests/themePackageInstaller.test.js
  - tests/scriptThemeApply.test.js
  - tests/themeBrowserService.test.js
  - electron/themePackageInstaller.js
  - electron/themePackageExporter.js
  - src/editor/stores/script.js
  - src/editor/services/themeBrowser.js
  - src/editor/builtinThemes.js
  - src/editor/builtinThemeAssets.js
autonomous: false
requirements:
  - THM-02
  - THM-03
must_haves:
  truths:
    - "All 5 shipped built-in themes pass the same apply → save/reopen → export → reimport → browser parity story."
    - "Built-in and imported themes still share one install/apply/export/browser path after the 4-theme rollout."
    - "A human can verify the 5 shipped themes are meaningfully different in material language, contour language, and overall style direction."
  artifacts:
    - path: tests/builtinThemeAcceptance.test.js
      provides: "parameterized 5-theme acceptance matrix"
      contains: "default"
    - path: tests/themePackageInstaller.test.js
      provides: "built-in installer parity coverage across the full shipped roster"
      contains: "modern-sky"
    - path: tests/themeBrowserService.test.js
      provides: "browser parity coverage for explicit preview/signature/full-coverage truth"
      contains: "标题界面"
  key_links:
    - from: src/editor/builtinThemes.js
      to: tests/builtinThemeAcceptance.test.js
      via: "theme-by-theme acceptance matrix over the shipped roster"
      pattern: "default|wafuu|modern-sky|fantasy-dark|minimal-white"
    - from: electron/themePackageInstaller.js
      to: electron/themePackageExporter.js
      via: "project-local materialization enabling self-contained export"
      pattern: "ui/themes/"
    - from: src/editor/stores/script.js
      to: tests/builtinThemeAcceptance.test.js
      via: "apply/save/reopen path preserving project-owned titleScreen.bgm"
      pattern: "titleScreen"
---

<objective>
Lock final 5-theme parity evidence and perform the blocking shipped-theme visual review so Phase 82 closes on truth, parity, and differentiation rather than browser copy alone.

Purpose: satisfy the final THM-02/03 closure bar with automated 5-theme acceptance evidence plus one human review gate.
Output: a parameterized built-in acceptance suite, focused parity fixes if needed, and a blocking human shipped-theme review checklist.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/82-4/82-CONTEXT.md
@.planning/phases/82-4/82-RESEARCH.md
@src/editor/builtinThemes.js
@src/editor/builtinThemeAssets.js
@electron/themePackageInstaller.js
@electron/themePackageExporter.js
@src/editor/stores/script.js
@src/editor/services/themeBrowser.js
@tests/themeGoldenAcceptance.test.js
@tests/themePackageInstaller.test.js
@tests/scriptThemeApply.test.js
@tests/themeBrowserService.test.js

<interfaces>
Use the existing golden parity baseline from `tests/themeGoldenAcceptance.test.js`:
```javascript
const builtinInstall = await installThemePackage({
  source: 'builtin',
  themeId: 'wafuu',
  projectPath: sourceProjectPath,
});
sourceStore.applyThemeBundle(builtinInstall.bundle, builtinInstall.packageMeta);
```

Phase 82 acceptance target:
```javascript
for (const themeId of ['default', 'wafuu', 'modern-sky', 'fantasy-dark', 'minimal-white']) {
  // install -> apply -> save/reopen -> export -> reimport -> browser reconstruction
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add a parameterized 5-theme acceptance matrix and fix only the parity gaps it exposes</name>
  <read_first>@.planning/phases/82-4/82-CONTEXT.md, @.planning/phases/82-4/82-RESEARCH.md, @tests/themeGoldenAcceptance.test.js, @tests/themePackageInstaller.test.js, @tests/scriptThemeApply.test.js, @tests/themeBrowserService.test.js, @electron/themePackageInstaller.js, @electron/themePackageExporter.js, @src/editor/stores/script.js</read_first>
  <files>tests/builtinThemeAcceptance.test.js, tests/themePackageInstaller.test.js, tests/scriptThemeApply.test.js, tests/themeBrowserService.test.js, electron/themePackageInstaller.js, electron/themePackageExporter.js, src/editor/stores/script.js, src/editor/services/themeBrowser.js, src/editor/builtinThemes.js, src/editor/builtinThemeAssets.js</files>
  <behavior>
    - Test 1: each built-in theme ID in the shipped roster passes install -> apply -> save/reopen -> export -> reimport -> browser reconstruction.
    - Test 2: each applied built-in theme preserves project-owned `titleScreen.bgm` while keeping the themed title visuals.
    - Test 3: built-in installer parity covers all 5 IDs and verifies project-local files exist for each declared bundled asset set.
    - Test 4: browser reconstruction for the applied package reports full 8-surface coverage and explicit preview/signature metadata without falling back to fake built-in fullness.
  </behavior>
  <acceptance_criteria>
    - `tests/builtinThemeAcceptance.test.js` exists and iterates exactly `default`, `wafuu`, `modern-sky`, `fantasy-dark`, and `minimal-white`.
    - The focused acceptance suite fails if any shipped theme cannot be exported/reimported as a self-contained `.gmtheme`.
    - `tests/themePackageInstaller.test.js` covers all built-in theme IDs, not only `wafuu`.
    - Any source changes stay inside the shared installer/exporter/browser/store path; no built-in-only shortcut pipeline is introduced.
  </acceptance_criteria>
  <action>Reuse the existing golden acceptance harness as the template, but generalize it into `tests/builtinThemeAcceptance.test.js` for all 5 shipped themes per D-06. Keep the acceptance path exactly shared: built-in install, `applyThemeBundle()`, save/reopen snapshot, `.gmtheme` export, reimport, and browser reconstruction. Extend `tests/themePackageInstaller.test.js`, `tests/scriptThemeApply.test.js`, and `tests/themeBrowserService.test.js` only where Phase 82 parity needs stronger assertions. If the new suite exposes drift, apply the minimum fixes in `electron/themePackageInstaller.js`, `electron/themePackageExporter.js`, `src/editor/stores/script.js`, `src/editor/services/themeBrowser.js`, `src/editor/builtinThemes.js`, or `src/editor/builtinThemeAssets.js`. Do not add a second built-in acceptance pipeline, do not weaken `titleScreen.bgm` ownership, and do not mark success based on browser badges alone.</action>
  <verify>
    <automated>npx vitest run tests/builtinThemeCompleteness.test.js tests/builtinThemeVisualContract.test.js tests/builtinThemeAcceptance.test.js tests/themeGoldenAcceptance.test.js tests/themePackageInstaller.test.js tests/scriptThemeApply.test.js tests/themeBrowserService.test.js tests/themePackageExporter.test.js tests/themePackageRoundTrip.test.js tests/themePackageInstallFlow.test.js tests/themePackageContract.test.js tests/themePackagePreflight.test.js tests/themeBrowserModal.test.js && npm run build</automated>
  </verify>
  <done>Phase 82 has focused automated evidence that all 5 shipped themes survive the shared install/apply/export/import/browser path and still preserve project-owned `titleScreen.bgm`.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human shipped-theme review for final visual differentiation and production readiness</name>
  <read_first>@.planning/phases/82-4/82-CONTEXT.md, @src/editor/builtinThemes.js, @tests/builtinThemeAcceptance.test.js</read_first>
  <acceptance_criteria>
    - Reviewer checks all 5 shipped themes in the browser and after apply.
    - Reviewer explicitly approves or reports issues for material language, contour language, and overall style direction for each theme ID.
    - Reviewer confirms the locked role mapping from D-08 matches the built result.
  </acceptance_criteria>
  <action>Use the automated green build from Task 1 as the baseline and perform only human-visible acceptance work here. Per D-07, D-08, and D-09, verify the shipped themes are not token-only recolors and that each theme’s `titleScreen`, dialogue box, button-family language, and major-screen chrome read as one coherent system. Do not hand-edit project files during review; this checkpoint exists only to confirm shipped visual quality after automation is already green.</action>
  <how-to-verify>
    1. Open Project Settings → `🎭 主题浏览器` and inspect the five built-in cards: `default`, `wafuu`, `modern-sky`, `fantasy-dark`, `minimal-white`.
    2. Confirm each card has explicit static preview content and that the five cards are visibly differentiated without relying on live preview.
    3. Apply `default`; verify it reads as a polished neutral baseline across title/dialogue/save-load/backlog/game menu/settings.
    4. Apply `modern-sky`; verify glass-panel / airy floating-card cues are visible across the same surfaces.
    5. Apply `fantasy-dark`; verify ornate dark-fantasy framing/crest/dialogue chrome are visible across the same surfaces.
    6. Apply `minimal-white`; verify bright editorial spacing/typography/thin-rule styling are visible across the same surfaces.
    7. Re-apply `wafuu`; verify the original golden baseline still feels distinct and intact.
    8. For each theme, confirm the difference is visible in material language, contour language, and overall style direction — not just color tokens.
  </how-to-verify>
  <verify>
    <automated>npx vitest run tests/builtinThemeCompleteness.test.js tests/builtinThemeVisualContract.test.js tests/builtinThemeAcceptance.test.js tests/themeGoldenAcceptance.test.js tests/themePackageInstaller.test.js tests/scriptThemeApply.test.js tests/themeBrowserService.test.js tests/themePackageExporter.test.js tests/themePackageRoundTrip.test.js tests/themePackageInstallFlow.test.js tests/themePackageContract.test.js tests/themePackagePreflight.test.js tests/themeBrowserModal.test.js && npm run build</automated>
  </verify>
  <done>Human review confirms the 5 shipped themes are production-ready and materially/structurally distinct, with `wafuu` preserved as the golden baseline and the remaining 4 promoted to the same completeness bar.</done>
  <resume-signal>Type "approved" if all five themes pass, or report `themeId + surface + expected + actual` for each mismatch.</resume-signal>
</task>

</tasks>

<verification>
Task 1 must be green before starting the blocking human review. The phase is not done if the browser cards differ but the applied UI silhouettes still feel interchangeable.
</verification>

<success_criteria>
- Automated parity evidence covers all 5 shipped themes through the shared install/apply/export/import/browser path.
- Human review confirms the locked role mapping and non-recolor differentiation bar.
- Phase 82 closes with five genuinely shipped full themes instead of one golden theme plus four partial variants.
</success_criteria>

<output>
After completion, create `.planning/phases/82-4/82-04-SUMMARY.md`
</output>
