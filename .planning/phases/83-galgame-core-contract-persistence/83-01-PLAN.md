---
phase: 83-galgame-core-contract-persistence
plan: '01'
type: execute
wave: 1
depends_on: []
files_modified:
  - src/shared/galgameContract.js
  - electron/main.js
  - src/editor/stores/script.js
  - tests/galgameContract.test.js
autonomous: true
requirements:
  - DATA-01
  - DATA-02
  - PERS-02
must_haves:
  truths:
    - "New and legacy-opened projects always end up with a stable `projectId` and explicit contract version in `script.json`."
    - "`script.json` always carries explicit `systems.variables`, `systems.endings`, and `systems.gallery.cg` registries instead of implicit future fields."
    - "Reset/version semantics are explicit and scoped, so Phase 83 does not depend on broad legacy migration work."
  artifacts:
    - path: src/shared/galgameContract.js
      provides: "shared contract seeding, versioning, and reset-scope helpers"
      contains: "ensureGalgameContract"
    - path: electron/main.js
      provides: "project create/load/save path seeded through the shared galgame contract"
      contains: "defaultScript"
    - path: src/editor/stores/script.js
      provides: "editor store load path normalizes the Phase 83 contract before mutation"
      contains: "loadFromData"
    - path: tests/galgameContract.test.js
      provides: "focused contract regression coverage for projectId, systems registries, and reset scope"
      contains: "projectId"
  key_links:
    - from: src/shared/galgameContract.js
      to: electron/main.js
      via: "defaultScript/create-project/load-project use the same seeding helper"
      pattern: "ensureGalgameContract"
    - from: src/shared/galgameContract.js
      to: src/editor/stores/script.js
      via: "loadFromData normalizes incoming script payloads"
      pattern: "ensureGalgameContract"
    - from: tests/galgameContract.test.js
      to: src/shared/galgameContract.js
      via: "focused unit assertions"
      pattern: "contractVersion|systems|projectId"
---

<objective>
Freeze the author-owned Phase 83 data contract so every project carries immutable runtime identity, explicit gameplay registries, and scoped reset/version semantics before any persistence boundary or effect work lands.

Purpose: satisfy DATA-01, DATA-02, and the contract side of PERS-02 per D-01, D-04, D-06, and D-07.
Output: one shared contract helper, normalized editor/electron load paths, and focused contract regressions.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/83-galgame-core-contract-persistence/83-CONTEXT.md
@.planning/phases/83-galgame-core-contract-persistence/83-RESEARCH.md
@.planning/phases/83-galgame-core-contract-persistence/83-VALIDATION.md
@src/editor/stores/script.js
@electron/main.js

<interfaces>
From `src/editor/stores/script.js`:
```javascript
function loadFromData(scriptData) {
  data.value = migrateLegacyAppliedThemeData(scriptData).script;
  history.value = [];
  historyIndex.value = -1;
  pushState();
}
```

From `electron/main.js`:
```javascript
function defaultScript() {
  return {
    characters: {},
    scenes: {}
  };
}
```

Current runtime read-key risk called out by research:
```javascript
readHistory = new ReadHistory(engine.script.meta?.title || 'untitled');
```
</interfaces>
</context>

<pitfalls>
- Do not keep title/name-derived identity anywhere in the seeded contract; per D-04 the stable key must be `projectId`.
- Do not put player-owned progression into `script.json`; per D-02 this plan is author-contract only.
- Do not build migration trees for old contracts; per D-07 additive seeding + explicit reset/version semantics is enough.
</pitfalls>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the shared galgame contract helper and failing coverage for seeded identity/registries</name>
  <files>src/shared/galgameContract.js, tests/galgameContract.test.js</files>
  <behavior>
    - Test 1: `ensureGalgameContract()` adds `projectId` and `contractVersion` once, then preserves the same `projectId` on repeated normalization.
    - Test 2: missing `systems.variables`, `systems.endings`, and `systems.gallery.cg` are seeded as explicit objects without disturbing existing author data.
    - Test 3: reset/version helpers expose explicit scopes for contract/profile/saves/all so later plans do not invent ad-hoc reset strings.
  </behavior>
  <action>Per D-01, D-04, D-06, and D-07, create `src/shared/galgameContract.js` as the single Phase 83 author-contract helper. Export a stable ID generator, `ensureGalgameContract(script)`, contract version constants, and explicit reset-scope enums/helpers that later plans can reuse. Write `tests/galgameContract.test.js` first in RED, covering stable `projectId`, explicit `systems` registries, and reset-scope/version semantics. Keep the JSON shape simple and inspectable; do not add UI concerns, do not seed player progression sections into `script.json`, and do not add old-project migration branches beyond additive backfill.</action>
  <verify>
    <automated>npx vitest run tests/galgameContract.test.js</automated>
  </verify>
  <done>`src/shared/galgameContract.js` exists, contract seeding is regression-tested, and the Phase 83 author data boundary is explicit enough for downstream plans to build on without reinterpretation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire the shared contract through project create/load/editor normalization paths</name>
  <files>electron/main.js, src/editor/stores/script.js, src/shared/galgameContract.js, tests/galgameContract.test.js</files>
  <behavior>
    - Test 1: new-project seeding produces a script shape with `projectId`, `contractVersion`, and explicit `systems` registries.
    - Test 2: loading a script missing Phase 83 fields normalizes it through the same helper without regenerating `projectId`.
    - Test 3: the editor store `loadFromData()` path starts from the normalized contract so later saves cannot silently drop the new registries.
  </behavior>
  <action>Use the shared helper from Task 1 everywhere Phase 83 author data enters the app. In `electron/main.js`, route `defaultScript()`, create-project, and load-project additive seeding through `ensureGalgameContract()` so projects always open/save on the new baseline. In `src/editor/stores/script.js`, normalize incoming script data before history state is pushed so downstream editing starts from the same contract. Per D-07, keep this contract seeding narrow and deterministic; do not add broad legacy content migration, and do not rewrite unrelated theme or cinematic data.</action>
  <verify>
    <automated>npx vitest run tests/galgameContract.test.js && npm run build</automated>
  </verify>
  <done>Project create/load/editor flows share one Phase 83 contract normalization path, and the repo builds with the new helper wired in.</done>
</task>

</tasks>

<verification>
Run the focused contract suite after each task. Before closing the plan, rerun the same suite with `npm run build` to prove the new helper integrates cleanly into both Electron and editor code paths.
</verification>

<success_criteria>
- `script.json` can no longer exist in-memory without `projectId`, `contractVersion`, and explicit `systems.variables/endings/gallery.cg`.
- The author contract is seeded additively instead of by title-based or migration-heavy special cases.
- Later Phase 83 plans can consume one shared contract helper instead of reimplementing registry defaults.
</success_criteria>

<output>
After completion, create `.planning/phases/83-galgame-core-contract-persistence/83-01-SUMMARY.md`
</output>
