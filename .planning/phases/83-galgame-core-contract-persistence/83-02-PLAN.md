---
phase: 83-galgame-core-contract-persistence
plan: '02'
type: execute
wave: 2
depends_on:
  - '83-01'
files_modified:
  - src/engine/PlayerDataRepository.js
  - src/engine/ReadHistory.js
  - src/engine/SaveManager.js
  - src/engine/WebSaveManager.js
  - src/main.js
  - electron/main.js
  - tests/playerDataRepository.test.js
autonomous: true
requirements:
  - DATA-01
  - PERS-01
  - PERS-02
must_haves:
  truths:
    - "Persistent profile progress is keyed by stable `projectId`, so renaming the game title no longer forks read history or unlock progress."
    - "Loading, deleting, or replacing a save slot never mutates persistent profile truth for read history or unlocks."
    - "Development reset/rebuild actions can target profile, saves, or both without manual file edits."
  artifacts:
    - path: src/engine/PlayerDataRepository.js
      provides: "single persistence facade for versioned profile data and scoped reset operations"
      contains: "reset"
    - path: src/engine/ReadHistory.js
      provides: "read-history adapter backed by profile truth instead of title-keyed localStorage"
      contains: "markRead"
    - path: electron/main.js
      provides: "separate profile IPC/file ownership from save-slot IPC/file ownership"
      contains: "save-slot"
    - path: src/main.js
      provides: "runtime initialization reads projectId-backed profile before gameplay starts"
      contains: "ReadHistory"
    - path: tests/playerDataRepository.test.js
      provides: "focused separation and reset regression coverage"
      contains: "save profile separation"
  key_links:
    - from: src/main.js
      to: src/engine/PlayerDataRepository.js
      via: "runtime init constructs repository using contract-normalized projectId"
      pattern: "projectId|PlayerDataRepository"
    - from: src/engine/SaveManager.js
      to: electron/main.js
      via: "slot IPC remains slot-only while profile IPC stays separate"
      pattern: "save-slot|load-slot|delete-slot"
    - from: src/engine/ReadHistory.js
      to: src/engine/PlayerDataRepository.js
      via: "profile-backed page read persistence"
      pattern: "readHistory"
---

<objective>
Establish the Phase 83 persistence boundary so cross-run profile data and run-owned save snapshots become separate authorities keyed by `projectId`, with explicit reset/rebuild operations and tolerant restore behavior.

Purpose: satisfy PERS-01, PERS-02, and the persistence side of DATA-01 per D-02, D-03, D-04, and D-07.
Output: `PlayerDataRepository`, profile-backed read history, separate slot/profile wiring, and focused persistence regressions.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/83-galgame-core-contract-persistence/83-CONTEXT.md
@.planning/phases/83-galgame-core-contract-persistence/83-RESEARCH.md
@.planning/phases/83-galgame-core-contract-persistence/83-VALIDATION.md
@src/main.js
@src/engine/ReadHistory.js
@src/engine/SaveManager.js
@src/engine/WebSaveManager.js
@electron/main.js

<interfaces>
From `src/engine/SaveManager.js`:
```javascript
export class SaveManager {
  async save(slot, state, previewText, thumbnail = null) {}
  async load(slot) {}
  async delete(slot) {}
  async quickSave(state, previewText, thumbnail = null) {}
  async quickLoad() {}
}
```

Current slot payload in `electron/main.js`:
```javascript
const data = {
  version: 2,
  state,
  previewText: previewText || '',
  sceneName: state?.currentScene || '',
  timestamp: Date.now(),
  date: new Date().toLocaleString('zh-CN'),
};
```

Current `ReadHistory` boundary:
```javascript
export class ReadHistory {
  constructor(projectId) {
    this._storageKey = `readHistory:${projectId}`;
  }
}
```
</interfaces>
</context>

<pitfalls>
- Do not duplicate endings/CG/read history into both profile and slot payloads; per D-03 profile is the only cross-run authority.
- Do not keep `ReadHistory` keyed by title or other mutable labels; per D-04 it must flow from `projectId`.
- Do not make reset semantics destructive by default; per D-07 keep reset scopes explicit and narrow.
</pitfalls>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add repository tests and implement the projectId-keyed profile/save separation facade</name>
  <files>src/engine/PlayerDataRepository.js, src/engine/ReadHistory.js, tests/playerDataRepository.test.js</files>
  <behavior>
    - Test 1: profile data loads/saves by `projectId` and preserves read-history/unlock truth independently from slot payloads.
    - Test 2: reset scopes only touch the requested surface (`profile`, `saves`, `all`, plus contract helper interoperability from Plan 01).
    - Test 3: tolerant restore backfills missing profile sections instead of crashing on absent unlock/read-history fields.
  </behavior>
  <action>Per D-02, D-03, D-04, and D-07, write `tests/playerDataRepository.test.js` first in RED, then implement `src/engine/PlayerDataRepository.js` plus the minimum `ReadHistory` changes needed to depend on repository-backed profile truth instead of title-keyed localStorage. The repository should own versioned profile serialization, scoped reset helpers, and tolerant default seeding for missing profile sections. Keep the on-disk/on-storage JSON inspectable, and do not add broad migration importers or any generic persistent-variable system.</action>
  <verify>
    <automated>npx vitest run tests/playerDataRepository.test.js</automated>
  </verify>
  <done>A single repository abstraction exists for Phase 83 player data, and the separation between persistent profile truth and run snapshot truth is enforced by focused tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire profile-backed persistence through runtime bootstrap and slot IPC without collapsing the boundary</name>
  <files>src/main.js, src/engine/SaveManager.js, src/engine/WebSaveManager.js, src/engine/ReadHistory.js, src/engine/PlayerDataRepository.js, electron/main.js, tests/playerDataRepository.test.js</files>
  <behavior>
    - Test 1: runtime bootstrap initializes profile/read-history using `script.projectId`, not title-derived keys.
    - Test 2: save-slot, load-slot, delete-slot, and quicksave flows remain slot-only and never rewrite profile unlock/read-history sections.
    - Test 3: repository reset/rebuild operations are reachable through the persistence boundary without requiring manual file edits.
  </behavior>
  <action>Wire the new repository into the actual runtime/electron boundary. In `src/main.js`, initialize profile-backed player data after the Plan 01 contract is loaded, then construct `ReadHistory` from that boundary instead of `engine.script.meta?.title`. In `electron/main.js`, keep slot files under the existing save IPC contract but add the separate profile ownership needed by the repository. Update `SaveManager.js` and `WebSaveManager.js` only as needed to preserve slot-only semantics and tolerant restore behavior. Per D-03, loading/deleting a slot must not mutate persistent unlock/read-history state; per D-07, prefer a clean repository boundary over legacy migration breadth.</action>
  <verify>
    <automated>npx vitest run tests/playerDataRepository.test.js && npm run build</automated>
  </verify>
  <done>The runtime boots against a projectId-keyed profile boundary, slot IPC remains slot-only, reset scopes are explicit, and the repo builds with the new persistence facade in place.</done>
</task>

</tasks>

<verification>
Run the repository suite after Task 1, then rerun it with `npm run build` after Task 2. Before closing the plan, manually inspect the resulting design only if the tests expose a profile-vs-slot ownership leak; otherwise stay automation-first.
</verification>

<success_criteria>
- Read history and future unlock progress no longer depend on title-derived keys.
- Slot load/delete behavior is structurally incapable of owning profile truth.
- Developers gain explicit reset/rebuild semantics without hand-editing save/profile files.
</success_criteria>

<output>
After completion, create `.planning/phases/83-galgame-core-contract-persistence/83-02-SUMMARY.md`
</output>
