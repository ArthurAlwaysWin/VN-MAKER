---
phase: 83-galgame-core-contract-persistence
plan: '03'
type: execute
wave: 3
depends_on:
  - '83-01'
  - '83-02'
files_modified:
  - src/main.js
  - src/shared/effectDsl.js
  - src/engine/ScriptEngine.js
  - src/editor/stores/script.js
  - tests/playerDataRepository.test.js
  - tests/effectDsl.test.js
  - tests/scriptEngine.test.js
autonomous: true
requirements:
  - DATA-03
must_haves:
  truths:
    - "Variable writes and unlock writes share one minimal `effects[]` contract instead of parallel special-case fields."
    - "Legacy `setVariable` data remains readable, but Phase 83 write paths normalize back to `effects[]`."
    - "Runtime effect execution can update run variables and profile-backed unlock state through the real main-runtime repository seam without breaking tolerant restore."
  artifacts:
    - path: src/shared/effectDsl.js
      provides: "effect validation, normalization, and application helpers for the Phase 83 DSL"
      contains: "applyEffects"
    - path: src/main.js
      provides: "runtime wiring that injects the Phase 82 repository/profile boundary into ScriptEngine effect execution"
      contains: "PlayerDataRepository|setPlayerDataRepository"
    - path: src/engine/ScriptEngine.js
      provides: "choice/runtime effect execution using the shared DSL and injected profile unlock hooks"
      contains: "selectChoice|setPlayerDataRepository"
    - path: src/editor/stores/script.js
      provides: "editor persistence emits normalized `effects[]` instead of expanding legacy fields"
      contains: "loadFromData"
    - path: tests/playerDataRepository.test.js
      provides: "fast persistence-seam regression for unlock writes reaching profile truth"
      contains: "unlock persistence"
    - path: tests/effectDsl.test.js
      provides: "focused DSL contract coverage"
      contains: "var:set"
    - path: tests/scriptEngine.test.js
      provides: "engine regressions for legacy normalization and runtime effect behavior"
      contains: "selectChoice"
  key_links:
    - from: src/shared/effectDsl.js
      to: src/engine/ScriptEngine.js
      via: "shared normalization/apply path for runtime choice effects"
      pattern: "applyEffects|normalize"
    - from: src/shared/effectDsl.js
      to: src/editor/stores/script.js
      via: "editor load/save normalization emits only `effects[]`"
      pattern: "effects"
    - from: src/main.js
      to: src/engine/ScriptEngine.js
      via: "runtime bootstrap injects repository-backed unlock hooks into effect execution"
      pattern: "PlayerDataRepository|setPlayerDataRepository"
    - from: src/engine/ScriptEngine.js
      to: src/engine/PlayerDataRepository.js
      via: "unlock:ending and unlock:cg writes flow to profile persistence through the injected repository boundary"
      pattern: "unlockEnding|unlockCg|setPlayerDataRepository"
    - from: tests/scriptEngine.test.js
      to: src/engine/ScriptEngine.js
      via: "focused choice and restore regressions"
      pattern: "setVariable|effects"
---

<objective>
Normalize all Phase 83 player-state writes onto the minimal effect DSL, then lock that contract with focused runtime/editor tests so later variable/endings/CG UI work inherits one execution path.

Purpose: satisfy DATA-03 per D-05 while preserving D-06 explicit unlock semantics and the clean-contract bias from D-07.
Output: shared effect helpers, runtime/editor normalization to `effects[]`, and focused node-based regressions.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/83-galgame-core-contract-persistence/83-CONTEXT.md
@.planning/phases/83-galgame-core-contract-persistence/83-RESEARCH.md
@.planning/phases/83-galgame-core-contract-persistence/83-VALIDATION.md
@src/engine/ScriptEngine.js
@src/editor/stores/script.js
@tests/scriptEngine.test.js

<interfaces>
From `src/engine/ScriptEngine.js`:
```javascript
selectChoice(optionIndex) {
  const option = page.options[optionIndex];
  if (option.setVariable) {
    for (const [key, value] of Object.entries(option.setVariable)) {
      const current = this.variables.get(key) || 0;
      this.variables.set(key, current + value);
    }
  }
}
```

Current state shape from `src/engine/ScriptEngine.js`:
```javascript
getState() {
  return {
    currentScene: this.currentScene,
    pageIndex: this.pageIndex,
    dialogueIndex: this.dialogueIndex,
    variables: Object.fromEntries(this.variables),
    history: [...this.history],
    expressionState: Object.fromEntries(this._expressionState),
  };
}
```

Current choice fixture contract from `tests/scriptEngine.test.js`:
```javascript
{
  type: 'choice',
  prompt: 'Pick one',
  options: [
    { text: 'Option A', target: 'sceneA', setVariable: { mood: 1 } },
  ],
}
```
</interfaces>
</context>

<pitfalls>
- Do not let `effects[]` and `setVariable` become co-equal write contracts; per D-05 only `effects[]` should be written after this phase.
- Do not infer ending/CG unlocks from assets or save state; per D-06 unlocks stay explicit actions with canonical IDs.
- Do not block on legacy data migration breadth; per D-07 runtime read-compat plus normalized future writes is enough for Phase 83.
</pitfalls>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing node-based coverage for the minimal effect DSL and implement the shared helper</name>
  <files>src/shared/effectDsl.js, tests/effectDsl.test.js</files>
  <behavior>
    - Test 1: the DSL accepts only `var:set`, `var:add`, `var:sub`, `unlock:ending`, and `unlock:cg`.
    - Test 2: legacy `setVariable` payloads normalize into equivalent `effects[]` entries for read-compat.
    - Test 3: applying effects updates run variables and profile unlock calls deterministically, including repeated unlock writes.
  </behavior>
  <action>Per D-05 and D-06, write `tests/effectDsl.test.js` first using `node:test`, then implement `src/shared/effectDsl.js` with validation, normalization, and execution helpers. The helper must expose one path for variable math and explicit ending/CG unlock writes, and it must support read-compat normalization from legacy `setVariable` without preserving that legacy field as a first-class saved contract. Keep the helper framework-free and ESM-only with named exports.</action>
  <verify>
    <automated>node --test tests/effectDsl.test.js</automated>
  </verify>
  <done>The repository has a single shared effect DSL helper, and the allowed write operations are enforced by focused tests before runtime/editor wiring begins.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire the shared DSL through editor/runtime choice flows and lock the regressions</name>
  <files>src/main.js, src/engine/ScriptEngine.js, src/editor/stores/script.js, src/shared/effectDsl.js, tests/playerDataRepository.test.js, tests/effectDsl.test.js, tests/scriptEngine.test.js</files>
  <behavior>
    - Test 1: `ScriptEngine.selectChoice()` executes normalized `effects[]` and still reads legacy `setVariable` input without breaking older data.
    - Test 2: editor-side script normalization persists `effects[]` as the write contract after Phase 83 edits.
    - Test 3: runtime bootstrap injects repository-backed unlock hooks so `unlock:ending` / `unlock:cg` writes persist through the real profile boundary rather than a test-only stub.
    - Test 4: restore/state regressions remain tolerant when effect-enabled pages or profile-backed unlock hooks are present.
  </behavior>
  <action>Use the shared helper from Task 1 in both runtime and editor paths. In `src/engine/ScriptEngine.js`, replace the inline `setVariable` branch with normalized effect execution so variable math and future ending/CG unlock writes share one boundary, and add an explicit repository injection surface (for example `setPlayerDataRepository()` or constructor wiring) instead of hiding persistence behind globals. In `src/main.js`, wire the already-established Phase 83 repository boundary from Plan 02 into the live engine bootstrap so `unlock:*` reaches profile persistence in the real runtime path. In `src/editor/stores/script.js`, normalize loaded/edited choice data so saves emit `effects[]` only while still tolerating legacy input on read. Update `tests/scriptEngine.test.js` for legacy read-compat plus the canonical `effects[]` path, and extend `tests/playerDataRepository.test.js` with one fast integration assertion over the real unlock-to-profile seam. Avoid new bespoke fields such as `unlockEndingId` or `unlockCgIds`, and do not introduce string/expression DSL scope that Phase 83 explicitly deferred.</action>
  <verify>
    <automated>npx vitest run tests/playerDataRepository.test.js && node --test tests/effectDsl.test.js tests/scriptEngine.test.js && npm run build</automated>
  </verify>
  <done>Runtime/editor write behavior converges on `effects[]`, legacy `setVariable` remains readable but no longer canonical, and the repo builds with the new DSL wiring in place.</done>
</task>

</tasks>

<verification>
Run the DSL suite after Task 1. After Task 2, rerun both node-based suites plus `npm run build` so the engine/editor normalization path and repository-wide compile surface stay aligned.
</verification>

<success_criteria>
- The only Phase 83 write contract for variables/unlocks is the minimal `effects[]` DSL.
- Older `setVariable` data still loads, but new edits no longer proliferate legacy write fields.
- Runtime execution and test fixtures prove the DSL is ready for Phase 84/85 authoring work without additional contract rewrites.
</success_criteria>

<output>
After completion, create `.planning/phases/83-galgame-core-contract-persistence/83-03-SUMMARY.md`
</output>
