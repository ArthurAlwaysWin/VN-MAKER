---
phase: 84-variable-registry-and-branching-gui
plan: '01'
type: execute
wave: 1
depends_on: []
files_modified:
  - src/shared/variableRegistry.js
  - src/shared/branchingContract.js
  - src/editor/stores/script.js
  - src/engine/ScriptEngine.js
  - tests/variableRegistryContract.test.js
  - tests/branchingContract.test.js
  - tests/scriptEngine.test.js
autonomous: true
requirements:
  - VAR-01
  - BRN-01
must_haves:
  truths:
    - "新游戏与容错读档都会先从 `script.json.systems.variables` 的 bool/number 默认值起步，而不是落回 `undefined` 或隐式 `0`。"
    - "条件页在加载、保存、运行时都收敛到同一 canonical shape，同时仍能读旧的单条件字段。"
    - "变量默认值、条件归一化、条件求值不再散落在组件里，而是走共享 helper。"
  artifacts:
    - path: src/shared/variableRegistry.js
      provides: "变量注册表归一化、默认值推导、运行时 seed 与引用扫描 helper"
      contains: "seedRuntimeVariablesFromRegistry"
    - path: src/shared/branchingContract.js
      provides: "条件页 canonical/read-compat/evaluate/summary 输入 helper"
      contains: "normalizeConditionPage"
    - path: src/editor/stores/script.js
      provides: "项目加载与 history snapshot 前统一规范 choice/condition 数据"
      contains: "loadFromData"
    - path: src/engine/ScriptEngine.js
      provides: "startGame/restoreState 默认值 seed 与 canonical condition 执行"
      contains: "startGame|restoreState|_execCondition"
    - path: tests/variableRegistryContract.test.js
      provides: "变量注册表 contract focused coverage"
      contains: "seedRuntimeVariablesFromRegistry"
    - path: tests/branchingContract.test.js
      provides: "条件页 canonical/read-compat focused coverage"
      contains: "normalizeConditionPage"
  key_links:
    - from: src/shared/variableRegistry.js
      to: src/engine/ScriptEngine.js
      via: "startGame/restoreState 先 seed 默认值，再 overlay 保存态"
      pattern: "seedRuntimeVariablesFromRegistry|mergeRuntimeVariables"
    - from: src/shared/branchingContract.js
      to: src/editor/stores/script.js
      via: "loadFromData 与 pushState 都归一化 condition page"
      pattern: "normalizeConditionPage|normalizeConditionPages"
    - from: src/shared/branchingContract.js
      to: src/engine/ScriptEngine.js
      via: "_execCondition 走共享 evaluator，不再手写单字段 switch"
      pattern: "evaluateConditionPage"
---

<objective>
Freeze the Phase 84 variable/branching foundation before any GUI work lands, so the editor and runtime share one truthful contract for registry defaults, canonical condition pages, and legacy read-compat.

Purpose: satisfy the contract/runtime side of VAR-01 and BRN-01 per D-84-01, D-84-02, D-84-03, D-84-05, and the locked “no expression language” boundary.
Output: shared variable/branching helpers, store normalization, runtime default seeding, and focused regression coverage.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/84-variable-registry-and-branching-gui/84-CONTEXT.md
@.planning/phases/84-variable-registry-and-branching-gui/84-RESEARCH.md
@.planning/phases/84-variable-registry-and-branching-gui/84-UI-SPEC.md
@.planning/phases/83-galgame-core-contract-persistence/83-03-SUMMARY.md
@src/shared/effectDsl.js
@src/editor/stores/script.js
@src/engine/ScriptEngine.js

<interfaces>
From `src/shared/effectDsl.js`:
```javascript
export function normalizeEffectContainer(container = {}) { /* canonical effects[] write path */ }
export async function applyEffects(source = [], { variables = new Map() } = {}) { /* runtime effect execution */ }
```

From `src/engine/ScriptEngine.js`:
```javascript
startGame(sceneId = 'start') {
  this.variables.clear();
}

restoreState(state) {
  this.variables = new Map(Object.entries(state.variables || {}));
}

_execCondition(page) {
  const val = this.variables.get(page.variable) ?? 0;
}
```
</interfaces>
</context>

<pitfalls>
- Do not add string variables, arbitrary expressions, or generic persistent vars; per D-84-03/D-84-04 Phase 84 is bool/number only.
- Do not keep legacy `page.variable/operator/value` as a live write shape; per D-84-05 they are read-compat only.
- Do not seed defaults in editor-only code; runtime boot must see the same registry truth.
</pitfalls>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add shared variable/branching contract helpers with failing coverage first</name>
  <files>src/shared/variableRegistry.js, src/shared/branchingContract.js, tests/variableRegistryContract.test.js, tests/branchingContract.test.js</files>
  <behavior>
    - Test 1: variable registry normalization preserves the canonical `systems.variables` object-map keyed by variable ID and clamps author data to `bool` / `number`.
    - Test 2: runtime default seeding returns stable initial values (`false`/`0` fallback, explicit `initial` override) without inventing string/expression behavior.
    - Test 3: legacy single-condition pages normalize to canonical `conditionMode` + `conditions[]` + `trueTarget` + `falseTarget`, while canonical pages round-trip unchanged.
    - Test 4: condition evaluation supports only `== != > >= < <=` with `all` / `any` over 1-3 rows.
  </behavior>
  <action>Per D-84-01, D-84-03, and D-84-05, create `src/shared/variableRegistry.js` and `src/shared/branchingContract.js` as the only Phase 84 contract owners. Write the new tests first in RED. In `variableRegistry.js`, export helpers for entry normalization, default-value derivation, runtime seeding/merge, and exact variable-reference provenance objects that later plans can reuse for counts/rename/delete. In `branchingContract.js`, export helpers to normalize legacy condition fields into canonical rows, validate the locked operator set, evaluate canonical condition pages, and format data needed by later summary UIs. Keep saved variable entry key as `initial` (UI may still label it “默认值”). Do not add affection/endings/CG helpers in this phase.</action>
  <verify>
    <automated>npx vitest run tests/variableRegistryContract.test.js tests/branchingContract.test.js</automated>
  </verify>
  <done>Shared contract helpers exist, focused tests lock the Phase 84 data rules, and downstream plans can consume one variable/branching contract instead of component-local logic.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire store normalization and runtime default seeding onto the new helpers</name>
  <files>src/editor/stores/script.js, src/engine/ScriptEngine.js, src/shared/variableRegistry.js, src/shared/branchingContract.js, tests/scriptEngine.test.js, tests/variableRegistryContract.test.js, tests/branchingContract.test.js</files>
  <behavior>
    - Test 1: `loadFromData()` normalizes incoming choice/condition data so save-open-save emits canonical `effects[]` plus canonical condition pages.
    - Test 2: `startGame()` seeds every registered bool/number variable from registry defaults before scene execution starts.
    - Test 3: `restoreState()` overlays saved values over registry defaults while preserving unknown legacy save keys for tolerant restore.
    - Test 4: `_execCondition()` evaluates canonical multi-row pages through the shared helper and still honors legacy read-compat input after normalization.
  </behavior>
  <action>Update `src/editor/stores/script.js` to call the new variable/branching normalizers alongside the existing Phase 83 contract/effects normalization before history snapshots. Update `src/engine/ScriptEngine.js` so `startGame()` and `restoreState()` seed from `script.systems.variables` per D-84-01/D-84-03, then execute condition pages through `branchingContract.js` instead of the current single-field switch. Keep tolerant restore behavior for old save payloads, but do not write new non-canonical condition fields back out. Leave page-creation UI and condition-page conversion UX to later plans; this task only freezes truthful runtime/store behavior.</action>
  <verify>
    <automated>npx vitest run tests/variableRegistryContract.test.js tests/branchingContract.test.js && node --test tests/scriptEngine.test.js && npm run build</automated>
  </verify>
  <done>The editor and runtime both consume the same variable/branching helper layer, registry defaults affect actual play, and canonical condition pages are stable enough for GUI work.</done>
</task>

</tasks>

<verification>
Run the two new focused Vitest files after helper changes, then rerun them with `tests/scriptEngine.test.js` and `npm run build` once store/runtime wiring is complete.
</verification>

<success_criteria>
- Runtime variable state always starts from registry truth for bool/number variables.
- Legacy condition pages remain readable, but canonical save paths converge on `conditionMode` + `conditions[]` + targets.
- Later GUI plans can reuse shared registry/branching helpers without re-deriving schema rules.
</success_criteria>

<output>
After completion, create `.planning/phases/84-variable-registry-and-branching-gui/84-01-SUMMARY.md`
</output>
