---
phase: 84-variable-registry-and-branching-gui
plan: '02'
type: execute
wave: 2
depends_on:
  - '84-01'
files_modified:
  - src/editor/App.vue
  - src/editor/views/StorySystems.vue
  - src/editor/components/story-systems/VariableRegistryList.vue
  - src/editor/components/story-systems/VariableInspector.vue
  - src/editor/components/story-systems/VariableImpactModal.vue
  - src/editor/stores/script.js
  - tests/variableRegistryWorkspace.test.js
  - tests/variableReferenceSafety.test.js
autonomous: true
requirements:
  - VAR-01
  - VAR-03
must_haves:
  truths:
    - "作者可以在顶层 `📊 剧情系统` 工作区里查看、搜索、筛选并创建项目级变量。"
    - "变量详情面板可以编辑显示名、内部 ID、类型、默认值、分组、备注，且范围仍只限 bool/number。"
    - "重命名或删除变量时，编辑器会展示引用次数与反向引用位置，并用同一事务去重写或清理引用，而不是静默破坏剧情。"
    - "跨 surface 的 `前往变量表` 修复入口会落到正确的剧情系统工作区选中项，且删除变量导致条件页失效时会触发保存闸门。"
  artifacts:
    - path: src/editor/App.vue
      provides: "新增剧情系统顶层 tab，并保持现有编辑器导航顺序"
      contains: "story-systems"
    - path: src/editor/views/StorySystems.vue
      provides: "变量注册表工作区外壳、选中态、搜索过滤、空状态与系统级 repair landing"
      contains: "剧情系统"
    - path: src/editor/components/story-systems/VariableRegistryList.vue
      provides: "变量列表、usage badge、搜索/筛选结果视图"
      contains: "usage"
    - path: src/editor/components/story-systems/VariableInspector.vue
      provides: "变量 CRUD inspector 与类型感知默认值控件"
      contains: "defaultValue"
    - path: src/editor/components/story-systems/VariableImpactModal.vue
      provides: "rename/delete 影响确认与反向引用预览"
      contains: "删除变量并清除引用"
    - path: src/editor/stores/script.js
      provides: "变量 reference scanner、rename/delete 事务，以及 invalid condition save gate"
      contains: "findVariableReferences|renameVariable|deleteVariable|canSaveConditionPages"
    - path: tests/variableRegistryWorkspace.test.js
      provides: "工作区 CRUD 与跨 surface repair 跳转 coverage"
      contains: "剧情系统"
    - path: tests/variableReferenceSafety.test.js
      provides: "引用计数、rename transaction、delete cleanup 与 invalid condition save gate coverage"
      contains: "references"
  key_links:
    - from: src/editor/App.vue
      to: src/editor/views/StorySystems.vue
      via: "TabBar 注入新的剧情系统工作区，并暴露 repair route 用于跨 surface 跳转/focus"
      pattern: "story-systems|剧情系统|openStorySystems|selectedVariableId"
    - from: src/editor/views/StorySystems.vue
      to: src/editor/stores/script.js
      via: "registry workspace 直接读写 `script.data.systems.variables` 并消费 repair-focus state"
      pattern: "systems\\.variables|selectedVariableId"
    - from: src/editor/stores/script.js
      to: src/editor/components/story-systems/VariableImpactModal.vue
      via: "同一 reference scanner 既供 usage badge，也供 rename/delete 事务"
      pattern: "findVariableReferences|renameVariable|deleteVariable"
    - from: src/editor/stores/script.js
      to: src/editor/App.vue
      via: "invalid condition markers and save gate surface through one app-level repair flow"
      pattern: "canSaveConditionPages|story-systems"
---

<objective>
Ship the project-level variable registry workspace so authors manage variables in one coherent place, with explicit reference-safety for rename/delete instead of fragile free-text edits.

Purpose: satisfy VAR-01 and VAR-03 per D-84-01, D-84-03, D-84-07, and the locked “no endings/CG UI yet” scope.
Output: new Story Systems tab, registry list + inspector, and transactional rename/delete safety UX backed by focused tests.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/84-variable-registry-and-branching-gui/84-CONTEXT.md
@.planning/phases/84-variable-registry-and-branching-gui/84-RESEARCH.md
@.planning/phases/84-variable-registry-and-branching-gui/84-UI-SPEC.md
@.planning/phases/84-variable-registry-and-branching-gui/84-01-PLAN.md
@src/editor/App.vue
@src/editor/stores/script.js

<interfaces>
From `src/editor/App.vue`:
```javascript
const tabs = [
  { id: 'scenes', icon: '🎬', label: '游戏内容' },
  { id: 'title', icon: '🖼️', label: '标题页' },
  // ...
];
```

From `src/editor/stores/script.js`:
```javascript
function loadFromData(scriptData) {
  data.value = ensureGalgameContract(scriptData);
}
```
</interfaces>
</context>

<pitfalls>
- Do not add disabled placeholder tabs for endings/CG; per D-84-08 Phase 84 workspace scope is variables only.
- Do not let display-name edits silently change internal IDs; ID rewrites must go through the safety flow per D-84-07.
- Do not move registry ownership into page-local state; the truth source stays `script.json.systems.variables`.
</pitfalls>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the top-level 剧情系统 workspace shell and searchable variable list</name>
  <files>src/editor/App.vue, src/editor/views/StorySystems.vue, src/editor/components/story-systems/VariableRegistryList.vue, tests/variableRegistryWorkspace.test.js</files>
  <behavior>
    - Test 1: the main tab bar inserts `📊 剧情系统` immediately after `🎬 游戏内容`.
    - Test 2: the workspace lists `systems.variables` entries with display name, ID, type badge, default value, and usage count.
    - Test 3: search/filter state matches display name, ID, and group without dropping the current selection when it still matches.
    - Test 4: empty/no-result states use the locked Chinese copy from the UI spec.
    - Test 5: App-level repair navigation can open `剧情系统` and focus a requested variable row from another surface.
  </behavior>
  <action>Write `tests/variableRegistryWorkspace.test.js` first in RED, then add `StorySystems.vue` plus `VariableRegistryList.vue` and wire the new tab into `src/editor/App.vue`. Keep the layout aligned with the UI spec: left registry sidebar, right detail pane placeholder, dark VS Code-like styling, and no extra endings/CG sections. List data must come from canonical `script.data.systems.variables` per D-84-01, not a component-local mirror. Use usage counts from the shared reference scanner seeded in 84-01 so the list stays truthful. This task also owns the app-level repair route: define how other surfaces can request `剧情系统` to open and focus a variable that needs repair.</action>
  <verify>
    <automated>npx vitest run tests/variableRegistryWorkspace.test.js</automated>
  </verify>
  <done>Authors can open a dedicated 剧情系统 tab and browse/search/filter project variables without touching raw JSON.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement variable inspector CRUD with type-aware defaults and first-create ID generation</name>
  <files>src/editor/views/StorySystems.vue, src/editor/components/story-systems/VariableInspector.vue, src/editor/stores/script.js, tests/variableRegistryWorkspace.test.js</files>
  <behavior>
    - Test 1: `+ 新建变量` creates a draft entry, focuses display name, and auto-generates the first internal ID from that name.
    - Test 2: inspector edits persist `name`, `type`, `initial`, `group`, and `notes` under the canonical variable registry entry.
    - Test 3: bool defaults render as `是 / 否`, number defaults render as numeric input, and invalid duplicate IDs surface the locked error copy.
  </behavior>
  <action>Extend the workspace with `VariableInspector.vue` and store mutations in `src/editor/stores/script.js`. Per D-84-03, only allow `bool` and `number`; do not add affection presets, strings, or expression fields. Save to the existing object map under `systems.variables`, keeping the saved key `initial` even though the UI label is “默认值”. Make new-variable creation optimistic and inline, but keep duplicate-ID validation immediate so authors cannot save a broken registry.</action>
  <verify>
    <automated>npx vitest run tests/variableRegistryWorkspace.test.js</automated>
  </verify>
  <done>The registry workspace supports full project-level variable CRUD with canonical saved shape and no raw JSON editing.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add rename/delete impact previews and transactional reference safety</name>
  <files>src/editor/components/story-systems/VariableImpactModal.vue, src/editor/views/StorySystems.vue, src/editor/stores/script.js, tests/variableReferenceSafety.test.js, tests/variableRegistryWorkspace.test.js</files>
  <behavior>
    - Test 1: usage badges and reverse-reference lists report exact scene/page/option/condition provenance from one shared scanner.
    - Test 2: renaming an in-use internal ID updates every supported reference site transactionally and reports the rewrite count before confirm.
    - Test 3: deleting an in-use variable shows reverse references first, then clears affected choice effects / condition rows per the UI spec instead of leaving stale IDs behind.
    - Test 4: if variable deletion would leave a condition page with zero valid rows, the store records an unresolved condition marker and save is blocked until repair.
  </behavior>
  <action>Write `tests/variableReferenceSafety.test.js` first, then add `VariableImpactModal.vue` and the store actions it calls. Reuse the shared scanner from 84-01 for count display, rename preview, rename mutation, and delete cleanup so the numbers cannot drift. Per D-84-07, never silently break logic: internal-ID changes with references must go through a confirm flow, and delete must explicitly show the impact before clearing references. Follow the locked scope exactly: only variable references are rewritten here; do not add ending/CG side effects. For condition pages whose deleted variable would leave an invalid row, record an explicit unresolved condition marker plus a save gate (`canSaveConditionPages` or equivalent) so the project cannot save again until the author repairs the page in the Phase 84 condition UI.</action>
  <verify>
    <automated>npx vitest run tests/variableRegistryWorkspace.test.js tests/variableReferenceSafety.test.js && npm run build</automated>
  </verify>
  <done>Variable rename/delete is reference-safe, counts stay truthful, cross-surface repair routing exists, and invalid condition pages cannot silently survive a save after destructive cleanup.</done>
</task>

</tasks>

<verification>
Use the workspace test file as the fast loop while building list/inspector behavior, then finish with the safety test plus `npm run build` so the new tab and modal integrate cleanly.
</verification>

<success_criteria>
- The editor exposes one project-level variable registry workspace with search, create, and detail editing.
- Saved registry data remains under `script.json.systems.variables` with bool/number defaults only.
- Rename/delete flows show exact affected references and execute transactional updates/cleanup instead of silent breakage.
</success_criteria>

<output>
After completion, create `.planning/phases/84-variable-registry-and-branching-gui/84-02-SUMMARY.md`
</output>
