---
phase: 84-variable-registry-and-branching-gui
plan: '03'
type: execute
wave: 2
depends_on:
  - '84-01'
  - '84-02'
files_modified:
  - src/editor/components/page-editor/VariablePickerField.vue
  - src/editor/components/page-editor/EffectRowEditor.vue
  - src/editor/components/page-editor/PageInspector.vue
  - tests/pageInspectorVariableEffects.test.js
autonomous: true
requirements:
  - VAR-02
must_haves:
  truths:
    - "作者在选择页里通过变量 picker 配置 `设为 / 增加 / 减少`，不再手写变量 key。"
    - "bool 变量只能做 `设为`，number 变量才允许 `增加 / 减少`，且值控件随类型切换。"
    - "选择页保存结果继续落在 canonical `effects[]`，并在缺失变量时显示明确修复提示。"
  artifacts:
    - path: src/editor/components/page-editor/VariablePickerField.vue
      provides: "可搜索变量选择器，显示 display name + id/type/group"
      contains: "前往变量表"
    - path: src/editor/components/page-editor/EffectRowEditor.vue
      provides: "单条变量效果编辑器，封装 action/variable/value 规则"
      contains: "设为"
    - path: src/editor/components/page-editor/PageInspector.vue
      provides: "选择页效果区改为 picker-backed multi-effect editor"
      contains: "effects"
    - path: tests/pageInspectorVariableEffects.test.js
      provides: "选择页变量效果 UI focused coverage"
      contains: "VariablePickerField"
  key_links:
    - from: src/editor/components/page-editor/EffectRowEditor.vue
      to: src/editor/components/page-editor/VariablePickerField.vue
      via: "效果行选择变量后限制可用动作和值控件"
      pattern: "variableId|type"
    - from: src/editor/components/page-editor/PageInspector.vue
      to: src/shared/effectDsl.js
      via: "选择页写回 canonical `effects[]`"
      pattern: "effects"
    - from: src/editor/components/page-editor/PageInspector.vue
      to: src/editor/App.vue
      via: "缺失变量/空注册表时调用 84-02 提供的 app-level repair route 打开剧情系统"
      pattern: "story-systems|openStorySystems|selectedVariableId"
---

<objective>
Replace the remaining raw-key choice-variable UX with picker-backed canonical effect editing, so authors configure variable writes through the registry rather than inventing free-text IDs.

Purpose: satisfy VAR-02 per D-84-01, D-84-02, D-84-03, and the locked “no raw-key first-class writes” rule.
Output: reusable variable picker/effect-row components and a choice inspector that writes only canonical `effects[]`.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/84-variable-registry-and-branching-gui/84-CONTEXT.md
@.planning/phases/84-variable-registry-and-branching-gui/84-RESEARCH.md
@.planning/phases/84-variable-registry-and-branching-gui/84-UI-SPEC.md
@.planning/phases/83-galgame-core-contract-persistence/83-03-SUMMARY.md
@src/shared/effectDsl.js
@src/editor/components/page-editor/PageInspector.vue

<interfaces>
From `src/shared/effectDsl.js`:
```javascript
export function normalizeEffectContainer(container = {}) { /* strips legacy setVariable */ }
export function normalizeEffects(source = []) { /* canonical effect array */ }
```

Current choice inspector seam in `src/editor/components/page-editor/PageInspector.vue`:
```vue
<input type="text" :value="optionVarKey(opt)" class="field-input var-key" placeholder="变量名" />
<input type="number" :value="optionVarValue(opt)" class="field-input var-value" placeholder="值" />
```
</interfaces>
</context>

<pitfalls>
- Do not reintroduce raw `setVariable` editing as a peer save contract; per D-84-02 the saved truth remains `effects[]`.
- Do not allow `add/sub` for bool variables; per D-84-03 the editor must enforce type rules instead of relying on runtime coercion.
- Do not hide missing-variable state; show repair affordances instead of silently dropping rows.
</pitfalls>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create reusable variable picker and effect-row components with focused UI coverage</name>
  <files>src/editor/components/page-editor/VariablePickerField.vue, src/editor/components/page-editor/EffectRowEditor.vue, tests/pageInspectorVariableEffects.test.js</files>
  <behavior>
    - Test 1: the picker searches by display name / ID / group and renders line-1 display name plus muted `id · type · group`.
    - Test 2: empty registry state shows the locked empty-state copy and exposes a `新建变量` / `前往变量表` action.
    - Test 3: selecting a bool variable limits actions to `设为` and swaps the value control to `是 / 否`; selecting a number variable enables `设为 / 增加 / 减少` with numeric input.
  </behavior>
  <action>Write `tests/pageInspectorVariableEffects.test.js` first, then add `VariablePickerField.vue` and `EffectRowEditor.vue`. Keep the picker anchored to row-level editing patterns already used in the editor, reuse the dark theme styling from the UI spec, and emit canonical effect row data instead of ad-hoc text fields. Per D-84-01/D-84-03, source choices only from the registry object map and keep variable types limited to bool/number. Include footer actions for empty/missing states, but do not build inline creation logic here; navigation back to the registry workspace is enough for this plan.</action>
  <verify>
    <automated>npx vitest run tests/pageInspectorVariableEffects.test.js</automated>
  </verify>
  <done>Reusable picker-backed effect row components exist and enforce the locked bool/number action rules before PageInspector wiring begins.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace choice-page raw variable inputs with canonical multi-effect editing in PageInspector</name>
  <files>src/editor/components/page-editor/PageInspector.vue, src/editor/components/page-editor/VariablePickerField.vue, src/editor/components/page-editor/EffectRowEditor.vue, tests/pageInspectorVariableEffects.test.js</files>
  <behavior>
    - Test 1: choice options render an `效果` subsection backed by zero-or-more canonical effect rows.
    - Test 2: save interactions update `option.effects[]` and never surface the old raw-key input as the primary UX.
    - Test 3: choice cards show a readable effect summary and inline warning when a stored variable reference no longer exists.
  </behavior>
  <action>Update `PageInspector.vue` so choice options use `EffectRowEditor` and `VariablePickerField` under the existing option cards. Per D-84-02, always write through canonical `effects[]`; keep legacy compat read support only via existing shared helpers. Render `+ 添加效果`, row deletion, and summary lines using current variable display names. Missing-variable rows must show the locked warning copy and consume the app-level repair route established in 84-02 so `前往变量表` lands on the correct workspace/variable instead of being decorative. Do not add scene-picker work here; keep this plan focused on variable authoring only.</action>
  <verify>
    <automated>npx vitest run tests/pageInspectorVariableEffects.test.js && node --test tests/effectDsl.test.js && npm run build</automated>
  </verify>
  <done>Choice pages author variable writes through picker-backed canonical effects, with type-aware controls and no raw-key first-class UI remaining.</done>
</task>

</tasks>

<verification>
Use the dedicated PageInspector variable-effects suite as the fast loop, then rerun it with `tests/effectDsl.test.js` and `npm run build` to prove the inspector still respects the Phase 83 DSL.
</verification>

<success_criteria>
- Choice authoring uses variables from the registry instead of free-text IDs.
- Saved option payloads remain canonical `effects[]`.
- Bool/number guardrails are enforced in UI, not deferred to runtime guesswork.
</success_criteria>

<output>
After completion, create `.planning/phases/84-variable-registry-and-branching-gui/84-03-SUMMARY.md`
</output>
