---
phase: 84-variable-registry-and-branching-gui
plan: '04'
type: execute
wave: 3
depends_on:
  - '84-01'
  - '84-02'
  - '84-03'
files_modified:
  - src/editor/stores/script.js
  - src/editor/components/page-editor/SceneTargetPicker.vue
  - src/editor/components/page-editor/BranchSummary.vue
  - src/editor/components/page-editor/PageInspector.vue
  - src/editor/components/page-editor/SceneTree.vue
  - tests/conditionPageEditor.test.js
  - tests/conditionSummary.test.js
  - tests/sceneTreeConditionSnippet.test.js
autonomous: true
requirements:
  - BRN-01
  - BRN-02
must_haves:
  truths:
    - "作者可以把页面切换为条件页，并在 GUI 中配置 1~3 条条件、all/any 模式与成功/失败目标。"
    - "条件页目标通过场景选择器和自然语言摘要展示 scene name / variable display name，而不是裸 ID。"
    - "SceneTree 与 inspector 的条件摘要都从当前 canonical page data 派生，不保存会漂移的 summary 字段。"
  artifacts:
    - path: src/editor/stores/script.js
      provides: "页面类型转换支持 condition page canonical defaults"
      contains: "convertPageType"
    - path: src/editor/components/page-editor/SceneTargetPicker.vue
      provides: "场景选择器，主文本为 scene name，辅文为 scene ID"
      contains: "目标场景不存在"
    - path: src/editor/components/page-editor/BranchSummary.vue
      provides: "条件/跳转自然语言摘要展示组件"
      contains: "否则"
    - path: src/editor/components/page-editor/PageInspector.vue
      provides: "condition page 专属 builder 与摘要区"
      contains: "conditionMode|conditions"
    - path: src/editor/components/page-editor/SceneTree.vue
      provides: "条件页转换入口与树节点摘要/tooltip"
      contains: "条件页"
    - path: tests/conditionPageEditor.test.js
      provides: "条件页编辑器 focused coverage"
      contains: "condition"
  key_links:
    - from: src/editor/stores/script.js
      to: src/editor/components/page-editor/PageInspector.vue
      via: "condition page 创建/转换后立即落到 canonical defaults"
      pattern: "type: 'condition'|conditionMode|conditions"
    - from: src/editor/components/page-editor/PageInspector.vue
      to: src/editor/components/page-editor/SceneTargetPicker.vue
      via: "成功/失败目标统一走 scene picker"
      pattern: "trueTarget|falseTarget"
    - from: src/editor/components/page-editor/PageInspector.vue
      to: src/editor/components/page-editor/BranchSummary.vue
      via: "摘要框展示当前条件和目标的自然语言"
      pattern: "summary"
    - from: src/editor/components/page-editor/SceneTree.vue
      to: src/editor/components/page-editor/BranchSummary.vue
      via: "树节点 snippet/tooltip 复用同一 summary formatter"
      pattern: "pageSnippet|condition"
---

<objective>
Expose condition pages as a first-class authoring surface with canonical builder controls, scene pickers, and derived summaries so branching logic becomes GUI-driven instead of hidden data.

Purpose: satisfy BRN-01 and BRN-02 per D-84-05 and D-84-06 while staying inside the locked 1~3-row condition scope.
Output: condition-page conversion/defaults, dedicated condition inspector UI, reusable scene picker + summary components, and focused UI regressions.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/84-variable-registry-and-branching-gui/84-CONTEXT.md
@.planning/phases/84-variable-registry-and-branching-gui/84-RESEARCH.md
@.planning/phases/84-variable-registry-and-branching-gui/84-UI-SPEC.md
@.planning/phases/84-variable-registry-and-branching-gui/84-01-PLAN.md
@.planning/phases/84-variable-registry-and-branching-gui/84-03-PLAN.md
@src/editor/stores/script.js
@src/editor/components/page-editor/PageInspector.vue
@src/editor/components/page-editor/SceneTree.vue

<interfaces>
From `src/editor/stores/script.js`:
```javascript
function convertPageType(sceneId, pageIndex) {
  if (page.type === 'normal') page.type = 'choice';
  else if (page.type === 'choice') page.type = 'normal';
}
```

From `src/editor/components/page-editor/SceneTree.vue`:
```javascript
function pageSnippet(page) {
  if (page.type === 'choice') return '[选择页]';
  if (page.type === 'condition') return '[条件页]';
}
```
</interfaces>
</context>

<pitfalls>
- Do not leave condition pages as hidden data-only types; the tree and inspector must both expose them.
- Do not persist summary strings; per D-84-06 summaries are derived from current variable/scene labels.
- Do not exceed the locked 1~3 condition rows or introduce expression syntax.
</pitfalls>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add condition-page conversion defaults and dedicated inspector builder UI</name>
  <files>src/editor/stores/script.js, src/editor/components/page-editor/PageInspector.vue, src/editor/components/page-editor/SceneTree.vue, tests/conditionPageEditor.test.js</files>
  <behavior>
    - Test 1: SceneTree/page conversion can create or convert to `type: 'condition'` with canonical defaults from 84-01.
    - Test 2: PageInspector replaces dialogue editing with a dedicated condition builder when `page.type === 'condition'`.
    - Test 3: the builder enforces 1-3 rows, all/any mode, operator restrictions by variable type, required success target, and optional failure target.
    - Test 4: unresolved condition markers produced by 84-02 surface clear repair UI here and disappear only after the author restores a valid row.
  </behavior>
  <action>Write `tests/conditionPageEditor.test.js` first, then update `convertPageType()` in `src/editor/stores/script.js` and the page-type actions in `SceneTree.vue` so authors can intentionally create condition pages. In `PageInspector.vue`, add a condition-page branch that consumes the canonical helpers from 84-01 and the variable picker from 84-03. Per D-84-05, the builder must stop at 1~3 comparisons with `== != > >= < <=` and `全部满足 / 任一满足`; do not add expression rows, nested groups, or other scripting affordances. Save back only the canonical condition-page fields. This task also consumes the unresolved-condition markers/save gate from 84-02 by surfacing a clear repair state and ensuring the page returns to savable status only after a valid condition row exists again.</action>
  <verify>
    <automated>npx vitest run tests/conditionPageEditor.test.js</automated>
  </verify>
  <done>Condition pages are visible and editable in the existing page workflow, with canonical builder controls instead of hidden raw fields.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add scene picker and derived branch summaries for inspector and tree surfaces</name>
  <files>src/editor/components/page-editor/SceneTargetPicker.vue, src/editor/components/page-editor/BranchSummary.vue, src/editor/components/page-editor/PageInspector.vue, src/editor/components/page-editor/SceneTree.vue, tests/conditionSummary.test.js, tests/sceneTreeConditionSnippet.test.js, tests/conditionPageEditor.test.js</files>
  <behavior>
    - Test 1: scene target selection shows scene name as the primary label and scene ID as helper text, with a missing-target warning when needed.
    - Test 2: the inspector summary box renders natural-language condition summaries using variable display names and scene names.
    - Test 3: SceneTree condition snippets / tooltips update from the same derived formatter and do not store summary text in page JSON.
  </behavior>
  <action>Implement `SceneTargetPicker.vue` and `BranchSummary.vue`, then wire them into the condition-page branch of `PageInspector.vue` and the page snippet/tooltip surface in `SceneTree.vue`. Per D-84-06, all author-facing target selection must go through readable scene names and summaries, not raw scene IDs. Reuse the summary inputs from `branchingContract.js` rather than building divergent string logic in each component. Keep failure-target empty semantics aligned with the UI spec (`继续下一页`) and do not add summary persistence fields to saved data.</action>
  <verify>
    <automated>npx vitest run tests/conditionPageEditor.test.js tests/conditionSummary.test.js tests/sceneTreeConditionSnippet.test.js && npm run build</automated>
  </verify>
  <done>Condition pages use scene pickers and readable summaries across inspector and tree surfaces, and all summary text stays derived from current data.</done>
</task>

</tasks>

<verification>
Use the condition editor suite as the fast loop while building the UI, then finish with the summary/tree suites and `npm run build` so the new picker/summary components integrate with the existing editor.
</verification>

<success_criteria>
- Authors can create and edit condition pages fully from the GUI.
- Branch targets are selected by scene picker, not raw IDs.
- SceneTree and inspector summaries remain truthful after renames because they are derived, not persisted.
</success_criteria>

<output>
After completion, create `.planning/phases/84-variable-registry-and-branching-gui/84-04-SUMMARY.md`
</output>
