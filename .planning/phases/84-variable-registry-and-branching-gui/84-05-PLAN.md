---
phase: 84-variable-registry-and-branching-gui
plan: '05'
type: execute
wave: 4
depends_on:
  - '84-04'
files_modified:
  - src/editor/components/page-editor/PageCanvas.vue
  - src/editor/components/page-editor/CanvasToolbar.vue
  - src/editor/composables/useVoiceMatch.js
  - tests/conditionPageSurfaceAudit.test.js
autonomous: true
requirements:
  - BRN-01
  - BRN-02
must_haves:
  truths:
    - "条件页不会再误显示普通页的对白/角色/语音编辑行为。"
    - "画布、工具栏、批量语音匹配等周边 surface 都显式区分 `normal`、`choice`、`condition`。"
    - "page-type assumption 的回归会被 focused tests 抓住，而不是等到作者手动踩到。"
  artifacts:
    - path: src/editor/components/page-editor/PageCanvas.vue
      provides: "condition page 画布不再走普通对话 UI 假设"
      contains: "page.type === 'choice'"
    - path: src/editor/components/page-editor/CanvasToolbar.vue
      provides: "toolbar normal-only affordance 明确 gated"
      contains: "页面"
    - path: src/editor/composables/useVoiceMatch.js
      provides: "批量语音匹配只处理 normal dialogue pages"
      contains: "page.type"
    - path: tests/conditionPageSurfaceAudit.test.js
      provides: "page-type assumption focused regression coverage"
      contains: "condition"
  key_links:
    - from: src/editor/components/page-editor/PageCanvas.vue
      to: src/editor/components/page-editor/CanvasToolbar.vue
      via: "normal-only canvas affordances与 toolbar affordances 保持一致"
      pattern: "page\\.type === 'normal'"
    - from: src/editor/composables/useVoiceMatch.js
      to: src/editor/components/page-editor/SceneTree.vue
      via: "条件页不会被当作对白页参与批量语音匹配"
      pattern: "page\\.type"
    - from: tests/conditionPageSurfaceAudit.test.js
      to: src/editor/components/page-editor/PageCanvas.vue
      via: "focused assertions lock explicit page-type branches"
      pattern: "condition"
---

<objective>
Audit and close the remaining page-type assumption leaks so condition pages behave like first-class non-dialogue pages everywhere, not just inside the primary inspector.

Purpose: harden BRN-01/BRN-02 by addressing the repo’s known brownfield risk that several surfaces still treat “not choice” as “normal”.
Output: audited canvas/toolbar/voice-match surfaces plus focused regression coverage for page-type gating.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/84-variable-registry-and-branching-gui/84-CONTEXT.md
@.planning/phases/84-variable-registry-and-branching-gui/84-RESEARCH.md
@.planning/phases/84-variable-registry-and-branching-gui/84-UI-SPEC.md
@.planning/phases/84-variable-registry-and-branching-gui/84-04-PLAN.md
@src/editor/components/page-editor/PageCanvas.vue
@src/editor/components/page-editor/CanvasToolbar.vue
@src/editor/composables/useVoiceMatch.js

<interfaces>
Current assumption in `src/editor/composables/useVoiceMatch.js`:
```javascript
if (page.type === 'choice') return; // skip choice pages — no dialogues
```

Current canvas branch in `src/editor/components/page-editor/PageCanvas.vue`:
```vue
<div v-if="page && page.type === 'choice'" class="canvas-choices">
```
</interfaces>
</context>

<pitfalls>
- Do not leave “not choice === normal” branches in place after condition-page rollout.
- Do not fake dialogue UI for condition pages just to fill space; keep them explicitly non-dialogue.
- Do not rely on manual smoke tests alone; lock the audit with focused automated coverage.
</pitfalls>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add a focused page-type audit test and fix canvas/toolbar gating</name>
  <files>tests/conditionPageSurfaceAudit.test.js, src/editor/components/page-editor/PageCanvas.vue, src/editor/components/page-editor/CanvasToolbar.vue</files>
  <behavior>
    - Test 1: condition pages do not render dialogue-only canvas affordances or “添加角色” controls meant for normal pages.
    - Test 2: toolbar state reflects `condition` explicitly instead of relying on a generic “not normal” fallback.
  </behavior>
  <action>Write `tests/conditionPageSurfaceAudit.test.js` first, using source-level or jsdom assertions that lock explicit page-type branching. Then update `PageCanvas.vue` and `CanvasToolbar.vue` so normal-only controls are guarded by `page.type === 'normal'`, choice preview stays choice-only, and condition pages show neutral non-dialogue state instead of inheriting dialogue assumptions. Keep the existing dark styling and do not introduce a fake preview runtime for condition pages.</action>
  <verify>
    <automated>npx vitest run tests/conditionPageSurfaceAudit.test.js</automated>
  </verify>
  <done>Canvas and toolbar surfaces no longer leak normal-page editing behavior into condition pages.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Finish the audit by hardening batch voice-match against condition pages</name>
  <files>src/editor/composables/useVoiceMatch.js, tests/conditionPageSurfaceAudit.test.js</files>
  <behavior>
    - Test 1: batch voice matching skips both choice pages and condition pages, processing only normal dialogue pages.
    - Test 2: the audit test fails if future code reintroduces “not choice” shortcuts on the covered surfaces.
  </behavior>
  <action>Update `useVoiceMatch.js` so the page scan checks `page.type === 'normal'` explicitly before touching dialogue rows. Extend the audit test to pin that behavior and any other direct assumption leaks discovered while patching the covered files. This task is intentionally narrow: it exists to close the known brownfield pitfall from the research, not to add new branching features.</action>
  <verify>
    <automated>npx vitest run tests/conditionPageSurfaceAudit.test.js && npm run build</automated>
  </verify>
  <done>Known page-type assumption leaks are audited and locked behind focused tests, reducing the risk of condition-page regressions outside the main inspector flow.</done>
</task>

</tasks>

<verification>
Keep the audit test green while patching each surface, then finish with `npm run build` to prove the non-normal-page hardening did not break editor integration.
</verification>

<success_criteria>
- Condition pages behave as explicit non-dialogue pages across secondary editor surfaces.
- Voice-match and canvas/tooling flows no longer treat condition pages as ordinary dialogue pages.
- A focused audit test guards the known brownfield page-type pitfall.
</success_criteria>

<output>
After completion, create `.planning/phases/84-variable-registry-and-branching-gui/84-05-SUMMARY.md`
</output>
